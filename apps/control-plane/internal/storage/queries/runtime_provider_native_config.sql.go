// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: runtime_provider_native_config.sql

package queries

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const GetRuntimeProviderNativeConfig = `-- name: GetRuntimeProviderNativeConfig :one
SELECT id, tenant_id, runtime_node_id, node_id, provider_type, config_key, resolved_path, format, managed_values, file_content_hash, exists_on_node, manageable, unmanageable_reason, source, node_mtime, snapshot_at, last_pulled_at, last_pushed_at, last_pushed_by, created_at, updated_at
FROM runtime_provider_native_configs
WHERE tenant_id = $1::uuid
  AND node_id = $2::varchar
  AND provider_type = $3::varchar
  AND config_key = $4::varchar
`

type GetRuntimeProviderNativeConfigParams struct {
	TenantID     uuid.UUID `json:"tenant_id"`
	NodeID       string    `json:"node_id"`
	ProviderType string    `json:"provider_type"`
	ConfigKey    string    `json:"config_key"`
}

func (q *Queries) GetRuntimeProviderNativeConfig(ctx context.Context, arg GetRuntimeProviderNativeConfigParams) (RuntimeProviderNativeConfig, error) {
	row := q.db.QueryRow(ctx, GetRuntimeProviderNativeConfig,
		arg.TenantID,
		arg.NodeID,
		arg.ProviderType,
		arg.ConfigKey,
	)
	var i RuntimeProviderNativeConfig
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.RuntimeNodeID,
		&i.NodeID,
		&i.ProviderType,
		&i.ConfigKey,
		&i.ResolvedPath,
		&i.Format,
		&i.ManagedValues,
		&i.FileContentHash,
		&i.ExistsOnNode,
		&i.Manageable,
		&i.UnmanageableReason,
		&i.Source,
		&i.NodeMtime,
		&i.SnapshotAt,
		&i.LastPulledAt,
		&i.LastPushedAt,
		&i.LastPushedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const ListRuntimeProviderNativeConfigsForNode = `-- name: ListRuntimeProviderNativeConfigsForNode :many
SELECT id, tenant_id, runtime_node_id, node_id, provider_type, config_key, resolved_path, format, managed_values, file_content_hash, exists_on_node, manageable, unmanageable_reason, source, node_mtime, snapshot_at, last_pulled_at, last_pushed_at, last_pushed_by, created_at, updated_at
FROM runtime_provider_native_configs
WHERE tenant_id = $1::uuid
  AND node_id = $2::varchar
ORDER BY provider_type ASC, config_key ASC
`

type ListRuntimeProviderNativeConfigsForNodeParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	NodeID   string    `json:"node_id"`
}

func (q *Queries) ListRuntimeProviderNativeConfigsForNode(ctx context.Context, arg ListRuntimeProviderNativeConfigsForNodeParams) ([]RuntimeProviderNativeConfig, error) {
	rows, err := q.db.Query(ctx, ListRuntimeProviderNativeConfigsForNode, arg.TenantID, arg.NodeID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []RuntimeProviderNativeConfig{}
	for rows.Next() {
		var i RuntimeProviderNativeConfig
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.RuntimeNodeID,
			&i.NodeID,
			&i.ProviderType,
			&i.ConfigKey,
			&i.ResolvedPath,
			&i.Format,
			&i.ManagedValues,
			&i.FileContentHash,
			&i.ExistsOnNode,
			&i.Manageable,
			&i.UnmanageableReason,
			&i.Source,
			&i.NodeMtime,
			&i.SnapshotAt,
			&i.LastPulledAt,
			&i.LastPushedAt,
			&i.LastPushedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const UpsertRuntimeProviderNativeConfig = `-- name: UpsertRuntimeProviderNativeConfig :one
INSERT INTO runtime_provider_native_configs (
    tenant_id,
    runtime_node_id,
    node_id,
    provider_type,
    config_key,
    resolved_path,
    format,
    managed_values,
    file_content_hash,
    exists_on_node,
    manageable,
    unmanageable_reason,
    source,
    node_mtime,
    snapshot_at,
    last_pulled_at,
    last_pushed_at,
    last_pushed_by
) VALUES (
    $1::uuid,
    $2::uuid,
    $3::varchar,
    $4::varchar,
    $5::varchar,
    $6::text,
    $7::varchar,
    COALESCE($8::jsonb, '{}'::jsonb),
    $9::varchar,
    $10::boolean,
    $11::boolean,
    $12::varchar,
    $13::varchar,
    $14::timestamptz,
    COALESCE($15::timestamptz, NOW()),
    $16::timestamptz,
    $17::timestamptz,
    $18::uuid
)
ON CONFLICT (tenant_id, runtime_node_id, provider_type, config_key) DO UPDATE SET
    node_id = EXCLUDED.node_id,
    resolved_path = EXCLUDED.resolved_path,
    format = EXCLUDED.format,
    managed_values = EXCLUDED.managed_values,
    file_content_hash = EXCLUDED.file_content_hash,
    exists_on_node = EXCLUDED.exists_on_node,
    manageable = EXCLUDED.manageable,
    unmanageable_reason = EXCLUDED.unmanageable_reason,
    source = EXCLUDED.source,
    node_mtime = EXCLUDED.node_mtime,
    snapshot_at = EXCLUDED.snapshot_at,
    last_pulled_at = COALESCE(EXCLUDED.last_pulled_at, runtime_provider_native_configs.last_pulled_at),
    last_pushed_at = COALESCE(EXCLUDED.last_pushed_at, runtime_provider_native_configs.last_pushed_at),
    last_pushed_by = COALESCE(EXCLUDED.last_pushed_by, runtime_provider_native_configs.last_pushed_by),
    updated_at = NOW()
RETURNING id, tenant_id, runtime_node_id, node_id, provider_type, config_key, resolved_path, format, managed_values, file_content_hash, exists_on_node, manageable, unmanageable_reason, source, node_mtime, snapshot_at, last_pulled_at, last_pushed_at, last_pushed_by, created_at, updated_at
`

type UpsertRuntimeProviderNativeConfigParams struct {
	TenantID           uuid.UUID          `json:"tenant_id"`
	RuntimeNodeID      uuid.UUID          `json:"runtime_node_id"`
	NodeID             string             `json:"node_id"`
	ProviderType       string             `json:"provider_type"`
	ConfigKey          string             `json:"config_key"`
	ResolvedPath       pgtype.Text        `json:"resolved_path"`
	Format             string             `json:"format"`
	ManagedValues      []byte             `json:"managed_values"`
	FileContentHash    pgtype.Text        `json:"file_content_hash"`
	ExistsOnNode       bool               `json:"exists_on_node"`
	Manageable         bool               `json:"manageable"`
	UnmanageableReason pgtype.Text        `json:"unmanageable_reason"`
	Source             string             `json:"source"`
	NodeMtime          pgtype.Timestamptz `json:"node_mtime"`
	SnapshotAt         pgtype.Timestamptz `json:"snapshot_at"`
	LastPulledAt       pgtype.Timestamptz `json:"last_pulled_at"`
	LastPushedAt       pgtype.Timestamptz `json:"last_pushed_at"`
	LastPushedBy       uuid.NullUUID      `json:"last_pushed_by"`
}

func (q *Queries) UpsertRuntimeProviderNativeConfig(ctx context.Context, arg UpsertRuntimeProviderNativeConfigParams) (RuntimeProviderNativeConfig, error) {
	row := q.db.QueryRow(ctx, UpsertRuntimeProviderNativeConfig,
		arg.TenantID,
		arg.RuntimeNodeID,
		arg.NodeID,
		arg.ProviderType,
		arg.ConfigKey,
		arg.ResolvedPath,
		arg.Format,
		arg.ManagedValues,
		arg.FileContentHash,
		arg.ExistsOnNode,
		arg.Manageable,
		arg.UnmanageableReason,
		arg.Source,
		arg.NodeMtime,
		arg.SnapshotAt,
		arg.LastPulledAt,
		arg.LastPushedAt,
		arg.LastPushedBy,
	)
	var i RuntimeProviderNativeConfig
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.RuntimeNodeID,
		&i.NodeID,
		&i.ProviderType,
		&i.ConfigKey,
		&i.ResolvedPath,
		&i.Format,
		&i.ManagedValues,
		&i.FileContentHash,
		&i.ExistsOnNode,
		&i.Manageable,
		&i.UnmanageableReason,
		&i.Source,
		&i.NodeMtime,
		&i.SnapshotAt,
		&i.LastPulledAt,
		&i.LastPushedAt,
		&i.LastPushedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
