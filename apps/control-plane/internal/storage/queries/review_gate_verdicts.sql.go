// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: review_gate_verdicts.sql

package queries

import (
	"context"

	"github.com/google/uuid"
)

const CreateReviewGateVerdict = `-- name: CreateReviewGateVerdict :exec

INSERT INTO demand_criterion_verdicts (
    tenant_id,
    project_id,
    demand_id,
    plan_revision_id,
    criterion_id,
    verdict,
    judge_type,
    judge_id,
    reason,
    evidence_refs,
    project_task_id
) VALUES (
    $1::uuid,
    $2::uuid,
    $3::uuid,
    $4::uuid,
    $5::text,
    $6::varchar,
    'review_gate'::varchar,
    $7::uuid,
    $8::text,
    COALESCE($9::jsonb, '[]'::jsonb),
    NULL
)
ON CONFLICT (tenant_id, demand_id, plan_revision_id, criterion_id)
    WHERE project_task_id IS NULL AND judge_type = 'review_gate'
    DO UPDATE SET
        verdict = EXCLUDED.verdict,
        reason = EXCLUDED.reason,
        evidence_refs = EXCLUDED.evidence_refs,
        created_at = NOW()
`

type CreateReviewGateVerdictParams struct {
	TenantID       uuid.UUID `json:"tenant_id"`
	ProjectID      uuid.UUID `json:"project_id"`
	DemandID       uuid.UUID `json:"demand_id"`
	PlanRevisionID uuid.UUID `json:"plan_revision_id"`
	CriterionID    string    `json:"criterion_id"`
	Verdict        string    `json:"verdict"`
	JudgeID        uuid.UUID `json:"judge_id"`
	Reason         string    `json:"reason"`
	EvidenceRefs   []byte    `json:"evidence_refs"`
}

// 检测门聚合行写入：demand_criterion_verdicts, judge_type=review_gate, project_task_id 为空，
// 一 criterion 一行。upsert 供任务重试/重跑幂等。镜像 CreateAdversarialVerdict，只是 judge_type
// 与命中的 partial unique index 不同（uq_demand_verdicts_review_gate，见迁移 073）。
// 检测门聚合行：judge_type 固定 review_gate、project_task_id 恒为 NULL，命中 uq_demand_verdicts_review_gate
// 唯一索引（谓词 project_task_id IS NULL AND judge_type='review_gate'）。不能复用 CreateDemandCriterionVerdict/
// CreateAdversarialVerdict，二者 ON CONFLICT 谓词各自只对 executor/adversarial 行去重，对本聚合行不命中。
func (q *Queries) CreateReviewGateVerdict(ctx context.Context, arg CreateReviewGateVerdictParams) error {
	_, err := q.db.Exec(ctx, CreateReviewGateVerdict,
		arg.TenantID,
		arg.ProjectID,
		arg.DemandID,
		arg.PlanRevisionID,
		arg.CriterionID,
		arg.Verdict,
		arg.JudgeID,
		arg.Reason,
		arg.EvidenceRefs,
	)
	return err
}
