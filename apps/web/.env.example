# Web Console 环境变量配置
# 复制此文件为 .env.local 并填入实际值

# Control Plane API 地址（浏览器访问）
# 本地开发请用 127.0.0.1（不要用 localhost）：cookie 按宿主隔离，混用会导致
# 一边已登录、一边 badge/SSE 401。入口也会把 localhost 自动跳到 127.0.0.1。
VITE_CONTROL_PLANE_URL=http://127.0.0.1:8080
