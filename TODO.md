# TODO — 刻意延后的工作

> 约定见 CLAUDE.md「延后工作」条。每条一行:日期 + 要做的事 + 参考文档。
> 完成后删除该行(历史归 git);这里不是任务系统,只是"别忘了"清单。

- [ ] 2026-07-24 P2 planner 判据中文 E2E 验证——提示词已约束 `statement` 必须中文;真实产出依赖 planner(F6 deepseek 不稳),需独占环境稳定规划后抽查新卡判据无英文原文。参考 §6.2 / §12
- [ ] 2026-07-27 飞书联调残余 2 项(原 7 项中 6 项已验证:结果结论卡/卡内签署/any-of-N 双人/投影不阻塞/通讯录反查含手机号正向命中/outbox 重推按钮——P1 G4 真实 E2E 重推后 connector 消费并 sent+feishu_message_id)——剩:①换绑 UI(spec 2026-07-27-feishu-channel-access-management §6)；②越权对照(需第三个不在合格集合的绑定账号)。参考 manual-test-plans/2026-07-19-feishu-remaining-verification.md
- [ ] 2026-07-26 团队宪法收尾两项：①`governance_status` 判据仍是 `constitution = '{}'`，应改成「有无生效规则」（`queries/tenant_team_config.sql` 两处 CASE）；②D4 删除既有规则需审批（接权限中心，独立治理层）。参考 `docs/superpowers/specs/2026-07-26-team-configuration-console-design.md` §5.3/§9.2
- [ ] 2026-07-26 团队宪法注入强度升级（P3 实测暴露，2026-08-11 复核+铺 seam）：宪法目前作为**普通用户消息**前置到 prompt（`payload.rs::provider_prompt()`，宪法→人格→任务三段拼接），真实 Claude Code 明确回应「这条团队宪法是通过普通用户消息传入的，并非真正的系统级规则」。**web 调研结论（2026-08-11）**：claude 有 `--append-system-prompt`；**codex 无 CLI flag**（[openai/codex#11588](https://github.com/openai/codex/issues/11588) 仍为 feature request）；**opencode `run` 无 flag**；codex/opencode 的 AGENTS.md 在二者内部是「追加 user 指令」/「优先级低于 system」**非真 system**，且工作区约定文件方案有 git 证据链污染（破坏 `artifacts.rs` 的 `git status` diff 基线）、与项目原生 AGENTS.md 冲突、需推翻 `project_session.rs:5`「宪法不落盘」等坑——**工作区文件方案否决**。**人类决策（2026-08-11）**：全部暂缓，等 codex/opencode 原生 system flag 后三家一起接（避免中间态宪法约束力跨 provider 不一致）。**已铺数据层 seam**：`ProviderRequest`/`RunSpec`/`RunSnapshot` 加 `system_prompt: Option<String>`（serde default，**dormant-None**），`payload.rs::system_prompt()` 取宪法；`provider_prompt()` 与三家 `build_command` **未动**（宪法仍前置进 user prompt，投递字节级不变）。**待触发**：codex/opencode 原生 flag 落地后三步——①`provider_prompt()` 拆出宪法段；②各 `build_command` 消费 `system_prompt`（claude `--append-system-prompt` / codex /opencode 各自 flag）；③跨 provider 回归。参考 `apps/runtime-agent/src/{commands/payload.rs,commands/executor.rs,runs.rs,providers/mod.rs,providers/{claude,codex,opencode}.rs}`
- [ ] 2026-07-26 团队能力依赖预检细化（P2 未做，spec §5.2）：①就绪矩阵只覆盖 MCP 的 env 缺失，技能的 runtime 依赖状态仍只在员工页可见，未做团队维度聚合；②技能接管已在写时执行并落审计，但绑定前没有预览弹窗（MCP 侧有）。参考 `docs/superpowers/specs/2026-07-26-team-configuration-console-design.md` §5.2/§5.2.1
- [ ] 2026-07-26 大屏投屏设备认证形态——运行总览大屏模式落地后，投屏设备需长驻登录态（现有 cookie 会话过期即黑屏）；候选：专用只读角色 + 长期凭证（需 authz 侧支持）。用户拍板另立运维项后期再考虑。参考 `docs/superpowers/specs/2026-07-26-run-overview-display-mode.md` §8-5
- [ ] 2026-07-31 Console 与 Control Plane 走 nginx 反代同源——人类拍板最终会用 nginx 反代，但现在不急着配。落地后跨源不再发生：`http.allowedOrigins` 可留空、`control-plane-url.ts` 的 `{hostname}:8080` 默认应改为同源相对路径、`environment: prod` 的 fail-fast 判据需相应放宽（同源部署下不配 origins 是合法的，不该拦启动）。参考 `apps/control-plane/internal/config/config.go` 的 `ResolvedAllowedOrigins`/`validate` 与 `apps/web/src/lib/config/control-plane-url.ts`
- [ ] 2026-08-06 automation 执行失败推飞书——收口批只做 Console 收件箱告警（`item_type=automation_alert`）；推飞书需新 outbox kind + connector 卡片模板（跨仓），按真实使用频率再决定。参考 `docs/superpowers/specs/2026-08-06-casting-truth-source-closure.md` §2/§5
- [ ] 2026-08-06 G9 协调线程自动提请扩编的真路径无自动化覆盖——收口批 E2E 按 B 方案收敛时，`browser-casting-design-g9-coordinator.mjs`（任务完成后 `MaybeRequestCastingExpansion` 自动开卡，非手工 POST `/casting-expansions`）未迁入 `casting-suite`；它依赖真实任务执行，成本高于本批收益。参考 `scripts/e2e/README.md`「覆盖边界」与 `docs/superpowers/specs/2026-08-06-casting-truth-source-closure.md` §7
- [ ] 2026-08-07 跨页体验 PR4b（长尾中文化巡检）：failure family 词表（`status-labels.ts` `FAILURE_FAMILY_LABELS`）与写端 producer、PR4a 失败/扩编摘要中文化**已完成**；`task_result_contract_test.go` 的英文 fixture 已清理（2026-08-13 核对），剩余仅 `project_store_test.go` 的 `"review me"` 等 2 处存量（写端是 planner 快照前端不回改，等下次开单自然刷新）与分派失败恢复长尾 summary。参考方案 `docs/superpowers/plans/2026-08-07-console-cross-page-ux-hygiene.md` §14
- [ ] 2026-08-07 跨页体验 Loading backlog：审计/logs/runtime/system-config/role-vocabulary 等非 in-scope 页 LoadingState→骨架（同上方案 §0 Explicit backlog）。已完成：Runtime 节点与 logs 三个子页（login/operation/delivery 已换 `TableSkeleton`，2026-08-13 核对）；剩余 audit（`routes/_authenticated/audit/index.tsx`）、system-config（`features/system-config/index.tsx`）、role-vocabulary（`features/role-vocabulary/index.tsx`）三页仍是 `LoadingState`
- [ ] 2026-08-10 L2 事件 envelope **批二**（批一已完成入库：信封字段扁平补齐 + golden 进 schema 校验 + 每家 ≥5 强制）——批二要把 payload 切成嵌套 `{信封字段, payload:{业务键}}` 并迁移 CP/Web 读路径，历史扁平行永久双读；`provenance` 需 parser 透出原生 type，一并在批二做。`seq`/`type` 仍是冗余投影，外层为唯一真相。参考 `docs/superpowers/specs/2026-08-10-l2-event-envelope-decision.md`
- [ ] 2026-08-10 L0 raw 离线重放工具——**触发式，不排期**：下次要改任一家 parser 映射、或接入新 provider 时**先做这个**，把存下来的 raw transcript 重灌进新 parser 看映射差异，别再靠 live smoke 试。这是 spec §12「golden 挡不住上游协议漂移」的正解（今年已两次需要它：opencode 1.17 事件改名、codex 映射收敛）。参考 `docs/superpowers/specs/2026-08-09-provider-semantic-unification-design.md` §10 Phase 3
- [ ] 2026-08-12 工作区供给/删除确认的确认人角色——`2026-08-12-project-workspace-provisioning-model.md` 的两处人工确认（供给到备节点、删除磁盘目录）当前固定为**管理员**。人类拍板：后续应有「项目负责人 / 管理员」独立角色，随角色权限体系完善一并做好（与本文件 2026-07-26「租户角色与控制台业务模块能力矩阵」同源）。参考该 spec §5.3/§5.5 与人类拍板记录第 6 条
- [ ] 2026-08-12 demand 级变更范围感知（这一单改了哪些文件、增删多少）——人类拍板本轮**只做项目级** git 状态，这块后置另立。立项时须带上已核实的约束：稳定项目目录多任务共用且不加锁（`2026-07-23` §0.9），因此**不可**按 commit author 归属、不得为采集加锁或恢复 per-task worktree；可诚实交付的只有「观测窗口内相对某 SHA 的净变化」+ 重叠会话污染标记。参考 `docs/superpowers/specs/2026-08-12-project-workspace-git-observability.md` §2 非目标 / §3
- [ ] 2026-08-12 `attestation.git_base_ref` 语义偏差——该列现落的是 repo binding 的 `default_branch` **名字**（`project_store.go:3354` → `runs.rs:66-68` → `executor.rs:2868`），不是测量基线 SHA；且 `2026-08-12-project-workspace-provisioning-model.md` §5.7 之后平台不再 checkout，这个名字更不能当原点。出路二选一：补一列真实基线 SHA，或把该列语义正名。git 可观测批**有意不动它**以免范围滑。参考 `docs/superpowers/specs/2026-08-12-project-workspace-git-observability.md` §2 / §9
- [ ] 2026-08-16 任务中枢对话面（Chat）体验重构——T1–T9；Chat 只展示工具与回答。思考映射若将来要做另立，勿写成「过程没有思考链」。参考 `docs/superpowers/plans/2026-08-16-task-hub-chat-revamp.md`
- [ ] 2026-08-14 OpenFGA 迁移债务——已拍板现阶段不迁移（继续 `db` 引擎），**长期台账**见 `docs/OPENFGA_MIGRATION_DEBT.md`（债务清单 D1–D6、重估触发信号、偿还顺序）；护栏测试 `internal/authz/openfga_coverage_test.go` 强制「映射或登记」二选一，新增 authz action 时先读该文档 §2/§2.1 再动手。
- [ ] 2026-08-12 卷宗右轨 `branch_ref` / `git_commit` 仍是 handoff 自报声明——`service_dossier.go:981-984` 只渲染声明的 Ref，不含任何平台测量到的变更事实；人看到的是「员工声称交到哪」。等 demand 级范围感知立项后一并接真数据（届时声明与测量应**并列**而非合并，两者不一致本身就是有用信号）。参考 `docs/superpowers/specs/2026-08-12-project-workspace-git-observability.md` §9 与 `2026-07-29-demand-workbench-design.md` §4.5
- [ ] 2026-08-17 恢复任务协调模式错位（H1b 真链发现，demand 3b644046）：createRecoveryReplacementTask 不设 accepted_plan_revision_id（project_store.go:2334），而 InspectTaskResultDecision 对 nil 修订回退 loop（tri-mode §8.4 回兼容规则），plan 模式需求的替换任务 blocked 申报会绕过人工卡直接自动补链。修法（双管）：① 替换任务继承源任务的 AcceptedPlanRevisionID（与 revision/rework/supplement 路径一致）；② 模式解析改以需求为准——project_demands.coordination_mode 列自 059 起存在且 NOT NULL，InspectTaskResultDecision 加载 demand 取模式，任务级修订仅作兜底（模式本是需求级属性，任务→修订的间接层对动态任务天然脆弱）。
- [ ] 2026-08-17 provider 崩溃后员工活跃 run 毒化（H1b 真链发现，demand 70b88ff0）：崩溃→runtime 失败写回遇 CP 500（no rows 泄漏）→持久重试被 409 superseded 丢弃→run 行停留非终态→GetActiveDigitalEmployeeRun 持续 `employee conflict`（run_service.go:605）。自愈缺口：reconcileTerminalReceipt 仅在命令回执已终态时对账；isStalePreConfirmationRun 明确不扫 running/cancelling；无人做「run 行活跃 vs 其 attempt 已终态」交叉核对。分层修法：L1 失败写回把 attempt 缺失/已取代收敛为 409 而非 500（消灭触发器）；L2 CreateRun 冲突路径加 attempt-终态交叉核对，命中即内联收口 run 再派发；L3 看门狗新增按 attempt 终态交叉核对的清扫（有死亡证据才扫 running，不靠时钟）；L4 派发失败恢复卡批准后仍见 waiting_human 任务被派发（invalid project）——查 applyTaskHumanWaitRelease 与工作流重派时序。
- [ ] 2026-08-17 命令级终态写回无持久重试（spec `2026-08-17-recovery-mode-and-active-run-poisoning-fix.md` L1.5，明确本期不做）：run 行终态化的真正载体是 `POST /runtime/commands/{id}/fail`，但 runtime 持久队列只覆盖 attempt 级（Complete/Fail/WaitHuman），且崩溃路径 `executor.rs` 的 `fail_with_envelope` 是 command-fail `?` 短路 + `let _ =` 吞错——一次 command-fail 瞬时失败即毒化 run 行。CP 侧 L2/L3 已兜底；从源头减少毒化需把命令级写回纳入持久队列，改动面大单独立项。
