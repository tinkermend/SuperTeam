# Changelog

All notable changes to the SuperTeam Control Plane project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

- 2026-08-10 11:45 收件箱分诊工作台（spec `docs/superpowers/specs/2026-08-10-inbox-triage-workbench-design.md` 阶段 1–4）：① `ListInboxItems` 风险优先 `ORDER BY` + `ListInboxItemsOldest`；`sort=risk|oldest`（非法回落 risk）；WHERE 同口径护栏。② 行身份按 `layer`、密度压缩、kind pill、why/进度迁详情。③ roving tabindex、提交成功自动前进（后台提交不抢他人选中）、他人 resolve 清空；URL 为 sort 事实源。④ 「优先处理」区；重置清 sort。真实 E2E 含 T9 连续补证 3 条、T15 高风险离优先区。零迁移。
- 2026-08-10 11:35 Provider 语义统一 Phase 3（spec 同 Phase 1–2）：Codex 解析收敛为显式 `type` 分支，未知类型产出 `native_unmapped`（废除跨 type 贪婪 `session_id`/`text` 命中）；stdout 非 JSON 与未映射 type 分计诊断；`SUPERTEAM_PROVIDER_EMIT_NATIVE_UNMAPPED` 默认关写回（开则每 attempt 最多 20 条）。Web 执行轨迹对 opencode/codex 显示「摘要模式」诚实文案；CP activity 补 `turn_error`/`native_unmapped` 中文；接入清单 `contracts/provider/ONBOARDING.md`。门禁：golden/provider 测试绿、activity 单测绿、trace 面板单测绿。
- 2026-08-10 01:16 Provider 语义统一 Phase 1–2（spec `docs/superpowers/specs/2026-08-09-provider-semantic-unification-design.md`）：Runtime 引入 `ErrorEnvelope` + `providers/error_map.rs`（code→family/retryable 单表），四条终态路径统一 attestation + fail writeback，stream `Err` 合成 `turn_error`；Claude `result.is_error` 正确判失败；CP 补 `budget_fuse`→`waiting_human`/`budget_approval` 与中文 lead。Phase 2：`contracts/provider/schemas/*` + golden + fixture，`verify-foundation-contracts` 消费 schema 树，Web `failureFamilyLabel` + 护栏扩 `failure_family`，catalog 静态 `capability()`。**真实链路**：假 claude（`--version` 健康、真实 run 非 0 + rate limit）经 Runtime→CP 落库 `project_task_attempts.failure_family=transient_provider`/`retryable=true`（attempt 1 failed），协调线程按瞬时失败重试（attempt 2 queued）。门禁：runtime-agent 全量测试绿、golden 3 家绿、CP BudgetFuse 单测绿、status-labels 护栏绿、contracts 守卫绿。Phase 3（codex 贪婪收敛 / native_unmapped / UI 能力诚实）未纳入本批。
- 2026-08-09 23:29 收件箱 §7.3 真实 E2E 补验 + 定高工作台回归修复（spec `docs/superpowers/specs/2026-08-07-inbox-ux-remediation-design.md`）：该 spec 于 `361aa3ac` 实施但 §7.3 的 12 项真实浏览器验证从未执行（CHANGELOG 仅记分层门禁），本次补跑并**揪出 1 个回归**——§4.1 迁移到 `MasterDetailLayout` 时丢了原有定高管道（旧代码外层 `xl:overflow-hidden` + 列 `xl:h-full`），而该组件本身不带任何高度（内层栅格 `items-start`，为整页滚动设计），两列的 `@5xl/master-detail:h-full` 因此永远解析不到约束，整个工作台退化成一条 2359px 长列：**滚动列表后选中任一可见卡片，决策按钮被顶到视口上方 863px，必须滚回顶部才能操作**，直接打掉 §4.1「选中即见可执行动作」与 §5.1 缩短决策路径的目的。修法：`MasterDetailLayout` 新增可选 `fill`（开启后把父级高度透传两列 `h-full min-h-0` + `items-stretch`；默认关，其余 15 个消费点类名等价，已实测 `/projects` 根/栅格类与改前一致），收件箱外层 `overflow-y-auto` → `overflow-hidden`、传 `fill`，`InboxItemList` 的容器变体 `@5xl/master-detail:h-full` → 无条件 `h-full`。修复后列表内部滚动（h=739/sh=2307），同场景决策按钮 top=337 全可见。**真实链路验证（浏览器 + 真实 CP + 真库，12/12 全过）**：G1 原 6 条裸 UUID 卡显示需求标题且全页 0 个 36 位 UUID；G2 注入不存在 `demand_id` → 列表/关联对象/关联引用三处均「未命名需求 (deadbeef…)」（数据已回滚）；G3 无英文枚举且 `why==summary` 的 3 条只渲染一段；G4 meta 不再重复分组表头、进度条完整、详情「当前节点」有值；G5 未选中时列表独占全宽且三块空态全无；G6 宽容器 in-flow 右栏 420px / 窄容器 Sheet；G7 翻转点在**容器 1024**（视口 1280/1200 仍双栏，证容器断点生效）；G8 高风险 4 点亮、阻断 0 灰阶；G9 行内 CTA 只开弹窗未提交、团队视图 0 个 CTA；G10 扩编真实批准全链路（词表外→人工映射 `network_diagnostics`、选人后才解禁提交 → 决策 `approved` + 卡片 `resolved` + `project_playbook_casting` 新增该角色 + 事件链「剧本编制已更新→扩编已批准→决策已提交→协调作业已创建」，且需求保持 `executing`，承重不变量「扩编不得回退 demand 状态」成立）；G11 前端禁用提交 + 服务端 400 `invalid inbox action` 且事项未被处理；G12 搜「批三」→2 项、选中后列表 16→5、chip 显示项目名、目标用户筛选仅团队视图。验证期内 `control-plane` pid=209 / `web` pid=804 未变且无 `owner=`，服务始终跑本 checkout。顺带修 `verify:web` 在 main 上既存的 5 个 typecheck 错误（均来自 `35448def` 任务中枢那批，该批只跑了定向测试未跑 `verify:web`）：删死函数 `stringifyValue`；`project-config-page` 的 `humanRole` 去掉不可达的 `=== "owner"` 分支（"owner" 本就在 `HUMAN_PROJECT_ROLES` 内，守卫假分支只剩 "executor"）；`projects/index` 归档按钮去掉多余的 `isLoading && !data` 条件（前两条已完全覆盖，多余条件把 query 联合类型收敛成 `never`）；`task-launch-form` 两处 `apiOptions` 可选性改显式守卫而非非空断言。门禁：`verify:web` 全绿（145 文件 / 1150 例 + typecheck + build + bundle guard）、`verify:design-system` 通过。零迁移、零契约变更。
- 2026-08-09 20:53 worktree 并行开发落地 + 宪法整理（`f94ff6d0`/`382163f1`，文档 `docs/PARALLEL_DEVELOPMENT.md`）：`package.json` 的 `dev:control-plane`/`dev:runtime-agent` 改读 `SUPERTEAM_DEV_CONTROL_PLANE_CONFIG`/`SUPERTEAM_DEV_RUNTIME_AGENT_CONFIG`（此前写死相对路径，从 worktree 启动必失败，且既有同名变量只影响脚本解析 DB URL、管不到二进制）；`dev-services.sh` 的 `status` 新增 `owner=` 字段（从进程 cwd 观测服务实际跑哪个 checkout，非记账故不会漂移，同源时省略），`stop`/`restart` 接管他人服务前告警；`AGENTS.md` 删除 codebase-memory-mcp 条、"codex chrome plug" 改中性表述、新增「声称 E2E 通过须证明验证期内服务未被接管（记 pid 收尾复核）」硬规则、共享 checkout 长段落拆 5 子条并补两坑（`git diff` 需 `--no-ext-diff` 否则无 `@@` 头、全仓重生成会吸收他人在途改动）；`.gitignore` 忽略 `**/_tmp-*`。真实验证：干净 worktree 仅靠 env 启动 CP 成功且监听进程 cwd 确为 worktree、主 checkout 可收回、双向切换 health 200、`owner=` 与告警实测出现、透传 `-- --config` 覆盖仍有效。已知未解：`restart` 应用当前 checkout 的迁移目录（带迁移分支切换会污染共享库且不回滚）、库与 :8080/:3100/Temporal 仍单份故「同一时刻一个会话做 E2E」是前提。
- 2026-08-09 20:53 plan 模式无条件停下等人类确认（`1e0494b3`，BREAKING）：撤销 autonomy posture Phase A Task 2 的出口深度缩放，删除 `planRequiresHumanConfirmation`/`planExitAtOrBeyondConfirmationDepth`，`coordination_mode=plan` 现只可能落 `ValidationFailed`/`PendingReview`。理由（人类决策）：原判据读的 `RequiresHumanReview`/`RequiresHumanApproval`/`RiskLevel` 均由 planner（LLM）自填，等于 AI 判断自己的计划要不要给人看，最需人看的恰是模型没识别出风险的那些；且自治已有显式开关（loop 模式），不应再有 AI 自选的隐式路径。`planTouchesHighRisk` 保留（`acceptance_criteria.go` 另用于注入宪法级 human_judgment 判据）。测试 `TestShallowExitPlanModeAutoDispatches` 反转为 `...StillHoldsForConfirm` 而非删除，钉住新不变量。真实验证：`software_delivery` 模板（3 出口）提需求选中最浅 `branch_ref`、`review_required=false`、无风险触发器 → `pending_review` 且收件箱出现 `plan_review` 卡；该形状撤销前即自动派发。回写 `plans/2026-07-16-autonomy-posture-phase-a.md`（含 Task 5 Step 1 判据已反转，照原文复跑会误判为回归）。
- 2026-08-09 20:53 chat 计入项目业务口径与 plan 闸门 fail-open 修复（`f5540229`）：`ListProjectRunSummaries`/`CountTaskRunsCompletedToday` 补 `JOIN tasks` + `run_kind <> 'chat'`——chat run 自 tri-mode spec §13 挂项目锚后 `project_id` 非空、跑完即 completed，闲聊被计入锚项目「今日完成」与大屏 KPI，违反 §5 不变量 2（根因：run-project-affiliation 重构把读路径从经 `project_tasks` 改为直接用 `task_runs.project_id` 一等列，丢了隐式过滤；同文件 `ListProjectDeleteRunBlockers` 仍走 JOIN 故未受影响）。`InspectTaskResultDecision` 中 `GetPlanRevision` 读取失败改 fail-closed 落 plan（此前吞错落 loop，会在 plan 模式需求上自动补链绕过人类闸；不抛错是因 Temporal 会无限重试致协调线程静默卡死而无存活告警）。真实验证：08-06 窗口三项目「今日完成」显示 3/2/2 实为 0/0/0 全是 chat 泄漏；造真实 chat run（claude-code 执行至 completed）后 API 项目计数与 KPI 均仍为 5，同刻直查旧口径 6 新口径 5。零迁移。
- 2026-08-08 00:53 任务中枢 UX 补洞（接 §3–§6）：① 父层 `projectById` 缓存搜索选出的项目，browse 首页外选中不再被 effect 打回，成功页项目名不落裸 UUID（`missingObjectLabel` 兜底）；② 表单/项目搜索 `apiOptions` 由父透传（含 fetcher）；③ 模板确认前关库 Dialog；④ chat 零项目空态与 compose 对齐。新增单测：搜索越界选中保持。

- 2026-08-08 00:49 review 跟进修复：task-hub ProjectPicker 搜索选中不回退；enrich 三维补名互不拖垮；MetricCard loud 跟随 iconTone；inbox 项目筛选 isFetching/按需 getProject。

- 2026-08-08 00:38 任务中枢 UX 整治（spec `docs/superpowers/specs/2026-08-07-task-hub-ux-remediation-design.md` §3–§6 前端）：双 H1 收敛为壳 H1+内容 h2；模式名走 `launchModeLabel`（计划任务（Plan）/循环任务（Loop）/对话（Chat））；「中枢指令区/命令中心」→「需求描述」、删除死按钮「保存草稿」与假约束 `/ 5000`；模板插入改三选一 Dialog（覆盖/追加/取消）；模式卡与项目选择 roving tabindex；页签补 aria-controls/tabpanel；项目选择防抖服务端 `q` 搜索；零项目空态+禁用提交；双页签统一 `TaskLaunchShell`（副标题/极光一致，宽 canvas|wide）；提交成功就地结果块+toast，「查看需求卷宗」深链保留，「再提一个」连续提交。门禁：task-launches/dashboard 定向 13 例 + task-launches 全量 33 例；真实浏览器 G1/G2/G4/G5/G6 通过。 G3（>50 项目搜索）当前库仅 5 项目，标记阻塞。

- 2026-08-08 00:24 收件箱 review 跟进：ListItems 补名 fail-soft；项目筛选改服务端 q 搜索并回查已选项目名；目标用户已选态短 id 展示；view 切换 rest 解构清 target_user_id。

- 2026-08-08 00:01 收件箱 UX 整治（spec `docs/superpowers/specs/2026-08-07-inbox-ux-remediation-design.md`）：① **读路径需求补名**——`ListInboxDemandTitles` + `enrichSourceNames` 批量补 `demand_title`/`demands[].title`（仅响应、不落库）；前端 `missingObjectLabel("demand")` 兜底裸 UUID。② **词表**补齐 `project_task_runtime_recovery` 等 6 个 task human-wait kind。③ **meta 去重**——列表只渲染真实工作流节点，why/summary 同句只显示一段；删重复「查看关联任务」跳转。④ **布局宪法**——`MasterDetailLayout` 两栏（详情+动作合一，动作置顶）、撤常驻空态、`MetricGrid`/`MetricCard` KPI、容器断点。⑤ **行内 CTA**——高风险 open 卡一击打开决策弹窗（不提交）。⑥ **筛选**——项目名搜索 + 团队视图成员选择，去掉手输 UUID。门禁：`verify:control-plane` / inbox+labels 75 例 / `verify:design-system`。

- 2026-08-07 23:41 技能市场文案收敛：主动作「加载」统一为「绑定」（含弹窗/右侧范围面板）；卡面与抽屉来源枚举中文化（upload→上传，共用 skillSourceLabel）；卡内 CTA 主次调整为「详情」次要 +「绑定」主按钮。真实浏览器验证 /skills 绑定弹窗与已绑定回执。

- 2026-08-07（跨页体验续补）：Runtime 节点首屏/接入/事件改为 Skeleton；orphan waiting_human 补建决策摘要去枚举与英文技术词，等待原因中文化。

- 2026-08-07（跨页体验 PR4a）：任务失败决策摘要英文化 detail 映射为中文；扩编/语义发现摘要角色用「名称（key）」；失败/取消默认摘要中文。

- 2026-08-07（跨页体验续）：in-scope 加载壳改为 DetailSkeleton/TableSkeleton（技能详情/运行总览/流程实例/团队详情）；`humanWaitLabel` 收敛人机等待词（KPI/badge「待人工」、员工「待人工确认」、自动化闸门与收件箱统一「待我处理」）。

- 2026-08-07（控制台跨页体验 MVP 止血）：对象名缺失统一 `未命名{类型} (短id)`（D3/`missingObjectLabel`/`ObjectRef`）；收件箱 meta 禁全 UUID 与 API 字段名 meta；技能绑定/列表短 id；运行总览与成本 Provider 走 `providerDisplayName`；侧栏「技能管理」→「技能市场」；认证壳 `usePageTitle`（`炬枢 · {页名}`）。规范见 DESIGN.md 面向用户文本与 `docs/superpowers/plans/2026-08-07-console-cross-page-ux-hygiene.md`。
- 2026-08-07 **派发投影可见性补齐 P1 + P2b**。抽取逻辑下沉 `internal/capabilityprojection`（避免 employee↔project 循环依赖）。`GET digital-employee run` 附带 `capability_projection`（command receipt + attestation 合并 + 技能补名）；员工 Run 抽屉打开时拉详情并复用能力投影块。MCP 注册表列表增加「项目绑定」列（N 个项目 + 名称 title）。验证：`go test` capabilityprojection/employee/project；web drawer/mcp/projection 19 例。

- 2026-08-07 **派发投影可见性 P3 落地**（spec `docs/superpowers/specs/2026-08-07-capability-projection-visibility-design.md`）。execution-trace attempt 内嵌 `capability_projection`：从 `runtime_command_receipts.payload`（start_session）白名单抽取 skills/MCP/`source_scope`，合并 attestation `skill_conflicts`（含 `workspace_native`），batch 补技能名；响应不含 env/archive URL/MCP url。执行证据链 AttemptRow 展示「能力投影」块（来源 pill + 冲突句）；任务详情显示最近投影摘要。词表补 `workspace_native`/`project`（全局 STATUS_LABELS）。验证：`verify:contracts`；`go test ./internal/project`（含 Extract/attach）；web `attempt-capability-projection` + `project-execution-trace-panel` 13 例。

- 2026-08-07 14:44 **S10（投影不污染用户仓库）承重判据真实链路通过；并更正一处布局误读**。真实链路：给 repo 绑定项目提需求 → 规划 → 人类批准计划 → 派发 → runtime 建稳定项目目录并检出仓库 → 投影技能；**运行中采样**到工作区内 `diagnose-smoke`、`linux` 两条软链在场且 `git status --porcelain` 为空。反向验证（摘掉 `.git/info/exclude` 的屏蔽段 → `?? .claude/` + `?? .superteam/`，还原后恢复干净）在软链仍在的产物上完成。派发 payload 侧顺带取得三层模型的端到端正面证据：`start_session` 里 `metadata.workspace_mode=branch`、`project_git` 指向绑定仓库、**`skills` 含该项目绑定的 2 个技能**、`mcp_servers` 2 个。**更正**：此前把「找不到 `{base}/workspaces/{proj}/{task}/{attempt}` 目录」判成「runtime 不建工作区、疑似缺陷」是**误读**——当前设计是**稳定项目目录** `{base}/{project_name}`，旧布局是 `resolve_project_workspace` 里显式标注的 legacy 分支（spec 2026-07-23 §8 / P2），且稳定目录项目**不会**创建 `repos/` 缓存。据此建议的「repo worktree 物化排查 spec」**撤销，不立项**。**时序注意**（已写进 spec §11）：S10 必须在**运行中**采样，会话结束时 unload 会撤掉技能软链，事后采样只能验到 `.superteam/` 一半、会得出偏弱结论。三层模型 spec 状态改为「已实施并验收」。验收探针残留已清理：5 条需求全部终态（4 completed / 1 cancelled）、该项目未决决策归零。
- 2026-08-07 11:35（接续会话可观测性硬化，spec `2026-08-07-session-resume-observability-design.md`）：把 resume 预检结论从 run metadata 提升到人看得懂的卷宗时间线与执行轨迹。**写路径**：`StartProjectTaskRun` 产出 `SessionResumeOutcome`（resumed/skipped/none）并写入 metadata + `run_dispatched` payload；`ProjectStore.DispatchProjectTask` 在 bind 成功后于**三处**（首次派发、queued resume、已绑定短路自愈）幂等写入 `project_task.session_continuity`（ActorID=attempt id，O5/O5b）；中文 summary 唯一源 `SessionResumeUserSummary`。**读路径**：`event_narrative` 标题「会话接续」、skipped 精化为 warn；执行轨迹 attempt 带 `session_resume_status`/`session_resume_label`。**真链**：项目 `bb823f28` 接续 demand `eb577d2f`——父会话改 10 天前后派发，DB/API 见 `session_stale` 中文「原会话超过7天未活跃，已主动开新会话」；同单后续任务见 `resumed`「已接上该员工上次会话继续执行」；浏览器卷宗时间线可见「会话接续」+ 已接上中文；执行轨迹 API 返回 `已开新会话 · 原会话过期`。门禁：定向 CP 单测 + `verify:contracts` 过；web 执行轨迹 9/9。

- 2026-08-07 11:20（配置与运营补完收口）：补齐审查缺口——归档确认仅在 `can_archive===true` 且已有 preview 时启用（修复 `isFetching` 误禁用）；打开归档对话框 refetch preview；恢复项目独立确认框写明不复活待办；人类成员移除二次确认；`project.archive.auto_close_deferred` 事件留痕；OpenAPI `/archive` 描述与 409 `ProjectArchiveBlockedError`；负责人缺失错误中文。真实浏览器：成员添加/确认移除、阻断归档确认禁用、可归档项目归档+恢复。真实 API：新建项目 preview→archive 200→inbox 0→unarchive。

- 2026-08-07 10:46（配置与项目运营补完 A/B/C，spec `docs/superpowers/specs/2026-08-07-project-ops-config-completion-design.md`）：①**轨 A 人类成员/负责人 UI**——项目配置「成员」tab 支持添加人类成员（候选=有 console access 的租户成员，默认角色 observer，可选 owner/reviewer/observer）、行级改角色与移除；至少保留 1 位 owner 前后端双保险；概览负责人文案链到成员 tab；底层仍 `PUT /members` 全量替换。②**轨 C 归档门禁**——`evaluateArchiveReadiness` 同源驱动 preview 与 `POST /archive`：硬阻断 `active_tasks`/`open_demands`/`pending_decisions`/`already_archived`；警告 `missing_evidence`（含原 missing_final_report）与 `open_inbox_will_cancel`；preview 增 `can_archive`/`blockers`/`warnings`/`message`；归档确认框展示阻断与提示；「通过并结项」遇 assert 失败**不冒泡**（`closed=false, err=nil` + 日志），避免签署成功后 API 死循环。③**轨 B 成员名读路径补名**——config/members 读路径批量 join users+employees 覆盖 `display_name_snapshot`；配置页去掉仅补名用的 `listUsers limit 200`。零迁移。

- 2026-08-06 23:43 **能力绑定控制台 UI 落地 + 复检修一处「假通过」缺陷**（spec `docs/superpowers/specs/2026-08-06-capability-binding-console-design.md`）。**落地**：项目配置页新增「能力绑定」tab（技能/MCP 两区各自独立的声明式全量替换、草稿态与整体替换告警、依赖闭包预览、四态齐、归档 disabled、`?tab=capabilities` 深链）；员工配置页「已生效技能」与「个人 MCP」按 `project_bindings` 标「通用」/「限 N 个项目」（悬浮出项目名，缺名降级为「未命名项目 (前 8 位)」，不裸 UUID）；技能详情页补「项目绑定」MetricCard 与绑定分组；`status-labels` 补 `dependency_closure`/`employee`/`project_binding`/`team`。**文案纠正（承重）**：`skills/detail.tsx` 原写「依赖不会自动为员工开通能力」，依赖闭包落地后已不准确——实施会话还找出了我 spec 里漏掉的第二处同文案，两处一并改为「env 配齐则自动一并投影并记 `dependency_closure`；未配齐则阻断——但依赖永远不会替员工开通缺失的凭据」。**复检修的缺陷**：`ListProjectSkillBindings` 内嵌的 skill 只扫主表、不调 `loadChildren`，`runtime_dependencies.mcp_servers` 恒为空 → **本页最有价值的依赖闭包预览对已保存的绑定行永远不显示**；而组件测试只覆盖「刚从市场添加」的草稿行（deps 来自市场响应），所以是绿着漏过去的。修法：耗尽游标并显式 `rows.Close()` 后逐行 `loadChildren`（顺带把 `project_bindings` 也补齐）。**验证**：真实浏览器 U1/U1b/U2/U3/U3b/U7/U8/U9 全过——U2 加入草稿后刷新页面绑定行数不变且 dirty 消失（证明声明式草稿没被做成即时写入）、U3 已保存行直接渲染出「该技能依赖 e2e-mcp-…（若执行者已配齐所需环境变量）」、U8 给员工绑上受限技能后标记从「通用」变「限 1 个项目」且悬浮出项目名；新增前端回归用例「已保存的绑定行也显示依赖闭包预览」与后端 live 用例 `TestLiveProjectSkillBindingEmbedsDependencies`。门禁 `verify:web`(141 文件 1113 用例) / `go test ./internal/skill` / `git diff --check` 全过。**既有噪音（非本批回归）**：登录后 `/api/auth/me` 两次 401，在未改动的 `/projects` 页同样复现。
- 2026-08-06 22:48 **能力供给三层模型 P0–P2 落地 + 复检修一处承重缺陷**（spec `docs/superpowers/specs/2026-08-06-capability-supply-three-layer-design.md`）。**落地**：迁移 `20260806170000` 新建 `project_skill_bindings` + 照 072 语义复原 `project_mcp_bindings`（形状取 087 之后的最终态，不带回已删的 status/disabled_at）；项目技能/MCP 声明式绑定 API 与绑定时逻辑校验（技能存在、有可安装包、依赖 MCP 均已注册）；§8.1 读字段（`Skill.project_bindings` 带 project_name、MCP 侧同款、`runtime_dependencies.mcp_servers`）供 UI 期免二次改契约；控制平面投影五步（并集 → 场地过滤 → 依赖闭包 → 去重 → 同名项目优先并留痕 `source=project_binding`）；runtime 对投影路径做 git 屏蔽（`.git/info/exclude` + 已跟踪路径退化 skip-worktree），覆盖 `.superteam/` 与 provider `skills/` 根。**复检修的承重缺陷**：依赖闭包无条件补全会让紧随其后的 `validateSkillMCPDependencies` **永真**——那道闸的契约写在它自己的注释里是 "dependency validates, **never grants**"，而闭包恰好把它要找的东西提前塞进 `deps.runtimeMCP`，于是「依赖缺失阻断派发」再也不会触发；更糟的是 `ResolveRuntimeMCPServer` 签名里没有 employeeID，结构上就不可能做 env 满足性检查，凭据没配的 MCP 也会被投影，故障从派发期清晰拦截退化成运行期莫名失败。**修法**：闭包只补「员工已配齐全部必需环境变量」的依赖（`deps.runtimeEnv` 在闭包之前已装载，就地判定零新增查询），env 不满足则不补、交回门禁拦截并报可读原因；定义查不到与 env 缺失两条跳过路径都改为落日志，不再静默。**根因**：spec §4.3 只写了「技能进则依赖 MCP 必须进」，没跟既有的 never-grants 门禁对账，实施会话是照字面实现的——spec 已同步更正。**验证**：新增 `dependency_closure_test.go` 正反两向（env 齐 → 闭包补上并放行；env 缺 → 门禁拦住且点名 server_key），并**反向验证**该用例非真空——临时禁用 env 判定后它确实 FAIL。真实链路：live DB 场地过滤（`linux` 绑 A 项目，员工带着进 B 不投、进 A 投、撤员工携带后项目供给仍投）、绑不存在技能 400、软删依赖 MCP 后绑定 400 且点名 server_key、两侧 PUT 缺 `items` 均 400（顺带还清 072 遗留小债）。门禁：`verify:contracts` / `verify:control-plane` / `cargo test`(163) / `atlas migrate validate` 全过。**S10 仅验到机制层**：单测在真实 git 仓库上断言 `git status --porcelain` 干净、调用点在唯一的会话装载路径上，但端到端未验——现网 `repo_binding_status='bound'` 一条都没有，非 git 工作区该逻辑直接 early-return；补验需先给项目绑 repo 再跑一次真实任务。
- 2026-08-06 11:58 收口批**验收复检修六项**（真实链路证，全部零迁移）：① **告警永不关闭（必修）**——`casting_invalidated` 没有人类动词（照 `channel_alert` 先例），也没有任何 resolver，实测真库已滞留 2 条「编制失效」而编制早已恢复，等于每个项目在每位负责人收件箱里压一条永久假告警。`PutCasting` 成功后调 `ResolveCastingAlerts` 收口（重新编制是唯一合理的关闭者）；同族缺陷 `DeleteRule` 一并补——规则删了，指向它的 `automation_alert` 只能由"下一次成功"关闭，而删掉的规则永远不会再成功。② **图终态断言接错数据源（必修）**——`casting-suite` 从 `GET /projects/{id}/tasks` 取任务，而 `ProjectTask` **没有任何依赖字段**：「blocker 已全解仍 blocked」永远无法求值，合法 `blocked` 反被误报，C10 反向之所以 PASS 是因为夹具注入了 API 从不返回的字段名。改吃 `/task-graph` 的 `nodes+edges`（实测边形状 `dependent_task_id`/`blocker_task_id`），反向夹具改用真实边结构，并新增「零边即判失败」防真空通过。③ **停用角色后可达收口仍显示可达**——级联删掉编制行后 `missingRoles` 回退到 pool，而 `ListDigitalEmployeesByRoleKey` 不看词表状态、员工旧绑定还在，于是选择器说「这档可达」而 `PutCasting` 因 `UnknownKeys` 永远 400。读路径改为先把非 active 词表角色判为缺口。④ **预检快照与删除不同事务（TOCTOU）**——影响面在 `Begin` 之前查、删除在事务内执行，两者之间新增的编制行会被删掉却不发事件与告警；快照移入同事务。⑤ 两处 `confirm_impact` 400 的响应体（前端实际消费 `affected_castings`/`references`）此前未进契约，补 `CastingImpactConfirmRequired` / `RoleVocabularyImpactConfirmRequired` 并重新生成。⑥ 文档面：CHANGELOG 曾出现两个 `## [Unreleased]` 段、TODO 重复两行、`casting_impact.go` 注释错位，一并修。**真实链路验证**：`casting-suite` 五 stage 全 PASS（C3 级联→告警→重新编制→告警关闭；G8 fire 失败文案「缺角色 developer（从未编制）」+ 负责人告警；graph-assert 扫 10 张图 9 条真实边；C10 反向必 FAIL），残留脏数据已清（open 告警归零）。
- 2026-08-06 11:57 收口批 **E2E 覆盖按人类拍板的 B 方案收敛**：G5/G6/G7 降级为 Go 单测（新增 `project/casting_closure_test.go`、`automation/casting_gate_test.go`）；G8 与 **G12 SoD 耐久产品面**进 `casting-suite`（`automation-fire` / `sod` stage，均自恢复）；G11 与 G12 判定逻辑复核确认已有 Temporal workflow 测试与 `activities_test.go` 覆盖，不重复造 E2E。**G12 需要新探针剧本** `e2e_sod_probe`：现网已无可用 SoD 夹具——`incident_response` 的 SoD 对是 `operator+verifier` 而全库无人持有 `operator`（批二 G2 判别条件，不得补发），`software_delivery` 的 developer+reviewer 已迁到 `adversarial_review`。真实链路：同一人编制到 `diagnostician+verifier` 两侧 → demand 终态 `failed` + `planning_gap` 点名「剧本编制将职责分离角色指派给同一数字员工，重规划无法解除」。**仍无自动化覆盖**：G9 协调线程自动提请真路径（记 TODO）。
- 2026-08-06 11:00 **收口批**（`docs/superpowers/specs/2026-08-06-casting-truth-source-closure.md`）：**编制事实源双向闭环**——`ValidatePlaybookCastingComplete` / readiness `missingRoles` 读路径自证「员工仍持有该角色 + 可用」，直改 DB 的失真行也判缺口，不依赖上游守规矩；`PUT .../roles` 与词表停用增加 `confirm_impact` 预检 + 同事务级联解除编制 + `project.casting.invalidated` 事件 + 负责人告警；新增 `GET /digital-employees/{id}/role-impact`。**基线 §4.7 的通知半边落地**——`failFire` 统一向项目负责人集合（any-of-N）投递 `automation_alert`，文案写明缺哪个角色、原选谁、为何失效，fire 成功或规则删除自动 resolve；批二 commit 曾声称兑现 §4.7，实为仅 fires 表留痕，通知到本批才真正落地。**E2E 收敛**——`scripts/e2e/lib/`（cp-client / fixtures 按名解析 / assert-graph）+ `casting-suite.mjs --stage`，历史 12 个 `browser-casting-*` 与编排器删除。收件箱新增 `automation_alert` / `casting_invalidated` 两类空动作告警（照 `channel_alert` 先例，零迁移）；`/automations?rule=` 深链选中规则。失真编制普查脚本 `scripts/casting-stale-census.mjs`（当前 dev 库 stale_count=0）。
- 2026-08-05 22:52 **编制硬校验：被编制的员工必须持有该剧本角色**（人类拍板了此前挂起的未决产品问题）。`PutCasting` 在词表校验之后追加持有校验，任一违规整批拒绝（`ErrCastingRoleNotHeld` → 400），拒绝时不写编制也不入池。理由：编制是「谁能干这个角色」的事实源，可达收口、缺员拦截、扩编候选全从这里读；前端候选列表按角色过滤只是便利，允许 API 绕过去写等于允许这些判断静默失真。**存量清理**：全库唯一违规行 `incident_response.operator ← 开发-A`（开发-A 不持有 operator）按批三 spec §0.0「不要顺手给谁补 operator」的原则**删掉该编制行**而非发角色，顺带恢复了批二 §10.3 期望表（「故障排查只能走到仅诊断根因，缺 operator」）在当前数据下成立。真实链路验证：重放含违规行的 PUT → 400 `employee does not hold the cast role: operator←0be393bb…`；去掉该行 → 200；全库违规行复查 = 0。单测 `TestPutCastingRejectsEmployeeWithoutTheRole`（拒绝/放行/整批拒绝三例）。
- 2026-08-05 22:19 批三 + 角色治理台**验收复检修四缺陷**（真实链路证）：① **图终态永久滞留（承重）**——`planned_task_key` 合并把已完成上游留在图里，挂它下面的新任务建出来即 `blocked`，而解锁只由"上游完成"信号驱动、那信号早已过去，无看门狗覆盖；`ListDispatchableTasks` 改为把 `blocked` 也纳入候选、按真实 blocker 判定并用与 `ResolveReadyDownstream` 同一把 CAS 提回 `planned`（活动签名不变，无 replay 分歧）。② **单任务终态拒绝拖停同批派发**——新增 `dispatchFailureTerminal`，终态拒绝只废该任务并留可见事件后继续派发，非终态仍返回让 Temporal 重试；`GetVersion("dispatch-terminal-not-batch-fatal")` 围栏。③ **扩编决策在一单里无主体归属（承重）**——9 条扩编决策均无 `coordination_job_id`/`project_task_id`，而卷宗决策读路径正按这两个外键反查，导致扩编卡不进该单待办、时间线只剩通用「待人工决策」；`RequestCastingExpansion` 增 `ProjectTaskID`，两个内部触发点必带、HTTP 入口可选，并在卷宗增 `decisionsByApproval` 索引（按 `approval_request_id` 反查，既有事件一并复原，零迁移）。④ 时间线补 `casting_expansion` 精化：待扩编批准/扩编已批准/扩编被驳回，kind 归 `staffing_gap`。新增 GATE **H12（图终态）**与脚本 `scripts/e2e/semantic-casting-h12-graph-terminal.mjs`（G1/G2/G3 全 PASS）。**根因教训**：三个既有 E2E 脚本对任务终态零断言，这一路是"绿着"漏过去的。
- 2026-08-05 16:05 批二 **干净 P1 全门禁串跑 + G9→G10 串联**：编排 `run-casting-design-full-suite.mjs`；串联 `browser-casting-design-g9-to-g10-chain.mjs`（协调自动提请 → 人批准选人 → 重规划 → 新人任务 + develop id 复用，**不** POST `/casting-expansions`）。修复：① residual G2 按 §10.3 临时去掉池内 reviewer/tester 持有者后再断言 `branch_ref`/`next=reviewer`；② 浅单任务写回先把 demand 标 `completed` 再跑协调 Activity → 允许 `completed` 仍提请扩编。**实测汇总**（`.scratch/e2e-casting-design-full-suite/suite-result-final.json`）：residual G1–G9 hard PASS；G9-coordinator PASS；G10–G12 design PASS；G9→G10 chain PASS；G13 contracts/control-plane/web/migrate-validate PASS。Web 配置页编制 soft `G_web_config_casting` 仍可能 miss。
- 2026-08-05 14:55 批二 **G9 协调线程真实提请扩编**（design §7.1/§7.2，非仅 API POST）：任务 `complete_accepted` 后 version-fenced Activity `MaybeRequestCastingExpansion` → 按**编制缺口**（非成员池可行性）取下一未编制角色 → `RequestCastingExpansion(actor=coordinator)`，`suggested_role_key` 必为词表/骨架角色。真 E2E：仅编制 developer → develop 完成后 inbox 扩编卡 `suggested_role_key=reviewer`、summary「协调线程：…缺角色 reviewer」；workflow 日志 Activity `MaybeRequestCastingExpansion` + marker `casting-expansion-after-task=1`。脚本 `browser-casting-design-g9-coordinator.mjs`。
- 2026-08-05 10:15 批二 design 残留门禁真 E2E（不伪造）：**G4** 同一人 developer+tester 编制 → 真规划 `pending_review:release_record` + `constraint_notes.kind=collapse`「角色折叠：开发 与 测试…」；**G8** 停用编制员工后 `POST …/automations/{id}/trigger` → fire `failed` `casting_incomplete`「developer (员工不可用)」；**G9** 扩编 `suggested_role_key` 词表外 400、词表内 collector 201 且 inbox 卡带回 `collector`（API 通路）；**G1/G3/G5/G6/G7** 复验通过；**G2** 按 §10 去掉 operator 员工角色后 IR deepest=`root_cause`/next=`operator`。脚本 `browser-casting-design-gates-residual.mjs`。
- 2026-08-05 09:30 P1 历史 e2e 脏数据 **pg 硬删**（demand/task/inbox/decision/event/plan 全清，保留项目/成员/编制）脚本 `scripts/e2e/cleanup-p1-hard-sql.sh`；API 复核 demand=0、open inbox=0。G13：`verify:contracts` / `verify:control-plane` / `verify:web` 通过；`migrate-status` 最新；`migrate-validate` 默认改成本机 `--dev-url "postgres://postgres:postgres@127.0.0.1:55432/atlas_migrate_validate?sslmode=disable"`（`make -C apps/control-plane migrate-validate` 即过，容器 `superteam-migrate-validate-pg`）。早期 E2E 脚本标 deprecated，权威 `browser-casting-design-g10-g11-g12.mjs`。
- 2026-08-05 02:32 批二设计门禁 G10/G11/G12 真实扩编路径证通：① **G10** `software_delivery` 仅编制 developer → 扩编 reviewer → 新 `review` 任务派给扩编人、`develop` 同 task id 复用；② **G11** replan 收口 `branch_ref→review_verdict` 落 `pending_review` 不自动开跑，浏览器可见计划确认；③ **G12** 执行中扩编 verifier=原 operator → demand `failed` + 本 demand 的 `planning_gap`（剧本编制职责分离）。产品修复：扩编 replan 自动 accept 前 supersede 当前 accepted（消 23505）；扩编重规划 `PinnedExitDeliverable=DeepestExitDeliverableWithCasting`。脚本 `browser-casting-design-g10-g11-g12.mjs`。
- 2026-08-05 01:40 批二 G10/G12 真实浏览器闭环（不冒充）：**G10** demand `53c85853…` 已有 completed `develop`+`browser_verification`，扩编 replan 后同 `planned_task_key` 复用原 task id（证据 `G10-evidence.json`）。**G12** 编制强制 operator=verifier 同一人（`incident_response` 经典 `role_independence`，非 migratable adversarial_review）→ 治理升格 structural gap：`demand=failed` + 收件箱 `planning_gap`「剧本编制将职责分离角色…」；真实浏览器可见异常处理分组。产品修复：① `ApplyPlaybookCasting` 后 SoD 若编制锁定同人则 `ErrNoSuitableEmployee`（不再空转 3× planner）；② 缺口文案区分「池太小」vs「编制锁定」；③ `planning_gap` 投影补 `demand_id` InboxContext。脚本 `browser-casting-g12-sod.mjs`（严匹配本 demand，禁止偷旧 G12 卡）。**未做**：扩编路径 G12（中途扩编撞 SoD）与 G11 越界仍 soft；`software_delivery` developer/reviewer 已 migratable。
- 2026-08-05 扩编后 plan_review 接受链路修复 + 真实验证：根因 ① `AcceptPlanRevision` 未 supersede 同 demand 已有 `accepted/decomposed` → 撞 `uq_project_plan_revisions_current_accepted`；② 扩编 `SupersedeOpen` 后旧 `plan_review` 卡仍可点，Accept 对 superseded 计划 409；③ in-memory `pendingReviews` 在 casting-expansion 路径不登记，旧 GetVersion 路径会丢到 predispatch。修复：Accept 前 `SupersedeCurrentAccepted…` + 幂等；CreatePlanRevision 后 `CancelStalePlanReviewDecisionsForDemand` 并投影 inbox；`handleHumanDecisionSubmitted` miss 时 version-fenced 回落 FromStore。**实测**：inbox 批准卡住的 `45ccd405…` → `LoadHumanDecisionRoute` → `ResolvePlanRevisionReview` → `DecomposeAcceptedPlanRevision`，plan **decomposed**；full E2E 扩编批准后 replan 直接 `accepted` 并为本 demand 创建 tasks（develop running）。G10「已完成不重复创建」仍缺 completed-before 前置，未冒充通过。
- 2026-08-04 批二扩编收件箱产品环（**G10 真实 planner 闭环未证通，不得冒充**）：① inbox `casting_expansion` 仅 `批准并选人`/`驳回`；② HumanTask kind=扩编请求；③ 收件箱弹窗选人写 payload；④ `POST …/casting-expansions`；⑤ 弹窗可滚动。**已实测**：浏览器开卡→选人批准→编制写入 + inbox resolved；协调可触发 `casting_expansion_replan`；亦曾见 replan 落 `pending_review` 与 `PlanDemandRoute` 超时。**未实测**：G10 新任务派新人 / 已完成不重复创建 / G12 职责分离。
- 2026-08-04 批二角色词表与编制（P0 可交付 + 扩编决策骨架）：① 迁移 `role_vocabulary` / `digital_employee_roles` / `project_playbook_casting`，默认租户种子 10 角色（含 operator 词表项、**不**绑 operator 员工）；② 剧本 Create/CreateVersion 校验 `roles[].key` 必须 active；③ 员工 `role_keys` 多值读写 `PUT /digital-employees/{id}/roles`；④ 编制整套替换自动入池、移除保护；⑤ `playbook-readiness` 复用 `PruneSkeletonForExit`/`ExitCondMet`；⑥ automation 保存期编制不全 400（G7）、fire 时编制失效失败写明原因（G8）；⑦ Web 提交需求剧本选择器展示可达收口 + 编制面板、项目配置「剧本编制」分区；⑧ 扩编 `casting_expansion`：批准写编制 → 协调线程 **不回退 demand** 重规划；`planned_task_key` 复用 completed/running；越界 ForcePendingReview（§7.4）。单元测试覆盖 in-bounds 自动分解与 overbound 退回人工。§0.0 勘察：`request_changes` 仅 plan_review、任务未 decompose，从不面对已完成任务。真实 E2E：G1/G2(incident 仅诊断+缺 operator)/G3 入池/G5 拒移除/G6 候选/G7 通过。
- 2026-08-04 16:30（剧本可落地化 批一：能力词表两侧对齐 + 清测试残留 + 补人类闸 + 空租户 seed）：起因是人类反问「当前剧本的定义在类别与流程上有没有优化空间」。核查真实数据后**修正了我自己先前的判断**——`log.analysis`/`incident.triage` 并非拍脑袋写的，它们**正式注册在 `capability_vocabulary` 里**；真正的问题更结构性：**词表校验是单边的**。场景模板侧有 `validateSpecVocabulary` 卡着，员工侧 `external_capabilities` 却无人校验，于是两头落空——词表里注册的 `log.analysis`/`incident.triage` **0 名员工声明**（运维分析与故障排查两个剧本永远匹配不到能力），而员工实际在用的 `research`/`writing`/`report_synthesis`/`information_retrieval` **根本没注册**。`planning_profile.go` 里那句「Both sides of the diff are free text with no registry」写于词表建立之前，早已过时却还在为"能力只记录不扣分"背书。本批修法：**①员工侧补词表校验**（新增可选端口 `CapabilityVocabularyValidator`，由 `scenariotemplate.Service` 实现、app.go 注入，employee 不依赖该包；未注入时放行，与包内其他可选端口同约定）——单边校验等于没有词表；**②先注册后校验**：把员工真实在用的 4 个键登记为正式词汇，否则存量员工一保存就 400；**③统一命名**点号→下划线（`log_analysis`/`incident_triage`，词表+模板+版本+员工声明四处同步，这两个键 0 人声明故无存量影响）——词表内部两种风格并存，不统一会随新键扩散；**④给"对外生效且难回滚"的步骤补 `human_gate`**：故障排查的 `fix` 是真去改生产系统却一直没闸（基线 §1「打断人的时机是权力边界变化」），而运维分析的 `collect` 是只读采集**故意不加**——给只读动作加闸正是基线否决的「每个动作都要人授权」；**⑤退役 E2E 残留**：`adv_review_e2e_123656`/`g4_sod_staffgap` 两个 active 的测试模板会出现在用户的剧本选择器里，连同其专用词表键 `change_request`/`change_approval` 一并置 disabled（不删除——历史 demand 仍引用，删了卷宗解析不出剧本名）；**⑥清死字段** `feasibility_thresholds`（解析了但从不使用，真实阈值取自项目 `coordination_policy`，留着会让人以为改它有用）；**⑦空租户补内置剧本**：内置剧本此前只写给默认租户，其余 5 个租户开箱 0 个剧本、连 generic 兜底都没有，「剧本」这套能力对它们等于不存在。**踩坑**：`capability_vocabulary.status` 的 CHECK 只允许 `active|disabled`，首版迁移写了 `inactive` 当场被拒。验证：真库核对六项逐条（活跃模板只剩内置 5 个、词表 9 active + 2 disabled 且命名统一、`incident_response` 出现 `human_gate:fix@fix_record`、死字段 0 命中、6 个租户各 ≥5 个剧本）；真实接口双向验证词表校验——带 `totally_made_up` 的配置修订被 400 拒并点名该键，只带 `code_review` 的返回 201。门禁：`verify:control-plane` / `migrate-validate` 全过。**未做（归批二讨论）**：planner 提示里仍不注入能力词表，`required_capabilities` 因此仍由模型自行合成、选角评分仍把能力当自由文本"只记录不扣分"；骨架仍只画顺利路径不表达返工。
- 2026-08-04 15:26（同单接续 P1：resume 预检降级，spec `2026-08-01-demand-continuation-design.md` §6.1）：接续做出来之后留着一个会翻车的洞——`--resume <失效 id>` 会让 provider 进程直接失败、**整个接续单跟着失败**，而接续的典型场景恰恰是"隔了几天回来接着做"，正是会话最可能已过期或 runtime 节点已换掉的时刻；人主动来接续换来一次莫名其妙的失败，是最坏的失败模式，且我把接续做出来后触发概率反而更高。**判据放控制平面**（runtime 侧做 resume 兜底属于改 provider 管道，基线 §4.8 越界）：派发前预检候选会话，命中「绑定节点 ≠ 本次派发目标节点」（会话文件在原机器上）或「最后活跃超过 7 天」时**主动不下发 `provider_session_id`**，正常开新会话。两个时间取更近的一个（`last_runtime_seen_at` 是 runtime 视角、`last_active_at` 是控制平面视角，任一新鲜即认为可能还在），两列都缺（历史数据）时不据此拒绝——缺一列就把所有老会话判死太粗暴。**降级必须留痕**：metadata 写 `session_resume_skipped` 原因码与被放弃的会话 id，不留痕的降级等于静默丢上下文、事后无法区分"该续没续"与"本就不该续"；"本来就没有候选会话"不算降级、不留痕。新增 `FindProviderSessionCandidateForTaskRoot` 只为把预检所需事实（节点、最后可见时间）带出来，既有 `FindProviderSessionForTaskRoot` 保留不动。runtime 侧**零改动**：不下发 session id 时它本就开新会话。验证：判据 7 个子用例 + 派发路径集成用例（过期会话下派发必须照常发出并留痕），后者经临时摘掉预检实证会挂（报错正是"过期会话不得下发"）；**真实链路**——把一条真实父单会话的时间戳改到 10 天前，批准接续单的计划触发派发，DB 直查确认派发 metadata 里 `revision_root_task_id` 仍正确指向父单任务根（P0c 未受影响）、`provider_session_id` **未下发**、`session_resume_skipped=session_stale` 且带上被放弃的会话 id，任务照常 running 并另起了新会话。门禁：`verify:control-plane` / `verify:contracts` 全过，`git diff --check` 干净。**遗留**：降级目前只落在 run 的 `params.metadata`（可查），尚未投影到卷宗时间线；`verify:runtime-agent` 的既有并发 flake 仍在（本批 runtime 零改动）。
- 2026-08-03 23:16（同单接续 P0a–P0d 落地，spec `2026-08-01-demand-continuation-design.md`）：闭环此前走到「验收完成」就断了——Web 全仓无接续入口、CP 从不下发 `send_input`、plan/loop 无「基于原会话追加指令」的 API，用户要接着做只能重提一条需求，丢血缘、丢上下文、丢 provider 会话。本批按拍板的「**接续 = 新 demand + 血缘链**」落地（原单终态永不回退，因为 demand 状态单调 + 验收闸在首次完成时已消费）。**①〔独立缺陷先修〕`recoveryPlannerMetadata` 只 clone 源 metadata、不写 `revision_root_task_id`**：替换一个**原始任务**时根落回自身 id，于是"人主动重试反而丢会话"——与隔壁 `revisionPlannerMetadata` 对齐后修复，三态单测并经临时移除修复实证会挂。**②血缘用真列** `continues_demand_id`（+ 部分索引，迁移 `20260803180000_*`）而非塞 `source_refs` JSONB：链遍历要走递归 CTE 且要能走索引；`source_type` 是渠道枚举（manual/飞书/automation），与血缘正交，不得挪用。**③核心是派发期的会话血缘传播**：`ResolveProjectTaskLineageRoot` 在既有三条规则后插入「任务所属 demand 有 `continues_demand_id` → 沿祖先链**逐代**上溯，取**同一 `assigned_digital_employee_id`** 的最近任务的根」。放派发期而非规划期，是因为员工绑定到派发时才可靠，从而**不改 planner、不改 Temporal 输入**，风险面收在一个仓储方法里；整条上溯是**一条递归 CTE**（派发是热路径，不能在 Go 里循环往返），且只有接续单会进这条分支（普通单零额外代价）。D1–D7 七条判定细则用**真库**测试锁住：逐代上溯不跳代、跨代时继承前序任务自己的**根**而非 id、同代内按 `created_at DESC` 取最新、**换人一律开新会话**（用户明确反对"续错人"）、未定人的任务不参与匹配、人为造环仍在深度上限收敛。**④卷宗按链聚合**：新增 `lineage`（全链摘要 + 服务端接续判据）、单头链条可跳转、左轨**按链折叠成一行**并标「接续 n 次」——不折叠的话每接续一次列表就多一行看似无关的单，正是「一单的身份是链而不是行」要防的。**实施期 E2E 又揪出三处原 spec 未覆盖的**：(a) 链**中间**的单也可接续会把线性链分叉（「第 k / n 次」与链条展示都建立在线性上），已收敛为**只有链尾可接续**（`already_continued`）；(b) 归档项目在卷宗判据里说"可接续"、点了却被 `SubmitDemand` 的既有归档门禁拒成 409——判据是唯一真相源，必须与写路径同源，已补 `project_archived`；(c) 接续成功后未失效父页需求列表，跳到新单时左轨里没有它、链也折不出来（不能只依赖尽力而为的 SSE）。**真实链路验证**：对一条已完成的真实单发起接续 → 新单继承剧本与协调模式、`continues_demand_id` 正确 → 协调线程真实规划出任务 → **派发元数据里 `revision_root_task_id` 指向父单任务、`provider_session_id` 正是父单该员工的会话**（DB 直查逐字符比对），即同一员工确实回到了自己上次那条会话；父单状态/`updated_at` 全程未变；对未结束的单接续得 409 且卷宗判据文案一致；浏览器 3100 完整走通「继续这一单 → 填写 → 创建 → 落到新单卷宗」，链条显示「本单为第 2 / 2 次」、左轨 3 条需求折成 2 行并标「接续 1 次」。门禁：`verify:contracts` / `verify:control-plane` / `verify:web`（137 文件 1101 用例 + typecheck + build）/ `migrate-validate` 全过。**未做**：P1 的 resume 预检降级（会话过期/跨节点时主动放弃 resume 并留痕）仍未实施，现网 resume 失败仍是 fail-fast。**另**：`verify:runtime-agent` 连续两次全量跑各挂一条不同用例（`project_task_budget_trip_*`、`command_loop_executes_start_session_commands`），单独跑均通过，且 runtime 目录本批零改动——判定为既有并发时序 flake，非本次引入。
- 2026-08-03（修复迁移乱序：continuation lineage 重排版本）：`20260803093136_project_demand_continuation_lineage` 在库已应用到 `20260803160000` 之后才进目录，atlas 报 non-linear / out of order，导致 `dev-services restart control-plane` 迁移失败。改名为 `20260803180000_…` 并 `atlas migrate hash`；dev 库已 apply（`continues_demand_id` + 部分索引落库）。**验证**：`atlas migrate status` Already at latest；无 `SKIP_MIGRATIONS` 正常 restart CP healthy。
- 2026-08-03（项目归档硬门禁 + 恢复回拨）：归档此前只改 `status/archived_at` 并 cancel 收件箱，**不检查**未完结任务，与「能归档=工作面已空」产品语义不符。现 `ArchiveProject`/`CreateArchiveSnapshot`/结项归档/验收批准归档统一走 `assertProjectReadyToArchive`（`GetProjectTaskStatusCounts.active_tasks>0` → 409「项目仍有未完结任务，无法归档」；终态=completed/done/success/failed/cancelled）。新增 `POST /projects/{id}/unarchive`：`archived→running`、清 `archived_at`、事件 `project.unarchived`（不复活旧待办、不重跑历史）。Console 菜单「恢复项目」+ 归档确认文案更新。开发库排查：当前 12 个已归档项目均 `active_tasks=0`，无需脏数据删除。**验证**：Go 单测挡/放/恢复三路径；真实 CP 对 `active=2` 项目归档 409 中文；对空活跃项目 archive→unarchive 200 回 running。
- 2026-08-03（项目配置去掉「任务历史」tab）：配置页职责应是名称/负责人/成员池/协调策略与配置修订，任务列表属于项目运行面（详情/任务 tab），混在配置里越界且还多拉一轮 `listProjectTasks`。已移除 tab、TaskHistoryPanel 与对应 query。
- 2026-08-03（项目配置概览去掉「协调线程」展示字段）：概览里该 Input 只读回显 `project-coordinator:{project_id}`，创建时由控制平面注册 Temporal 协调 Workflow 自动生成，不进配置保存、用户无法也不应填写；放在名称/目标/描述之间像可配项，纯噪音。已从概览表单移除（协调策略 tab 与成员变更 callout 仍说明策略/成员变更会 signal 协调线程）。
- 2026-08-03（跨通道体验 PR-3：收件箱 toast + 飞书终态通道文案）：Console 批完 project_decision 成功 toast「决策已提交 / 飞书通知将同步更新为已处理」；飞书终态卡区分「你在飞书处理的」与「处理人…（平台收件箱或其它通道）」。
- 2026-08-03（飞书 outbox 长轮询唤醒 PR-2）：connector 原 2s 固定轮询 `ListOutbox`，Console 批完飞书卡最多再等一轮才 `card_update`。现 `feishu_outbox` pending 写入 `NOTIFY feishu_outbox_changed(tenant_id)`；CP `ListOutbox?wait_ms=` 空队列挂起至通知/超时；connector 改紧循环长轮询（默认 wait 2s，有货立即投）。connector 仍不连库。迁移 `20260803160000`。
- 2026-08-03（废止规划执行池「外团队踢出」）：产品语义=团队是员工归属单位、项目是运行载体，项目可挂任意团队的数字员工。协调器 `LoadProjectCoordinationSnapshot` 却用 `project.team_id` 过滤执行池（借调下线后的错误反向），UI 已加入的员工规划时池仍为空。现只保留「无团队归属」踢出；跨团队项目成员一律入池。单测改钉「外团队成员保留」。
- 2026-08-03（项目配置成员页：UI 添加/移除数字员工）：项目配置「成员」标签原先只能展示人类成员与数字员工池，增删只能走「高级：成员完整替换 JSON」。用户在页面里看不到可操作入口，高级区若只剩一个名字（如 Jason）也无业务意义。现于数字员工区块提供「添加数字员工」对话框（从租户目录多选、过滤已在池中与无团队归属员工，因后端门禁拒绝 teamless 成员）与行级移除，底层仍走既有 `PUT /projects/{id}/members` 全量替换并保留人类成员/负责人。高级 JSON 保留作排障。人类成员/负责人 UI 增删仍未做（概览提示文案仍写「成员标签页增删负责人」，属既有缺口）。**验证**：`config.test.tsx` 12 项通过；真实 CP `PUT …/members` 对项目 `35c84af4` 添加「开发-小王」后还原成功；真实浏览器登录后打开 `/projects/…/config` → 成员 tab 见「添加数字员工」并打开对话框（含「加入项目」）。
- 2026-08-03（提交需求硬门禁：项目至少一名数字员工）：产品期望「无数字员工连规划都不应启动」。此前 `SubmitDemand` 允许空池入库并 signal 协调器，协调侧才 `no_plannable_digital_employee` / 规划器胡填空 UUID 后落 `planning_failed`（文案还被 humanize 成「规划器输出无法解析」）。现于 `SubmitDemand` 入口查 active `digital_employee` 成员，零则 400 `项目至少包含一个数字员工`，不写 demand、不 signal。**验证**：单测门禁正/反例 + 真实 CP 对空池项目 `POST …/demands` 返回 400 中文句。
- 2026-08-03（跨通道决策投影收敛 P0：修 Web resolve 后飞书卡仍可点的竞态）：根因是 connector 已 SendCard 但尚未 ack 时 Console resolve 会把 `decision_card` 标 `superseded`，随后 ack 因要求 `status=pending` 静默 `already_acked`，`feishu_message_id` 丢失、永不入队 `card_update`，飞书活卡成僵尸。修法：①`MarkFeishuOutboxSent` 接受 `pending|superseded` 并强制回填 message_id+status=sent；②ack 成功后对 `decision_card` best-effort 调 `EnsureDecisionCardsTerminal` 补 `card_update`；③`supersedeDecisionOutbox` 按已有 card_update 去重、sent 却缺 message_id 写 `last_error` 留痕；④幂等 resolve（同 decision 再点）再跑 Ensure 自愈。单测：纯函数 message_id 提取/终态 payload 合并、Ack 触发/不触发 terminalizer、幂等 resolve 必调 Ensure。未做：NOTIFY 唤醒与 Web toast（PR-2/PR-3）。
- 2026-08-01 20:55（本单收口显性化 + attestation git 四列真实回填 + 基线两条不变量落文档）：起因是人类要求对「一单处所与专家团剧本」基线做可行性与致命项审查。审查中核出**两处必须先拍板的结构性冲突**，人类已拍板并写回基线（`2026-07-27-...-baseline.md` §4/§5/§6/§8/§9）：**①接续 = 新 demand + 血缘链**，不在原 demand 上追加——`ProjectDemandStatusCanAdvance` 的单调性是承重不变量（completed/failed/cancelled 同为最高 rank，`advanceProjectDemandStatusWithQueries` 不满足即静默 `return nil`），要支持同单追加就得同时放开单调性、让收敛闸二次触发 `acceptance_pending`、把终态通知改成按终态代次幂等；且追加工作会落在**已消费过的验收闸之后**，等于产出永不过闸。**②chat 不进卷宗**——`CoordinationMode` 只有 plan/loop（`isAutonomousCoordinationMode` 注释的 "chat once it lands" 是未落地设想），chat 是 `employee` 域的 `task_run`，无 demand；基线一句话据此从「每一单」改为「每一次**受控协作**」。顺带把「#2 是否触及 Temporal」这个待勘察项收敛掉：决策路由早已改为 `handleHumanDecisionSubmittedFromStore` 从 DB 重建、不依赖 workflow 内存，接续复用「写 DB + 发 signal + 从 store 解析」即可，无 replay 风险。**代码两项**：**（a）卷宗单头显示本单收口**——此前只显示剧本名，等于只说了"按哪套打法"、没说"打到哪一步"，而 exit 恰是平台把「走多深」做成服务端可校验结构（`pruneSkeletonForExit`/`validateSkeletonAdherence`）的那个键。`DemandDossierPlaybook` 增 `exit_deliverable`/`exit_label`/`exit_pending`，取自**计划修订载荷**而非模板现值（`available_exits` 在成案时已快照 label，历史单因此不会被模板改版改写口径）；取生效修订，无生效修订时退到最新一版并标 `exit_pending`，前端显示「拟收口 · X」——需求停在等计划确认恰是人最需要看见走多深的时刻，留白等于在最该说话时闭嘴；剧本解析失败降级 none 时**收口仍保留**（当初收口到哪是既成事实）。**（b）attestation 的 `git_branch/git_base_ref/git_head_sha/git_diff_sha256` 四列从 runtime 硬编码 `None` 改为真实回填**——表与契约早就有这四列，只有 runtime 一直没填，变更范围因此完全不可见。新增 `artifacts::collect_workspace_git_facts`（branch 在 detached 下不落字面量 "HEAD"、非 git 工作区整体降级 None 而不失败），base_ref 走派发下发值经 `ResolvedProjectWorkspace → CommandWorkspace → RunSpec.workspace_base_ref` 串联（不从 git 反推）；`diff_sha256` 摘的是 **raw** `git diff HEAD` 字节而非 diff 工件字节——工件入库前经脱敏截断且自带 sha256，attestation 要的是能被同状态重跑 git 复算的独立指纹。**真实链路验证**：新建两个 E2E 项目跑真实 deepseek 规划器——浅出口需求得 `source_list/仅出来源清单/pending=false`（真实数字员工执行至 completed，右轨落 3 个工件 + 声明式交付物），深出口需求得 `final_report/成稿/pending=true` 且带 open plan_review；浏览器 3100 确认单头分别渲染「收口 · 仅出来源清单」与「拟收口 · 成稿」（含 title 提示）。git 证据侧另建 git 绑定项目跑真实 provider：`provider_start` 落 branch=main/base=main/head=efbf80f 且 diff 为空（干净基线），`provider_terminal` 落 diff_sha256=93e77977…，**在工作区独立重跑 `git diff HEAD | shasum -a 256` 与落库值逐字符一致**。**顺带收编并修复了工作树里一份无人接手的队列球权改动**（人类确认已无并发会话）：该改动把项目队列「当前处理者」列从"任务上粘着的 assignee"改成**球权语义**（`ProjectQueueHandlerMode` = executor / waiting_review / pending_dispatch / idle，`selectCurrentTask` 不再从终态任务里取历史 assignee，归档项目显示 `—`），产品代码与其单测自洽，但 `index.test.tsx > project-first master-detail triage queue` 这条集成断言仍编码旧语义，导致 `verify:web` 一直红。修法**不是**改回旧行为：①`getByText("负责人甲")` 因等待审核态处理者列也带负责人名而 strict-mode 命中两处——给负责人 span 补 `data-testid="project-queue-owner"` 并改为按列断言（断言意图本就是"负责人列展示负责人名"，就该锚定到列；该组件的标题/待办/摘要/处理者列早已各有 testid，负责人是唯一漏的）；②`验收执行员工`/`运维检索员工` 两条断言直接与新语义冲突（project-1 停在人工确认→球在负责人，project-2 只剩失败的历史任务→无在办），改成正向锁定新规则：队列含「等待审核 · 负责人甲」与「无在办」，且**不含**这两个已停摆的执行 DE。另清掉其 `project-risk-home.tsx:377` 的行尾空格。**真实链路复核**：浏览器 `:3100/projects` 对真实 CP 数据确认四态齐活——有待决的两个 E2E 项目「等待审核 · 开发管理员」、归档项目「—」、无在办的 running 项目「无在办」。门禁：`verify:contracts` / `verify:control-plane` / `verify:runtime-agent` / `verify:web` **四项全过 exit=0**（web 134 文件 1083 用例全通过），`git diff --check` 干净。
- 2026-07-31 16:59（CORS 白名单从代码硬编码改为配置驱动 + prod 安全默认收紧）：起因是人类反问「为什么把前端端口写死在 `cors.go` 里」。核对后确认**不是**「不支持多实例」的问题（十个前端副本挂同一域名仍只有一个 origin），但确有三条真缺陷：①**开发默认值编进生产二进制且无法移除**——原实现把 `127.0.0.1:3100`/`localhost:3100` 无条件写进列表、env 只能追加不能删，叠加 `AllowCredentials: true`，等于生产 CP 永久接受来自开发机 origin 的带凭据请求；②**绕过平台自己的配置体系**——中间件里直接 `os.Getenv`，而其余配置都走 `internal/config`（yaml+env+校验），运维读 `config.yaml` 看不出放行了谁；③**无环境概念、失败模式差**——漏配时不是启动报错，而是让用户在浏览器里撞 CORS 错误再反推。改法：`Config` 新增 `environment`(dev|prod) 与 `http.allowedOrigins`，`ResolvedAllowedOrigins()` 规则为「显式配置优先且**不再叠加任何 localhost**；dev 留空才回落本地默认；prod 留空则 `validate()` 拦成**启动即失败**并指明该配哪个键」；`middleware.CORS(origins)` 改为纯注入，不再读 env、不再内置端口，由 `api.Server.SetAllowedOrigins` 从 app 层接线。**过程中发现并修掉一个更危险的点**：`go-chi/cors` 把「AllowedOrigins 为空」解释为通配 `*`，即我原本以为的「留空=拒绝」实际是最宽松策略——已改用 `AllowOriginFunc` 显式判定，并加了会真失败的护栏测试（临时换回空列表实证：`"http://127.0.0.1:3100" 却拿到了 "*"`）。验证：`verify:control-plane` 全过；真实二进制实证四项——dev 预检矩阵（3100 两来源放行 / 3000 与 evil.example.com 拒绝）、显式配 `console.example.com` 后 localhost 被拒（证明不再叠加）、`CONTROL_PLANE_ENV=prod` 漏配 origins 时进程 exit=1 并打印所需配置键、prod 配齐后起得来且 localhost 拿不到允许头。
- 2026-07-31 16:41（本地开发 Web 端口 3000 → 3100，3000 退役让给其他进程）：**端口事实源收敛到 `apps/web/vite.config.ts` 的 `DEV_SERVER_PORT`**（原来散在 package.json 的 `dev`/`preview` 两个 `--port` 里），并加 `strictPort: true`——端口被占时 vite 默认静默换一个，而 CORS 白名单与 dev-services 探活都钉死在该端口，一漂就成「页面能开、接口全 CORS 失败」的难查故障。同步四处生效配置：`middleware/cors.go` 默认允许来源（**移除 3000**，继续放行它等于给占用该端口的第三方应用发 `Access-Control-Allow-Origin`）、`app.go` 的 feishuWebOrigin 默认、`feishu-connector/main.go` 的 `CONTROL_PLANE_WEB_ORIGIN` 默认、`dev-services.sh` 的 `WEB_WAIT_URL`；另更新两个本地 e2e 辅助脚本与 `design-qa.md`。**vitest 例外**：浏览器模式复用同一段 `server` 配置，钉死+strictPort 会让「开着 dev server 跑测试」直接起不来，故 `isTest` 时不钉端口——已实测（dev server 占着 3100 时定向 vitest 10/10 通过）。验证：`dev:web` 实测绑定 127.0.0.1:3100；`dev-services status` 探活 healthy；curl 预检确认 3100 拿到 `Access-Control-Allow-Origin`、3000 拿不到；浏览器 `localhost:3100/inbox` 仍正确跳 `127.0.0.1:3100`，一单卷宗页在默认 CORS 下真实渲染（时间线+右轨三槽）；`verify:contracts`/`verify:control-plane` 全过，web typecheck+build 通过，测试 1080 passed（唯一失败为并发会话在改的队列球权语义，与本次无关）。未改的 3000 残留只剩测试内的任意端口字面量（`canonical-local-dev-host.test.ts` 测的是 hostname 改写、飞书用例用虚构域 `web.local:3000`、logger_test 的 Referer 样例），与生效配置无关。
- 2026-07-31 16:18（一单卷宗 Demand Dossier，spec `2026-07-29-demand-workbench-design.md` R2 全量落地）：项目详情需求区从「状态快照」升级为**这一单的处所**——中栏协调时间线讲发生了什么、右轨按剧本 produces kind 分槽讲交出了什么。**新增只读聚合** `GET /api/v1/project-demands/{demandId}/dossier`（零迁移：证据/工件按任务过滤走新 sqlc + 迁移 015 就有的 `idx_project_*_refs_tenant_task`；交接判定复用既有纯函数 `buildProjectTaskGraphHandoffAssessments`，不建全图；剧本 produces kind 经既有 `ScenarioTemplateResolver` 加一个方法拿，解析仍留在 scenariotemplate 包内）。**事件叙事归一表放服务端**（`internal/project/event_narrative.go`）成为唯一词表源，需求卷宗与项目级事件流共用：此前 `opsEventTitle` 只映射 8 个事件类型、default 直接把 `event.event_type` 吐给用户，而 `ProjectEventType` 有约 60 个常量——`project_task.dispatch_gate.replan_required` 这类英文蛇形串一直在现网页面上；护栏是一条**扫 types.go 源码取全部常量**的表驱动测试（新增事件类型漏登记即失败，且标题含 `.`/`_`/小写英文即失败，两条均经临时改坏实证会挂）。**范围上刻意砍掉四项**：disabled「继续此任务」入口（#2 未排期时那是点不动的承诺按钮，挂点留在契约与组件边界）、`include_graph`、服务端硬判 density（改为服务端只回 signals、密度由前端算并允许用户切换记 localStorage）、`name_directory`。时间线**显式自证是叙事不是审计**（常显说明 + 深链既有执行轨迹），因为事件是尽力而为写入的、约 60 类型映射到 20 kind 必有落 `other` 的。**真实 E2E 跑通完整闭环**：提交需求→计划确认卡→人类批准→真实数字员工执行→任务完成→需求 completed，卷宗全程正确投影；并**揪出 3 个单测全绿时看不见的集成缺陷**：①活动 SSE 只 invalidate 图与 launch-detail、卷宗只能等 30s 轮询才追上（已补 key + 加了摘掉修复就会挂的回归测试）；②现网 `decision.requested` 不带 resource_*、身份只在 payload 的 `plan_revision_id` 里，只认 resource_* 会让「待计划确认」退回成通用「待人工决策」（已补 payload 外键兜底）；③`project_coordinator` 类 actor 的 id 是 workflow 标识、匹配不到成员导致无处理人（已补角色化中文回落）。收件箱的关联需求引用在自带 `source_project_id` 时改指 canonical URL，`/workflows/{id}` 重定向壳保留兜底旧数据与飞书历史卡片。门禁：`verify:contracts` / `verify:control-plane` 全过；`verify:web` 1080 passed，唯一失败 `index.test.tsx > project-first master-detail triage queue` 属并发会话正在改的队列球权语义（工作树 `project-risk*` 为其未提交改动），与本次无关、未代改。
- 2026-07-29 16:24（接 code review：修 34c27918 引入的验收回滚死锁 + 统一终态口径）：**①〔严重〕验收回滚后重试永久 409（我自己上一批引入的回归）**——`UpdateProjectTaskStatus` 终态分支会清空 `waiting_reason`/`waiting_request_id`，而验收写回的**补偿动作** `restoreProjectTaskHumanWaitState` 只把 status 退回 `waiting_human`、不还原指针。于是：人类点验收通过 → 写回事务提交 → 后续**非同事务**的 `RecordProjectTaskResult`/`LinkProjectTaskResultDecisionRequest` 任一步失败 → 任务退回 `waiting_human` 但 `waiting_reason` 已空 → 重试撞上 approve 守卫（`waiting_reason` 必须是 `acceptance_required`）**永久 ErrProjectConflict**，任务卡死、只能取消或标失败。改这批之前重试是好的，是我把"可恢复失败"变成了"不可恢复"。修法：新增 `RestoreProjectTaskHumanWait` 查询/仓储方法，补偿动作从写回前的内存快照还原两列（补偿动作必须还原它清掉的每一样东西）；顺带收紧"别人抢先补偿成功"的短路判断——原来只看 status，会把"退回了但指针为空"当成功放过去，现在要求 `waiting_reason` 也一致。**②〔轻〕终态集不一致**——`GetProjectTaskStatusCounts` 与新加的等待指针 CASE 用的是三值集（completed/failed/cancelled），而同一批的 `isTerminalProjectTaskStatus`、`CancelProjectTasksForDelete` 与回填迁移都按五值集（含 done/success）；若真出现这两种拼法，任务会被永久算作「执行中」、破坏 `total = active+completed+failed+cancelled` 恒等式、并重新积累刚清掉的粘性指针。两处 SQL 已统一到五值集（与文件里既有 F5 判据同集）。**验证**：新增/加强两条 Go 用例（回滚后指针必须还原 + 故障恢复后重试必须成功），**经临时把还原参数改回 nil 实证两条都会失败**（报错正是"回滚后重试必须能成功，而不是被 approve 守卫永久拒绝"）；**并修正了测试夹具**——`memoryRepository.UpdateProjectTaskStatus` 原先不模拟终态清指针，与真实 SQL 不同口径，这个回归在夹具里根本复现不出来，现已对齐。`verify:control-plane` exit 0；`make migrate-validate`（自建干净库）exit 0；重启 CP 后 14/14 项目 API 与 PG 直查七桶逐项对拍全一致。**踩坑记一笔**：本次误把 dev-url 指到容器里的 `postgres` 库（他人重放过、非空），atlas 直接拒绝 "connected database is not clean"——正是 Makefile 注释里刚写下的那条，自己第一时间就踩了。
- 2026-07-29 16:04（补跑 make migrate-validate：撤回 15:22/15:49 两批"迁移门禁阻塞"的说法）：用户起了本地 PG 容器（`superteam-migrate-validate-pg`，127.0.0.1:55432）后补齐。**结果**：`make migrate-validate DEV_URL=…` **exit 0**；另在全新空库上 `make migrate-up` 从零重放**116 个迁移 / 2326 条语句全过**（含本次的 `20260729071624_clear_terminal_task_waiting_pointer`，日志可见该 UPDATE 执行 ok），`migrate-status` 报 `Current Version: 20260729071624 / Pending Files: 0`。**因此**：前两批 commit message 与 CHANGELOG 里"dev-url 重放没能跑、只做了不带 dev-url 的校验和检查"这一边界现已解除，迁移的从零重放已真实验证。用后即删了自建的两个临时库，容器内他人的 `atlas_dev`/`dev`/`superteam_task6_validate_*` 未动。**顺带在 Makefile 记下踩坑**：本机 `docker` 是 podman 的 shell alias，atlas 直接 exec `docker` 可执行文件，alias 对它无效，所以默认 `docker://postgres/16/dev` 会报 executable not found；且 dev-url 指向的库会被 atlas clean，多会话共用一个库会互相清掉，必须各建各的空库。
- 2026-07-29 15:49（P1b HTTP 全链补证 + Track B 概览任务列表退役 + 生成物新鲜度门禁）：清掉上一批留下的三项。①**P1b HTTP 全链**：上一批只做到 SQL 级验证。这次经 HTTP 自建项目（`POST /api/v1/projects`，不碰他人 fixture），直插一条 waiting_human/acceptance_required 任务 + pending 的 `project_task_acceptance` 决策，再走真实 `POST /projects/{id}/decisions/{id}/resolve` 批准——**HTTP 200，任务落 completed 且两列归 NULL**，事件链 `decision.submitted → project_task.completed → workflow.signaled`（两条协调信号均成功）；验证项目已按产品自身删除语义软删。②**Track B 退役 `ProjectOverview.active_tasks`**：勘察确认它与 `GET /projects/{id}/tasks` 是**同一条** `ListProjectTasks(limit 20, updated_at DESC)`，Web 三处消费（`selectDispatchGateTask` 候选、ops-home `[...active_tasks, ...tasks]` 合并、任务弹层查找兜底）全是重复来源。直接删字段而非改名：契约移除该数组、Go 域/响应类型移除 `ActiveTasks`，**`GetOverview` 连带省掉这次任务查询**（计数已走聚合），Web 三处收敛到单一 `tasks`。夹具连带修正：`index.test.tsx` 的 stub 过去让概览与 `/tasks` 返回**不同**任务（真实后端不可能出现的分叉），现统一由 `/tasks` 给全。③**生成物新鲜度门禁**：`verify:contracts` 过去只比对路由与客户端路径，56b39666/6a531a86 那次"改了契约没跑生成器"（`control_plane.gen.go` 落后约 290 行）一路绿灯。新增 `assertGeneratedGoIsFresh`——契约 `components.schemas` 里每个 schema 必须能在生成物里找到同名 Go 类型；纯 Node 实现，不给 contracts 门禁引入 Go 工具链依赖。**边界写在函数注释里**：只覆盖"新增 schema 未重新生成"，既有 schema 内部字段/枚举改动检测不到，彻底做法是重跑 oapi-codegen 后 diff。**验证**：**真实链路**——重启 CP 后 curl 概览确认响应键只剩 7 个、`active_tasks` 数组已消失、`task_summary` 七桶齐全；浏览器复看项目详情（头部执行中 0 / 阶段格「执行中 0 · 共 2 项」/ 工作台与脉搏正常渲染）+ 任务 tab 点开任务详情弹层正常（弹层第三条兜底已删）。门禁本身经**两轮实证会失败**：手工摘掉生成物里一个类型 → 报错并点名；把 904ef68e 的旧生成物放回 → 精确列出 `FeishuChannelAppStatus`/`FeishuChannelHealth`/`ConnectorHeartbeatRequest` 等历史漂移 schema。`verify:control-plane` + `verify:contracts` + `verify:web`（133 文件 / 1054 用例）全绿。
- 2026-07-29 15:22（粘性等待指针写侧清列 + 存量回填：一-P1a/P1b/P2 三步走完）：接 15:01 的读侧守卫，按"先拆依赖再清列"的顺序把 `waiting_reason`/`waiting_request_id` 收敛为单一含义。**P1a 拆承重消费者**：结项摘要 `buildFinalDemandSummaryPayload` 原来用任务上的粘性 `WaitingRequestID` 产出 `human_decision_refs` 的 `task_waiting_request` 条目——DB 实证 5 条 cancelled 任务的决策 ref 只有这一个载体（结果侧 `decision_request_id` 为空），盲清会丢人类决策溯源。改为从权威表 `project_decision_requests` 取（新增 `demandSummaryDecisionsByTask` 一次批量查询按任务归集，配额按任务数估算并设上限，不发 N 次），来源标签 `task_decision`；结果关联的决策先登记以保留更具体的 `task_result_decision` 标签。执行上下文包早已是这个模式（`service.go` 按 task 过滤 `ListDecisionRequests`），本次只是让摘要跟上。**P1b 补写侧**：`UpdateProjectTaskStatus` 的终态分支（与既有 `terminal_event_id` CASE 同形）清掉两列——四条"回活跃"查询一直清、唯独终态不清是历史缺口；`CancelProjectTasksForDelete`（项目软删级联）同口径补上。**P2 回填**：迁移 `20260729071624_clear_terminal_task_waiting_pointer` 清存量。**验证**：**真实链路**——`dev-services restart control-plane` 真实跑 atlas 迁移，终态残留 **7→0**，而唯一一条非终态 waiting_human 任务的指针**未被误清**（仍为 1），迁移已入 `atlas_schema_revisions`；把已完成锚点任务可逆置回 waiting_human 后，用**生成物里逐字取出的 UpdateProjectTaskStatus 语句**在真实 PG 上跑两个分支——目标 `queued` 保留指针、目标 `completed` 两列归 NULL，夹具自然复位到 `completed / NULL / NULL`；浏览器复看锚点项目横幅无、执行中 0、节点已完成。`verify:control-plane` + `verify:contracts` 全绿，projects+flow-graph 定向 238 用例全绿；新增 Go 用例钉住"任务没有粘性指针但有决策请求时溯源仍完整"，并修正一条原先靠粘性列断言的旧用例（夹具补建真实决策行）。**边界（如实报告）**：`make migrate-validate` 的 dev-url 重放**没能跑**——本机无 docker、无本地 postgres，远端 dev 库账号无 CREATE DATABASE 权限；只跑了不带 dev-url 的 `atlas migrate validate`（校验和/可解析，exit 0）+ 真实 apply。刻意没有把共享 dev 库当 atlas dev-url（会被 clean）。P1b 的终态写路径也只做到 SQL 级真实验证，未经 HTTP 验收接口全链走一遍（唯一可达路径需改动他人 fixture 项目的业务状态）。
- 2026-07-29 15:01（评审接单：闸门横幅改读闸门记录 + 终态任务不再带粘性等待指针）：①**修复 441dec04 引入的漏报**——评审指出闸门事件按 (任务, 事件类型) 至多发一次（`predispatch_gate.go` 的 `ProjectTaskEventExists` 去重），任务重试后二次卡人工不会再产生 `waiting_human` 事件，那版"取最新闸门生命周期事件"的判据会永远只看到 `dispatched`，横幅再也不亮。DB 实证：任务 `ba07d830` 有 5 条闸门记录却只有 1 条 waiting_human 事件。改为读闸门记录本身——闸门结果按 (任务, 分派原因+尝试序号) 唯一，重评估必新增/更新行，最新一行即当前裁决。CP 新增 `ListProjectTaskLatestDispatchGates`（DISTINCT ON 每任务最新一条，一次批量查询不 N+1），task-graph 响应新增 `dispatch_gates[]`（契约同步，附"别用闸门事件推断"的警示）；Web 横幅判据变为"最新裁决非 passed **且**任务非终态"，`ProjectDemandsSection` 的 `events` prop 随之退役，pill 也从裸事件类型改为中文状态词。②**一-P0 读侧守卫**：`classifyProjectTaskLiveness` 的终态分支清掉 `WaitingRequestID`——该列是粘性列（终态时 `UpdateProjectTaskStatus` 不清，只有四条"回活跃"查询才清），否则 liveness 会投影出 `is_terminal=true` 却又带着"在等某个决策"的自相矛盾状态；顺带把该函数原地另写的终态 case 收敛到既有 `isTerminalProjectTaskStatus`（原版漏了 done/success 两种拼法）。写侧清列未做，需先拆掉结项摘要 `buildFinalDemandSummaryPayload` 对该列的依赖——DB 实证 5 条 cancelled 任务的决策 ref 只有粘性列这一个载体（`ref_also_on_result=f`），盲清会丢人类决策溯源。**验证**：**真实链路**——按评审场景造可逆夹具（新增更晚的 waiting_human 闸门记录 + 任务回 planned，而事件流最后一条仍是 `project_task.dispatched`），API `dispatch_gates` 返回 waiting_human，浏览器实测横幅**如期亮起**（这正是上一版会漏报的场景）；夹具删净后横幅消失、执行中 0、节点已完成，二次复核归位。`verify:control-plane` + `verify:contracts` 全绿；新增 Go 用例（终态五种拼法均不带等待指针 + 反向钉住 waiting_human 仍带）与 Web 用例（闸门 waiting_human 亮/passed 灭/任务终态灭/事件说 dispatched 但闸门说 waiting_human 仍亮）；projects+flow-graph 定向 238 用例全绿。Go 侧新守卫经临时删除断言实证会失败。
- 2026-07-29 14:38（项目概览 task_summary 三缺陷：契约违约 + 分页片统计 + cancelled 误算 active）：接 10:14 那批的遗留分析 Track A。查证发现问题比"命名不当"严重得多。①**契约违约（最致命）**：`ProjectStatusSummary` / `ProjectTaskSummary` 内嵌进 `projectOverviewResponse` 序列化但**没有 json tag**，API 实际吐 `{"ActiveTasks":0,"CurrentPhase":"archived"}`，而 openapi 声明的是 snake_case——前端按契约取键全是 undefined，**`task_summary` 从来就没被前端读到过**（这也是计数错了长期没人发现的原因）。补 tag 到位。②**计数在分页片上统计**：`GetOverview` 用 `ListProjectTasks(limit 20, ORDER BY updated_at DESC)` 取任务页后**循环这 20 行数数**，任务超 20 条即漏计，且窗口随更新漂移会让计数非单调抖动（本地最大项目已 18 条任务，一步之遥）。改为新 sqlc 查询 `GetProjectTaskStatusCounts` 全表聚合（分桶口径抄 `ListProjectRunSummaries` 先例，排除 dismissed 与任务页窗口对齐）。③**cancelled 被算进 active**：旧 switch 的 default 分支吞了 cancelled，终态任务永久贡献"进行中"。新 SQL 的 active 口径 = 非终态，与 project.sql 各处 F5 判据同源。**契约**：`ProjectTaskSummary` 增 `running_tasks`/`cancelled_tasks`/`total_tasks`（`total = active+completed+failed+cancelled`），并给 `ProjectOverview.active_tasks` 写上"未过滤的前 20 条任务页、计数请用 task_summary"的警示（改名属 Track B 待偿）。**Web**：项目头「执行中」与执行阶段格「执行中 N · 共 M 项」双双改吃服务端聚合（overview 未到达时才按已加载任务兜底），一并消除 10:14 那版前端自算残留的 20 条天花板。**验证**：**真实链路**——重启 CP 后逐项目 curl 对拍 PG 直查，14/14 项目七个桶全字节一致（含一个含 cancelled 的项目，旧口径会把它算成 active）；可逆夹具把归档项目推到 25 条任务→API `total_tasks=25` 而 `active_tasks` 数组仍 20，浏览器实测头部「执行中 3」与阶段格「执行中 3 · 共 25 项」一致且总数为 25（旧实现只会显示 20），夹具已删净并二次复核归位。`verify:control-plane` + `verify:contracts` 全绿；新增 CP 用例（25 条任务跨页计数 + 桶恒等式）与 Web 用例（计数取自 summary 而非任务页）。**注意**：`verify:web` 有 1 例失败在 `features/inbox/index.test.tsx`，属另一会话正在改的收件箱 primary_demand 排序工作（其 `inbox-item-list.tsx`/`inbox-shell.tsx`/该测试文件均为其未提交改动），与本次无关——本次涉及面 `src/features/projects` + `src/features/flow-graph` 定向 20 文件 237 用例全绿。**另**：本次 `generate:control-plane` 顺带修复了 HEAD 既有的生成物漂移——`control_plane.gen.go` 落后于已提交的 openapi.yaml（缺 FeishuChannelAppStatus/channel_alert 等，约 290 行），系 56b39666/6a531a86 提交时漏跑生成器；`verify:contracts` 不检查生成物新鲜度，护栏有缺口。
- 2026-07-29 11:45（结项确认卡把已取消/失败需求说成"已完成"修复）：用户实证——飞书结项卡的需求清单里混进一条 07-24 E2E 夹具需求「also_close E2E：输出一行中文问候」，摘要句却统一缀"已完成"。DB 直查证实该需求真实存在且状态为 `cancelled`（项目 `e3f8a1c5` 被复用提了新需求）。两处缺陷：①`projectDemandTerminalStatus` 把 completed/failed/cancelled 一律当"终态需求"喂给 `BuildProjectAcceptancePresentation`，后者对全集统一缀"已完成"——把取消/失败的需求叙述成完成，是事实性错误；②飞书卡「需求清单」与 Web 收件箱「关联需求」都只渲染 title，丢掉 context 里已有的 `status`，取消项与完成项无法区分。修法：清单仍保留全部终态需求（结项要看全貌），但摘要主句只覆盖 `completed`，非完成项单列为「需求「X」已取消/失败，未计入本次结项」；`primary_demand_id` 改为优先指向已完成需求（原取最新终态，会指到刚取消的那条）；context 每条 demand 增补 `status_label`；飞书卡每行缀「· 状态」（缺 `status_label` 的老快照按 `status` 兜底），Web 关联引用缀「（状态）」走 `demandStatusLabel`；零终态需求的兜底文案由"全部需求已完成"改为"已达结项条件"（同类无据断言）。**验证**：**真实链路**——新建项目 `8c707939` 造混合终态（需求 A 真跑数字员工至 completed、需求 B 经 `/close` 转 cancelled），走真实计划审批→执行→结项闸门开卡：收件箱 context 两条 demand 各带正确 `status_label`、`primary_demand_id` 指向 completed 项、摘要句如期分列；飞书 outbox `decision_card` 与结项后 `card_update` 均 sent 且载荷正确；浏览器（真实 Web + CP）确认列表摘要与详情「关联需求 · …（已完成）/（已取消）」；点「确认结项并归档」项目转 archived 闭环。门禁 `verify:control-plane`、`verify:web` 全绿，新增 3 条 Go 用例 + 1 条飞书卡用例。**评审跟进（14:40）**：`/code-review` 揪出并已修两处——①**摘要 400 runes 截断会把取消项披露整段吃掉**（实测 10 条已完成需求即触发，摘要退回"只叙述完成"，正是本次要治的缺陷的大项目版）：拆成 prefix/completed/unfinished/tail 四段，`composeAcceptanceSummary` 只把预算花在已完成枚举上，未完成子句与结尾恒保留；②**前端 `refs[0]` 与服务端 `primary_demand_id` 口径不一致**（服务端按 updated_at 倒序，若某需求最后被取消则 headline 会挂到它上面）：`readDemandRefs` 把 primary 提到首位。两条新增用例均做过反向验证（临时还原旧逻辑确认会失败）。评审第三条（`project-demands-section` 派发闸事件去重导致横幅不复现）属另一会话未提交改动，未代改，已转述。**未验证边界**：「非完成需求最后转终态」的卡片形态当前 API 走不出来（`CloseProjectDemand` 不触发结项闸门，只有签署不通过路径可达），②的真实链路实例暂无从观测，仅有组件级真渲染用例覆盖。
- 2026-07-29 10:14（前端粘性/历史投影三处误报"待人工"修复）：用户实证——已完成任务节点常挂「需要人工审批」、需求区常挂「等待负责人确认」红条、全完成项目头显示「执行中 2」，而收件箱为空。DB 直查证明**不是孤儿数据**（4 条 decision_requests 全 approved+resolved、4 条 inbox_items 全 resolved、闸门事件 `waiting_human`(seq36)→`checked`(seq40)→`dispatched`(seq41) 已闭合），根因是三处前端把历史/粘性事实渲染成当前状态。①**粘性 `requires_human_approval`**：该列终态后仍为 true（CP 侧 `project.sql` F5 早有 `status NOT IN (...)` 护栏，前端未跟上）。新增单一出口 `@/lib/task-status`（`isTerminalTaskStatus`/`isAwaitingHumanApproval`，终态集与 F5 SQL 同口径），四处消费点改走它：活图节点 adapter、任务详情弹层、计划任务图、`project-risk`（后者原会让已完成项目永久报 `human_decision` danger 风险 + 把完成任务归给人类负责人）。②**派发闸横幅只按类型优先级取一条历史事件**：`currentDemandDispatchBlocker` 改为按载体（任务，无 task id 时退需求）取 `sequence_number` 最大的一条闸门生命周期事件，仅当它仍是 blocker 类**且**对应任务非终态才渲染；新增解除类判据 `dispatch_gate.checked` / `project_task.dispatched`（events 分页窗口截断时由终态判据兜底）。③**「执行中」计数取了任务总数**：CP `GetOverview` 的 `active_tasks` 实为**未过滤**任务列表（名不副实，真值在同响应 `task_summary.active_tasks`），项目头直接取长度；改为与下方执行阶段格同判据（running/waiting_human，排除 dismissed）自算，两处数字一致。**验证**：**真实链路**——浏览器登录后打开真实项目 `6e45fb1a`（炬枢平台）实测三处全部归位（横幅 null、执行中 0、节点无审批 pill、任务弹层无「需人工审批」）；正向路径用可逆 DB 夹具（任务临时改 waiting_human + 追加一条更高 seq 的 waiting_human 闸门事件）实测横幅/pill/执行中 1 全部**如期重现**，夹具已完整回滚并二次浏览器复核。新增 8 条回归用例，其中 4 条经"临时还原旧逻辑"实证会失败（非空跑绿）；`corepack pnpm verify:web` 全绿（133 文件 / 1051 用例 + typecheck + build + 体积护栏 98.0KB/150KB）。**未改**：终态任务残留的 `waiting_reason='acceptance_required'` / `waiting_request_id`（前端不读、CP F5 判据走 status，暂无影响面），以及 `overview.active_tasks` 的服务端命名。
- 2026-07-27 02:23（修复 verify:design-prototypes 断链）：v3 迁移(68a72c23) 删除 docs/prototypes/design-system/ 后 package.json 仍指向其中的 verify-prototypes.mjs，门禁永久 MODULE_NOT_FOUND。恢复脚本到 docs/prototypes/verify-prototypes.mjs，递归扫描全部 HTML 原型；布局严检仅对显式目录（flow-live-graph 或带 .prototype-strict-gate 标记的目录）生效，历史概念稿只做 HTTP+console error+pageerror 硬检（--strict-all 可全开）；截图输出 docs/prototypes/__screenshots__/ 已 gitignore。验证：corepack pnpm verify:design-prototypes 全绿（86 原型 × 2 视口 = 172 检查，其中 4 项 strict-layout）。
- 2026-07-27 02:05（飞书接入管理 P0：系统配置「消息通道」+ 保存时连通自检 + 服务凭据面板）：按 spec `2026-07-27-feishu-channel-access-management.md` §4。**CP**：①`UpsertAppConfig` 保存前真实探测飞书——tenant_access_token / 通讯录 batch_get_id / bot info，返回中文 `hint` 结构化报告；失败仍落库但 status=`unverified`，通过则 `active`。②管理列表改列全部状态(含 unverified/disabled)；新增 `POST …/app-configs/verify`(只测不存)、`PATCH …/app-configs/{id}/status`(停用/再启用，再启用最多回 unverified 需重检)。③`GET /admin/service-tokens` 列出租户凭据(status/created_at/last_used_at/revoked_at，无明文无哈希)。契约+sqlc 同步。**Web**：系统配置顶层新增「消息通道」tab——飞书通道表(状态徽章/轮换 Secret/停用)、保存并自检对话框(逐项探针结果)、Connector 服务凭据签发/吊销与「明文只显示一次」复制条+部署路径提示(`FEISHU_CONNECTOR_TOKEN` / dev token 文件)。验证：`go test` feishu/serviceauth/api 全绿、`verify:contracts`、web typecheck 过；**真实链路**——错 Secret 保存→status=unverified+中文「app secret invalid」提示；verify-only 错 Secret 不改变既有行之外的语义；签发新 token→bootstrap 200、吊销后同 token bootstrap 401、原 token 仍 200；浏览器 `/system-config` 消息通道 tab/未验证徽章/签发按钮/轮换对话框字段全在。**注意**：G1 负向用例曾用同 App ID 写入错 Secret 覆盖库内密文，dev 配置现为 unverified、bootstrap configs 为空；需在消息通道 UI 填回开放平台正确 App Secret 并保存自检通过后恢复。P1 可观测/outbox 运营面未做。
- 2026-07-27 01:53（项目详情去重：撤高级事实冗余执行图 + 任务行补所属需求深链）：IA 重构后项目详情「需求流程」与「任务」两 tab 视角轴不同但存在真重复与缝合缺口。①高级项目事实区「当前执行图」（仅覆盖最新需求的 FlowGraphCanvas）已被需求流程区（可切需求+血缘+活图+回放）完全取代，整块撤除，原位留「执行图已迁入需求流程区 →」深链（?tab=demands）；?tab=trace 轨迹面板定位逻辑未误伤。②任务 tab 补「所属需求」列：按 demand_id 补名显示需求标题（缺标题回退 8 位短 id mono，无归属显示 —），点击深链 .?tab=demands&demand= 到需求流程区对应需求——两视图用导航缝合而非合并。验证：**真实链路**——浏览器实测高级事实区「当前执行图」归零、深链 href=?tab=demands；任务 tab 三行样本均显需求标题且深链带 demand id。定向 26 用例 + typecheck + verify:web 全量全绿。
- 2026-07-27 01:42（安全:用户治理变更端点补管理员判权——回顾自审揪出并实测复现的越权洞）:任务回顾期以 member 身份实测:`PATCH /api/auth/users/{id}/status|contact` 与 `POST …/reset-password` **只验会话不判权**——普通成员真实把 admin 账号禁用成功(200,已即时恢复)、可改任何人联系方式、可重置任何人密码。status/reset-password 为既有洞(创建/成员管理一直有 `ActionUserProjectTeamScopeManage` 判权,这三个变更端点漏了),contact 是 00:54 照既有模式新增时被一并带入。修复:三个 handler 补 `authorizeUserProjectTeamScope(ActionUserProjectTeamScopeManage)`(与创建/成员管理同一动作、同一审计路径);新增测试钉住三端点拒绝时 403 且零副作用、允许时 200。验证:`go test` auth/api 全绿;**真实链路**重启 CP 后 member 三端点全 403、admin 正常 200、admin 状态与密码未受影响。
- 2026-07-27 01:35（飞书双人 any-of-N 真实 E2E 全通——第二真人账号长期阻塞项解除）：接续 00:54 手机号腿。**接入排障两发现**(探针=connector bootstrap 取凭据直打 batch_get_id):①通讯录授权范围未含新成员→查询 code 0 但无 user_id(历史上通讯录同步从未正向命中的根因);②应用可用范围未含→发卡 `code=230013 Bot has NO availability`,outbox 3 次重试落 failed。用户在开放平台改两范围并发布后:探针命中 `ou_e1bc…`→真实 contact-sync 正向绑定(`bound_via=contact_sync`);failed outbox 4 行 DB 重置 pending(预演 spec §5.3 重推按钮语义)→connector 自动重投全部 sent。**双人 E2E(双负责人项目 0225c673,纯飞书驱动、Console 零点击)**:决策卡对两绑定负责人双扇出;计划确认=admin 第一手机批;需求验收=卡内签署;**结项确认=第二真人第二手机批准,归因 feishu-second**(any-of-N 非主负责人先到生效)→双方卡 card_update 同秒转终态→项目 archived;Console 收件箱 3/3 同步 resolved(H4 双向闭环)。原 TODO 飞书 7 项中 5 项就此验证完成(结果结论卡/卡内签署/any-of-N 双人/投影不阻塞——failed 期间业务全程未被阻断/通讯录反查),TODO 收敛为 3 残项(换绑 UI/重推按钮/越权对照)。
- 2026-07-27 01:31（活图 P2 批 2：SSE 驱动替代轮询 + 回放执行时序——spec §3 四项后续全部清完）：①**P2-E SSE**：复用既有跨员工活动流 `/digital-employees/activity/stream`（事件项带 project_id，不自造通道），新 hook `use-project-activity-invalidate` 按项目过滤事件、leading+trailing 1s 节流合并 invalidate（project-task-graph 前缀族+workflow-detail）；graph 轮询 5s→30s 保底（注释说明 SSE 不可用不回升的取舍）。②**P2-R 回放**：`flow-replay.ts` 纯函数时间轴（runs 投影优先回退节点时间，真实时长线性压缩 8 秒窗口，全图无时间数据按钮禁用并 title 说明），虚拟状态 queued→running→**真实终态**（词表既有键，不发明剧情）；虚拟图直接喂 `buildFlowGraphElements`——边活性/reduced-motion/大图降级分支零复制天然生效；回放持快照隔离 SSE/轮询新数据（结束即应用最新），进度标注「T+X 秒 / 8 秒 · 压缩自 Y」延续时间单位口径。已知局限（如实报告）：回放中边文字标签仍显真实状态（虚拟化需发明边状态语义，刻意不做）、节点弹层数据保持实时。验证：**真实链路**——真实 colors 双任务项目浏览器点「回放执行」：进度实测「T+1.4 秒 / 8 秒 · 压缩自 4 分 45 秒」、节点走排队中→运行中虚拟时序、8 秒自动回实时双节点落真实「已完成」、二次点击即停；SSE 实测页面建立 activity/stream EventSource 连接 + task-graph 请求实测 30s 间隔（事件驱动 invalidate 属组件级测试覆盖，真实触发需真实运行事件）。新增测试 19 用例；verify:web 全量 exit 0。测试基建发现（备忘）：vitest browser 单用例内手动 unmount+二次 render 触发 overlapping act() 连坐后续用例超时。
- 2026-07-27 01:01（活图 P2 批 1：结构化交接 verdict + 大图性能分层）：按 phase3 spec §5。①**P2-V 交接 verdict（CP 读路径，零新端点/零持久化/零 sqlc 变更）**：task-graph 响应新增 `handoff_assessments[]`——勘察发现声明交付物真身在 `ProjectTaskResult.Contract`（v2 Ref 回填管道）且既有 `ListProjectTaskResults` 查询即可取到（原设想的降级路径未触发，无数据缺口）；纯函数 `buildProjectTaskGraphHandoffAssessments`：声明集=契约 deliverables ∪ planner produces 去重，delivered 判据=Ref 已回填或 Value 非空（与 `validateCompletedTaskResult` 平台自身核对判据一致，非启发式），无契约/零声明→unknown 不预判；汇总 fulfilled/partial/unfulfilled/unknown。浮层右列新增「声明交付物核对」逐条徽章（已交付/缺失），有 verdict 时底注改「符合性由控制平面按声明交付物核对」，unknown 维持「暂无」+「不构成符合性判定」。②**P2-S 大图分层**：adapter 按最终渲染元素计数（>40 降级、40 整不降、非 live 恒不降），flowing 边自动降级为呼吸边（与 reduced-motion 同一渲染分支取或），canvas 打 `data-live-degraded="scale"/"motion"`（scale 优先）；阈值内 data 形状逐字节不变（既有精确断言零改动）。验证：**真实链路**——重启 CP 后 curl 实测 colors 双任务链返回 fulfilled/delivered、无声明失败需求返回 unknown；真实浏览器点边浮层显示「colors_json_file→deliverables/colors.json→已交付」+底注切换（V1/V2 过）；S1=组件测试 41/40 元素边界断言（spec 允许）。CP 单测 5 case + overlay 5 例 + 降级 53 例定向全过；verify:control-plane（含契约 guard）+ verify:web 全量 + typecheck 全绿。
- 2026-07-27 00:54（飞书通讯录反查手机号腿 + 管理员联系方式维护——spec §6 提前实现）：解"第二个真人飞书账号绑不上"的根因组合:国内飞书档案手机号必填、邮箱常空,而反查只有邮箱腿;且**管理员创建用户根本不收集 email、也无入口替既有用户补联系方式**(email 只能目标用户本人登录自服务填,管理员单点完成绑定无路可走)。落地:①迁移 `20260726170001` `auth_users` 加 `mobile`(可空,列注释注明含区号格式);②`BatchGetOpenIDsByEmail`→`BatchGetOpenIDs` 单请求双数组(emails+mobiles,响应按 email/mobile 双键回带);③`ContactSync` 逐用户合并判定:任一键命中即绑、**双键命中不同 open_id 计 conflicts 报出不静默绑任一边**、报告新增 conflicts 字段;④auth 域:`CreateManagedUserInput` 增 email/mobile(创建代填)+新端点 `PATCH /api/auth/users/{id}/contact`(nil=不改/空串=清除/`user.update_contact` 审计),手机号规整(去分隔留+和数字)+"非空输入规整后不合法必须报错,不得静默降级成清除"(测试先揪出该缺陷再修);⑤契约 `auth.yaml`:CreateUserRequest+UserSummary+UpdateUserContactRequest+contact 端点,oapi 重新生成;ListUsers 搜索加手机号;⑥Web:创建抽屉"邮箱(可选)/手机号(可选)"两字段、用户详情动作区新增「联系方式」弹窗(预填当前值,PATCH contact,与重置密码同形态)。测试:Go 新增 ContactSync 手机号腿+冲突用例/联系方式维护用例/BatchGetOpenIDs 双键用例,api 路由 fake 补齐;web 新增创建payload+弹窗 PATCH 断言用例(users 14/14)。验证:`go test ./...` 全绿、`verify:contracts`、`verify:web`(全量+build+体积护栏)全过;**真实链路**——重启 CP 自动迁移(列已建),curl 创建带 `+86 138-0000-0000` 入库规整为 `+8613800000000`、PATCH contact 200 双字段落库、非法手机号 400,**真实飞书 contact-sync 200**(mobiles 数组打真 API,假号正确 unmatched=2/already_bound=1/conflicts=0);真实浏览器(headless)详情「联系方式」弹窗预填→UI 提交 PATCH 200→弹窗关闭,创建抽屉双字段在。**交接**:给第二账号填真实手机号(或企业邮箱)后点「同步飞书绑定」即完成正向命中,解锁 any-of-N 双人验证。
- 2026-07-27 00:48（员工任务日历空周引导）：周视图默认当前周（周一起始），新周刚开始时日历必然空窗且无引导——真实用户困惑实证：开发-小王 14 条运行全在上周，周一凌晨打开日历一片空白，误判为任务数据丢失（排查确认后端 run-calendar API 与 run→project 归属数据完好，纯窗口口径问题；顺带澄清其看到的「写入统计报告并设为只读」任务从未执行过，仅是任务图上的负责员工指认，无 run 记录）。修法：当前周为 0 条时按需查上一周计数（同族 queryKey，点入命中缓存零重复请求），空态提示「上周有 N 条运行记录」+ 主按钮「查看上一周」；上周也无记录时保持原引导文案。真实浏览器验证：开发-小王页空态显示「上周有 14 条运行记录」→点击切至 7/20-7/26 窗口 14 条全部呈现。verify:web 全绿（131 文件/1012 测试，新增空周引导用例）。
- 2026-07-27 00:37（数据流活图 Phase 3 P1：ReactFlow 权威组件上的概念 A + 时间单位）：人类拍板选概念 A 并指出原型缺时间单位后，按 spec `2026-07-27-flow-live-graph-phase3.md` 在 `features/flow-graph/` 落地（开关式 `live` prop，仅项目需求流程区开启，其余消费方零变化）。①**活性边**：xyflow 自定义 edge（`flowLive`，BaseEdge+SMIL animateMotion 三粒错峰沿 path 流动，无 rAF 无 overlay）；activity 由 adapter 纯任务/运行状态推导（failed 红停流>cancelled 灰>上游 running 或完成交棒下游在跑=flowing>两端完成=done 静态通电），不发明新边语义；`prefers-reduced-motion` 降级为呼吸边（概念 B 兜底）。②**时间单位**：节点卡起止+耗时（Phase 1 投影字段）、运行中「已运行 X 分钟」（轮询驱动+30s 局部 tick，无整图 interval）、浮层交付时间；`formatRunDuration` 从任务弹层私有副本上移 `@/lib/format-time` 共享。③**交接对照浮层**：点边弹出——左=契约（expected_outputs/acceptance_criteria），右=execution_summaries 结论+产出引用+交付时间；无产出显式「暂无产出（上游任务尚未回写执行结论）」，底注「不构成符合性判定」（v1 不做 verdict，升级 CP 结构化 verdict 见 spec §3）；指称「任务名（员工名）」无 UUID。④需求流程区 graph query 补 5s 轮询。**真实链路验证（L1–L4 全过，真实 Web+CP+PG）**：L1/L4=直插 DB 翻任务状态，浏览器实测 5s 轮询驱动边三态切换 failed（红停流零粒子）→idle（下游 cancelled 优先规则）→flowing（3 粒子流动+节点「运行中/排队中」），夹具已复原；L2=真实失败任务节点显示「起 07/24 10:42 止 … 耗时 18 秒」；L3=点边浮层契约/实际对照+诚实暂无边界；reduced-motion=emulateMedia 实测 flowing 边零粒子+呼吸边兜底。`verify:web` 全绿（131 文件/1011 测试+build+体积护栏）。回放/SSE/大屏分层与 verdict 升级列 spec §3 后续。
- 2026-07-27 00:33（大屏项目镜头补摘要浮层）：spec §3.2 的「项目摘要浮层」在 P2 首版遗漏——无侧栏形态下项目镜头期间观众看不出聚焦的是哪个项目（G2 验收时只核了高亮与轮转，摘要浮层缺失属实施疏漏，主动披露并补齐）。项目镜头时底部卡切换为项目摘要（名称/需求/参与/交接/阻塞 + 运行带计数徽标），复用透镜与运行带既有数据零新请求；员工镜头仍显员工快照卡。真实浏览器复验：静置进入项目镜头后摘要卡显示「项目镜头 · 验收菜单复验 · 参与 1 人 · 交接 0 段 · 待人工 1」。verify:web 全绿（128 文件/985 测试，新增浮层组件 2 用例）。
- 2026-07-27 00:16（运行总览大屏模式：?mode=display KPI 带 + 三型镜头轮巡 + 底部动态流）：大屏 spec P2 完结（纯前端，零新后端）。**形态**：`/run-overview?mode=display` 去 MasterDetail 侧栏，地图全宽；顶部大字 KPI 带六格（待人工处理[inbox badge team_open_count+高危角标]/异常/运行中/今日完成运行[run-summary 租户级]/Runtime 在线[runtime overview]/今日 tokens），全部取权威端点不做前端推导，语义色仅非零/掉线点亮；底部最新动态行；工具栏刷新/暂停按钮隐藏、交互不禁用，指示器显示「镜头轮巡中 · N 个镜头」。**镜头编排**（`display-camera.ts` 纯函数+hook 分离）：员工焦点（沿用轮播紧迫度序，跟层）→ 活跃项目自动进透镜（15s，取 run-summary 运行带同源）循环；活动流新增 failed 条目即异常插队（20s 加倍停留，首批数据不插防历史失败霸屏）；任意交互暂停轮巡 45s 自动恢复；项目镜头不回写 URL（投屏地址稳定）。既有员工轮播在 display 模式整体停用（`forcePaused`），普通模式行为零变化。**真实链路验证（G1–G4 全过，真实浏览器+CP+PG）**：G1=KPI 数字与 curl 权威端点逐项一致（badge 0=0、今日完成 0=0[恰逢午夜 Asia/Shanghai 日窗重置，与 today tokens 口径一致归零]、Runtime 1/1）；G2=静置实测员工镜头 10s 后自动进项目透镜（1 高亮/15 降暗）；G3=直插真实 `run_failed` task_event → SSE 5 秒内镜头插队聚焦失败运行的员工（fixture 已清理）；G4=点击员工头像→「轮巡已暂停」且 URL 无 `?project=` 漂移→45s 后实测自动恢复轮巡。`verify:web` rebase 前后两轮全绿（127 文件/983 测试，新增镜头纯函数 4 组+display 组件 2 用例）。遗留：大屏认证长驻凭证为已立项运维项（TODO 07-26 条）。
- 2026-07-27 00:07（需求 created_at 补投影 + P2a-2 推进管道 + 活图原型三件套）：①**demand 时间投影**——`demandResponseFromDomain` 补 `created_at`/`updated_at`（domain/DB 早有值仅 HTTP 投影缺失，契约 required 声明+生成物同步）；需求切换器每行补相对时间。真实验证：重启 CP 后 curl 实测三条需求时间返回、浏览器需求列表显示「2 天前」。②**P2a-2 推进管道（TODO 07-22 清偿，IA Phase 2 收官）**：概览顶部新增「需求→计划→执行→结果」四格横向管道（ProjectStagePipeline，纯布局导航零新接口），需求/执行/结果格深链对应 tab、计划格就地展开 PlanConfirmationCard——决策历史 tab 里停放的 222 行计划确认卡逐字节搬迁成组件（JSX 零改动），决策历史瘦身为纯审批面板；attentionTone 扩展覆盖需求/计划版本/验收状态（既有键零改动）。真实验证：浏览器实测四格渲染+三格深链 href+计划格展开出确认卡（aria-expanded）。③**Phase 3 活图 design-prototype（拍板材料，不进产品代码）**：`docs/prototypes/flow-live-graph/` 两个自包含 HTML——概念 A=数据包粒子沿边流动+交接对照浮层（契约 vs 实际交付物差异高亮）+执行回放；概念 B=零帧成本呼吸边+节点内嵌事件流。独立 chromium 自查 console 零错误。发现 `verify:design-prototypes` 脚本本体已在 68a72c23 被删（package.json 指向断链，仓库级预存损坏，非本次引入）。门禁：verify:web 全绿（含全部新用例）+ verify:control-plane（含契约 guard）全绿。
- 2026-07-27 00:05（docs:飞书通道接入与管理 spec——通道即平台资源）：固化 07-26/27 两轮讨论。盘点结论:消息面(卡片/签署/分级/词表)完善,接入管理面严重缺失——app 配置 curl 驱动无自检、service token 全手工、**connector 是平台盲区**(无注册/心跳,断连数小时零感知,对比 runtime-agent 待遇完全不对称)、outbox 无运营面(实查 19 条 failed 静默躺库)、绑定只有正向路径、多租户 schema 预留面为零。**拍板**:通道模型收敛两层(通道租户级+身份用户级,投递目标不建模,寻址从身份推导,项目级绑定明确否掉);不新增菜单(系统配置中心「消息通道」分区+绑定治理留用户管理页+告警进收件箱);分区按通道注册表通用形态(飞书是第一条目);人类成员必须绑定从口头约定变产品约束;项目群不做仅留两条触发条件(私聊量成负荷问题/客户明确要求)。分期:P0 接入闭环(app 配置 UI+保存时连通自检含 scope 探测中文缺项提示+service token 面板)/P1 通道可观测(connector 注册心跳+断连收件箱告警防自指+outbox 失败重推收编延后项)/P2 绑定治理(换绑解绑 UI+未绑负责人聚合+多 app 管理)。G1–G6 真实链路门禁已定;飞书双人项继续等第二真人账号不阻塞。spec `2026-07-27-feishu-channel-access-management.md`。
- 2026-07-26 23:58（运行总览项目运行带：run-summary 跨项目聚合端点 + 侧栏常驻计数）：大屏模式 spec（`2026-07-26-run-overview-display-mode.md`，已拍板：内部运维墙受众/今日吞吐执行口径/大屏认证另立运维项）的 P1 先行批。**CP（无迁移）**：新增 `GET /api/v1/projects/run-summary`——`project_tasks` 按项目一次聚合（运行/排队/待人工/失败/待派发/参与员工/最新活动）+ `task_runs` 今日完成（Asia/Shanghai 日窗与员工 today tokens 完全同口径，依赖 07-26 `task_runs.project_id` 一等列）+ 租户级 `today_completed_run_count`（与逐项目求和口径差异显式：总数含归档项目当日完成，供 P2 大屏 KPI）；服务端排序失败/待人工优先；authz 同 `ActionProjectRead`，路由测试锁定 `/projects/run-summary` 静态段不被 `{projectId}` 吞。**Web**：侧栏「项目透镜」区块升级「项目运行」运行带——不选中也常驻显示各项目 `运行/失败/待人工/待派发` 计数徽标（零值不显；「失败」不叫「阻塞」——`project_tasks` 无独立阻塞状态，failed 是权威落点，spec §2.2 已回写），run-summary 为权威源（不受员工 overview limit 100 与参与者盲区影响），端点不可用降级员工反向聚合（`buildProjectRunBandOptions` 纯函数 + `source` 标记）；透镜点选/需求切换/退出交互不变。**验证**：**真实链路**——curl（真实登录）实测端点与 PG 直查逐项一致（租户今日完成 12=12、待人工/失败逐项目吻合）；P1-G2 直插「全待派发无参与员工」fixture 项目→API `unassigned_count=2` 且**浏览器运行带出现「待派发 2·0 人」**（该项目在旧反向聚合下不可见，盲区已偿）；浏览器实测运行带常驻计数「待人工 1·1 人」「失败 2·0 人」、点选进透镜 URL 回写/摘要/退出全过；fixture 已清理。`verify:control-plane` + `verify:web` rebase 前后各两轮全绿（新增 service/路由/纯函数/组件用例）。P2（`?mode=display` 大屏模式）未开工。
- 2026-07-26 20:27（IA 重构 Phase 2：权威图落项目详情 + 河道迁任务中枢 + 流程编排菜单退役）：按 spec `2026-07-26-ia-phase2-workflow-retirement.md`（P2a-1/P2b/P2c 三批，P2a-2 横向管道独立后续轮）。**项目详情需求维度**：新增「需求流程」区（`?tab=demands&demand=`）——需求切换器 + 按需求拉权威图（queryKey 与预载/弹层同族命中缓存）+ 验收血缘迁入项目域（criteria-panel→demand-criteria-panel）+ 待决面板（launch-detail 服务端按 demand 收敛，含无 task 关联的 plan_review）；原流程编排详情的**阻塞横幅/规划缺口面板（补员/豁免）/派发闸横幅**原语义迁入（demand.status=failed 权威门控保留），退役不丢功能。执行轨迹面板补按任务过滤下拉 + `?task=` 深链预选（弹层「查看执行轨迹」携带任务参数，TODO 对应行清偿）。**任务中枢**：「提出任务｜流程实例」页签，河道视图迁入并接通服务端 `q` 搜索（300ms 防抖，浏览器 network 实证请求携带 q 且无前端过滤）与 `project_id` 项目下拉；KPI 收敛：运行中 scope 隐藏恒零「已完成」卡、「已归档」页签更名「已结束」、「最久已持续」副文案按视图切换。**路由收编**：`/workflows`→`/?view=instances`（beforeLoad redirect）；`/workflows/:demandId`→重定向壳（launch-detail 解析 project 后 replace 跳需求区，404 明确空态不劫持，飞书卡片 deep_link 兼容）；侧栏撤「流程编排」；`features/workflows/` 整目录退役（含死代码 workflow-entrance），workflow-status 随河道迁 task-launches；提交成功跳转/河道行/弹层/缺口诊断行等全部内链改指项目需求区；弹层 demandLink 双分支收敛为单一「查看需求流程」。**真实链路验证（H1–H4 全过）**：H3=/workflows/{demand} 浏览器实测重定向落位需求区+侧栏零「流程编排」；H1=多需求项目切换需求，状态头/图/血缘随选中刷新；H4=失败节点弹层→执行轨迹深链 `?tab=trace&task=`→过滤下拉预选该任务（2 次尝试）；H2=流程实例页签搜索「清单」→network 实证 `workflow-instances?q=%E6%B8%85%E5%8D%95` 服务端过滤返回 1/1；/workflows 列表实测跳 `/?view=instances`。`verify:web` 全绿（126 文件/968 测试+typecheck+build+体积护栏 95.9KB/150KB）；三处既有测试断言随链接改向更新（提交跳转×4/弹层深链/缺口深链/侧栏守卫）。
- 2026-07-26 19:58（人类待办域四缺陷修复 · 全链真实复验闭环）：清偿 18:44 裁决式复验揪出的全部缺陷。①**〔严重〕任务重试会话死循环**——`standaloneDispatchCommandType`（`employee/run_service.go`）从"仅 chat 续聊走 resume_session"推广为"凡 metadata 带 provider_session_id 一律 resume_session"（该函数注释里早已记录任务谱系是 same latent bug）：旧行为对任务重试派 start_session→runtime 打 `claude --session-id <旧id>`→"Session ID already in use" 5 秒速败→再开卡→人类按钮永远走不出去。**真实复现+修复验证**：长任务执行中 kill claude（attempt 1 失败、会话 c314b8f8 已记录）→批卡重试→receipt `command_type=resume_session`、进程参数实测 `--resume c314b8f8`→attempt 2 **succeeded** 且回写同一会话 id（连续性成立）。②**〔中〕决策解决动词不投影→僵尸卡+500**——`statusFromDecisionSnapshot`（`inbox/adapters.go`）从"枚举已知解决动词、未知回落 open"反转为"pending→open、cancelled→cancelled、其余非空一律 resolved"（§5.5 的 retry_planning/close_demand 正是漏网动词，第三次打补丁证明枚举法结构性脆弱）；`ResolveDecision` 幂等分支补自愈重投影（历史僵尸卡再点一次即收敛而非 500"inbox projection not applied"）。真实验证：planning_failed 卡以 retry_planning/close_demand 解决后卡即 resolved（修复前该动词必留僵尸 open 卡）。③**〔中〕部分契约拒收→人类替机器补签**——runtime `executor.rs` 新增 `backfill_partial_contract_gaps`：provider 返回 completed 契约但缺 `acceptance_results`/`verification` 回签时按既有"无契约合成"同源语义补齐（passed+evidence refs；非 completed 状态绝不伪造），消除"同一任务这次回签就过、下次忘了就拉人类"的抛硬币税。④**〔轻/G8〕卡片英文技术转储**——planning_failed 卡改"规划失败 · {需求标题}"身份+`humanizePlanningFailureReason` 中文原因分类（超时/路由无效/输出无法解析），原始错误保留在审批 `context_payload.diagnosis_raw` 作证据；失败落 waiting_human 的卡经 `humanReadableFailureSummary` 按失败族给中文引导（"执行器启动或运行失败：{原文}"）。真实验证：无效 planner 模型确定性触发→卡标题/摘要全中文、diagnosis_raw 留存原始 400。**顺带回归**：G1 收件箱验收一键通过（3 verdict+需求 completed）、plan_review Console 批准、closure_confirm 归档、飞书卡内点击↔Console 卡同步（复验期间真人飞书操作两次抢先，双渠道同步实测成立）。测试：4 个 Go 新用例（映射反转/派发类型/两个中文化）+1 个 Rust 新用例（backfill 三分支）；`go test ./...` 全绿、cargo lib 159/159（一例 ws flaky 复跑即绿，与本批无关）。夹具：两复验项目均已结项归档。
- 2026-07-26 19:50（决策值域两腿合一：connector resolve 接同一契约枚举门）：跟进值域治理的飞书腿——契约抽共享 `ResolveDecisionValue` schema（11 值+分片说明），`ResolveProjectDecisionRequest.decision` 与 `ConnectorResolveDecisionRequest.decision` 均 $ref 之（单一事实源，不留两份枚举拷贝）；connector `ResolveDecision` handler 加同一道生成枚举门（契约外值 400「invalid decision」，业务层 validHumanDecision 仍按 decision_type 把关）；生成物类型随之更名（ResolveProjectDecisionRequestDecision→ResolveDecisionValue，Web 腿 handler 引用同步）。新增 connector handler 测试 2 用例（契约外值不触业务网关/5 新值原值透传）。验证：**真实链路**——重启 CP 后以真实 connector 服务凭据+admin 真实飞书绑定 open_id 冒烟：契约外值 400、新值 cancel_downstream 过门落业务层；对照实验证实假 id 下 500 为 connector 业务错误映射既有行为（新旧值一致，Web 腿同场景 404），非本次引入。verify:control-plane + verify:contracts + 定向双腿测试全绿。
- 2026-07-26 19:38（决策 resolve 契约值域治理·TODO 清偿）：契约 `ResolveProjectDecisionRequest.decision` 枚举 6→11 值与 `validHumanDecision` 精确对齐（补 retry/cancel_downstream/reassign/retry_planning/close_demand，description 按 decision_type 分片说明）；handler 解码后显式过生成枚举 `Valid()`（契约外值 400「invalid decision」，生成校验不再是死代码；TrimSpace 提前防带空白合法值误拒）；`status_snapshot` 响应侧补双语义值域文档（pending→动词原值+cancelled/requested，前向兼容不设闭合 enum，写入侧未动）。核对：Web/飞书实际发送值全部在新枚举内；飞书 connector 走独立 resolve 端点未加枚举门（业务层 validHumanDecision 仍覆盖），如需补属另改动。验证：**真实链路**——重启 CP 后 curl（真实登录）契约外值 `totally_made_up`→400、新值 cancel_downstream/retry_planning 过枚举门落业务层 404；定向 handler 测试 2 新用例 + `verify:contracts` + `verify:control-plane` 全绿。
- 2026-07-26 18:44（人类待办域裁决式复验 · 全链真实闭环 + 3 新缺陷）：对 07-24/07-25 已落地的人类待办统一在 main 当前代码上做裁决式复验（真实 Temporal/CP/Web/runtime-agent/claude/deepseek/飞书全链，项目「人类待办H5复验B」2f65db8c 两步依赖需求跑到归档,证据 scratchpad reverify-evidence.md）。**裁决**：①收件箱 SSE 自动刷新**已修复**——07-24 审计"不自动刷新"不再成立：卡落库→SSE `inbox-changed` 同秒→headless 浏览器停留 /inbox 无任何操作 1.7s 出卡,归档后 2s 自动归零,全程 16 事件（07-18/07-24 两记录矛盾就此裁决）。②**H4 平台侧 PASS**：plan_review 推飞书→卡内真实点击批准→Console 收件箱卡同秒 resolved（反向与 any-of-N 双人仍待第二真人飞书账号）。③**G4/G7 回归 PASS**：有下游低风险任务不开下游放行、叶子不拦、无双拦；归档级联把未决卡置 cancelled。closure_confirm 标题=项目名、落点 ?tab=closure 正确；§5.5 planning_failed 全链真实触发（planner 3 连败→高风险卡+三动作),补员自动重开真实工作。**H5**：无同义重复,acceptance_sign 0（判据全自动核实）+closure_confirm 1；但缺陷使单任务多出 3 张不该有的澄清卡。**3 新缺陷**：①〔严重〕澄清卡「同意」触发的重试**复用上次 provider session id**（claude.rs `--session-id`）,claude 报 `Session ID already in use` 5 秒速败→再开卡→确定性死循环,人类用按钮走不出去（实测 3 连败;清 `~/.claude/projects/<workdir-slug>/<sid>.jsonl` 后同一按钮即成功→任务 completed）;旁证:runtime 拉起的 claude 会话存储落在操作者本人 ~/.claude,HOME 未隔离。②〔中〕决策被**自动路径**解决（补员自动重开）时收件箱不投影→僵尸 open 卡,对其执行动作幂等空转后 500 `inbox projection not applied`（inbox/service.go:315;直接人工路径投影正常）。③〔中〕provider 返回**部分契约**（有 status/deliverables 缺 acceptance_results 回签）被 CP 校验整体拒收→validation_failed→人类收到"卡文=完成报告"的澄清卡替机器补签;runtime 仅对"无契约"合成回签（executor.rs:2587）,部分契约不兜底;同任务重跑一次回签就 accepted,属"抛硬币税",与人类负荷预算原则冲突（同族已知债:deliverables 类型宽容/韧性家族4）。另〔轻〕planning_failed 卡标题与重试失败卡正文打英文技术转储（G8 违例）;planning_failed primary_surface 落 projects?tab=approval 与 spec §4.2 表 /workflows/{demand} 不一致待定性。G1/G6 本轮未触发（需求未经 acceptance_pending）,07-24 证据仍有效。（流程图单一权威组件 Phase 1：run 投影增补 + flow-graph 画布合并 + 显示债清偿）：按 spec `2026-07-26-canonical-flow-graph-phase1.md`（IA 按视角轴重划四阶段之一，不动菜单路由）。**CP（无迁移）**：①`ProjectTaskGraphRun` 投影增补 `started_at`/`finished_at`（`COALESCE(finished_at, completed_at)` 语义）/`error_message`——`task_runs` 三列早已存在只是投影没带出，失败 run 从「结果暂无」变可诊断；错误信息经新建 `internal/project/prose_redaction.go:redactProse` 脱敏（CP 侧此前**无**Go 自由文本脱敏出口，既有函数全是 key-based，故逐条镜像 runtime `redaction.rs` 五模式，文件注释要求两处同步）。②`ListWorkflowInstances` 提交人三级补名（`source_refs` 外部通道名 → `auth_users.display_name`/`username` → id 兜底），时间河道「提交 36e24cb9-…」裸 UUID 消失。契约+生成物同步。**Web**：③两套画布合并为 `features/flow-graph/` 单一权威模块——`buildWorkflowGraphElements`/`buildPlanTaskGraphElements` 并为 `buildFlowGraphElements(graph, {includeBlockingFallback, includeDecisionAttachments})`，删 `PlanGraphCanvas`，项目详情「当前执行图」升级为可点节点+attachment/blocking 渲染（信息增益），`taskStatusTone` 收敛唯一出口（plan-task-graph 列表随之 waiting_human mute→warn、assigned mute→info；脉搏 chip 色算法域语义不同刻意未动）。④`ProjectTaskDetailDialog` 成为项目详情与流程编排两页**唯一**任务详情面（`WorkflowNodeInspector` 组件退役，基元迁 `inspector-primitives.tsx`）；运行段增起止/耗时/失败错误块（line-clamp+展开），泛化 `/runtime` 链接改执行轨迹深链 `?tab=trace`（三处 section 登记+自动展开高级项目事实+scrollIntoView，局限：轨迹面板尚无按任务过滤）；弹层深链按语境切向（编排页显「打开项目」、项目页显「查看流程编排」）。**显示债**：⑤`decisionStatusLabel` 改 `labelWithOverrides` 域内补 5 键（取消下游/重试/改派/重新规划/关闭需求）——根因调查坐实 `status_snapshot` 是「pending→人类决策动词」双语义**设计**（inbox adapters 与协调线程都按动词消费），写入侧不动；契约 decision 枚举(6值)与 `validHumanDecision`(11值)不一致+handler 裸 string 绕过生成校验记入 TODO 独立治理。⑥阻塞自指（blocker resource_id==task.id）改「流程停驻在本节点：{原因}」，blocker 类型经词表；⑦`required_inputs`/`acceptance_criteria` 等 schema 键中文化（所需输入/验收判据/预期输出）；⑧血缘展开区任务指称改「任务名 (8位短id)」。**过程事故与自愈**：并发提交 fcc6ff5c 裹挟了本批对 project-operational-detail 的懒 import 致 HEAD 短暂引用未跟踪模块，本批提交（80c27be6）补齐 flow-graph/ 目录后 main 恢复自洽；另代并发会话按显式路径提交了其已完成的脉搏弹层批次（1327cf21）以清理交织基线。验证：**真实链路**——重启 CP 后 curl（真实登录 cookie）实测 `workflow-instances` 返回「开发管理员」、失败实例 `task-graph` 返回真实起止时间与 codex 配额错误原文；真实浏览器走 G1（失败节点弹层错误摘要/耗时 18 秒/展开全文）、G2（项目详情图与编排页点开同一节点内容逐项一致）、G3（列表四行提交人全显名）、G4（停驻语义无自指 UUID、验收判据中文标签、血缘「写入统计报告并设为只读 (64b32147)」、执行轨迹深链真实落地 `?tab=trace` 且高级事实自动展开）；`verify:control-plane`（含契约 guard）、`verify:web`（125 文件/980 测试+build+体积护栏）、web typecheck 全绿。
- 2026-07-26 18:09（任务详情弹层：历史任务编排切片懒加载补齐）：闭掉 17:56 遗留的降级缺口——页面只预载最新 demand 的执行图，历史需求的任务打开弹层永远走「当前执行图未包含该任务」降级。修法纯前端（按 demand 查图端点现成）：弹层新增 `fetchTaskGraph` 懒查询，任务不在预载图中且带 `demand_id` 时按其 demand 补一次 `task-graph`，queryKey 与页面预载同族（`["project-task-graph", projectId, demandId]`），最新 demand 任务直接命中缓存零请求；React Query staleTime 30s，同 demand 任务复开不重复请求。编排区补加载态「正在加载编排数据…」与失败态文案；图数据到达后阻塞/输入/预期输出/交接契约/运行/执行结论响应式补全，employees/runs/summaries 一律取自包含该节点的那张图。降级文案仅剩真正无 demand_id 或图已重规划清掉的情形。测试：`renderDetail` 包 QueryClientProvider（弹层含 useQuery），新增懒加载用例（fetchTaskGraph 按 demand-old 调用 + 切片渲染 + 降级行消失）。验证：**真实链路**——历史任务「统计 /tmp 顶层文件数量并编写中文简报」点开后实测补发 `task-graph?demand_id=dbd24727…`、降级行消失、真实交接契约与执行结论（30 条目统计简报）渲染、关闭重开命中缓存零新请求；`verify:web` 全绿（125 文件 / 972 测试 + typecheck + build + 体积护栏）。
- 2026-07-26 17:56（项目详情三缺陷修复 + 任务详情弹层 UI 重构）：跟进 17:44 弹层落地时发现的三项既有缺陷并重做弹层视觉。①**脉搏「对话」虚假图例撤除**——chat 运行（task_runs run_kind=chat）不产生 project_task，且无项目维度运行列表 API，脉搏数据源里「对话」模式永远不可能出现；撤图例与副标题提法并留代码注释（`resolveTaskMode` 的 chat 分支保留类型但不再对外宣传）。把 chat 活动真正纳入项目脉搏需要新的项目维度 run 查询端点，属跨层工程，未在本次范围。②**详情路由风险信号洪峰闸除**——`useProjectRiskSignals` 在详情路由也为队列分页的每个项目发 4 个信号请求（实测 40+ 并发把 `task-graph`/`plan-revisions` 压后数秒且详情页根本没消费 riskLabel）；加 `enabled: !routeProjectId`，实测详情页 API 请求 53→33、跨项目信号请求归零、task-graph 页面加载后 ~3s 内到达。③**任务 tab 员工列/筛选下拉裸 UUID 补名**——经 `principalNamesById` 显名（名称缺失回退 id 保持 mono），清偿 07-24 三模式审计遗留项之一。④**弹层 UI 重构**（Soft-Flat 体系内）：头部改 IconTile+标题+状态行（状态 pill 为该行唯一语义色，风险/需人工审批降为中性 chip）；正文分区制——摘要段落、负责员工/时间双元信息格（bg-card-soft）、「编排」区（阻塞状态点行、输入/预期输出改 chips 且空结构 `foo: []` 折叠为「暂无」、交接契约按顶层键逐行加粗标签展示、右上角流程编排深链）、「运行与结论」区（运行状态点行 + 结论 brand 左边线引用块）、「待决事项」区（warn 左 accent 卡片 + 就地处理按钮 + 收件箱深链）；各区配 lucide eyebrow 图标。验证：**真实链路**（真实 Web+CP+PG headless chromium）——task-graph 3.0s 内到达且零跨项目信号请求、脉搏无「对话」图例、任务 tab 员工列与下拉均显「报告员小王」无 UUID、弹层图切片/降级态/深链全过并截图核视觉；`verify:web` 全绿（125 文件 / 971 测试 + typecheck + build + 体积护栏）。
- 2026-07-26 17:44（项目详情：运行脉搏/任务列表接通任务详情弹层）：项目详情页「运行脉搏」周历芯片此前是纯展示 `div`（仅悬浮 title），点击无任何响应；「执行中与阻塞」列表与任务 tab 行同样不可点。落地（综合信息结构 + 复用构件，零新增接口）：①新增 `project-task-detail-dialog.tsx`——项目视角任务详情弹层，纯投影已加载的项目详情数据（tasks/overview/taskGraph/decisionRequests），三段式：任务自身事实（状态/模式/风险/需人工审批/摘要/负责员工带员工页跳转/创建-开始-结束时间）+ 当前执行图切片（阻塞/输入/预期输出/交接契约/运行/执行结论，复用 `WorkflowNodeInspector` 导出的行基元与格式化函数）+ 该任务待决事项（复用工作台 `BlockerActions` 走同一条 `onResolveDecision` 出口，与收件箱 any-of-N 语义一致，附「收件箱」跳转）；footer 深链「查看流程编排」`/workflows/$demandId`。②三处入口共用一个弹层实例（状态提升到 `ProjectOperationalDetail`）：脉搏芯片改 `<button>`（hover/focus 可视）、执行中与阻塞行、任务 tab 标题（原标题点击跳员工页的错位语义移除，员工列链接保留）。③降级路径：页面执行图只取最新 demand，历史任务查不到图节点时只渲染任务自身段并明示「当前执行图未包含该任务」。④顺手修流程编排节点详情既有失效跳转：决策行「审批」按钮指向 `/approvals`（已退役重定向权限中心），改为「收件箱」`/inbox`（项目任务决策按拆域规则留收件箱），provider 枚举经 `providerDisplayName` 中文化。已知未修：脉搏模式标注 chat 分支是死代码（`resolveTaskMode` 只返回 plan/loop，对话中枢任务显示为 Plan）；员工列裸 UUID 为既有遗留。验证：**真实链路**（真实 Web+CP+PG，headless chromium 走真实登录 cookie）——项目「验收菜单复验」上脉搏芯片/任务 tab/执行中列表三入口均弹层成功；历史任务走降级路径、最新 demand 任务渲染完整图切片含真实执行结论；「查看流程编排」真实导航到 `/workflows/{demandId}`；员工名显示「报告员小王」非 UUID。`verify:web` 全绿（125 文件 / 971 测试 + typecheck + build + 体积护栏），含新增 2 个弹层交互用例与 workflow inspector 断言更新。排查副产物：项目详情页 `task-graph`/`plan-revisions` 请求会排在风险队列几十个并发请求之后数秒才返回（既有现象，非本次引入），弹层数据到达后响应式补全。
- 2026-07-26 17:08（verify:web 两个 flaky 用例根因修复）：两个长期偶发失败、只能靠复跑规避的用例查明根因并因果修复（非重试/加超时）。①`inbox > guards rapid duplicate action submissions`——原写法 `Promise.all` 两次 `userEvent.click` 同名「提交」按钮，但首次点击后按钮会禁用**并改名「提交中」**，userEvent 的自动等待会一直等一个不复存在的 enabled「提交」按钮直到 15s 超时（两次点击与 React 重渲染抢时序，输赢随机）。改为拿 DOM 节点同步派发两次原生 click：守卫已禁用则第二次天然 no-op，未重渲染则由提交处理器去重——两条路径都只产生一次 POST，正是用例要钉住的行为；另补「提交中」可见断言确认状态推进。②`sidebar-menu-sizing > uses readable 15px labels...`——失败形态 `rgb(11,13,18) ≠ rgb(31,41,55)`，收到值恰是 `--ink`(#0b0d12)：全量跑时共享浏览器页面的鼠标停留在上一测试文件最后点击的坐标，本文件把按钮渲染在页面左上角，按钮出现在陈旧指针下方 → `:hover` 规则（`index.css` `color: var(--ink) !important`）盖掉静息态颜色；同一规则的 height/fontSize 断言通过、仅 color 失败与此完全吻合。修复：beforeEach 把指针 hover 到右下角 4px 停靠点再断言。验证：两用例单跑 5 连过 + 全量 969 用例 3 连过 + verify:web 门禁过。
- 2026-07-26 17:07（运行必须归属项目 · 前置项落地）：按 spec `2026-07-26-run-project-affiliation-invariant.md`（A1–A5+A4b 全部拍板）。此前「运行属于哪个项目」无 schema 承载，靠 `project_tasks.digital_employee_run_id` 单值指针 + `tasks.params.metadata.anchor_project_id` JSON 两级 join 兜底，指针被重试覆盖即失联；且 `POST /digital-employees/{id}/runs` 的 `run_kind=task` 分支主动丢弃 project_id，是唯一能绕过任务中枢造无项目运行的后门。落地：①**C2 一等列**——迁移 `20260726170000` 给 `task_runs` 加 `project_id`，三路回填（当前指针→attempt 血缘→metadata 锚点）后物理清理 60 条全终态孤儿 run（级联 task_events/tasks 残迹），收紧 NOT NULL + 项目维度索引；三条派发路径（StartProjectTaskRun/CreateRun chat/自动化）在写入时落值。②**C1+A3 封后门**——`CreateRun` 删除 task 分支，收敛为 chat 专用，`run_kind=task` 返回 `ErrInvalidRunKind`，`project_id` 无条件必填；契约 `CreateDigitalEmployeeRunRequest` 的 `run_kind` 枚举去 `task`、`project_id` 进 required。③**C3 读路径切一等列**——`ListDigitalEmployeeRunsDetailed`/Calendar/单 run/ProjectOptions/Count 全部改读 `tr.project_id`，删掉 `project_tasks` 指针 + metadata 锚点的 COALESCE join 与 chat anchor fallback WHERE（指针覆盖不再影响归属，误判路径关闭）；项目删除级联（CancelInboxItemsForProjectDelete / AcknowledgeTaskRunsForProjectDelete / GetProjectDeletePreviewCounts）同步简化。④**C4b 整链退役 standalone 失败恢复**——`RetryFailedRun`/`AcknowledgeFailedRun`/`ensureStandaloneRun` + 生产者 `projectStandaloneFailureBestEffort` + `RunFailureInboxProjector` 接口与装配 + `RunRecoveryProjectorAdapter` + `ItemTypeDigitalEmployeeRunRecovery` + 收件箱 `DefaultActions` 的「重试/确认关闭」分支 + 契约两 path + 路由 + Web 抽屉两按钮与 api 函数；迁移同时 cancel 库内存量 open 态事项防哑动作卡。项目任务失败重试链不受影响（自动退避重试 → 耗尽转 waiting_human → 收件箱 `task_failure_recovery` 的「重试任务」，与被删端点完全独立）。验证：**真实链路** chat 发起后 PG 验 `task_runs.project_id`=所选锚点项目；curl `run_kind=task`→400 `invalid run_kind`、chat 缺 project_id→400 `project_id is required`；全库 `project_id IS NULL`=0、NOT NULL 已生效、无孤儿、无 open run_recovery 事项；`verify:foundation`+`verify:web`(969)+`migrate-validate` 全绿。**留合并前复验**：G1/G2(plan/loop 真发)、G5(返工指针覆盖)、G7(停 runtime 造失败的重试链回归)——核心断言已被 Go 测试 + G6 实测覆盖。
- 2026-07-26 15:58（团队宪法：分类标签改用诚实措辞，不再暗示强制力）：P3 上线后真实复验发现分类名"禁止/必须/需审批"在暗示宪法有实际强制力，而它只是软文本提醒（D9）。人对面（控制台 UI、契约枚举描述、审计详情）改为「尽量避免 / 尽量遵循 / 重点提醒」；枚举 key（forbid/must/require_approval）保持不变，不动已落库数据与契约结构。**关键取舍**：模型对面（注入 provider 的实际 prompt）不跟着软化——`RenderConstitutionPrompt` 拆出独立的 `constitutionPromptHeaders`，forbid/must 仍保留「禁止/必须」的强措辞以维持说服力，唯独 `require_approval` 连对模型也不写"需审批"（改用"重点关注"），因为那会让模型误信存在真实审批流程、可能诱发等待一个永远不会来的批准。同时把 `team-constitution-summary.tsx` 里"硬性规则"这个更严重的过度承诺措辞一并改掉。验证：真实链路保存宪法确认枚举值未变、浏览器确认 UI 显示新标签、真实派发 payload 确认模型侧 prompt 头仍是「禁止/必须/重点关注」（不含"需审批"）。verify:control-plane + verify:web 全过。
- 2026-07-26 11:27（团队配置 P3：宪法接通派发链 + 结构化版本化）：按 spec §5.3 落 P3（D1 接通 / D9 仅文本注入）。**关键发现**：不只团队宪法是空转，员工 `persona_memory_markdown` 在 runtime 侧同样是死字段——`payload.rs` 有该字段但全仓无读者，provider 只拿到 `prompt`；因此 P3 必须动 runtime，否则新增字段会落进同一个死区。①**规则结构化**——裸字符串数组改为 `{id,text,category}`，分类 forbid/must/require_approval 服务端注册校验；`tenant_teams.constitution` 同时写 `rules`（结构化）与 `hard_rules`（既有读者如员工创建基线），两者同源派生不漂移；旧快照只有 `hard_rules` 时按「必须」回退。②**版本化**——新表 `team_constitution_revisions` + 迁移 `20260726025333` 把现有宪法回填为 v1；保存 = 追加新版本且 `change_note` 必填；`PUT/GET /teams/{id}/constitution/revisions` 与 `POST .../{n}/rollback`（回滚以旧内容创建新版本，历史只增不改）。③**接通执行链**——CP 派发时按 `employee.team_id` 读当前宪法，渲染成分组 markdown 注入 payload 的 `team_constitution` 并附 `team_constitution_revision` 供回溯；**runtime `provider_prompt()` 把团队宪法与员工人格拼进 provider 实际收到的提示词**（团队约束 → 员工人格 → 本次任务）。④**字符预算**——新增系统配置 `team.constitution_max_chars`（默认 4000），超限保存被拒。⑤**明确不做（D9）**——`require_approval` 分类不触发任何强制审批点，宪法不进 `planTouchesHighRisk` 一类调度判定。⑥配置页「约束」分区升格为规则条目编辑（分类下拉、批量粘贴迁移入口、保存前 diff 预览 + 必填变更说明、版本历史与回滚）。验证见下方说明。
- 2026-07-26 10:29（团队配置 P2：能力接管收敛 + 就绪矩阵）：按 spec §5.2.1 落 P2（D2/D10）。①**团队接管写时物理收敛**——团队绑定 MCP/技能时同事务收敛本团队成员的同 server/skill 个人绑定（MCP 软删、技能硬删，团队胜出），落 `team.mcp.takeover` / `team.skill.takeover` 审计并记录每人被收敛前的 `credential_env_var`；此前只有读路径靠 `NOT EXISTS` 静默屏蔽，库里重复行常驻，导致员工页「个人绑定」看得见、「生效配置」看不见，凭据变量名被无声丢弃，且团队一解绑陈年旧绑定就自动复活。②**员工侧写时拒绝**——所属团队已提供同一 MCP 时返回 409 `team.capability.provided_by_team`（新增共享 `teamguard.CapabilityProvidedByTeamError`）；技能侧原本 `InstallSkill` 把 `ErrTeamAlreadyInherited` 吞成 `AlreadyBound=true` 静默成功，改为显式冲突冒出（含技能名），并给 capability/skill 两个 handler 接上 `apierror.Write`。③**接管预览**——新增 `GET /teams/{id}/capability-conflicts?mcp_server_id=`，与执行共用同一条查询（看到的就是接管的）；绑定弹窗展示将接管几名成员、逐人「原变量名 → 团队变量名」，并单独警示其中 N 名会缺变量。④**成员就绪矩阵**——新增 `GET /teams/{id}/capability-readiness`（MCP × 成员的缺失 env 一次铺开，避免 N+1），配置页能力分区新增矩阵，按 MCP 汇总「全员就绪 / 部分就绪 M-N / 未就绪」并支持逐格就地补 env；解决「团队绑了需要凭据的 MCP，在每名成员各自配好前对全队都是空壳、且团队页完全看不到」这一后端注释自认的洞（原文 `Team-level env-var preflight is advisory`）。⑤**历史数据一次性收敛迁移** `20260726021028`（D10 开发环境直接执行）；能力面板补影响面标注「对 N 名成员生效」。验证见下方说明。
- 2026-07-26 09:55（团队配置 P1：独立配置页 + 观察面分离 + 成员角色闭环）：按 spec `2026-07-26-team-configuration-console-design.md` 落 P1（D7）。①**新增 `/teams/$teamId/config` 五分区配置页**（编制/能力/约束/身份与生命周期/审计），成为团队唯一写入口；`$teamId.tsx` 改为命中子路由让位 `Outlet`（与 employees 同处理）。②**详情页转为观察面**：写操作全部迁走，头卡「配置团队」改 Link，数字员工表/能力/宪法只读，新增「最近变更」审计摘要与带 hash 的「去配置」深链。③**成员角色闭环**——新增 `PATCH /teams/{teamId}/members/{memberId}`（`team.member.change_role`，仅 member⇄viewer；特权角色 400 引导走审批；降级最后一名 owner 400）；tenant_members 唯一键含 role，实现为同事务「停旧行 + upsert 新行」，membership_id 会变。接线此前零调用的 `requestTeamPrivilegedRole`，收编并删除 TODO.md 2026-07-25 条目。④**身份与生命周期分区**：接线此前零调用的 `updateTeam`（名称/标识/说明/图标/负责人集合，负责人集合为空时禁止保存）+ 删除团队迁入危险区。⑤**审计分区**：接线此前零调用的 `GET /teams/{id}/audit`，动作名经新增词表 `teamAuditActionLabel` 中文化。⑥**审计补齐 + 口径统一（D5）**：新增 `team.update`、`team.constitution.update`、`team.member.remove`、`team.member.change_role`，并把 `team.member.add`/`grant_privileged_role` 的 `resource_type` 由 `team_member` 改为 `team`（团队审计流按 team 过滤，此前成员事件在团队视角完全不可见；全仓无消费者读 team_member）；顺带发现并修复「事后单独加人不写审计」（只有建团时的初始成员会记）。⑦**`pending_draft_count` 接真实数据（D6）**：改为统计 `approval_requests` 中 `context_payload.team_id` 命中本团队的 pending 数，头卡「待审批 N」pill 不再恒不出现。验证见下方说明。
- 2026-07-26 01:30（团队配置 P0：归属可逆 + 计数/门禁/审计止血）：按 spec `2026-07-26-team-configuration-console-design.md` 落 P0。①**数字员工归属可逆**——新增 `DELETE /teams/{teamId}/digital-employees/{employeeId}`（移出回候岗大厅，门禁 team.update），前端团队页每行补「移出/换队」+ 后果确认弹窗；此前员工进团队后产品内无任何出路（reassign 契约 `team_id` required、且前端零调用）。②**脱离守卫**——新增 `teamguard` 包与共享 sqlc 查询 `ListDigitalEmployeeDetachBlockers`，移出与换队同一套判据：有在役执行（task_runs queued/dispatching/running/cancelling）或仍被非归档项目引用即 409 `team.digital_employee.detach_blocked`，message 点名阻断对象。③**换队加固**——补目标团队员工数限额（此前绕过 `ensureTeamDigitalEmployeeCapacity`）、源/目标双边 `team.update`、同团队幂等 200、补 `team.digital_employee.transfer_out/in` 团队维度审计（原审计只写 digital_employee 维度，团队审计流看不见）。④**capability_count 恒 0 修复**——`ListTenantTeamSummaries`/`GetTenantTeamSummary` 原硬编码 `0::integer`，改为 team_skill_bindings+team_mcp_bindings 真实计数（排掉指向已删注册项的行）。⑤**权限门禁对齐**——`overviewActions` 补 `team.capability.manage`（契约枚举同步），前端能力面板由统一 `team.governance.edit` 拆成 bind/unbind/manage 三个动作。⑥**能力绑定补审计**——team.skill.bind/unbind、team.mcp.bind/unbind 均写 `resource_type=team`（幂等 no-op 不记）。顺手修 main 上已存在的两处断裂：`internal/api` 测试包因 auth fake 漏 4 个方法编不过、`TestAuthUserManagementRoutesAreRegistered` 缺 `tenant_role` 400。验证见下方说明。
- 2026-07-26 00:03（租户 IAM P0–P2 收口）：在 23:55 API/登录闭环之上补齐 UI 与文档——①用户详情可编辑 `project-team-scopes`；②用户管理顶部当前租户摘要 + 幽灵/有访问筛选；③团队加人候选仅 `console_access` 租户成员，文案与后端闸门对齐；④权限中心成员页标明只读并深链用户管理；⑤design/团队管理文档修订注记。验证：users Vitest 13/13；`tsc -b`；真实 API：创建 `iamtest*`（tenant_role=member）→ login/`/me` 200；撤销成员后 `/me` 403；缺 `tenant_role`→400；无租户成员加人团队→400。延后不变：团队内角色变更与高权限申请 UI（TODO）。
- 2026-07-25 23:55（租户成员 / 控制台访问闭环）：补齐单租户 IAM 断层——此前建用户只写 `auth_users`+scopes，无租户级 `tenant_members`，登录成功但 `/api/auth/me` 因无 `console.access` 403，前端误报密码错误。落地：①design `2026-07-25-tenant-membership-and-console-access.md`（修订 2026-06-16「创建不写 membership」）；②迁移部分唯一索引 `idx_tenant_members_active_tenant_level_principal`；③`POST /api/auth/users` 必填 `tenant_role`，同事务写租户成员；`GET/PUT/DELETE .../tenant-membership`（最后 owner 保护、仅 owner 可授 owner；OpenFGA `SyncMembership`）；④团队加人要求已有租户成员；⑤authzcenter `console_access` 仅认 `team_id IS NULL`；⑥用户管理创建抽屉选租户角色、详情可授/撤；登录错误区分密码/禁用/无控制台访问；指标「无控制台访问」。验证：`go test` auth/tenant/authzcenter；`verify:contracts`；真实链路：admin 创建 `iamtest*`（tenant_role=member）→ 登录 → `/me` 200；缺 `tenant_role`→400；历史幽灵用户 `test` 经 PUT 授 member 成功。`migrate-validate` 本机无 docker，已用真实库 `migrate-up`。延后：团队内普通角色变更 UI、高权限申请 UI 接线（见 TODO）。
- 2026-07-25 21:30（P1-D 收尾 · Step 3 manualChunks + Step 4 体积护栏）：spec §4 Step 3/4，D 全部完成。**Step 3**：`vite.config.ts` 加 `build.rollupOptions.output.manualChunks`（Vite 8 内置 rolldown 兼容 rollup 配置），把长期稳定的框架 vendor 从入口切出——`vendor-react`(56 KB gz)/`vendor-radix`(47 KB gz)/`vendor-tanstack`(36 KB gz)独立成块，业务代码更新不再使这 135 KB 框架缓存失效。入口 **305→85 KB gz**。只改 build 段，不碰他会话正在改的 alias hunk。**Step 4**：`apps/web/scripts/check-bundle-size.mjs` 校验入口 chunk gzip ≤ 150 KB（当前 85.8 KB，留余量防脆），超限 fail 并给整改提示；链入 web `build` 脚本，`verify:web` 跑 build 即强制。顺带修一处**本会话 nav 修复的连带回归**：`converge.tsx` 的 Breadcrumb 改用 TanStack `Link` 后，`sign-in/index.test.tsx` 的 `@tanstack/react-router` mock（原只有 `useSearch`）因 superteam barrel 静态引用 `Link` 而导入失败——补 `Link` 桩。验证：**`verify:web` 全绿（125 文件 / 957 测试 + typecheck + build + 护栏）**；护栏证伪路径已验（阈值调 10 KB 时 exit 1，含提示）。**P1-D 累计**：入口 471→85 KB gz（**省 386 KB / 82%**），xyflow+dicebear 重依赖移出入口、框架 vendor 切成可缓存块、护栏防回潮。
- 2026-07-25 20:22（P1-D Web 入口包瘦身 · Step 1 xyflow 懒加载 + Step 2 头像 2b 预生成）：spec `2026-07-25-p1-platform-hardening.md` §4。基线入口 `index-*.js` **471 KB gz**，实测重依赖只有 `@xyflow/react`（流程图）与 `@dicebear/*`（头像）在入口，`monaco`/`recharts` 等已不在。**Step 1**：`workflow-detail`/`project-operational-detail` 的两个图画布改 `React.lazy`+`Suspense`，xyflow 移出入口（→413 KB gz）。**Step 2（人类拍板 2b：存渲染后 SVG + 自愈 + 回填）**——后端 Go 跑不了 dicebear，故预渲染：①迁移 `auth_users.avatar_svg TEXT`；②`UserAvatarConfig.SVG` 经 `toDomainUser` 透出，契约 `UserAvatar.svg` + 新端点 `PUT /api/auth/me/avatar-svg`（仅写当前用户自己，因后端无法校验他人提交的 SVG）；③前端 `user-identity.tsx` 优先渲染存储 SVG（零 dicebear），缺失才动态 `import()` dicebear 兜底（独立 chunk、不进入口），本人则自愈写回；同步生成器移到 `lib/avatar-dicebear.ts` 仅供头像选择器（users 懒路由）用；④一次性回填脚本 `apps/web/scripts/backfill-avatar-svg.mjs`（psql 导出→Node+@dicebear 渲染→发 UPDATE）。**结果**：入口 **471→305 KB gz（省 166 KB / 35%）**，xyflow 与 dicebear 重样式（`data:image/svg` 部件）全部移出入口（入口仅剩 provider 枚举字符串）。验证：`tsc -b`、workflows/projects/account/users/user-identity Vitest（103+24 过）、auth `go test`、`verify:contracts`、`migrate-validate` 全量重放；**真实链路**：`PUT /auth/me/avatar-svg` 往返 + 空值 400；回填脚本对 dev 库 14→0；`/auth/me` 返回回填后 svg；**真实浏览器** `/users` 页 5 个头像全部从存储 data-URI 渲染、`dicebearChunkLoaded=false`（dicebear 在展示页完全不加载）。**剩余（未做）**：Step 3 `manualChunks`（框架 vendor 按稳定性切块，改善缓存，`vite.config.ts` 正被他会话改需按 hunk 合并）、Step 4 入口体积护栏挂 `verify:web`。
- 2026-07-25 12:38（P1-A 简化版 token 预算熔断）：spec `2026-07-25-p1-platform-hardening.md` §3（人类拍板简化版：只做 token、只一道派发前闸、砍掉 ledger/软闸/金额）。**一句话**：给项目 token 额度，超了就不让开新任务；运行中不打断，任务结束下次开工才拦。落地：①迁移 `20260725041840` 给 `projects` 加 `budget_token_limit BIGINT`（可空=不限，对存量零影响）；②`SumProjectConsumedTokens` 对项目下所有 attempt 的 `budget_consumed_tokens`（runtime 心跳单调累加，天然含失败/返工）求和得「已消耗」，**不复活 ledger**；③单闸落在 `loadPreDispatchGateSnapshot`：已消耗≥额度时置 `ProjectBudgetAllowed=false`，复用既有 pre-dispatch 机制产出 `budget.token_exhausted` + waiting_human 人类待办（闸在派发前→运行中 attempt 不受影响；覆盖协调线程/自动化/chat 全入口；解除方式是提额，`||` 使一次性审批无法绕过）；④block 消息按耗尽语义定制（"已用 X / 上限 Y，提高额度后可继续"），不再泛泛「确认预算」；⑤`GET/PUT /projects/{id}/budget-summary` 露出 `token_limit`/`consumed_tokens`/`exhausted` 并支持提额/设限/清限（负数 400）；⑥前端任务发起页选中项目耗尽时禁用「提交任务」+ 提示条，`service.go:798` 心跳软闸维持只看 wall clock（不做 token 软闸）。**明确不做**：金额维度、`budget_cost_limit`、`budget_period`、attempt 级限额传播、cost 模块改造。验证：新增 gate 评估单测（耗尽→waiting_human+token_exhausted、不限→放行）、ProjectStore 集成测（超额→DispatchProjectTask 不启 run 落 waiting_human）、pool/service/handler/api 全绿，`verify:control-plane` 0 FAIL、`migrate-validate` 全量重放通过（独立 schema 用后即删）、web `tsc -b` + task-launches/projects Vitest 44/44。**真实链路**（真实 CP + 远程 PG + 真实浏览器）：直插 2 个 attempt（700+500）→ `budget-summary` 返 `consumed_tokens=1200`；设额度 1000→`exhausted=true`，提额 2000→`false`，清限 null→`token_limit` 消失，负数→400；浏览器任务发起页选中耗尽项目「提交任务」按钮 `disabled=true` + 提示「已用 1200 / 上限 500」，提额 5000 后按钮恢复、提示消失。测试项目与直插数据已清理。
- 2026-07-25 12:25（人类待办负荷预算 · P0/P1/P2 代码落地）：spec `2026-07-25-human-task-load-budget-and-channel-grading.md`。①**P0 词表**——`downstream_release` 中文「阶段放行」→「下游放行」（status-labels、收件箱分组、`humanTaskProgress.label`）；共享 fixture `contracts/control-plane/human-task-kind-labels.json`，Web/飞书双边逐值护栏（§5.4 选 option 2）。②**P1 终态契约**——OpenAPI 具名 `HumanTask`/`HumanTaskProgress`/`HumanTaskEvidenceItem`；`InboxItem` allOf 嵌入；`primary_surface` 提升为具名顶层字段；handler 输出 typed progress；Web `HumanTask` 类型与深链优先读顶层。③**P2 渠道**——抽出 `internal/humantask.KindAndLayer`；outbox 两处载荷补 `kind`；飞书 `kindInteractionGrades` 显式分级表 + 净新增 `dispatch_release`/`downstream_release`/`closure_confirm`/`planning_failed` 卡内动作；词表与 Console 对齐（计划确认/验收签署等）。验证：`verify:contracts`；`go test` inbox/humantask/project/feishu cards；Vitest kind-labels guard + inbox。**阻塞 H4 真推送**：需飞书绑定用户与第二个真人账号（见 TODO 飞书联调）；H5 单需求闸门次数抽检待服务重启后实跑。
- 2026-07-25 12:15（修 navigation-rules 护栏红 · Breadcrumb 站内链接改 SPA）：设计系统基元 `components/superteam/converge.tsx` 的 `Breadcrumb` 在 `href` 分支用了原生 `<a href={item.href}>`（另一会话 `6c6c8155` 引入），站内 URL 点击会整页刷新，并触发 `navigation-rules` 护栏红。改为 TanStack `Link to={item.href}`——既渲染真 href（可访问 / 右键新开）又走 SPA 导航；对齐 `ShellPageHeaderBack` 等既有模式，`to: string` 直接可用无需 cast。同步修订组件底部迁移边注。验证：`navigation-rules` 3/3、converge 组件 9/9、`tsc -b` 全绿（该基元当前零使用点，属轻量局部变更，未做浏览器实链路）。
- 2026-07-25 02:44（P1-C2 收件箱 SSE 去轮询 · LISTEN/NOTIFY）：spec `2026-07-25-p1-platform-hardening.md` §1.2 C2 落地。此前每条收件箱 SSE 连接各自每 2 秒打一次库探测游标（`PeekInboxChange`，含 `project_members` EXISTS 子查询），而 SSE 已提升到全局布局——每个打开的标签页一条连接，库压随标签页数线性增长，远程库上每次探测还要占一个连接一整个 RTT。改为：迁移 `20260724183529_inbox_change_notify` 给 `inbox_items` 装 INSERT/UPDATE/DELETE 三个**语句级**触发器（`FOR EACH STATEMENT` + transition table，批量写只发一条通知而非逐行放大），广播 `inbox_changed`、载荷仅 tenant_id；CP 侧新增 `inbox.ChangeNotifier`，一条常驻连接 LISTEN 后在进程内扇出给本进程所有流（订阅通道深度 1、非阻塞发送，突发合并成一次唤醒，避免慢写者拖住整条监听）；handler 改为 `select { 通知 | 兜底 ticker }`，兜底轮询从 2s 放宽到 **30s**。**偏离本 spec 原设计并说明理由**：spec §1.2 原写「放应用层而非触发器」，实地清点写路径后改用触发器——`inbox_items` 除 inbox 自己的两条 upsert 外，`tenant_team.sql` 也直接改它、历史迁移里还有两处 UPDATE，应用层逐点插桩漏掉任一处的后果就是「后端已有待办、界面不刷新」（已修过一次的严重缺陷）；NOTIFY 在此是缓存失效信号而非业务事实，放 DB 不违反分层。NOTIFY 不保证送达（监听重连期间的通知会丢），故保留兜底轮询 + 客户端既有 onopen 强制 invalidate 双保险。验证：新增 5 条扇出单测（租户隔离、同租户多流全唤醒、突发合并为 1、退订后不再唤醒、nil notifier 安全）；`migrate-validate` 全量重放通过（独立 schema，用后即删）；`verify:control-plane` 0 FAIL。**真实链路**（真实 CP + 远程 PG）：三个触发器已装库；独立 psql `LISTEN` 实收 `inbox_changed` 载荷 `00000000-…-0001`；真实 SSE 流从 DB 写入到收到 `event: inbox-changed` **561ms**（含 psql 启动与两次 39ms 往返），确认由通知驱动而非 30s 兜底；3 条空闲 SSE 流 40 秒内 80 次采样 `pg_stat_activity` **0 次**命中探测查询（旧 2s 间隔同窗口应约 60 次，采样证据非精确计数）。注：全量 `verify:web` 有两处失败均非本次改动——`navigation-rules` 违规出自另一会话已提交的 `components/superteam/converge.tsx:69`（原生 `<a href>`），`inbox/index.test.tsx` 的 duplicate-submission 用例单跑 29/29 全过，属全量并发下的浏览器超时抖动。
- 2026-07-25 02:32（P1-C1 连接池定容 + P1-B 数据保留作业）：spec `docs/superpowers/specs/2026-07-25-p1-platform-hardening.md` 前两项落地（拍板结论见其 §7：只做 token 维度、保留分类授权「该删就删」、P0 不插队）。①**连接池按 RTT 定容**——`pgxpool` 此前零调优、取 pgx 默认 `max(4, NumCPU)`；但这库是远程的（实测 ping **39ms**），单连接每秒最多约 1/RTT 次往返、池吞吐 ≈ MaxConns/RTT，CPU 形状的默认值把整个进程压在约 100 次往返/秒。新增 `PostgresConfig` 的 maxConns/minConns/maxConnLifetime/maxConnIdleTime/healthCheckPeriod（config.yaml + `DATABASE_*` env 可覆盖，默认 25/5/30m/5m/30s），并给连接打 `application_name=superteam-control-plane`（此前所有 backend 都是空 application_name，池问题无法归因）。②**数据保留作业**——新增 `internal/retention`：策略是一张可读的规则注册表（runtime_events / provider_session_events / runtime_command_receipts 30 天、task_events 90 天、authz 放行日志 7 天、其余操作日志 90 天、过期会话即删、已软删满 30 天项目的事实行随项目清），保留天数与单批行数走 system_config（新增 `retention.*` 8 项 + `DomainRetention`）；`retention.sql` 每表一条静态语句（表名不进参数，保住 sqlc 类型检查），全部 `ctid + LIMIT` 分批以避免远程库上的长事务；单跑保护用会话级 `pg_try_advisory_lock`（P0 leader 选举落地后可平滑替换 `Singleton` 接口）；启动即扫一轮。**业务事实表（project_events / execution_ledger_events / audit_events）永不按时间清**——前者 11 处读路径撑项目时间线，次者由 `ListProjectExecutionLedgerEvents` 撑执行轨迹面板。验证：新增 10 条单测（含护栏 `TestRulesNeverSweepBusinessFactTablesByAge`、批次上限、单规则失败不阻断其余、无锁不删、缺配置不退化为 days=0；池侧 2 条断言配置真达 pgxpool 且 MinConns>MaxConns 被钳制）。**真实链路**（真实 CP + 远程 PG）：池——240 请求 / 60 并发全 200，`pg_stat_activity` 连接峰值**恰好 25**（配置值，且高于旧默认 NumCPU=18），空闲预热 5~8 条；保留——首轮实删与预测逐条吻合（authz 放行 100,000 / 预测 104,311 受单轮上限 20×5000 截断、过期会话 910、runtime 事件 161、命令回执 35），次轮补删剩余 4,311，而 `project_events` 1999→1999、`execution_ledger_events` 3002→3002、19 条拒绝日志 19→19 **全部未动**（负向对照）；单跑保护——外部持同一把 advisory lock 时 CP 启动后静默跳过（0 条 sweep 日志、无报错、零删除），释放后恢复正常清扫。authz 放行日志累计 177,090→72,790（余量在 7 天窗口内会自然老化），已 `VACUUM (ANALYZE)`；注意 DELETE 不把空间还给 OS，表体积仍 138MB，空间已可复用。`verify:control-plane` 0 FAIL。
- 2026-07-25 02:18（Soft-Flat Web：Batch D + P0/P1 收敛 + 技能/团队目录 + 顶栏护栏）：落地 `converge`（Empty 预设/SoftTabs/AvatarStack/notify* 等）；features 内 `ui/tabs`→SoftTabs、`ui/alert`→Callout、直引 sonner→notify*，扩展 `design-import.guard`；`/skills` `/teams` 目录改用 MetricGrid/ListToolbar/EntityCard/空态骨架；`ShellPageHeader` 去掉 action/actions，主 CTA 只放 Main，清工作台/Runtime/成本等顶栏控件。验证：护栏+skills/teams 等 Vitest、`tsc -b`；真实浏览器两目录页。
- 2026-07-25 01:55（并发三缺陷修复 · 共享 task queue / 项目事件序号 / 配置修订号）：修掉上一条 CAN 修复的 E2E 顺带查实的三个既有缺陷。①**单 worker 服务整条 task queue**——`app.go` 曾在同一队列（`superteam-project-coordination`）起两个 Temporal worker，协调侧只注册 `ProjectCoordinatorWorkflow`+协调活动、自动化侧只注册 `AutomationFireWorkflow`+`FireAutomationRule`，任务被随机派给任一 worker，约一半报 `unable to find workflow type/activityType` 再走重试，既放大 history（反过来加速逼近 CAN）又实测吃掉过一次 handler。改为自动化经新增的 `automation.RegisterWith` 注册到协调 worker 上（同步加 `projectcoordination.RegisterWith`），删除 `Container.AutomationWorker` 与其启动块；**不改队列名、不动既有 Schedule**。②**项目事件序号竞态**——`AppendProjectEvent` 走池连接调 `pg_advisory_xact_lock`，而该锁是事务级的：裸语句自成隐式事务，语句一返回锁就释放，根本护不住后面的「取 max + INSERT」，锁在这条路径上是空操作，只剩 3 次重试兜底；并发下撞 `uq_project_events_project_sequence`，严重处在于它也能打掉 `recordCoordinatorHandlerFailure` 的失败留痕，使失败信号在库里零痕迹。改为经 `withProjectQueries` 进事务（`stores.Postgres` 已作 beginner 接入；调用方全在服务层、不持有该锁，无自死锁）。③**配置修订号竞态**——`CreateConfigRevision` 同样走池连接且**连锁都没取**，并发 `PUT /projects/{id}/config` 撞 `uq_project_config_revisions_project_rev`，重试打光后裸 pgx 错误逃到 handler 成 **500**。抽出 `createConfigRevisionWithQueries`，进事务并复用同一把每项目咨询锁（demand 状态推进已在复用它）。验证：新增 3 条护栏单测——`TestCoordinationAndAutomationRegisterOnOneWorkerWithoutNameCollision`（锁住"共享 worker 的前提是两侧注册名不冲突"，重名会 panic）、`TestAppendProjectEventTakesItsAdvisoryLockInsideATransaction` 与 `TestCreateConfigRevisionTakesItsAdvisoryLockInsideATransaction`（注入式假 beginner + SQL 记录，断言咨询锁走事务而非池；回退修复即红，实测报"append must open a transaction"）；`verify:control-plane`（contracts + 全量 go test）PASS。**真实链路**（真实 CP + 真实 Temporal + 真实远程 PG，项目 `d5a215b9`）：同一压测脚本 6 并发 90 请求 → HTTP 全 200（此前 23–28 个 500）、90 发 90 收 0 差额；加压 12 并发 120 请求 → 全 200、120 发 120 收 0 差额；两轮新增日志里 `unable to find workflow type/activityType`、`uq_project_events_project_sequence`、`uq_project_config_revisions_project_rev`、handler 500/失败留痕 **四类签名计数全为 0**。缺陷①另有正证：在带团队的项目 `7773a575` 建 60s interval 自动化规则 → Temporal Schedule `automation-rule:2febd4c8` 生成 → `AutomationFireWorkflow` 在合并后的单 worker 上 **COMPLETED** → fire `succeeded` → 真实需求 `b63e89ab` 落库；验证后已删除该规则（Schedule 同步清零）。
- 2026-07-25 01:25（协调线程 continue-as-new 丢信号修复）：`ProjectCoordinatorWorkflow` 主循环每轮 `selector.Select` 只消费一条信号，随后若 `GetContinueAsNewSuggested()` 为真就直接 `return NewContinueAsNewError`，把同一次 workflow task 已投递进 channel 缓冲的其余信号**静默丢弃**——这些事件已随该次 workflow task 交付，不受 server 的 unhandled-command 保护，丢的是 `EmployeeTaskCompleted`（任务永久卡 running）与 `HumanDecisionSubmitted`（项目死锁无恢复路径）；触发条件与负载正相关（history 变大才有建议，而那正是信号成批到达的时候）。修复：`if shouldContinueAsNew(ctx) && !selector.HasPending()`，缓冲排空后再续期。不加 `GetVersion` 围栏：`HasPending` 只读内存 channel 状态、不写 history，replay 确定；`&&` 短路使运行中历史的既有决策点（`shouldContinueAsNew` 必为 false，否则执行早已关闭）永远走不到新调用，不会分叉。验证：新增单测 `TestProjectCoordinatorDrainsBufferedSignalsBeforeContinuingAsNew`（回退修复即红，SDK 报 `unhandled signals [ProjectMemberChanged EmployeeTransferRequested]`）；**真实链路对照 E2E**（真实 CP + 真实 Temporal，`limit.historyCount.suggestContinueAsNew=200` 临时下调后已还原默认，项目 `d5a215b9`，90 并发 `PUT /projects/{id}/config` 打真实 `ProjectPolicyChanged` 信号，用 `project_events` 对账）：修复后 65 发 / 65 收 / **0 丢**，回退对照 62 发 / 20 收 / **42 丢（68%）**，两轮均实测发生真实 CAN（run 血缘 `continuedExecutionRunId` 链可查）；复验轮 67 发 / 66 有痕 / 1 条无痕已归因（见下第 2 条缺陷，信号已收到、连失败记录也被序号竞态吃掉，非 CAN 丢失）。`verify:control-plane`（contracts + 全量 go test）PASS。
  - E2E 顺带查实三个**既有缺陷（本次未修，需单独立项）**：①`app.go:628` 与 `app.go:771` 在同一 task queue（`superteam-project-coordination`）起两个 Temporal worker 且注册类型互不相交，协调线程的 workflow/activity task 随机落到 automation worker → `unable to find workflow type: ProjectCoordinatorWorkflow` / `unable to find activityType=AppendProjectEvent`，靠重试兜底，会放大 history 并实测吃掉过一次 handler；②并发追加项目事件撞 `uq_project_events_project_sequence` 唯一约束，严重处在于它也能打掉 `recordCoordinatorHandlerFailure` 的失败留痕，使失败信号在库里零痕迹；③并发 `PUT /projects/{id}/config` 撞 `uq_project_config_revisions_project_rev` 直接返回 **500**（应 409 或串行化），实测 90 并发下 23–28 次。
- 2026-07-25 00:15（收件箱首屏加速）：①`ListItems`/`GetBadge`/`enrichSourceNames` 远程库查询改 `errgroup` 并行，砍掉串行 RTT 叠加；②侧栏「收件箱」hover/focus 预取默认列表（`mine`+`open`），点击命中 React Query 缓存。验证：`go test ./internal/inbox` PASS；Vitest app-sidebar+inbox 31/31；restart CP 后 curl admin：`inbox/items` ~209ms（此前 ~325ms）、`badge` ~170ms（此前 ~276ms）；浏览器 hover 预取 200 后点击就绪 ~106ms。
- 2026-07-24 22:34（人类待办统一收尾三件 · POST /acceptance 下线 + §5.4.1 + Temporal planning_failed）：①彻底下线 `POST /api/v1/projects/{id}/acceptance`（OpenAPI + generate + 路由/handler/`CreateAcceptance`；保留 GET 与仓储 `CreateAcceptanceRecordWithEvent`；web `createProjectAcceptance` 删除）。②§5.4.1——`ApprovalProjectorAdapter` 在已有对应 `project_decision_request` 时跳过投影（`ExistsProjectDecisionRequestByApproval` + inbox 回退检查），resolve 不再覆盖 decision 卡的 kind/why/progress。③§5.5——`PlanDemandRoute` 重试耗尽后 `MarkDemandPlanningFailed`：demand→`planning_failed` + `planning_failed` HumanTask（重新规划/已补员/关闭需求）；GetVersion `planning-failed-human-task` 护旧历史；`Reopen` 接受 planning_failed；注册表与 `mapDecisionForApproval` 补齐。验证：`go test` inbox/project/coordination/api PASS；`verify:contracts`；定向单测 `TestApprovalProjectorAdapterSkipsWhenDecisionOwnsApproval`、`TestProjectCoordinatorUntypedPlannerErrorMarksPlanningFailed`。
- 2026-07-24 21:00（人类待办统一缺口收尾 · P1.9/G1/also_close）：①P1.9 外科降级——项目「审批」→只读「决策历史」、资产「验收」→「结项」只读结论、下线自由验收表单与 `createAcceptanceMutation`（保留他会话 `principalNamesById` 交织 hunk；`POST /acceptance` 端点残余见 TODO）；②真实 E2E「通过并结项」——项目 `a7d19a55` 需求 `fbeb4764` 勾选签署后 demand=completed、project=archived、验收结论含「通过并结项」、`project.acceptance.submitted.also_close_project=true`、无 `project_acceptance`/`closure_confirm` 卡；③G1 收件箱「同意」补齐——项目 `9ea36c4d` 需求 `c7bc767c` 点同意→human satisfied verdict + demand completed + 卡 resolved（截图 `.scratch/htu-e2e-20260724/`）。跳过：§5.4.1 approval 去重、Temporal auto-planning_failed（仍需安全夹具）。Vitest projects 相关 69/69。
- 2026-07-24 20:16（人类待办统一 P2 收尾 · why/progress/通过并结项）：①P1.6 补齐 HumanTask `why`/`evidence`/`progress`（投影写入 context + InboxItem 契约上送）；②收件箱闭环进度条渲染 `progress.label`；③§5.3「通过并结项」——签署 API `also_close_project`、需求页默认不勾选复选框、项目可结项时直接归档写验收结论且不开 closure_confirm 卡；④结项卡标题改为「结项确认 · {项目名}」、动作文案「确认结项并归档/退回返工/要求补证」、`primary_surface`→`?tab=closure`（兼容映射到资产·验收子 tab）；⑤planner 提示词约束判据 `statement` 必须中文。跳过：§5.4.1 approval 去重、Temporal auto-planning_failed、P1.9 写入口降级（`index.tsx` 他会话交织）。验证见本会话 E2E 矩阵。
- 2026-07-24 17:50（人类待办统一 P2 首片 · 收件箱领域分组 + 去英文枚举）：①§6.1 收件箱按人类待办类型分组——`InboxItemList` 依服务端 `kind`(§4.2)把卡分入「计划确认 / 执行放行 / 阶段放行 / 验收签署 / 结项确认」,其余(planning_gap/task_failure_recovery/其它 project_task_*、审批/运行恢复/团队待删)归「异常处理」,每组带表头+计数,组内保持上游关注度排序;新增 `groupInboxItems` 纯函数 + 单测。②§12/F3 去裸英文枚举——`formatCurrentNode` 不再把 `decision_type` 原样打到 meta 行(旧 `· project_acceptance`),改经 `humanTaskKindLabel(item.kind)`→中文(`验收签署` 等),回退 `decisionTypeLabel`,全部走 `status-labels.ts`。验证：web inbox Vitest 28/28(新增 groupInboxItems 分组/回退断言)、status-labels 14/14、`tsc -b` PASS;**真实浏览器**(Playwright 登录 admin,web 热加载新码):新需求跑到 acceptance_pending 后 `/inbox` 出现「验收签署」分组表头(计数 1)+卡片,meta 行显示中文「验收签署」而非 `acceptance_sign`/`demand_acceptance` 原文,列表头「1 项 · 按类型分组」,「查看上下文」深链落 `/workflows/{demand}`(截图 `p2-inbox-grouping.png`)。延后:闭环进度条(依赖 HumanTask.progress 字段,未落)、planner 判据中文化(§12,需 planner 运行验证,F6)、「通过并结项」合并动作(§5.3,跨层);P1.9 仍因 `features/projects/index.tsx` 被另一会话占用而阻塞。
- 2026-07-24 17:37（人类待办统一 P1.6 首片 · HumanTask 读模型 kind/layer）：安全增量落地 HumanTask 契约的分类元数据(§4.1/§4.2),不改内部 decision_type、不动有风险的 approval 投影去重。①服务端在 `DecisionProjectorAdapter` 投影时按 §4.2 映射写入规范化 `kind`(plan_review/dispatch_release/downstream_release/acceptance_sign/closure_confirm/planning_gap/task_failure_recovery/planning_failed)与 `layer`(task/demand/project),并持久化 `decision_type`(顺带补 F2 里 demand_acceptance 开放态缺 decision_type 的问题);②`InboxItem` 契约新增只读 `kind`/`layer` 字段(OpenAPI + `generate:control-plane` + `verify:contracts` 通过),`itemResponse` 从 context 读出上送;③前端 `InboxItem` 类型加 `kind?`/`layer?`,`status-labels.ts` 增 `humanTaskKindLabel`/`humanTaskLayerLabel`(§4.2 中文名:执行放行/阶段放行/验收签署/结项确认…)供 P2 收件箱分组用;④顺带修好上轮 P1.10 改词后遗漏的 `status-labels.test.ts`(planning_pending→「排队规划中」、加 planning_failed 断言)。验证：`test:go` 全绿(新增 `TestDecisionProjectorAdapterProjectsHumanTaskKindAndLayer` 覆盖 6 类)、`verify:contracts` PASS、`tsc -b` PASS、status-labels 14/14;真实链路(CP 重启加载新码)：新提交需求跑出的 plan_review 卡经真实 `/api/v1/inbox/items` 返回 `kind=plan_review, layer=demand, context.decision_type=plan_review`。延后：HumanTask 的 `why`/`evidence`/`progress` 字段与 §5.4.1 approval 投影去重(仍需实链路验证以免误删合法卡;P0.2 已消解 F2 急性字段清空);P1.9 仍因 `features/projects/index.tsx` 被另一会话占用而阻塞。
- 2026-07-24 17:26（人类待办统一 P1.7 · 注册表/CI/姊妹 F1）：①(kind,action)→handler 注册表(§4.4)——新增 `inbox/decision_action_registry.go`,`DecisionActions` 改为从注册表派生(`demand_acceptance`/`planning_gap`/`task_failure_recovery` 专有动作 + 通用 `同意/驳回/要求补证`),每个动作显式声明执行它的服务端 handler;新增 CI 断言 `decision_action_registry_test.go`:凡收件箱能下发的项目决策动作都必须命中 `implementedDecisionHandlers` 中已实现的 handler、且 `DecisionActions` 只下发注册表内动作——从结构上杜绝 F1「按钮无 handler」再现。行为与旧 switch 完全一致(护栏另测 `genericDecisionActions` 与 `DefaultActions` 不漂移)。②姊妹 F1 修复(交接 §6)——`FailProjectTaskAttempt` 失败落 `waiting_human` 时曾直接 return、不建决策不投影收件箱(任务静默永久阻塞,现场 `a144f12d`);现按 `WaitHumanProjectTaskAttempt`/`recoverProjectTaskAttempt` 同款:落 waiting_human 即按失败族的 wait reason 建 decision request 并投影 inbox,人类才有可点的卡。验证：`test:go` 全绿(新增 `TestFailProjectTaskAttemptWaitingHumanEmitsHumanTask` 断言建卡+投影;注册表 3 项断言),CP 重启加载新码 `/health` 200。阻塞：姊妹 F1 的真实链路需诱发运行时任务失败落 waiting_human(runtime/provider + planner F6 依赖),本轮以单测覆盖;注册表为纯服务端逻辑护栏,不需实链路。延后：P1.6 HumanTask 契约/投影合一(最大件)、其 §5.4.1 approval 投影去重(无实链路验证下贸然按 ResourceType 跳过会误删合法卡,且 P0.2 已消解 F2 急性字段清空,故不冒进);P1.9 仍因 `features/projects/index.tsx` 被另一会话占用而阻塞。
- 2026-07-24 17:08（人类待办统一 P1 部分 · F4/F6）：①P1.10 关闭需求 API——新增 `POST /api/v1/project-demands/{demandId}/close`(契约 + 生成 + 事务化 `CloseProjectDemand`:状态置 cancelled + `demand.cancelled` 审计,幂等,终态需求 409),终于能关掉 F6 里永远"规划中"的僵尸;新增 `planning_failed` 需求状态常量与 `acceptance_pending`/`planning_failed` 补入 `ProjectDemandStatus` 契约枚举,`status-labels.ts` 拆 `planning_pending`→「排队规划中」并加「规划失败」(planner 侧自动置 planning_failed 属 Temporal 活动改动,因 planner F6 不稳 + 重放约束本轮延后)。②P1.8 任务级拦截按下游依赖裁剪 + 去双拦(F4)——重写 `projectTaskRequiresAcceptance`:仅当"有未终态下游依赖任务 AND (provider 请求复核 OR runtime RequiresHumanReview OR 高/危风险)"才开 downstream_release;删除 `task.RequiresHumanApproval` 作为完成后拦截条件(它已在派发前拦一次),叶子任务不再拦;下游口径复用 `ListDependentsOfTask`。验证：`test:go` 全绿(新增 `TestProjectTaskRequiresAcceptanceGatesOnDownstreamDependency`、`TestCloseDemandCancelsPlanningZombie`,并按新闸修正 6+ 既有 waiting_human/acceptance 用例的夹具补下游依赖)、`verify:contracts` PASS、status-labels 护栏 2/2;真实链路：CP 重启后 `POST /project-demands/84fd333d…/close`→HTTP 200、DB demand=cancelled + `demand.cancelled`(prev=planning_pending)、`workflow-instances?scope=active` 不再列该僵尸、all 视图显示"已取消"(反证 F6/G5)。延后：P1.9 下线自由验收端点因其 web 调用方 `features/projects/index.tsx` 正被另一会话编辑,按共享工作树规则不动;P1.6 HumanTask 契约/投影合一、P1.7 kind→handler 注册表+CI 断言、及 waiting_human 无卡姊妹缺陷(见 TODO.md)未做。
- 2026-07-24 16:44（人类待办统一 P0.2–P0.5 · 止血）：①F2 投影不清空——`UpsertInboxItem*` 的 DO UPDATE 对 `source_project_id`/`source_task_id` 用 `COALESCE(EXCLUDED,原值)`、`context_payload` 空对象时保留原值,resolve 触发的 approval 投影不再抹掉 decision 投影的项目归属与展示上下文;②F5 等待人工信号——`ListWorkflowInstances` 状态改 demand_status 优先(acceptance_pending→waiting_human/待验收、planning_failed→failed)、`waiting_human_nodes` 与 `task_blockers` 按终态裁剪粘性 `requires_human_approval`,并修掉 Go 侧 `normalizeWorkflowInstanceStatus` 用任务计数把 waiting_human 盖回 completed 的覆盖;③F8 归档收敛——`ArchiveProject` 事务内级联取消该项目 open 收件箱待办,已归档项目提交需求返回 409 中文文案;④F3 深链单一来源——服务端 `primary_surface`(`inbox/adapters.go` 单一函数计算)为唯一权威落点,下线前端 `reviewHref`/`resolveWorkflowInstanceHref`/`resolveWorkflowTemplateHref` 三处各自推导。验证：`test:go` 全绿(含更新的 project、inbox 断言)、web inbox/approvals Vitest 34/34、`tsc -b` PASS;真实链路(CP 重启加载新码)：`workflow-instances?scope=active` 里卡死夹具 `dbd24727`(acceptance_pending) 现为 `waiting_human`/待验收 且留在运行视图、真完成需求仍 completed 无假阳性(反证 F5/G6)；向已归档项目 `6e45fb1a` 提交需求得 `HTTP 409 项目已归档，无法提交新需求或执行该操作。`(反证 F7/F8)；新提交需求跑出的真实 `project_decision` 卡 `deep_link.primary_surface` 由服务端下发(反证 F3)；F2 投影保留与 F8 级联取消用 dev 库回滚事务(BEGIN…ROLLBACK)验证语义,不落库。
- 2026-07-24 16:07（创建前审阅补可运行节点）：新建项目右侧「创建前审阅」表增加「可运行节点 / 已选节点」行（名称回写，未选显示「未选择」）；必备项同步纳入节点就绪（4/4）。验证：projects Vitest 52/52；restart web 后真实浏览器 `/projects/new` 审阅表见该行，勾选 `runtime-local-dev-node` 后当前值回写节点名。
- 2026-07-24 16:04（人类待办统一 P0.1 · 治 F1 验收卡死）：收件箱「需求验收」卡不再是哑动作。①`inbox` 空动作兜底不再套用于 `project_decision`——`demand_acceptance` 不再被静默改写成无 handler 的 同意/驳回/要求补证（F1 根因之一）；②`DecisionActions("demand_acceptance")` 下发真实动作 同意/驳回；③`project.ResolveDecision` 新增 `demand_acceptance` 分支：同意 → `SignAllPendingDemandCriteria`（逐条签署全部待签人类判据、复用 `SignDemandCriterionVerdict` 内核收敛需求、解决决策、开结项闸），驳回 → 首条待签判据写 unsatisfied 使需求失败；不再落到派发前闸的死路。验证：`go test ./internal/project ./internal/inbox` PASS（含新增 `TestResolveDecisionDemandAcceptanceApproved/Rejected*`）；真实链路：CP 重启加载新码后经真实 HTTP 提交需求并在收件箱批准 plan_review→任务 queued/running/completed 全链跑通，证明 inbox→decision→coordinator→runtime 未被破坏。阻塞：因 deepseek planner 不稳定（超时/EOF，spec F6）且试跑需求未被 planner 注入 blocking human_judgment 判据，未能在实时 HTTP 栈上产出 acceptance_pending 的 demand_acceptance 卡以直点 G1；G1 的签署内核路径由针对真实 service kernel 的单测覆盖。
- 2026-07-24 15:58（项目负责人裸 UUID 补名）：项目列表风险队列在成员 `display_name_snapshot` 为空时不再回退裸 `human_owner_user_id`——列表页始终拉取用户目录补名，并用 `resolveProjectOwnerLabel` 在 pending 摘要时也能显示「开发管理员」；负责人 popover「在用户管理中查看」深链 `/users?user=<id>` 预选对应用户。验证：project-risk + operational-detail Vitest 33/33、`tsc -b` PASS；真实浏览器 `/projects` 队列「验收菜单复验」等行显示「开发管理员」无 `36e24cb9-…`；详情 popover 链到 `/users?user=36e24cb9-…` 且页面预选开发管理员。
- 2026-07-24 15:13（团队人类成员移除权限闸门）：`allowed_actions` 补齐并投影 `team.member.remove`（OpenAPI + overview CheckBulk）；配置 Sheet 仅在有 add/remove/update 时打开，移除钮严格按 `team.member.remove` 显示，头像栈空态/「配置」入口同步闸门。验证：teams Vitest 22/22、`go test ./internal/tenant` PASS、`verify:contracts` PASS；重启 CP+web 后 overview 含 `team.member.remove`；浏览器「安全响应组」配置 Sheet：观察者有「移除…」、负责人无移除钮。
- 2026-07-24 14:43（收件箱 P1）：①侧栏 badge 不再 catch 401 成全零——失败抛出，走 QueryCache→/login，失效会话不再伪装「无待办」；②本地规范宿主 `127.0.0.1`——入口把 `localhost`/`::1` replace 过去，DEFAULT CP URL/.env.example 对齐，CORS 仍兼容跳转前的 localhost 首跳。验证：Vitest canonical-host+control-plane-url+sidebar 10/10；浏览器打开 `http://localhost:3000/inbox` 落点变为 `http://127.0.0.1:3000/inbox`。
- 2026-07-24 14:34（收件箱 P0 自动刷新自愈）：根治「后端已有 open 项、UI 仍 0 项须点重置」——①SSE `onopen`（含重连）立即 invalidate items+badge，不依赖 5s 游标回看；②断流时列表/角标 5s 快轮询，连上后 15s/60s；③收件箱强制 `refetchOnWindowFocus`；④显式「刷新」钮 +「同步于…/同步中断」状态；⑤服务端 `streamCursorSlack` 5s→60s 双保险。验证：Vitest inbox/stream/layout 35/35、`go test ./internal/inbox` PASS；真实浏览器守在 `/inbox`，DB 重开计划复核项 → ≤2s 侧栏「收件箱 1」+ 列表出现「确认项目计划版本」，无需重置/刷新。
- 2026-07-24 14:10（补齐 `permission-changes` OpenAPI）：登记既有 `POST /api/v1/digital-employees/{employeeId}/permission-changes` 及请求/响应 schema；顺手把 empty/未配置→400、busy→409 与契约对齐。验证：`generate:control-plane` + `verify:contracts` PASS；employee 定向测试 PASS。
- 2026-07-24 11:33（数字员工创建后可编辑「员工说明」）：新增 `PUT /api/v1/digital-employees/{id}/profile`（`UpdateDigitalEmployeeProfileRequest.description`，空串清空；authz `employee.profile.update` 走员工 owner）；配置页「身份资料」区可编辑保存，不进入 config-revision。验证：Go employee/api/authz 定向 PASS；config Vitest 6/6；restart CP+web 后真实 API 写读清空/回写 PASS，浏览器配置页编辑保存见「已保存」且 GET 一致。`verify:contracts` 仍被既有 OpenAPI 缺 `permission-changes`（无关）挡住。
- 2026-07-24 10:33（流程编排空态如实呈现待人工决策）：launch-detail 空图且存在 pending 决策（如计划版本 pending_review）时，流程编排详情不再显示误导性的「任务正在规划」——标题改为「计划版本待确认」/「等待人工决策」，正文渲染 `PendingDecisionPanel`（决策类型 pill + 风险 pill + 决策标题/摘要 + 「前往收件箱处理」入口，项目决策已回归收件箱，`/approvals` 已退役故不链向它）；数据源复用 launch-detail 已有的 `decision_requests`，无后端改动。验证：workflows Vitest 61/61（新增 pending 决策用例）、`tsc -b` 全绿；真实 API：临时项目「流程空态验证」提交 Plan 需求 → launch-detail 返回 `plan_review:pending` 且 0 任务，title/summary/risk 快照字段齐全（该需求留在 `d52727ee` 供页面直链 `/workflows/e7274b71-…` 人工核对）；本会话无浏览器驱动工具，视觉核对以真实 Chromium 的 vitest browser 测试为准。
- 2026-07-24 10:27（OCR 高/中修 + 项目/团队测试对齐）：项目运营首屏执行锚点迁到「执行中与阻塞」、负责人头像按 `principal_type` 跳转并兜底「负责人」、团队 MCP 绑定 Select 补加载/空/错态、数字员工分页 page-size 写回收窄为 `5|10`；原型 STATUS_COLOR 对齐生产色阶。同步修正运营首屏改版后的 Vitest（无 plan-graph、提交需求深链、阻塞卡批准、已就绪作用域）。验证：`corepack pnpm verify:web` PASS；`restart web` 后浏览器：`/projects` 队列、`客户接入试点` ops-home（`project-ops-home`/`提交需求`→task-launches）、`默认团队`「绑定公共 MCP」下拉可见注册项。
- 2026-07-24 09:49（项目决策审批放开 any-of-N 执行）：收件箱/审批中心的项目决策类事项（plan_review、项目/需求验收等）不再严格限 target 一人执行——`ExecuteAction` 对 `project_decision` 事项放行，资格统一由 `project.ResolveDecision` 的 `isEligibleDecider` 判定（负责人 + active 人类成员，先到生效），与可见性查询的 any-of-N 口径对齐；审批/催办/run 恢复等其他事项仍严格限指定处理人。下游 `ErrProjectDecisionForbidden` 经 `normalizeSourceActionError` 映射为 `DecisionForbiddenError`（403「只有该项目的人类成员（含负责人）可以处理该决策」），不再落 500。验证：`go test ./internal/inbox` PASS（新增非 target 成员可执行、非成员映射 403 两例）；重启 control-plane 后真实 API：admin 非成员时 POST approve → 403 上述文案且事项保持 open；将 admin 以 reviewer 加入「炬枢平台」成员后再次 approve → 200，plan_review 决策 `approved`、计划版本 `decomposed`、分解出任务「列出当前目录文件」（running），收件箱事项 resolved。
- 2026-07-24 01:25（审批 403 提示可读化）：收件箱/审批中心对非指定处理人执行动作时，403 响应由裸 `inbox action forbidden` 改为「该事项由 {处理人展示名} 处理，只有指定处理人可以执行此操作」（新增 `ActionForbiddenError` 携带处理人名，`errors.Is` 仍匹配 `ErrActionForbidden`；inbox Repository 增加 `UserDisplayName` best-effort 补名）；项目决策路径 `ErrProjectDecisionForbidden` 提示同步中文化；审批弹窗错误展示优先取服务端 `detail` 而非 `request failed with status 403` 前缀。验证：`go test ./internal/inbox` PASS（含指定处理人姓名断言）、project 定向 forbidden 用例 PASS、inbox Vitest 30/30；重启 control-plane 后 admin 真实会话对 plan_review 事项 `c87e74cc` POST approve → 403 `{"error":"该事项由 Codex Smoke 处理，只有指定处理人可以执行此操作"}`，事项保持 open 未被误处理。全量 web typecheck 受 HEAD 既有 `projects.ts` `postJson` 双泛型参数错误阻断（非本次改动）。
- 2026-07-24 00:42（任务中枢项目选择改下拉）：平铺项目卡改为「触发器 + Popover 下拉」结构——触发器单行显示状态点+名称+状态词，下拉内搜索框（名称/目标过滤）+ 富信息选项卡滚动列表（max-height 收口），项目多时不再撑破排版；任务表单与对话锚点仍共用 `ProjectPicker`。顺带清理测试工厂里已被 `badabba2` 退役的 `approval_policy`/`evidence_policy` 残留并补 `directory_name`。验证：task-launches Vitest 20/20；task-launch 相关 tsc 零错误；真实浏览器验证下拉开合、22 项注入滚动收口、搜索过滤。全量 verify:web 仍受其他会话未完成改动（workflows/lib/api）阻断。
- 2026-07-24 00:28（任务中枢发起页改版）：删除顶部「提交后由协调线程动态编排」eyebrow；三模式卡升级为带选中圈/默认徽标/分色图标块的新设计；项目选择由下拉改为卡片单选（状态点+名称+目标+状态词），任务表单与对话锚点共用 `ProjectPicker`；顺带修复 HEAD 遗留的 index.test.tsx 多余 `}));` 语法错误。验证：task-launches Vitest 20/20；真实浏览器（admin 登录 + 真实项目数据）核对 Plan/对话两模式渲染。全量 verify:web 受工作树中其他会话未完成的 workflows/lib/api 改动阻断，未全绿。
- 2026-07-23 23:22（项目详情运行脉搏空态）：去掉独立「启动」文案卡；空项目直接展示本周七日格，空说明居中叠在日历切面中间（借鉴员工工作节奏空周，但不整页替换）。验证：detail Vitest 18/18；浏览器空项目见七日格+居中「本周暂无任务活动」。
- 2026-07-23 23:12（项目详情首屏视觉迭代）：区段导航改全宽底边线；空态右栏合并为单卡行级空态；去掉启动区重复 CTA；左右列等高；高级事实收成虚线轻栏。验证：Vitest 21/21；restart web 后空项目 1440 视觉自验满意。
- 2026-07-23 22:57（项目详情首屏重做）：七等分 Tab → 头卡下四区段（工作台/任务/审批/资产）；默认工作台；配置仅更多菜单→配置页；资产合并工件+预算+验收；工作台用 `lg:` 双栏（不再依赖未挂载 master-detail 容器）；空项目压缩为启动卡+单行空态。验证：定向 Vitest + restart web 浏览器空项目核对。
- 2026-07-23 22:45（项目详情运营首屏）：概览改为指挥台布局（运行脉搏+执行/阻塞+员工∥Runtime / 本项目阻塞+事件流+项目脉搏）；提交需求深链任务中枢 `?project=&mode=plan`；去掉运行总览出口；计划确认下沉审批 Tab。验证：ops-home/detail Vitest 19/19；restart web 后浏览器视觉核对。
- 2026-07-23 22:15（删项目级联员工失败态）：删除项目时一并 cancel `failed` 任务、ack 锚在该项目的失败/超时 run；总览 `operational_has_task_failure` 忽略已删项目上的 failed 孤儿。存量迁移清掉 2 条 failed 任务与未 ack 的失败 run，避免运行总览长期「异常」。
- 2026-07-23 16:10（已删项目 run 展示）：员工 run 详情/列表/日历 API 增加 `project_deleted`；所属项目保留名称并标注「已删除」、不再链进项目详情；筛选选项同标注。验证：employee Go 定向 PASS；drawer/history Vitest PASS。
- 2026-07-23 16:00（项目状态文案）：项目生命周期 `running` 展示由「运行中」改为「已就绪」（`projectStatusLabel` 覆盖；任务/run 的「运行中」不变）。验证：status-labels Vitest 14/14。
- 2026-07-23 15:55（删项目级联自动化规则）：删除项目前移除锚点规则与 fires，并删 Temporal Schedule；列表 JOIN 过滤已删项目；删除预览增加 `automation_rule_count`。存量已删项目上的 1 条规则+2 条 fires 已清。验证：`go test ./internal/automation` PASS；`GET /api/v1/automations` → `items:[]`；open inbox=0。
- 2026-07-23 15:50（删项目级联收件箱补洞）：open `digital_employee_run_recovery` 若 run 锚在已删项目的 `anchor_project_id`/`project_id` 一并 cancel；新建失败卡写入 `source_project_id`；删除预览 `open_inbox_count` 同口径。验证：迁移后 open inbox=0（4 条 recovery→cancelled）；`go test ./internal/inbox ./internal/employee` PASS。
- 2026-07-23 15:30（项目展示名/目录名拆分 + 存量清空）：`projects.directory_name` 与展示 `name` 分离（中文展示可改；ASCII 目录名全局唯一且创建后不可改）；创建向导拆 Git/非 Git（Git 填 URL，目录可从仓库名推导；非 Git 手填目录；去掉随机生成）；派发/mkdir/clone 元数据改走 `directory_name`；迁移软删清空平台存量项目及任务/决策/审批/inbox/成员/节点绑定/自动化规则关联。验证：`go test ./internal/project` PASS；draft Vitest 3/3；重启 CP 迁移后 active projects=0（再创建 1）；真实 API 创建 `客户接入试点`/`customer-onboarding-pilot` → 201 + `/var/superteam/workspaces/customer-onboarding-pilot` 落地 + ready；仅中文名无目录名 → 400。
- 2026-07-23 10:48（项目目录 P2 收口）：Claude Code / OpenCode 真实 chat 装卸 smoke 与 Codex 对齐（稳定 `{base}/{project_name}`、执行中技能软链+`.superteam/sessions/{cmd}`、结束后按清单卸净且业务文件保留）；旧 `workspaces/…/attempt`/`repos/{id}` 收窄为无 `project_name` 历史兼容并加注释/测试；无 MCP 时不设 `CODEX_HOME`/`OPENCODE_CONFIG`；改工作区根不迁存量（UI/注册表已警示）；清本机 smoke 项目目录与根下误污染/旧 attempt 子树。验证：`project_session` 9 + `project_workspace` 25 + `workspace_cleanup` 6 PASS；Claude/OpenCode 各 1 条 completed chat；capability healthy_count=1×3。
- 2026-07-23 09:50（项目目录落地 P2）：稳定项目目录会话装卸——技能按清单软链/unlink、MCP 写入 `.superteam/sessions/{command_id}/` 旁路投影并随 manifest 清理；Claude 继续 `--mcp-config`；Codex 用会话 `CODEX_HOME` overlay（auth.json 软链宿主，不改员工 auth home）；OpenCode 设 `OPENCODE_CONFIG` 指向会话投影（不设 `OPENCODE_CONFIG_DIR`）；新执行一律 `{base}/{project_name}`，旧 `workspaces/…/attempt` 与 `repos/{id}` 仅无 `project_name` 历史任务兼容；工作区根默认/示例/本地 config 对齐 `/var/superteam/workspaces`。验证：`project_session` 6 + `project_workspace` 23 + `mcp_config` 13 + `provider_command` 16 PASS；`SUPERTEAM_P2_VAR_SMOKE=1` 在 `/var/superteam/workspaces` 完成装载→卸载且业务文件保留；runtime 已 restart 且 env=`/var/superteam/workspaces`。阻塞：control-plane 未启动，未做真实 Provider 派发 E2E。
- 2026-07-23 02:05（项目目录落地 P1）：Git 绑定创建后异步 `clone_project_repository`（回写成功→ready+主节点；同批全失败→error，不自动重试）；人工 `POST .../workspace/reclone|mark-ready` + Console 详情菜单；派发前 `validate_project_workspace` 与主节点亲和联动；加节点 mkdir+clone 不降级已 ready。验证：`go test ./internal/project`（含 clone 回写）PASS；runtime `project_workspace` 23 PASS；Web 定向 77 PASS。
- 2026-07-23 01:49（项目目录落地 P0）：项目名=全局唯一 Linux 友好目录名（禁中文）；创建向关联 Runtime fan-out `ensure_project_directory`（失败回滚 DB+补偿删目录）；Provider CWD 对新项目走 `{base}/{project_name}`；系统配置 `runtime.workspace_base_dir`（execution/string，经心跳 platform_limits 下发，节点本地覆盖优先）；`workspace_ready_status` 非 Git 直接 ready 并记主节点，未就绪挡派发。验证：`go test ./internal/project|systemconfig` PASS；runtime `project_workspace`/`platform_limits` 26 测 PASS；Web 定向 38 测 PASS；真实链路创建 `ws-smoke-*` → 201 + 节点目录落地 + 重名 409 + 中文名 400；sysconfig 可见「系统工作区根目录」。P1 Git 异步 clone/人工出口未完成（pending 门禁已留）。
- 2026-07-22 23:47（自动化首页 OCR 修高/中）：事实条「72h 待触发」改为无上限计数（右栏仍限 8）；`pending` fire tone；inbox 计数与列表同源并避免「有数却空态」；下次触发页面级 `buildNextFireById` memo；interval 算术跳步；`hourCycle: h23` + 午夜单测。验证：automations Vitest 10/10。
- 2026-07-22 20:45（自动化任务首页工作台重构）：`/automations` 改为事实条 + 规则表（下次触发/健康）+ 常驻右栏（即将触发 / 待你处理 inbox / 最近触发）；空态三场景模板；新建抽屉分区（模式三卡、cron 大预设、闸门摘要）。下次触发客户端估算。验证：automations Vitest 7/7；浏览器 `/automations` 见事实条与右栏、新建抽屉模式卡与「到点后你还可能需要」。
- 2026-07-22 19:47（自动化任务 P1 真链路 PASS，分支 `feat/automation-tasks`）：迁移 `20260722150001` 已 apply 远端库（`automation_rules`/`automation_fires`）；worktree 重启 CP(:8081)/Web(:3000)。API：admin 创建 loop 规则 → trigger `succeeded` 产出 demand `source_type=automation`（含 rule/fire refs）→ 二次 trigger `skipped_overlap` → disable=`user_disabled`。浏览器 `/automations` 见规则行（项目名/循环/已手动停用/最近触发）。飞书投影未启 connector，未验。验证：`go test ./internal/automation` PASS。
- 2026-07-22 18:14（dev-services 默认 all 含飞书）：`scripts/dev-services.sh start|restart all` 纳入 `feishu-connector`（排在 control-plane 之后；缺 service token 仍只告警）。验证：`bash scripts/dev-services.test.sh` PASS。
- 2026-07-22 17:07（日历/GetRun chat 所属项目为空修复）：任务中枢对话 run 的项目落在 `tasks.params.metadata.anchor_project_id`，原先只 join `project_tasks`，日历点开几乎全是「无关联项目」。GetRun/run-calendar/runs 列表改为优先 `project_tasks`、回退 metadata 锚点。验证：`go test ./internal/employee` PASS；restart CP 后「开发-小王」本周 10 条 calendar 均有 `project_name`；chat `ca7e3150` GetRun=`多owner决策可见性E2E`；浏览器点「续接旧对话验证」抽屉「所属项目」同名。
- 2026-07-22 16:56（数字员工详情团队与绑定项目）：`GET /digital-employees/{id}` 补 `team_name` + `project_summary`；`GetRun` 补 `project_id`/`project_name`。详情头卡展示团队（无队「无团队归属」可点进团队）；右栏「绑定项目」列表链到具体项目；运行抽屉摘要增加「所属项目」。验证：Go employee/api PASS；Vitest 31/31；restart CP/web 后 API「开发-小王」team=`G4补员E2E团队`、3 个绑定项目；浏览器头卡/右栏可见；日历点「执行 echo 命令」抽屉所属项目=`多owner决策可见性E2E`。
- 2026-07-22 16:40（数字员工工作日历条目状态回 StatusPill）：上行恢复时分 + 紧凑 StatusPill；下行标题 `line-clamp-2` 超出省略。验证：日历 Vitest 4/4；restart web 后 pill 可见，长标题实测约 2 行收束。
- 2026-07-22 16:37（数字员工工作日历条目排版）：条目改为上行「时分 + 轻量状态文案」、下行标题独占列宽（去掉 StatusPill），避免状态挤占任务描述。验证：日历 Vitest 3/3；restart web 后「开发-小王」今日首条标题完整可见「续接旧对话验证：只回一个字好」（标题约占列宽 84%）。
- 2026-07-22 16:32（Runtime 总览加载慢）：远程 Postgres ~40ms/RTT 下，`GET /runtime/overview` 原先串行约 9 次查询且每次允许鉴权还写 `web_operation_logs`、会话每次强制 `last_seen` 写；总览页进门还并行打 events/enrollments。改为 overview 查询 errgroup 并行、允许决策不记审计写、会话 last_seen 60s 节流、前端仅打开对应 Tab 才拉 events/enrollments。验证：warm curl overview 由约 0.5–1.1s 降至约 0.19–0.25s；authz 允许路径不再插入 `runtime_scope.manage` 日志；浏览器 `/runtime` 首屏已渲染节点/事件，无长停「加载 Runtime 总览中」。
- 2026-07-22 16:30（数字员工工作日历条目显示时分）：日历日列每条运行在状态旁展示本地 `HH:mm`（来自 `created_at`），无需点开抽屉。验证：日历 Vitest 3/3；restart web 后「开发-小王」今日列可见 15:54/15:53/15:48/15:45。
- 2026-07-22 16:26（数字员工工作节奏空周居中）：空周改用 `V3EmptyState`，标题/说明/「去任务中枢」水平居中，不再贴左。验证：日历 Vitest 3/3；restart web 后「测试一下」空周 `text-align/align-items=center`。
- 2026-07-22 16:22（数字员工详情剔除「开始任务」）：详情页头卡与工作日历不再提供半吊子的直接开跑入口；任务发起统一走任务中枢对话。删除 `StartTaskDrawer` 及相关 create-run 接线；空周 CTA 改为「去任务中枢」。验证：Vitest 22/22；restart web 后浏览器「开发-小王」无「开始任务」仅「编辑配置」；「测试一下」空周见「去任务中枢」，点击后 SPA 进入 `/` 任务中枢。
- 2026-07-22 15:58（数字员工详情页去混乱重构，真实浏览器三档 PASS）：去掉独立 KPI 四卡与页底人格/预算双卡；身份头卡吸收一行 KPI；工作节奏限高（空周矮空态、有数据列内滚）；右栏合成「可调度能力」单卡并折叠项目调度就绪度。验证：Vitest 23/23；restart web 后 1280×800 / 1440×900 / 1920×1080 页级 scrollOverflow=0，「测试一下」空周矮空态、「开发-小王」日历点「重试失败任务」开抽屉；无人格记忆.md 双份。
- 2026-07-22 15:52（Codex 续对话 `--cd` 崩溃修复）：`codex exec resume` 不接受 `-C/--cd`，adapter 误传导致接续旧会话直接 exit 2（用户报错原文）。resume 路径去掉 `--cd`，工作目录只靠 `Command.current_dir`；新会话仍保留 `--cd`。验证：provider_command 两测 PASS；restart runtime-agent 后对原失败会话 `cbefee85`/`019f8054-…` 真实 resume，进程 argv 已无 `--cd`（`HAS_CD_ERR=false`）；本机 CLI 对照带 `--cd` 仍复现原报错、去掉后发出 `thread.started`。本次续跑失败改为 Codex 上游 `Reconnecting… request timed out`，与本修复正交。
- 2026-07-22 15:30（数字员工详情页信息架构重构，真实浏览器 PASS）：详情页按「是谁 → 忙不忙/稳不稳 → 做过什么 → 能干什么」重排。身份 SoftCard（大头像/角色·Provider/职责说明，主 CTA「开始任务」+「编辑配置」，审计/删除进更多菜单）；4 KPI MetricGrid；主区默认「工作节奏」周日历（可切列表）；右栏「能力与上下文」优先、「项目调度就绪度」次之。验证：employees 相关 Vitest 23/23；restart web 后 admin 打开 `/employees/1a677c19-…`（开发-小王）见身份头卡+KPI+本周 6 条日历，点「重试失败任务」打开运行抽屉。
- 2026-07-22 15:00（数字员工详情「工作日历」周视图，真实链路 PASS）：新增轻量 `GET .../run-calendar`（仅 id/标题/状态/时间，≤31 天、≤500 条，不含 result/diagnostic/session_state）；员工详情运行区「列表|日历」切换，周列条目点开复用 `RunDetailDrawer`（按需 `getRun`）。索引 `idx_task_runs_employee_created`。验证：同窗 6 条 calendar 1142B vs 重列表 7164B；浏览器 `/employees/1a677c19-…` 日历见 6 条并打开「重试失败任务」详情抽屉。
- 2026-07-22 11:56（收件箱同意弹窗改「结论+后果」优先，真实浏览器 PASS）：项目验收等决策弹窗去掉重复摘要与裸 `decision_type`/UUID 堆砌；主区展示你在确认什么、同意/驳回后果、触发需求/任务/项目、风险原因、「查看需求流程与产出」；技术 ID 收入折叠「技术详情」。验证：Vitest inbox+status-labels 36/36；restart web 后打开「验收 · 帮我分析…」同意弹窗可见「项目整体可关闭」与归档后果说明。
- 2026-07-22 11:26（项目验收收件箱卡以需求/任务为主身份，真实链路 PASS）：`project_acceptance` 技术上仍挂项目，但对人标题/摘要/context/关联引用改为需求优先（`验收 · {demand}`，context 含 `demands`/`primary_demand_id`/任务标题；DeepLink→`/workflows/{primary_demand_id}`）。两处打开路径（判据签署收敛 `maybeOpenProjectAcceptanceReview` + 协调器 `RequestProjectAcceptanceReview`）共用 helper；inbox adapter 投影 `InboxContext`；Console 关联引用需求→任务→项目。不回填旧「验收项目交付」卡。验证：Go helper/adapter/store + Vitest inbox 24/24；restart CP/web 后对 `多owner决策可见性E2E` 触发新卡标题「验收 · 跑一个任务」、详情关联需求可进流程编排，旧卡仍为「验收项目交付」。
- 2026-07-22 10:59（单次人类验收闸——planner 折叠 + Console 最终验收 UI，真实链路 PASS）：blocking `human_judgment` 强制 ≤1（多余 demote 为 non_blocking 清单）；planner prompt 约束最多一条人类总闸；Console 验收面板去掉逐条签署，改为顶部「最终验收」通过/不通过（存量多条 fan-out 顺序签 satisfied）。验证：`go test` acceptance_criteria 相关 PASS；Vitest criteria-panel 10/10；restart CP/web 后浏览器打开需求 `6cfc23eb`（原 3 条 blocking human）见单闸列表、无行内签署钮；API fan-out 三签 → demand `completed`，刷新后闸消失、三条均为「已满足/负责人判定」。
- 2026-07-22 09:48（项目事件流露出 created_at，真实链路 PASS）：DB/领域层本就有 `project_events.created_at`，契约与 API 响应此前未暴露。OpenAPI `ProjectEvent` 补必填 `created_at`；handler `projectEventResponse` 映射；Web 事件流条目显示相对时间（悬停绝对时间）。验证：`go test ./internal/project ./internal/api` PASS；定向 Vitest operational-detail/status-labels 全过（index 仍 1 失败=并发会话 create-project，与本次无关）；restart CP 后真实 curl events/overview 含 RFC3339 `created_at`；admin 登录项目 `77bbfe97-…` 右栏事件流可见「8 小时前」+ datetime（截图 /tmp/st-verify/04-event-timestamps.png）。横向管道可视化仍延后，见 TODO.md。
- 2026-07-22 02:07（项目详情页概览 UI 重构，真实浏览器 E2E PASS）：①头部去重——detail 模式 ShellPageHeader 只留返回+「项目详情」，项目名/状态/goal 由 hero 卡唯一承载；hero 四格 FactTile 统计行删除，改紧凑事实锚点行（阶段·待处理·执行中·需求，点击平滑滚动到对应卡片）。②操作分层——主按钮仅「提交需求」，「在运行总览查看」outline（图标改 SquareArrowOutUpRight 外链语义），配置/归档/删除收进「更多项目操作」DropdownMenu（删除 destructive），allowed_actions 门控与确认弹窗流程不变。③运行落点面板折叠化——就绪默认折叠为一行摘要（状态 pill+节点+命令通道+Provider chips），未绑定/阻塞默认展开，绑定/释放逻辑不变。④空态压缩——EmptyLine 单行紧凑化+可选 action，「当前需求」空态带「提交需求」CTA。⑤事件流移至右栏（负责人组/服务池之后，slice 6）+右栏 sticky；条目去同义 summary、补操作者补名（系统/人类/数字员工经 principalNamesById，无裸 UUID）。⑥demandStatusLabel 本地词表上移 status-labels.ts。验证：定向 Vitest 74 例 73 过（唯一失败为并发会话 create-project 改动所致的创建向导用例，与本次无关）；admin 真实登录 :3000 项目详情页复核头部去重/更多菜单/落点折叠展开/空态 CTA/右栏事件流补名（截图 /tmp/st-verify/01~03）。
- 2026-07-22 01:52（数字员工网格卡视觉层级重设计，真实浏览器 E2E PASS）：卡片从"浅底身份顶栏 + 表单式标签值堆叠"改为清晰层级——状态 pill 独立置于右上角一行（不再挤压名称宽度，长名称不再被 pill 截断）；头像 64→72px（`EmployeeAvatar` 新增 `xxl` 档）；「最近运行」升为主角指标（小灰标签 + 17px 粗体大字，成功绿/失败红双编码）；治理行与 Token 预算降为次级元信息；操作区「详情」改实心主按钮占主宽、「配置」降 ghost 次按钮，不再两键平铺。验证：Vitest employees 16 文件 137 例全过；admin 真实登录 :3000/employees，overview API 200、12 张卡按新结构渲染（截图 .scratch/employees-card-real.png）。
- 2026-07-22 00:59（项目级 approval_policy/evidence_policy 退役，spec `2026-07-22-project-dead-policy-retirement.md`）：迁移 `20260722004800` DROP `projects.approval_policy`/`evidence_policy`；OpenAPI/生成物与 Go/sqlc 读写删除；创建向导只映射 `coordination_policy.require_human_review_for_new_demands`；配置页 draft/revision 去掉审批/证据误导面。验证：`migrate-validate`（podman:55432）PASS；`go test ./internal/project ./internal/api` PASS；Vitest config/risk/operational 37/37；restart CP apply 后列仅剩 `coordination_policy`；真实创建项目 `77bbfe97-…` 响应无死字段，PUT 带 `approval_policy` → 400 `unknown field`。
- 2026-07-22 00:41（项目级 MCP 绑定后端退役，spec `2026-07-22-project-mcp-bindings-retirement.md`）：产品裁定真实项目 MCP=仓库 `.mcp.json`。迁移 `20260722003600` DROP `project_mcp_bindings`；删 `GET/PUT /projects/{id}/mcp-bindings` 契约/路由/handler/service/sqlc；`ListEffectiveMCPConfigForRuntime` 忽略 projectID，投影仅员工侧；删 Web 孤儿面板与 client。`verify:contracts` 顺手去掉 DEI 已删的 `execution-instance` 硬编码必检路径。验证：`go test ./internal/capability ./internal/api` PASS；restart control-plane 自动 apply 迁移后 `to_regclass('project_mcp_bindings')` 为空；真实登录后对项目 `9a9418d6-…` 的 GET/PUT `.../mcp-bindings` 均 404；随后 `migrate-validate`（podman:55432）PASS。全量 `verify:contracts` 仍被并发会话遗留的 OpenAPI 缺 `permission-changes`（TS client 仍有）挡住，与本次无关。
- 2026-07-22 00:38（dei 物理拆表）：`DROP VIEW digital_employee_runtime_readiness` + `DROP TABLE digital_employee_execution_instances`；删 dei CRUD/sqlc、死 provider-session JOIN、skill required-tools 旧链、GET `/execution-instance` 与 OpenAPI/Web 客户端；standalone preflight 改租户最少负载在线节点 + 合成 compat `execution_instance_id`。远程库表/视图均已消失；列表/详情页眉去掉「执行实例」残留文案。admin 浏览器走查：`/employees` overview 200、详情无 `execution-instance` 请求、调度就绪度指向项目 Runtime、开始任务抽屉可开；`/run-overview` 仅读 overview。
- 2026-07-21 23:31（dei 外科手术版退役 A–D）：杀协调备用腿 `AreRuntimeReady`/dei readiness 兜底与 planning/preflight 的员工级执行实例回退；总览 `execution_summary` 落点改最近一次 `task_runs`；封删 `PUT .../execution-instance`；迁移重建 readiness 视图为身份+治理+租户 provider 判据并 `DELETE` dei 数据行。
- 2026-07-22 00:40（收件箱全局 SSE——任意页角标秒级更新，真实浏览器 E2E PASS）：原 SSE 只挂收件箱页，其它页仅靠侧栏 badge 30s 轮询，任务完成后须手动刷新才见验收卡。抽出 `useInboxChangeStream`，挂到 `AuthenticatedLayout`（每登录会话一条长连接）；收件箱页去重双连；badge 轮询 30s→60s 兜底。不引入消息队列。验证：Vitest hook/layout/inbox 29/29；项目页停留时 DB 插入 open inbox 项→侧栏「收件箱 1」出现，进收件箱即见条目无需 F5。
- 2026-07-21 23:28（Gate 型高风险审批驳回无终结修复，真实链路 E2E PASS）：`ApplyPreDispatchGateDecision` 对带 `DispatchGateResultID` 的 `project_task_approval` 原先只在 approved 时放行重派，rejected 直接 `{}, nil`，决策卡已消费、任务永滞 `waiting_human`。现与非 gate 等待族对齐：非 approved 走 `applyTaskHumanWaitRelease`（rejected/cancelled→MarkFailed→任务 failed+需求重算）。定向测试 `TestProjectStoreApplyPreDispatchGateDecisionFailsGateLinkedApprovalOnReject` PASS。真实链路：restart CP→种 gate-linked pending 卡→`POST .../decisions/{id}/resolve` rejected 200→任务 `waiting_human`→`failed`、决策 `rejected`、事件 `project_task.failed`。
- 2026-07-21 22:35（数字员工网格卡去掉 Provider 行）：Provider 仅保留在右侧选中侧栏，避免与侧栏同构。Vitest employees index 20/20；restart web。
- 2026-07-21 22:32（选中员工侧栏大头像+完整说明，与网格卡差异化，真实浏览器 PASS）：网格卡去掉说明预览，只扫读名称/团队/事实行；右侧 `GallerySelectedPanel` 头像升 `hero`(80px)、说明全文展示（超长 `max-h-40` 滚动），保留运行态/Provider/最新事件/查看详情。验证：Vitest employees index 20/20；浏览器选中「测试一下」侧栏含完整说明、网格卡无说明正文。
- 2026-07-21 21:56（数字员工卡展示说明 + 创建填说明，真实浏览器 PASS）：列表卡去掉「身份：就绪」工作台态与「类型·团队」拼接，改为名称下仅团队（无则「无团队归属」）+ `description` 两行截断；事实行「运行」展示运营态。创建身份步新增可选「员工说明」并提交已有 `description` 字段（模板默认带入类型描述）。配置后编辑说明因无身份更新 API 延后（TODO.md）。验证：Vitest employees index+create 71/71；restart web 后 `/employees` 卡头团队/说明与运行行符合预期。
- 2026-07-21 21:42（数字员工画廊卡身份顶栏放大头像，真实浏览器 PASS）：列表卡从居中 48px 头像改为浅底身份顶栏——左侧 `hero`（80px）头像相对底板轻微上溢、右侧名称/归属/运行状态；侧栏选中摘要改 `xl`（64px）。`EmployeeAvatar` 增 `xl`/`hero` 档。验证：定向 Vitest employees index 20/20；restart web 后浏览器 `/employees` 实测 avatar `size-20`、上溢 10px、侧栏 62px，指标行与详情/配置仍在顶栏下方。
- 2026-07-21 01:09（失败任务软了结 soft dismiss，真实 API E2E PASS）：终态 failed 任务在恢复决策已 resolved、收件箱已空时仍留在列表并点亮项目「执行失败」。新增 `project_tasks.dismissed_at/by` + `POST .../tasks/{taskId}/dismiss`（仅 failed/cancelled 且无未决决策；status/审计保留），读路径与员工运营 join 默认排除，前端风险模型跳过 dismissed，项目任务表提供「清理任务」。真实链路：对「执行 echo 命令」`ba07d830` dismiss 200→列表 0 条、overview failed=0、事件 `project_task.dismissed`；planned 夹具 dismiss→409 中文拒绝。定向 Go project/api/employee + Vitest project-risk 13/13。`verify:contracts` 仍有既有缺 path `/permission-changes`（无关）。
- 2026-07-21 00:31（项目列表「等待超时/待处理」假阳性修复，真实浏览器 E2E PASS）：`project-risk` 原先仅因 `status=running` 且 `updated_at` 超 2h 就点亮「等待超时」并计入「N 项待处理」，导致无任务/无决策的僵尸 running 项目（如配置页体验走查E2E）列表有待处理、详情无可操作项。收紧为仅当存在超时的未决决策或 `waiting_human` 族任务时才报 SLA。定向 Vitest 12/12；浏览器 `/projects` 该行「暂无阻塞 / 无待处理项」、筛选「等待超时 0」。
- 2026-07-21 00:22（失败任务人类介入 + 员工双态消歧，真实链路 E2E PASS）：standalone run 失败原先只标终态、不进收件箱，运营态被历史 failed `project_tasks` 永久钉死「异常」，详情头「就绪+异常」双 pill 无层次。修复：①UI 主状态改「运行：…」+次文案「身份：…」并露出 reasons；②`task_runs` 增 `failure_acknowledged_at/by`，Fail/TimedOut writeback 投影 `digital_employee_run_recovery` 收件箱（重试/确认关闭），运营态优先 `waiting_human`，确认或重试后收敛；③`operational_has_task_failure` 仅计仍无已解决 recovery 决策的 failed 行；④项目失败恢复卡/收件箱暴露重试/取消下游（裸 approved 默认 retry）。真实链路：开发-小王 sticky error→idle；强制 Fail writeback→inbox 出现条目+`waiting_human`→acknowledge 后 idle；retry API 开新 run。定向 Go employee/inbox/api/projectcoordination + Web drawer/header/inbox 测试 PASS。
- 2026-07-20 23:40（数字员工详情头像解析统一到 avatar-library，真实浏览器 E2E PASS）：详情页 `EmployeeDetailHeader` 原先本地强转 `metadata.avatar`、无兜底；真实数据里大量员工只有 `{avatar:{id}, avatar_asset_id}` 无 `thumbnail_url`，左上角变 Bot 图标而列表有图。改接 `employeeAvatarAsset`，共享库与后端一致优先解析顶层 `avatar_asset_id`。定向 Vitest 6/6；浏览器打开 `开发-小王` 详情头像 src=`engineer-m-14-256.webp`（naturalWidth=256）。
- 2026-07-20 23:35（项目详情侧栏负责人组/服务池裸 UUID 补名，真实链路 E2E PASS）：概览 `display_name_snapshot` 为空时「项目负责人组」「项目服务池」直接渲染 `principal_id`。详情页接入与配置页同口径的用户/数字员工名称 map，侧栏与计划执行员工缺快照时回退真实名称。定向 Vitest 15/15；真实浏览器打开 `多owner决策可见性E2E` 侧栏显示「开发管理员」「Codex Plan Smoke」「开发-小王」，无裸 UUID。
- 2026-07-20 18:42（项目多人类负责人——merge 回退后重入 main，真实链路 E2E PASS）：权限中心 refactor 合入时把已落地的多负责人源文件回退（生成物/atlas.sum 残留、迁移 SQL 与 authz/service/web 源码丢失，HEAD 与 sqlc 漂移）。本提交恢复完整链路：①迁移 `human_owner_user_ids UUID[]` 双写回填；②authz 任一 owner 授权；③创建/成员同步 + ≥1 护栏；④任务验收放开给任一合格负责人；⑤配置页 owners 列表 + 创建页多 owner 提交；⑥宪法协作模型条同步。**真实 E2E**：restart CP/Web 后 `POST /projects` 带双 owner→DB 数组双写；`PUT /members` 空/无 owner→400 `project requires at least one human owner`，恢复双 owner 后数组重同步；第二负责人 `codex-plan-smoke` GET config 200 + PUT goal 200（非仅主 owner）；浏览器 `/projects/{id}/config` 概览同时渲染「开发管理员」+「Codex Plan Smoke」与多负责人提示文案。spec `docs/superpowers/specs/2026-07-20-project-multiple-human-owners.md`。
- 2026-07-20 15:37（权限中心重构——「审批中心」→「权限审批域」拆分，S2 全链 + 域分离真实 E2E PASS）：把原「审批中心」(/approvals，实为收件箱 `project_decision` 薄过滤视图) 重构为平台「操作权限审批域」，并入既有 `/permissions`「权限中心」页作新「权限审批」tab。①数据地基：`approval_requests` 加 `category`(permission|project_task，默认回填 project_task) + (tenant,category,status) 索引；**category=permission 的审批彻底脱离 inbox 投影**，权限中心直读审批域(`GET /permission-approvals` + `POST /{id}/decision` 新端点)，project_decision 留收件箱。②通用主体注册表(D5)：`PermissionApprovalSubject` 插件式(resource_type/决策词表/上下文渲染/路由/Apply 幂等)，`ResolveRequest` 批准**先跑主体 Apply 再 resolved**(失败保 pending)、reject/补证不触发 Apply、非 permission 类拒绝(域守卫)。③审批路由 v1：作用域内 approver>owner>admin 排除申请人。④**S2 团队特权角色申请全链**(真实消费者)：产生端点 `POST /teams/{id}/privileged-role-requests`→路由 team approver→批准→`tenant.GrantTeamMemberRole` 审批门控授予(绕过直接赋权拒绝, 多角色模型 upsert 角色行 + `team.member.grant_privileged_role` 审计)。⑤S1 数字员工配置修订审批占位(`ConfigRevisionActivator` 接口边界, nil activator→`ErrConfigActivatorNotReady`, 待员工配置 spec 阶段4剩余接 `ActivateConfigRevision`+产生侧)。⑥前端：权限审批 tab(三卡 metric/视图·状态·风险筛选/按事项并行/决策弹窗 当前 vs 变更后 diff) + `/approvals` 退役重定向到 `/permissions?tab=permission-approvals` + status-labels 补词。**真实链路 E2E**(restart CP 应用迁移+加载新码后 curl 走真实 admin/codex-smoke 会话直插 DB 造)：S2 闭环 request→admin view=mine 可见 / codex view=mine 不可见(路由域分离)→批准→codex 获 approver 角色 + audit + decision record；驳回腿不授权(admin 行 0)；补证腿 needs_more_evidence；域分离——DB 39 条 project_task approval **全不**出现在 `/permission-approvals` 而 `/inbox` 仍出 17 条 project_decision；浏览器(PM isolated chromium)admin 登录 `/approvals`→重定向 `/permissions?tab=permission-approvals`、权限审批 tab[selected] 三卡 0/0/0 + 筛选 + 空态真实渲染。门禁：verify:control-plane(契约+全 Go) + migrate-validate(双迁移重放) + web typecheck/build/受影响 30 用例 PASS。合并走隔离 worktree 全量 merge 后 `update-ref` 快进 + 仅我的 37 文件 path-checkout 同步(避免卷入多负责人 staged WIP)。**遗留**：S1 E2E 阻塞待员工配置 spec；requester_name 补名 / 真候选审批人集合路由为债。
- 2026-07-20 14:30（修「审批中心高风险动作审批完又弹一条」死循环，真实链路 E2E PASS）：根因是三因叠加。①派发后 `StartProjectTaskRun` 因员工 runtime 会话未激活而失败（`runtime session is not active`），attempt 全 `lost`；②失败恢复把任务回退 `planned` 并清空 `waiting_request_id`，而风险批准判据 `preDispatchRiskApprovalGranted` 只认 `waiting_request_id` 指向的那条已批准 decision → 凭证丢失 → 重派发时门禁重铸一条新的风险审批，无限循环；③审批卡只有模板文案，不说是哪个动作。修复：A 批准持久化——`preDispatchRiskApprovalGranted` 增加扫描兜底，任一已批准的 gate 链接 `project_task_approval` decision 即视为已授权（真实改动会 replan 成新 task id，旧批准不会跨计划泄漏）；B runtime 会话不活跃直接拦在门禁——`loadPreDispatchGateSnapshot` 用 `snapshot.Runtime.NodeOnline && resolution.NodeID != nil` 保留 preflight 的会话活跃信号，会话断了走 `retry_later` 退避而非放行到起 run 才炸；C 卡片自描述——摘要嵌入任务名 + 可读触发原因（`riskReasonLabel`），context 补 `task_title`/`risk_reason`/`risk_reason_label`。真实链路：control-plane 重启加载新码→对活体协调线程发真实 `HumanDecisionSubmitted` 信号→观察 `ApplyPreDispatchGateDecision→DispatchProjectTask→RunPreDispatchGate` 实跑，风险审批计数停在 5（未铸第 6 条）、真实 run 绑定成功（无 session 报错）、任务进 terminal `failed`、收件箱 0 条挂起高风险。新增定向测试（durable grant 复现循环 + 卡片描述），verify:control-plane 全 PASS。
- 2026-07-20 01:00（配置项目 UI 重构——成员人性化 + 数字员工池/死策略 Tab 下线 + 协调策略结构化 + 全页 UUID 补名，真实链路 E2E PASS）：走查 `项目管理→配置项目` 页四类问题落地。①成员/数字员工池合并为单一「成员」Tab（人类成员+数字员工两组富卡片：头像/`ObjectRef` 名称(id)/描述/角色·类型经词表/加入时间/员工深链），删「数字员工池」Tab，全量替换 JSON 降级为「高级」折叠区。②死策略下线：`approval_policy`/`evidence_policy` 项目级零行为读取，下线「审批规则」「证据归档」Tab（后端 API/数据/修订对比保留，非破坏式）；`coordination_policy` 改结构化开关（`require_human_review_for_new_demands`/`max_plan_iterations`）+完整 JSON 双向绑定。③UUID→名称(id)：负责人/修订创建人/成员经 `ObjectRef` 补名（数字员工 join `listDigitalEmployees`、人类 join `listUsers`），角色/类型加 `projectRoleLabel`/`principalTypeLabel` 词表。后端 `ProjectMember` 响应+契约补 `created_at`（加入时间）。真实链路 E2E PASS（真实 API 造项目→浏览器成员卡→保存持久化→revision 创建人补名）；verify:contracts/control-plane/web 全 PASS。（提交 6a45dba5 UI 腿 + 23b50c3d created_at 后端腿，因共享 index 竞态拆两提交。）
- 2026-07-20 01:12（下线侧栏「审批中心」菜单，真实浏览器 E2E PASS）：应用户要求删除侧栏「治理平台」组下的「审批中心」导航项（`apps/web/src/components/layout/data/sidebar-data.ts` 移除该 item 及仅其使用的 `ShieldCheck` 图标导入）。同步更新两处依赖该菜单结构的测试：`sidebar-data.test.ts`（治理平台组期望标题数组去掉「审批中心」，首项断言改 Runtime 节点、成本管理索引 3→2）、`search-provider.test.tsx`（命令面板导航用例改点「Runtime 节点」→/runtime，因该项已不在菜单/搜索索引中）。/approvals 路由与 features/approvals 页面本体保留（仍可经 URL 访问，非本次范围）。验证：定向 Vitest（sidebar-data + search-provider）16/16 过；真实链路 admin 登录 :3000 浏览器复核侧栏「治理平台」组已不含「审批中心」（首项为 Runtime 节点）。
- 2026-07-20 01:00（流程编排河道口径治本——`scope` 过滤 + 遗留测试数据清理，真实全链 E2E PASS）：用户报告流程编排与运行总览脱钩、堆积大量遗留测试数据。对账 dev 库证实两类根因：①**数据未清**——`project_demands`/`projects` 全是 E2E 夹具，`ListWorkflowInstances` 唯一过滤仅 `projects.deleted_at IS NULL`+成员可见，既不看 `archived_at` 也不排除终态，导致任何 demand 只要项目未软删就永久堆在河道；②**与运行总览异构**——河道以 demand 为脊柱、运行总览以员工/团队为脊柱，天然不同源。先经真实级联删除端点 `DELETE /api/v1/projects/{id}` 软删 4 个测试项目（连带取消任务/决策/审批/收件箱+落审计），河道清空。治本加 `scope` 口径（默认 `active`）：SQL 层把最终 SELECT 包成 `instances` CTE、引入 `project_archived_at`，在 `LIMIT` 之前按 scope 过滤——`active`=未归档且非终态（排除 completed/cancelled；**failed 属"需要介入"保留在运行视图**，不埋进归档）、`archived`=已归档项目或终态实例、`all`=不过滤；契约加 `scope` 查询参数（enum active/archived/all，default active）+ handler 校验（非法→400）+ service 兜底默认 active。前端河道加 `V3Segmented` 运行中/已归档页签（默认 active，页签在空/加载/错误态也常驻以便切回），queryKey 纳入 scope，空态文案随 scope 区分。真实 E2E（restart CP 后 curl 走 admin 真实 API + 浏览器走真实 /workflows）：直插三类夹具（活跃/已完成终态/归档项目下需求）验证 `scope=active` 仅返回活跃 1 条、`archived` 返回终态+归档 2 条、`all` 返回 3 条、非法 scope→400；浏览器默认"运行中"仅显 1 条、切"已归档"显 2 条（显示 2/2），夹具清理后三 scope 均空。注：河道与运行总览仍为异构脊柱，scope 只让河道语义收敛到"在跑"，非合并两页数据源。
- 2026-07-19 22:47（"待确认"跨视图一致性治本——决策批准后任务释放死路修复 + 员工待确认判据收窄，真实全链 E2E PASS）：用户报告运行总览多员工"待确认"但收件箱仅 1 条。对账 dev 库证实两类根因：①**释放死路**——收件箱决策卡 approve 后任务永滞 waiting_human：`ResolveDecision` 只桥接 `project_task_acceptance`，clarification/recovery 家族落入 coordinator `handleHumanDecisionSubmittedFromStore` default 分支空转（9 条任务决策已 approved 仍卡 waiting_human，含看门狗新产 `project_task_recovery` 卡全家族不可解）；②**判据过宽+死词表**——员工级 human blocker 计入 `requires_human_approval` 非终态任务（blocked/running 也点亮待确认），pending 决策腿过滤 `('task_failure_recovery','route_review')` 与实际创建类型脱节从未触发。修复：**释放统一收进 coordinator 派发管线**——`ApplyPreDispatchGateDecision` activity 内按决策类型数据判别（workflow 层加 case 需 GetVersion 围栏，而 GetVersion 每 execution 粘滞会把历史里已有此类信号的长命协调线程钉死在旧死路直到 continue-as-new；activity 不参与 replay，存量线程立即受益），任务等待家族（recovery/runtime_recovery/clarification/missing_context/permission/plan_invalid/budget_approval/human_wait/无 gate 的 approval）approved→`ReleaseProjectTaskHumanWaitForRedispatch`（旧 waiting attempt 让位 supersede→cancelled 容忍已终态 0 行、任务回 planned 清 waiting/current_attempt/**run 绑定**——残留 `digital_employee_run_id` 会被 DispatchProjectTask 幂等短路静默 no-op）→正常 gate+run 启动派发；rejected→任务 failed+需求重算。E2E 中揪修 3 个埋伏缺陷：resume 直建 queued attempt 撞 `uq_project_task_attempts_active`（waiting_human 计活跃）、supersede no-rows 误报 project not found、释放后 dispatch 被旧 run 绑定短路。判据收窄：blocker 只认任务 waiting_human/pending_review 或其上未决决策（类型除 project_acceptance 外全计入），rha 非终态子句删除，`pg_repository_test` 护栏 pin 同步更新。真实 E2E（restart CP 后 curl 走 admin 真实 API）：recovery 卡 approve→"Run uname"任务 planned→gate→run→runtime 领取→**completed**；三轮熔断任务 approve→released→真实 claude 复跑→再次产生 clarification 卡（终态对账：总览待确认 1 ⟺ 收件箱 open 1，全部可解释可操作）；rejected 腿 3 例真实落 failed；G4 死夹具 10 条任务（员工无 runtime 永不可派发）DB 置 cancelled。`go test ./...` 全绿。已知残留（未立项，待人类拍板）：ResolveDecision 决策落库与 signal 非事务，signal 失败决策已消费需 `RetryWorkflowSignal` 补发；`ResolveProjectTaskHumanWait` 的 resume_same_task 分支（无任何调用方）仍是建 attempt 无 run 的死路；gate 型 approval 卡 rejected 无终结路径；failed 任务无限期点亮员工"异常"（`operational_has_task_failure` 无时间界）。（注:本条中"判据收窄"的 SQL 改动位于 employee_execution.sql，与并发会话"员工总览 Provider 判据"改动共享同一生成物，随该次提交一并落地；本提交仅含释放死路修复的 project/coordination 部分。）
- 2026-07-19 22:38（员工总览卡 Provider 行改身份级 + "待配置"判据去员工级 Runtime 绑定化，真实 API+浏览器 E2E PASS）：用户指出卡片"等待绑定 Runtime Agent"错误——运行落点由项目派发时动态解析（`run_service.go` 已注明员工级执行实例 preflight designed-off），员工卡只应显示身份级 agent 能力（claude/codex/opencode），且 dei 派生判据致新员工恒显"待配置"。修复四层：契约 `identity_summary` 增必填 `provider_type`、`DigitalEmployeeWorkbenchStatus` 枚举 `pending_binding`→`needs_configuration`、summary/queue_summary `pending_runtime_binding_count`→`needs_configuration_count`、filters 删 `runtime_nodes`/`execution_statuses` 维度及对应查询参数；SQL 四查询（summary/items/facts/filter options）去 dei 依赖，新增 `available_provider_types` CTE（租户内在线节点的 healthy provider 集合）产出 `tenant_provider_available`，provider 筛选与选项改源 `digital_employees.provider_type`（items 保留 dei 字段仅供 execution_summary 展示——run-overview 员工地图仍读 node_id 定位座位）；Go workbench 判据收敛为身份状态+配置治理+租户级 provider 可用性（`waiting_runtime_count` 重定义为 provider 无在线可用 Runtime 的员工数）；前端卡片/侧栏 Provider 行改 `providerDisplayName(identity_summary.provider_type)`、趋势卡与待处理队列"待绑定"→"待配置"（深链 /approvals）、删"执行"筛选。验证：`verify:contracts` PASS，`go test internal/employee internal/api` 除并发会话在途改动的 2 例（waiting_human 判据收窄死词表+容量配置中心化）外全绿，web 定向测试 121 例全过+tsc 零错；restart control-plane 后真实链路：curl overview 确认报障员工"测试一下"ready+codex、唯一待配置为治理 pending 员工、计数一致，admin 浏览器 :3000/employees 复核卡片/趋势/队列渲染。遗留债（未立项）：`digital_employee_runtime_readiness` 视图（协调线程 AreRuntimeReady fallback）仍 dei 基与新 runnable 判据不一致（代码注释已标记）；创建链路 runtime/provider provisioning 选项仍为员工级绑定概念。
- 2026-07-19 19:27（收件箱/审批中心处理动作按事项并行化——用户报告"批准一条后下一条卡提交中"，真实浏览器 E2E PASS）：根因不在后端（`inbox.ExecuteAction`/`project.ResolveDecision` 无跨事项锁，Temporal Signal 投递即返回），而是前端两层缺陷：①`features/inbox/index.tsx` 页面级全局单飞锁 `actionInFlightRef` + 单一 mutation 实例——上一条在飞时第二条的提交被 `Promise.resolve()` **静默吞掉**（弹窗卡"提交中"，再被第一条的 onSuccess 顺带关闭，第二条从未发出）；②弹窗 `inbox-action-dialog.tsx` 跨事项复用的布尔 `inFlightRef` 在 useEffect 里重置不触发重渲染，上一条提交成功后重开任意弹窗按钮滞留禁用态（修复中经组件测试实锤的既有病灶）。修复：**提交按事项并行**——页面层改 `pendingItemIds` 集合（onMutate 入/onSettled 出），弹窗 `pending` 只看当前事项；onSuccess 仅关闭"完成事项===当前弹窗事项"的弹窗，切走不打断；弹窗在飞标记改按 `(事项,动作)` 键（键不匹配自然失效+同键同步防双击），迟到失败经键比对不回写到已切换的事项；**提交中允许关闭弹窗**（取消钮变"关闭"+后台继续提示语），后台失败升级页面"操作未完成"横幅不静默（弹窗仍停在该事项则维持内联展示）。审批中心 `features/approvals/index.tsx` 复用同弹窗，同步套用同一模式（否则弹窗新"关闭"钮会被其旧 isPending 守卫拦成点击无效）+新增后台失败横幅。测试：新增并发双事项提交（deferred 卡住第一条，断言第二条 POST 真实发出——旧实现的回归锚）与关闭后失败上横幅两例，inbox 26 + approvals 2 全过，web tsc 零错。真实链路验证：admin 浏览器 :3000 收件箱连续批准两条 G4 补员 E2E 遗留高风险决策（正是报障场景），第二条弹窗提交钮即时可用无卡滞，开放事项 3→2→1，DB 复核两条 decision 11:23:29/11:24:05 落 resolved；审批中心页队列/弹窗开关复验正常。真实并发在飞窗口因 dev 后端响应快无法手工卡出，由组件测试（真实 Chromium+deferred fetcher）覆盖。
- 2026-07-19 19:03（收件箱处理弹窗对象指称布局重构 + ObjectRef 共享组件，真实浏览器 E2E PASS）：18:10 来源补名落地后，弹窗内 `名称 (uuid)` 整串拼接 + 右对齐 justify-between 键值布局在长名称/UUID 下折行成锯齿多行块。**①新组件 `components/superteam/object-ref.tsx`**（DESIGN.md「对象指称用名称，标识符只作补充」的组件落点）：`ObjectRef` 名称为主文本 + `ObjectIdChip` mono 短 id chip（UUID 首段 `4be304f7…`，点击复制完整 id、1.5s 已复制反馈、title 悬停全量），名称缺失（来源已删除）回退完整 id 同样可复制；barrel 导出 + 4 例组件测试（含 clipboard spy）。**②弹窗 `inbox-action-dialog.tsx`**：ContextPair（label 左/value 右 justify-between）改 ContextField（标签在上、值在下左对齐，长值折行齐整）；风险等级由纯文本改 StatusPill（riskTone 双编码，与列表同语言）；来源项目/来源任务走 ObjectRef、审批请求走 ObjectIdChip；上下文摘要区同步迁移。**③详情面板 `inbox-shell.tsx`「关联任务」**同迁 ObjectRef（原 truncate 整串截断）；`source-ref.ts` 拼接 helper 随之删除（全部调用点已迁）。真实 E2E：浏览器 admin 登录 :3000 收件箱 → 打开"声明式交付物v2GATE"事项 → 同意弹窗呈现新布局（风险 pill/名称主文本+短 id chip/左对齐无锯齿，截图核对）→ 点击 chip 实测 writeText resolve（title 翻转"已复制"）；定向测试 object-ref 4/4 + inbox 21/21 + status-labels guard 过，web tsc 零错。（收件箱来源补名 + 状态中文统一——两项细节修正，真实 E2E PASS）：**①收件箱来源项目/任务补名**：收件箱读路径（ListItems/ExecuteAction 返回）批量解析 `source_project_id/source_task_id` → 新增响应字段 `source_project_name/source_task_name`（sqlc 新查询 `ListInboxProjectNames`/`ListInboxProjectTaskTitles` 按 `id = ANY()` 批量取名，读时解析不入库快照，来源已删除时缺省由前端回退裸 id；契约 InboxItem 同步增字段并重新生成）。前端"同意/驳回/要求补证"弹窗的来源项目/来源任务、收件箱详情"关联任务"、关联引用卡、审批中心来源行改为 `名称 (uuid)` 或纯名称显示（新 helper `features/inbox/source-ref.ts`；`formatContext` 优先服务端补名再回退 context 快照）。**②状态中文统一**：`lib/status-labels.ts` 补缺失键（approved=已批准、request_changes、restaffed、exempted、held、not_applicable、online/offline、ok），新增 `riskLevelLabel`/`deleteBlockerTypeLabel`；接入此前漏掉映射的 10 处裸英文显示点：审批中心状态 pill+筛选 chips（项目 chip 顺带显示项目名）、MCP 管理状态、账号页用户状态、Runtime scope 节点在线状态、员工能力面板 server/binding 状态、成员角色摘要、授权引擎状态、项目当前阻塞/协调线程状态、工作流节点与计划图风险等级、员工/项目删除阻塞项（类型+状态）。真实 E2E：control-plane 重启后 curl 实测 items 带名称字段；浏览器走通收件箱详情+同意弹窗（名称 (uuid) 呈现）、项目审批 tab（已批准/待处理）、审批中心（中文 chips/pill+项目名来源行）、MCP 页（启用中）。定向测试：Go inbox/api 全过（含新增补名单测），web 涉改 feature 测试全过（两处断言旧英文原文的测试已更新），`verify:contracts` PASS。遗留观察：纯技术上下文的 mono UUID 行（归档面板 project_id、执行轨迹 source_id、判据面板 task_id 等）未动，属技术详情展示非来源指称。**③规范沉淀（应用户要求，防同类问题复发）**：DESIGN.md 新增「面向用户文本与枚举显示（中文优先）」章节（机器枚举必经 status-labels 词表/缺键在词表补/对象指称"名称 (id)"服务端补名/新增枚举的完成定义）+ 实施检查清单加项；AGENTS.md 工程约定加硬性指引；新增护栏测试 `status-labels.guard.test.ts`（vite ?raw 扫 features 源码拦 `{x.status}` 裸枚举直渲，已验证种入违规即红并报 file:line，扫描集非空断言防假绿）。
- 2026-07-19 18:07（收件箱 SSE 推送替代 5s 前端轮询——spec 2026-07-19-inbox-sse-push-design 全项落地，spec §4 四项真实 E2E 全 PASS）：根治收件箱页"每隔几秒刷新一次"观感（实为 5s 盲轮询 + "正在刷新"胶囊插拔造成的三栏布局跳动）。**①服务端脏通知流**：新端点 `GET /api/v1/inbox/stream`（SSE），照抄 StreamActivity 的"游标增量探测→推帧+keepalive"模式；sqlc 新查询 `PeekInboxChange` 按 `(updated_at, id)` 游标探测 actor 可见范围内最新变更（可见性谓词与 ListInboxItems 同口径，含 any-of-N 项目决策成员可见；具团队读权放宽到全租户，建流时判权一次），只推 `event: inbox-changed` 脏通知不推数据——授权与筛选收敛在既有 ListItems 路径，无二次判权面；流启动游标回拨 5s 兜 DB/应用时钟偏差（宁多推一帧不漏）。**②迁移 20260719094919**：`idx_inbox_items_tenant_updated_id` 索引服务游标谓词（atlas hash+throwaway PG validate 过）。**③前端**：inbox 页挂 EventSource（收 inbox-changed → invalidate items+badge），5s 轮询降为 60s 兜底流断开窗口，删除"正在刷新"胶囊（布局跳动元凶）；新增 SSE 推送触发重拉的组件测试，删除胶囊断言。**④契约**：openapi.yaml 增 `/api/v1/inbox/stream` path（照 activity/stream 先例）。**真实 E2E 四项**（浏览器 Playwright + curl 双腿）：⑴ 双会话推送——页面开着时 DB 侧新增/resolve，新事项 ~7s 内自动出现、已解决事项自动消失，无布局跳动，服务端日志证实浏览器持有 stream 长连接；⑵ 外部渠道等价——resolve 走 DB 直写（等价于飞书回调经 projector 落库同一张表，触发器推进 updated_at，流按表感知；未走真实飞书回调，已在此声明）；⑶ 断流自愈——页面开着重启 control-plane，EventSource 自动重连，恢复后插入 ~6s 内推达；⑷ 团队视图权限隔离——判别性结果：admin 流收到第三人事项+本人事项两帧，无团队读权账号只收到目标为自己的一帧。curl 腿时延实测：插入→推帧 ~2.5s（2s 探测间隔内）。E2E fixture（5 条事项+临时用户）已清理。取舍记录见 spec §5（服务端仍是轮询 DB→推流，与 activity 流同构，并发流量级大再统一升级）。
- 2026-07-19 14:14（系统配置中心 P2：runtime 平台限额心跳下发 + 心跳超时可配——spec 2026-07-19-system-config-p2 全项落地，主体提交 00437cc6、merge daa17d24，spec §8 六项真实 E2E 全 PASS）：根治"CP/runtime 双份硬编码限额、平台改了 runtime 不知道"。**①下发通道**：心跳响应携带 `platform_limits` 快照（版本指纹 `plv1:sha256:<对六限额有序序列化>`，字段全可选，任一侧缺失回退本地默认值零破坏）；runtime 新增 `platform_limits.rs` 进程内 RwLock 快照，`artifacts.rs`（工件截断/附件三限额/声明式交付物单文件上限跟随工件上限）与 `skills.rs`（解包两限额）读取点全部改走快照，按任务粒度取值，指纹变更 info 留痕一条 old→new。**②注册表 +6 key**：附件单文件/数量/总量、技能解包大小/文件数（domain=artifact）、`runtime.heartbeat_timeout_seconds`（domain=execution，10–600s）；新增 `ValueType int` 纯计数型，前端弹窗免单位换算（units 单测钉住 + 弹窗空单位括号修正）。**③工件上限解锁 100MiB+版本偏斜护栏**：心跳请求新增能力自报 `supports_platform_limits` 落节点 metadata（仅变化时写）；presign 时租户内存在在线未自报节点 → clamp 回 10MiB + warn 留痕（探测失败也保守 clamp），全员具备后自动解除——调高动作最坏"暂未生效"，永不静默丢文件。**④心跳超时可配三使用点显式穿参**（拒绝全局可变状态）：`IsOnlineAt(timeout)` model 方法不做 IO；runtime Service/Scheduler `SetHeartbeatTimeoutResolver` 闭包注入（recordToNode 派生状态同口径）；authz 仓储构造注入（不 import systemconfig 防环）。**⑤契约**：control-plane 契约 heartbeat 200 改 `RuntimeHeartbeatResponse`（allOf RuntimeNode+platform_limits）+请求能力位+`PlatformLimits` schema；runtime 契约补心跳 sideband schema 片段（既有"心跳协议不在契约"债不在本次补全）。**Go/Rust 字段对照清单（人工核对一致）**：请求 `supports_platform_limits`(Go handler 匿名 struct bool / Rust HeartbeatRequest bool)；响应 `platform_limits{version, artifact_max_file_size_bytes, attachment_max_file_size_bytes, attachment_max_count, attachment_total_max_bytes, skill_archive_max_bytes, skill_archive_max_file_count}`(Go platformLimitsResponse int64×6+string / Rust PlatformLimits Option<u64>×6+Option<String>，Rust 侧全 serde default)。**sqlc**：`PatchRuntimeNodeMetadata`（jsonb merge）+`CountOnlineLegacyLimitRuntimeNodesForTenant`，零迁移。**真实 E2E（merged main 服务）**：①附件上限调 2MiB → 真实 claude 任务产出 3MB big-notes.md → runtime 日志按新值跳过（对照旧运行 5MiB 行）→ CP `project_artifact_refs` 落 `execution_output_skipped` 痕；②工件上限调 20MiB → 同任务 15728640 字节 deliverables/big-report.bin 全链成功（declared 工件+declared_output 证据）；混布=legacy 节点（enroll/hello 换真 session token、心跳不带能力位）在线时 presign 15MiB 400 clamp 回 10MiB+warn 日志，节点"升级"自报后同请求拿到 upload_url（clamp 自动解除）；③心跳超时 10s → 停 agent 37s 后 overview 判 offline（默认 60s 下应仍 online），恢复默认后回线；④断连期间改配置 → 重连单条 old→new 收敛日志（三段指纹链 local-defaults→7190c7→314fc0，E2E 后复位再收敛回 3595aa）；⑤旧协议心跳（无新字段）200 零报错、响应新字段旧 agent serde 忽略；⑥verify:control-plane/runtime-agent/web/contracts 全绿。浏览器走查：系统配置页三 tab（执行与调度 tab 首个条目就位）、int 弹窗"取值，范围 1 – 100"无空括号。E2E 现场已清理（覆盖复位、legacy 节点归档+token/enrollment 吊销）。非目标不变：不做租户级差异化下发、provider 超时/LRU 维持节点 env、不做推送通道（30s 心跳轮询）。
- 2026-07-19 13:50（验收判据卡深链声明式交付物——证据紧邻签署按钮，遗留立项 §3 落地，全链真实 E2E PASS；**代码已单独提交 b446071e**，本 CHANGELOG 条目留工作树随并发下次提交带走，不自建 CHANGELOG 提交以免捕获并发活跃编辑的半成品）：验收面板判据卡此前只显示满足任务的结论文本，人要核对交付物得跳工件页自己找。落地思路=沿平台既有事实链（判据 satisfied_by → 任务 → 契约 → declared 工件）再投影一跳，不发明"判据-文件"新边、零新建模：`ListDemandAcceptanceCriteriaDetail` 在其反橡皮图章早已构建的 taskIDs union 上多批量拉一组 declared 交付物（新 sqlc `ListProjectDeclaredArtifactsByTaskIDs`，`WHERE artifact_type='declared' AND object_ref LIKE 'artifacts/%'` 只取内容寻址可取回行——declared_skipped/外部引用天然排除，不下发不可点击 chip），挂到每个任务的 `TaskSummary.Deliverables`。契约扩展 `DemandCriterionDeliverable{artifact_ref_id,title,content_type,size_bytes}`。Web：判据卡"查看满足任务产出"折叠块内每个任务结论下渲染交付物 chip（可预览类型给预览按钮+下载，其余仅下载），点击复用工件面板的 `ArtifactPreviewSheet`——顺手把它的入参从完整 `ProjectArtifactRef` 解耦为 `PreviewableArtifact{id,title,content_type}` 最小形状，跨 feature（projects→workflows）复用不耦合完整工件类型，既有工件面板调用方因 ProjectArtifactRef 是超集不受影响。预览走 §1 落地的 content 端点两步取回（credentialed 拿 URL→无凭证取桶），sandbox srcDoc 安全模型不变。单测：CP `resolveDeclaredDeliverableRefs`... 实为 `ListDemandAcceptanceCriteriaDetailSurfacesDeclaredDeliverables`（只取本任务 declared 可取回行，排除附件/非内容寻址/他任务）+ web 判据卡 chip 渲染（HTML 有预览按钮、bin 仅下载、下载 href 为 artifact UUID）。真实 E2E：①API——demand 06449a1c（含 declared summary.md、任务 86a621a8 completed）的 acceptance-criteria 端点投影出交付物 `{title:summary.md, content_type:text/markdown, artifact_ref_id:89c58adb}`；②浏览器——真实登录 `/workflows/{demandId}` 判据卡展开"查看满足任务产出"→交付物 chip 可见（下载 href=content 端点+artifact UUID）→点预览→Sheet 打开渲染 summary.md 的 markdown（"Summary"标题+正文）。截图 gate-criteria-deeplink.png。sign 流程未动（仅给 CriterionRow 加 onPreview prop），既有 web 判据签署测试 7 例全过佐证无回归。至此附件遗留立项三项：§1 format=json（已入 main）、§3 判据深链（本条 b446071e）落地；§2 生产桶 CORS 引导命令入 TODO 延后。
- 2026-07-19 13:40（数字员工页打开即报错修复——迁移 087 漏改读侧，真实浏览器 E2E PASS）：`/api/v1/digital-employees/overview` 500，根因是 `employee_execution.sql` 的 `ListDigitalEmployeeOverviewItems` `mcp_counts` CTE 仍过滤 `mcp_servers.status='active'` 与 `digital_employee_mcp_bindings_v2.status='active'`——两列已被迁移 087（MCP 禁用生命周期废除）删除，属 087 读侧清扫漏网（同文件其余三条 overview 查询已清干净）。修复：删两谓词，有效性口径统一为既有 `deleted_at IS NULL`；sqlc 重生成。全库回查确认无其他漏网引用（tenant_members 的 status/disabled_at 为另一表，087 未动）。验证：修复前四条 overview SQL 直连 dev 库定位仅 items 报 `column m.status does not exist`，修复后同参执行返回 12 行；`go test ./internal/employee/` 过；重启 control-plane 后浏览器真实登录走 `/employees`，overview 200，摘要卡/员工卡/待处理队列全量渲染。
- 2026-07-19 13:45（迁移命名改用 UTC 时间戳版本号，根治并发撞号，用户拍板方案 1）：三位顺序号是无分配机制的全局计数器，并发分支必然撞号（当天实际撞号两次：系统配置迁移被 085/087 连抢、两度重编号→088）。新约定：新迁移一律 `make -C apps/control-plane new-migration NAME=snake_case_描述`（内部 `atlas migrate new`，产出 `20260719133045_描述.sql` 并自动更新 atlas.sum）；存量 001–088 保持原名，Atlas 按版本字符串字典序排序、时间戳天然排在存量之后，混用安全。DATABASE_DESIGN.md §10.1 已更新。验证：make 目标真实生成探针迁移（20260719053817_）→ podman 一次性 pg16 全量重放 validate exit=0（混排排序实证）→ 探针删除 + atlas.sum 复原。
- 2026-07-19 13:19（MCP 禁用生命周期废除 + 存储层集成测试套件放弃——用户拍板，迁移 087，真实两轮 provider E2E PASS）：废弃审计第三批处置。**①MCP 注册表/绑定只承认创建/删除两态**：删 mcp_servers/team_mcp_bindings/digital_employee_mcp_bindings_v2/project_mcp_bindings 的 status+disabled_at 与 runtime_capabilities.disabled_at 共 9 列（全部为只建读侧过滤、从未有写入路径的半成品常量'active'；runtime_capabilities.status 是能力同步真实数据保留、runtime_nodes.disabled_at 节点归档在用保留、env status 删除级联在写保留）；就绪视图与执行可用性部分索引因谓词依赖在 087 内先重建再删列；读侧全部 status='active' 过滤与投影列移除，员工删除级联收敛为 deleted_at 单口径（不再顺手写 status='disabled'）；对外 API 兼容——绑定 status 本就派生（active|blocked_missing_env）、定义与技能依赖的 status/server_status 改常量 active 投影（可见即活跃），Web 零改动。**用户关切的删除级联同步经真实两轮 claude-code 会话验证**：建定义→绑定员工→轮1真实 chat run 的 start_session payload 含 e2e-delete-sync（会话期注入家目录 .mcp.json、结束回滚）→DELETE 定义→effective 投影立即为空（绑定行残留但 JOIN 注册表按 deleted_at 过滤）→轮2真实 run payload mcp_servers=[]、家目录无残留——"删注册表项，挂靠员工下一次运行即不再有该配置"由每 run 一次性投影结构性保证，无需额外版本级联。**②放弃 internal/storage/queries 集成测试套件**（6076 行删除）：6 个存量失败（约束漂移/参数类型 bug）已证与近期改动无关且该套件因清库清单陈旧长期无人跑通；testenv 与 employee/auth/project 等模块自己的集成测试保留未动。过程中迁移撞号 086→087（另会话 drop_skill_installations 先占）。门禁：atlas 全量 replay + dev 库应用 087（9 列 DROP 后 DB 内省=0）+ verify:control-plane/contracts 全绿 + web tsc -b。
- 2026-07-19 12:59（声明式交付物闭环 v2 全链落地，spec `2026-07-19-declared-deliverables-v2-design.md`，GATE 全项真实 E2E PASS；**代码已入 main（随 8359a52f 裹挟提交，未提交工作树修改被并发会话 commit 整体带走，内容经 HEAD 状态单测复验无损，归属特此澄清；活跃并发会话下不改写历史）**）：v1 附件是"兜底捕获人眼分辨"，v2 让交付物由契约声明、平台核对、血缘直达文件。**①执行约定注入**：dispatch prompt"结果契约要求"段新增——文件形态交付物必须写入工作目录 `deliverables/` 且 ref 填相对路径。**②Runtime declared 管道**（与附件管道并行）：complete 分支扫 `deliverables/` 全部常规文件（目录即白名单，不限扩展名，隐藏组件仍排除），`artifact_type=declared`/`is_evidence=true`（CP 既有映射 → declared_output/submitted 计入真证据），熔断单文件 10MiB（CP presign 硬顶）/20 个/100MiB 超限跳过留痕（declared_skipped 自报行）；**上传整批失败语义与证据同格**（契约承诺的交付物不许"声称交付了但没落库"），区别于附件 best-effort；v1 附件管道排除 `deliverables/` 防双采。**③CP Ref 血缘回填**：物化(同事务)构建 relative_path/文件名→artifact_ref_id 映射，契约落库前把 `deliverables[].ref` 改写为 artifact_ref_id（原路径挪入空 summary；未物化/纯 value 项原样保留不伪造血缘），读侧契约自带血缘；produces 缺口沿用 handoff_deliverable_missing。**④Web**：工件面板新增"正式交付物"区（declared/declared_skipped，置附件区上方，复用预览/下载与"未采集(超限)"留痕展示，双表抽共享 OutputFilesSection）。真实 E2E 六轮（真实 demand→planner→人批/自动过→claude-code 真跑）：正向轮 declared 行+declared_output/submitted 证据+附件不双采+契约 ref 回填为工件 UUID（DB 断言 90f8cb4c）+浏览器"正式交付物"区 sandbox srcDoc 预览；缺口轮 `handoff_deliverable_missing:missing_doc` 进 validation_errors、任务路由人类澄清（waiting_human+clarification，平台既定姿态）；熔断轮 declared_skipped 行落库+面板"未采集(超限)"+被熔断项契约 ref 保持原路径不伪造。**过程发现（非本线缺陷，记档）**：①双 runtime-agent 抢同一节点身份（并发会话 worktree agent）致首轮 GATE 假阴性——用户拍板杀重复 agent 后复跑即过；根因是旧二进制抢走命令，环境问题非代码缺陷；②验收判据文本匹配启发式脆弱（autonomy 线已知残债"review 信号子串启发"）——planner 生成长句判据时 agent 逐字覆盖成功率低，三轮 completion 被 `acceptance_result_missing` 拦在物化前，极简判据轮次即通过；该残债建议随对抗评审线偿还。单测：runtime artifacts 13/13（declared 采集/无双采/目录缺省）+CP resolveDeclaredDeliverableRefs 2/2（回填/缺前缀兼容/不伪造）。遗留：验收面板判据行深链交付物预览（spec §6 P2）。
- 2026-07-19 03:40（系统配置中心 P1 全量落地，spec `2026-07-19-system-config-center-design.md`，门禁全绿+隔离全链真实 E2E PASS；**已合 main（df52809c，迁移两度重编号 085→087→088，适配 main 新增 format=json 端点 TTL 取值），main 复验 PASS**：dev-services 重启自动应用 088 至远端 dev 库、admin 真实登录改 skill.upload_max_bytes=1MiB→真传 2MiB 包 400→恢复→审计落账、浏览器 :3000 UI 改值/已修改 pill/恢复默认闭环、覆盖表清零）：治理平台新增"系统配置"菜单，收编散落硬编码的平台运行态参数。**Registry-first**：配置定义（key/类型/默认值/min-max 防呆边界/domain/中文文案）在服务端 Go 注册表（`internal/systemconfig/registry.go`），DB 仅一张覆盖表（迁移 085 `system_config_overrides`，tenant+key 唯一），恢复默认=删行；加配置项零迁移零契约变更（列表接口按注册表动态返回，前端 tab 按 domain 动态分组未知落"其他"）。**P1 收编 7 项**：工件单文件上限（MaxValue 锁 10MiB 只准调低——runtime 采集侧独立硬编码，P2 配置下发前调高会静默丢文件）、工件上传/下载 presign TTL、技能包上传上限（上限 200MiB=runtime 解包限额）、技能归档 presign TTL、runtime/登录会话 TTL；心跳超时实施中发现横跨 runtime models(无ctx)/scheduler/authz 三包，移出 P1。**API**：`GET/PUT/DELETE /api/v1/system-configs[/{key}]`，authz 新增 system_config.read/manage 走 checkTenantAdminAccess（OpenFGA 映射按 mcp_registry 先例不覆盖）；未知 key 404、越界 422、update/reset 全量审计（old/new/default）。**读路径韧性**：Reader 窄接口+进程内 15s 缓存+同进程写后即时失效，DB 故障回退注册表默认值；auth/runtime 因 api/middleware 反向依赖成环改闭包注入（登录会话固定读平台默认租户）。**Web**：`features/system-config/`（脆数据面 V3Table+单位感知编辑弹窗 MiB/分钟/小时+恢复默认确认+已修改 pill 带修改人）。门禁：migrate-validate（podman 一次性库）/verify:control-plane/web/contracts 全绿（顺手修 capabilities.test.ts 两个 main 上既有失效 type import，typecheck 本已红）。真实 E2E（worktree 独立 CP:8082+Web:3001+隔离 PG 全量迁移+真实 S3/Redis）：管理员浏览器全流程（两 tab/编辑/越界就地报错/恢复默认）；技能上传限额调 1MiB→真传 2MiB 包 400 带动态限额→恢复→同包 201 入 S3；presign TTL 300→120→300 经 302 Location X-Amz-Expires 逐次跟随；登录会话 TTL 改 2h 新会话 expires_at 跟随；审计 6 事件 old/new 完整；普通成员 GET/PUT 403；越界写 DB 无覆盖行。遗留：合并 main 后需复验一例；P2（runtime 配置下发+心跳超时+工件上限解锁）未立项。
- 2026-07-19 03:24（能力绑定统一残债清偿：安装机件遗骸清理+模板推荐治理闭环+契约验证立项，用户逐项拍板；分支 chore/install-relic-cleanup）：①**迁移 086 drop `skill_installations`**(原085,与并行会话烂尾下线迁移撞号 renumber)（079 冻结的物理安装事实表正式下线，物化事实由派发 attestation/capability_convergence 承载；migrations_test 补表缺失断言，atlas 全量 replay 过）。②**runtimecommand 通用回执写回服务删除**（该服务唯一支持 install_skills 回执，命令退役后永不命中——整包删除+装配收敛为 runWritebackService 独家承接；同链清掉 run_writeback 的 provisioning 回执分支：completeProvisioning/failProvisioning/validateProvisioningReceipt 删除，无 run 回执只剩 sync_workspace_files 一种，历史 provision 回执回 ErrNotFound 且零状态改写有测试锁定）。③**runtime `provision_instance` 死命令删除**（CP 全仓无构造点）：handler/枚举 variant/失败写回/终态辅助全删，"provision_instance" 标签落 Unsupported 兜底（accepted:false 不 panic，新增 payload+executor 两测试仿 install_skills 先例）；共享件甄别——sync_workspace_files 共用的 payload 结构改名 `RuntimeWorkspaceSyncCommandPayload` 保留（字段不变，CP 侧形态无感）、persona/工作区物化/ensure_instance 均有存活调用方保留，仅被 provision 独占的 `materialize_provider_skills` 删除（会话路径走 skills_convergence）；10 个借 provision 载体的 payload 校验/persona 测试改挂 sync 命令保住覆盖。④**模板推荐治理闭环**（用户批准的体验债）：模板管理列表行与详情页交叉核对技能/MCP 注册表，模板推荐（recommended_* ∪ capability_bindings）中注册表不存在的 key 显示"推荐未上架 N"警示 pill、详情逐 key"未上架"标注+去技能市场/MCP 注册表的修复指引——与创建向导 available=false 禁选同口径，只警示不拒绝（模板先行、技能后上架是合法工作流）；注册表接口失败时静默降级不告警不阻塞。顺手修并发会话 018 清库遗留的 capabilities.test 死类型导入（main 上 tsc 红）。⑤**runtime/provider 契约自动验证立项**（spec `2026-07-19-runtime-provider-contract-verification.md`，未实施）：P1 runtime openapi 纳入 verify-foundation-contracts 三向核对、P2 派发 payload/Provider 协议 JSON Schema 化+golden 样例双向校验，销 CLAUDE.md 两条已知债。门禁：verify:control-plane/contracts/runtime-agent/web 全绿+migrate-validate（本地一次性 pg16 replay）。真实链路验证见合并记录。
- 2026-07-19 03:16（artifact content 端点 format=json 变体，附件遗留立项 §1 落地；**内容已入 main（随 23c551c2）**——并发会话 docs 提交在本会话 stage→commit 窗口内把已暂存的四文件（openapi/gen/handler/projects.ts）一并带走，内容为经临时 worktree 构造 HEAD+本变更、重生成+契约守卫 PASS 的干净版本，无损但该提交信息未涵盖此变更，特此澄清归属；活跃并发会话下不改写历史）：`GET /api/v1/artifacts/{id}/content?format=json` 返回 200 `{url, expires_at}`（presigned URL 本体），默认 302 下载不变；web 预览 `getArtifactContentText` 改两步取回——credentialed 向控制面拿 URL → `credentials:"omit"` 直取对象存储，第二跳 Origin 干净，**摆脱 fetch 跨域重定向 Origin 置 null（redirect taint）对桶 CORS 放行 null origin 的依赖**。真实链路验证：CP 重启后 curl format=json 200（url_host=TOS 桶域、expires_at≈5min）+ 无参 302 回归不变；**dev 桶 CORS 已移除 null 规则**后浏览器实测 report.html sandbox srcDoc 预览（含内容断言）与 notes.md markdown 渲染均正常、控制台零 CORS 错误——立项验收门槛达成。工程注：openapi/gen 与并发 team-lending 下线在途改动同文件，采用临时 worktree（HEAD detach）应用本变更→`go generate`→契约守卫→`hash-object`+`update-index` 写索引的外科手术保证提交纯净。**共享 checkout 新教训：暂存区（index）全会话共享**，另一会话 `git commit` 会带走你已 stage 的文件——stage 后立即 commit，且 commit 前必须复核暂存集。
- 2026-07-19 04:22（四个烂尾/废弃功能整体下线——用户逐项拍板，迁移 085，全链真实 E2E PASS）：延续废弃审计的第二批处置，删除"接线完整但历史零数据、产品方向已放弃"的四个功能。**①团队借调（026）**：删 team_lending_policy/team_lending_request 两表 + internal/teamlending 模块 + 8 条路由 + 契约 6 path/5 schema + authz 四动作 + inbox team_lending 类型 + web 死函数与"发起借调"幽灵按钮（纯 Link 无 API）；协调器 LendingGatekeeper 降级为 TeamBoundaryGatekeeper（跨团队员工一律排除——与授权表恒空时的现行行为一致，跳过事件沿用 project.lending.employee_skipped 保历史连续），planning_gap 出路选项收敛为 restaff/exempt。**②团队成员角色申请（004）**：删表 + tenant 模块全套方法/4 路由/契约/web 函数与测试 mock。**③员工工作区文件下发（017）**：删三表 + employee 模块仓储孤岛方法（service/handler 层本就零消费）+ 回写 sync_workspace_files 分支 + 员工删除级联段；runtime-agent 侧删 SyncWorkspaceFiles 命令与文件物化机件（标签落 Unsupported 兜底有测试，沿 install_skills 先例），materialize_workspace 保留家目录/provider 私有目录创建与符号链接防御。**④保留（拍板）**：project_budget_ledger 确认为项目 Token/成本流水保留；project_report_refs 保留；project_transfer_requests 经核实为"数字员工任务转派请求"（此前审计误标为项目转移，三层半成品：发起入口未接/协调只记事件/列表恒空）——按结构化协作协议定位保留待补完。**过程揪修自伤 3 处**：teams.test.ts 切块致括号不配平（vite 错误浮层拦截点击造成全量 95 假失败，isolated 全过定位）、teams.ts AllowedTeamAction 被正则连带误删、workflows 页残留未用 Link 导入。验证：atlas 全量 replay + dev 库应用 085（六表 DROP 后 DB 内省确认、三保留表在位）；verify:control-plane/contracts/runtime-agent（131+ Rust 测试）全绿；web 全量 826/827（唯一失败为 sidebar 主题色顺序 flake，isolated 6/6 过、与本轮无关）+ tsc -b + build 绿；真实链路——admin 登录后三退役端点 404、团队 GET/overview/members、inbox、项目 readiness 全 200，浏览器实测团队列表/详情（概览/能力/宪法三 tab）正常渲染。存量债注记：dev 库另有 E2E 残留团队行对 GET 呈行级不可见（c9644555，非本轮回归）。
- 2026-07-19 03:21（数字员工任务详情抽屉：Markdown 渲染 + 加宽 880px，真实浏览器 E2E PASS；**内容已入 main（随并发会话提交 8359a52f 带走，该提交信息未涵盖此变更，特此澄清归属；内容经逐文件与本会话工作副本比对确认无损）**）：用户报告任务详情展开内容不渲染 Markdown 且抽屉 576px 太窄。评估后维持抽屉形态（overlays.md 分工 Drawer=长内容；长文可读行宽 ~700-900px，弹窗多余面积无增益且丢失队列逐条排查的原地切换优势），宽度对齐工件预览抽屉 `w-[min(880px,calc(100vw-2rem))]`。**MarkdownProse 公共组件**：工件预览 sheet 的 react-markdown+排版样式抽入 `@/components/superteam/markdown-prose.tsx`（react-markdown 默认转义内嵌 HTML 免 XSS），run-detail-drawer 结论块、run-event-timeline 正文块（text_delta 聚合）、artifact-preview-sheet 三处共用；「回合完成」marker 的 detail 摘要保持单行元信息语义，新增 summaryExcerpt 去 Markdown 语法字符压平+120 字截断（消除原始 `##`/`**` 直接可见且与正文块整段重复）。验证：定向 vitest 13/13（含 `**` 必须渲染成 strong 的回归断言）、typecheck 除并发会话在途文件外干净；真实链路——admin 登录后打开员工「P2a开发工程师」真实 completed 运行（provider 真产出含 `## 结论`/加粗/列表/代码块），实测抽屉 880px、标题/加粗/列表/行内代码/围栏代码全部富文本渲染、DOM 全文零残留原始 Markdown 语法字符、无横向溢出。注：全量 web 测试 4 文件 35 失败归因并发会话在途改动——失败文件（workflows/index.tsx+test、teams、staff-gap 等）本身就在其未提交 diff 中且与本变更零代码交集（本变更仅加导出与渲染组件，未触碰其断言路径）；workflows 单跑仍失败 2 例（「发起借调」link 定位不到），为其在途状态非资源竞争。后续方向（未立项）：运行报告若继续变重，升级为可路由任务详情页而非弹窗。（artifact content 端点 format=json 变体，附件遗留立项 §1 落地；**内容已入 main（随 23c551c2）**——并发会话 docs 提交在本会话 stage→commit 窗口内把已暂存的四文件（openapi/gen/handler/projects.ts）一并带走，内容为经临时 worktree 构造 HEAD+本变更、重生成+契约守卫 PASS 的干净版本，无损但该提交信息未涵盖此变更，特此澄清归属；活跃并发会话下不改写历史）：`GET /api/v1/artifacts/{id}/content?format=json` 返回 200 `{url, expires_at}`（presigned URL 本体），默认 302 下载不变；web 预览 `getArtifactContentText` 改两步取回——credentialed 向控制面拿 URL → `credentials:"omit"` 直取对象存储，第二跳 Origin 干净，**摆脱 fetch 跨域重定向 Origin 置 null（redirect taint）对桶 CORS 放行 null origin 的依赖**。真实链路验证：CP 重启后 curl format=json 200（url_host=TOS 桶域、expires_at≈5min）+ 无参 302 回归不变；**dev 桶 CORS 已移除 null 规则**后浏览器实测 report.html sandbox srcDoc 预览（含内容断言）与 notes.md markdown 渲染均正常、控制台零 CORS 错误——立项验收门槛达成。工程注：openapi/gen 与并发 team-lending 下线在途改动同文件，采用临时 worktree（HEAD detach）应用本变更→`go generate`→契约守卫→`hash-object`+`update-index` 写索引的外科手术保证提交纯净。**共享 checkout 新教训：暂存区（index）全会话共享**，另一会话 `git commit` 会带走你已 stage 的文件——stage 后立即 commit，且 commit 前必须复核暂存集。
- 2026-07-19 02:30（数据库废弃表/字段全面清库——审计驱动的架构债偿还，迁移 080-084，全链真实 E2E PASS）：基于全库 95 表×1392 列的废弃梳理（sqlc 查询+Go raw SQL 逐语句交叉+线上行数/非空率验证），分四类处置。**①死表下线**：`task_state_history`（迁移 084——写入错误被 `_ = err` 静默吞掉、全仓库零读取方，260 任务 0 历史，真实事件流在 task_events；连 task 模块写入块/sqlc 查询/接口/mock 全链删除）、`tenant_profiles`（083，001 时代零引用零行）、018 双层能力管理旧层三表 `team_mcp_servers`/`digital_employee_mcp_bindings`(v1)/`user_credentials`（080——web 九个对应 API 函数零调用、派发投影走 v2+项目绑定，同步删除 10 个 sqlc 查询、capability 模块旧 service/handler/repo 方法、9 条路由、契约 5 path+5 schema、前端死函数与团队/员工删除级联旧步骤）。**②placements 僵尸写路径转正**（081）：项目详情"运行落点"面板 PUT 原写 `project_placements` 而就绪度/派发已全读 `project_runtime_nodes`（"绑了没效果"的误导交互）——新增 `PUT/DELETE /projects/{id}/runtime-nodes/{nodeId}` 管理 Plan B 权威集合（AddProjectRuntimeNode/RemoveProjectRuntimeNode + 事件沿用 runtime_placement.updated/released），面板绑定/释放切换到新端点后删表。**③死字段清理**（082）：auth_users.last_login_at、auth_sessions.revoked_at（现行登出走 DELETE）、task_runs.lease_expires_at（租约在 attempts 层）、project_tasks.{terminal_reason,cancelled_by,failed_by}、attempts.{lost_at,timeout_at}、env metadata——均无任何写入路径且线上全 NULL；**实施中撤回两列**：max_attempts/budget_wall_clock_limit_sec 有预派发闸门/重试上限/预算熔断真实消费逻辑（外环设计支柱），属"缺写入方的策略旋钮"非死字段。**④修合并交互回归**：能力统一分支的规划画像在系统上下文调用控制台口径 ListEffectiveMCPConfig（强制 user_id）致 runtime-readiness 500——改走 ForRuntime 口径，并给项目 handler 500 兜底补错误日志（本次排障即受害于静默 500）。工程坑：sqlc 不解析 `DROP COLUMN IF EXISTS`（须 `DROP COLUMN`）；`go generate .` 不含 sqlc 需显式 `sqlc generate`。存量债顺手修：queries 集成测试清库清单陈旧（068 删 task_artifacts 后未更新）+3 处 RunKind 缺失（基线 worktree 归因证明 6 个残余失败为存量、与本次无关）。验证：atlas 全量 replay（podman pg16）+ dev 库 migrate-status 084 无漂移；verify:control-plane/contracts 全绿、web 828/828+tsc；真实链路——迁移经 dev-services 重启自动应用（DB 内省确认五表已删/task_state_history DROPPED）、admin 真实登录后旧端点 404×2、注册表/v2 investigation 200、runtime-nodes PUT/DELETE 200/204 且 DB 行与 project_events 同步、readiness 修复后 ready（含 6 员工画像）；浏览器 E2E：运行落点面板 绑定→已就绪→释放→缺少运行节点(阻塞原因上屏/按钮禁用)→再绑定→已就绪 全闭环。
- 2026-07-19 01:50（执行输出附件自动采集+平台预览/下载，spec `2026-07-19-execution-output-attachments-design.md` v1 全量落地，GATE 五项真实 E2E 全 PASS）：远端 runtime 任务产出的报告文件（HTML/MD/TXT/CSV/JSON/DOCX/DOC/XLSX）此前躺在执行机工作目录平台不可见。**采集规则（白名单反选，无"中间文件界定"启发式）**：本次执行 untracked 新文件（`git ls-files --others --exclude-standard`，respect .gitignore 为已知局限）∩ 8 类扩展名白名单 ∖ 噪音目录（node_modules/target/dist 等 + **任何隐藏路径组件**——首轮 E2E 实证 `.superteam/mcp/claude.mcp.json` 被误采后补上，确认轮验证根除）；modified 不收（transcript+diff 证据已覆盖，用户拍板）；非 git 目录用启动快照对比兜底。**熔断（no silent caps）**：单文件 5MiB（用户拍板，<CP 现行 10MiB 故 CP 零改动）超限跳过不 truncate、20 个/50MiB 总量、丢弃以无 sha256 自报行留痕（`execution_output_skipped`，面板显示"未采集(超限)"）。**语义**：新 `artifact_type=execution_output`，is_evidence=false（CP 自动映射 self_report/unverified），不走 prose 脱敏（UI 标注"原样产出"），metadata 带 relative_path；上传 best-effort 独立于证据三工件的整批失败语义，单附件失败降级为留痕不拖垮 completion；复用 presign→sha256 内容寻址→302 管道，CP 零代码变更。**Web**：工件面板分区（执行输出附件/证据与其他工件），按 content_type 分流——md/txt fetch 后渲染（react-markdown 默认转义内嵌 HTML）、HTML 经 fetch+srcDoc 注入 sandbox iframe（仅 allow-scripts，**绝不 allow-same-origin**，报告内脚本运行于 opaque origin 拿不到平台 cookie），其余仅 302 下载；预览容器为右侧 Sheet。**E2E 揪出并修 2 个集成缺陷**：① 隐藏目录误采（上述）；② iframe src 直连 302 预览空白——TOS 对原始对象强制 `Content-Disposition: attachment`，sandbox 内下载被静默拦截，改 fetch+srcDoc 根治；另实证 fetch 经 302 跨域后 Origin 变 `null` 的 CORS taint，桶 CORS 规则含 localhost:3000/127.0.0.1:3000/null（presigned URL 本身即持有可读凭证，放行 null 不扩大可达面）。真实 E2E（真实 demand→deepseek 规划→人批计划→claude-code 真跑两轮→complete writeback 物化）：①report.html/notes.md 采集入库、node_modules 探针文件被排除；②浏览器实测 HTML 报告 sandbox iframe 渲染+sandbox 属性断言、notes.md 富文本渲染、下载 302 curl 内容一致；③README.md 追加不入附件、diff 证据含该变更；④6MB big-output.txt 熔断留痕行可见;⑤仓库原有 tracked 文件零触碰。门禁：verify:runtime-agent 全绿（含新增 5 单测），web typecheck 除并发会话既有 capabilities.test 错误外干净。遗留：md/txt 预览依赖桶 CORS（已配 dev 桶，生产桶需同规则）；JSON presign 变体（消除 null-origin 依赖的干净方案）待 CP 可安全重启时立项；attachments 限值迁配置管理功能为既定方向。注：共享 checkout 存在并发会话未提交改动，本工作后续已以 c5733467 提交（仅本会话 9 文件）。
- 2026-07-19 01:33（能力逻辑绑定统一 + 派发指纹懒收敛，用户报告"员工创建能力步与技能市场脱节"立项并拍板一次性解决；分支 feat/capability-binding-unification 全链真实 E2E PASS，**合 main 因并发会话活跃占用主 checkout 暂缓**）：根治两套互不相干的绑定模型（派发消费的绑定表 vs 创建向导写的 config JSON 声明）。**候选源统一**：create-options `capability_options` 从"仅员工类型模板推荐"改为"租户注册表(skills/mcp_servers 表) ∪ 模板推荐"，契约升级为结构化 item（key/id/label/description/recommended/available，模板推荐未上架项 available=false 禁选展示）；能力步空态给去市场/注册表的 Link。**创建即真实绑定**：创建请求新增顶层 skills/mcp_servers（slug/server_key，未知 400，团队继承静默跳过），事务内直写 skill_agent_bindings/digital_employee_mcp_bindings_v2；模板能力默认改为前端预勾选（推荐∩可选），服务端不再静默合并。**capability_bindings JSON 退役 skills/mcp_servers 两 key**：迁移 079 存量回填成真实绑定行（可解析的 slug/server_key，团队继承已覆盖跳过）+全量 revision strip；config-revisions 请求带非空两 key 一律 400；planning 画像改由绑定表合成（含团队继承，规划口径从"声明"升级为"有效绑定"）；governance mcp_servers_count 与 skills_count 同口径改绑定表计数。**市场"安装"改纯逻辑绑定**：POST /skills/{id}/install 不再触达 runtime（同步 WebSocket 安装+preflight+回执轮询机件全删，runtime InstallSkills 命令删除、"install_skills" 落 Unsupported 兜底有测试），语义=绑定表 upsert，幂等返回 already_bound（含团队继承）；installations 端点/表冻结退役（物化事实改由派发 attestation 承载）；Web 弹窗改"加载技能"文案（即时逻辑绑定、下次派发同步）。**版本化懒收敛（用户模型，实现取内容指纹）**：CP 派发时对全量能力清单（技能 key+checksum + MCP 投影含项目级）算 `cmv1:sha256:` 指纹写 payload metadata.capability_manifest_version（休眠字段激活，流入 attestation；skillLister 未配置不发=prune 保险丝）；runtime 家目录存 per-provider stamp（本地 skills-only 指纹+逐 key marker 复验），命中→零 IO 跳过，未命中→物化新增(沿用 checksum 幂等)+**删除清单外 stale 技能目录（补上解绑永不物理清除的缺口）**+stamp 最后写崩溃自愈；并发防护（active run 家目录跳过 prune 下次补删+per-home 互斥）。真实 E2E 闭环（worktree 服务全链+真实 claude 会话×5）：注册表 4 技能 2 MCP 全部出现在创建向导（原"暂无可选项"修复实证，浏览器+API 双确认）→创建即落绑定行+revision 无 skills key+unknown 400→市场安装 1.1s 瞬时+重复 already_bound→真实派发家目录物化 2 技能+双指纹 stamp→二次派发 stamp_hit 零下载→解绑再派发 pruned=1 目录物理消失→手删目录再派发自愈重物化→仅解绑 MCP 再派发技能门 stamp_hit 且 payload cmv1 变化。门禁全绿：verify:contracts/control-plane/runtime-agent/web + migrate-validate（本地一次性 pg16 重放）。留存：E2E 员工 5dd6e09a 与其绑定/实例留 dev 库；残债=skill_installations 冻结待另行 drop、runtimecommand 通用 writeback 服务成静默死路径待随回执清理、provision_instance 死路径待评估、员工换 provider 旧 root 技能残留（家目录本不清理，接受）。
- 2026-07-19 00:45（dev 库残留 schema 清理收尾，用户指令）：atlas_validate_provider_runtime_* ×2（07-03 migrate-validate 一次性校验残留，一个全空一个仅剩 2 个触发器函数）DROP CASCADE；test_schema/test_schema_migration 复查时已不存在（推测被后续迁移测试自清）。库面收敛至 atlas_schema_revisions（Atlas 账本，未动）+public+superteam 三个应然 schema；清理后 health 200、nodes 接口正常仅返回 local-dev-node（online）。
- 2026-07-19 00:38（Runtime 接入审批不再伪造节点活性，用户报告）：用户误批 3 条 06-21 测试 enrollment 后页面出现"4 节点在线"——根因是 ApproveEnrollment 在人类点同意时把节点写成 online 并盖 time.Now() 假心跳，从未连上来的 agent 靠审批动作就能"在线"60 秒。修复：审批只授权不断言活性——新建节点以 offline+无心跳落库，已存在节点由 ApproveRuntimeEnrollmentWithNode 的 ON CONFLICT 分支保留其真实 status/心跳（真活着的节点重新接入不受影响），在线状态只能由 agent 自己的心跳建立；sqlc 已重生成，enrollmentFake 同步镜像新语义，回归单测两条（新建 offline 无心跳/复用保留活性）。数据面：3 条误批 enrollment 走真实 API revoke（200×3）+ 节点行重新软归档；4 个 smoke_codex_provider_runtime_* 残留 schema（07-03 smoke 全库副本，两周无活动）全部 DROP CASCADE（用户拍板，atlas_schema_revisions 账本未动；atlas_validate_*/test_schema* 未经确认保留）。真实 E2E GATE：直插 pending enrollment 夹具→真实 API approve 200→节点落库 offline+心跳 NULL→overview KPI online_nodes=1 不虚增→夹具三表清净；浏览器复验 Runtime 页节点在线 1/1、待接入 0、列表仅剩真实心跳的 local-dev-node。注：共享 checkout 存在并发会话，本次仅提交自有文件，测试定向跑受影响四包全绿。
- 2026-07-19 00:32（团队创建中文名 slug 拼音自动生成，用户拍板方案 1）：团队创建表单承诺"默认按名称生成"，但 `slugify` 走 NFKD 去非 ASCII，中文名产出空串，99% 中文命名场景下承诺落空、提交被 3-64 位 pattern 校验打回。修复：`create-team-draft.ts` 的 `slugify` 引入 `pinyin-pro` 对中文逐字转拼音（无声调、ü→v、中英混排保留英文段）后再走原有 kebab 规整，"研发团队"→`yan-fa-tuan-dui`；两处调用点（configuration-canvas / step-identity）共享此函数一并生效，`slugTouched` 手动覆盖机制不变。新增 slugify 单测 5 例（纯中文/混排/纯英文回归/ü 转写/emoji 空串），teams 特性 35 测试 + typecheck 全过；真实 E2E：浏览器 /teams/new 输入"安全响应组"→slug 自动填 `an-quan-xiang-ying-zu`（顺带确认 inferTeamDisplay 中文关键词推断图标/主题色生效）→走完两步创建，DB 落库 slug/name/status 核实。E2E 产物团队 ee516f4d 留存 dev 库。
- 2026-07-19 00:24（陈旧 Runtime 节点注册清理，用户拍板）：dev 库 20 行 runtime_nodes 中除唯一存活的 local-dev-node 外 19 行全部软归档（其中 6 行此前已归档——这也解释了此前"库 20 行/向导 8 个"的差异：列表本就排除 archived）。清理前逐节点核对 25 处引用（1 个声明 FK CASCADE + 24 处无约束软引用列）：smoke 节点各挂几十条 tasks/ledger/事件历史，硬删会留孤儿引用，故采用平台自带 DeleteRuntimeNode 语义（status=offline + disabled_at + archived_at），所有列表/调度/派发查询天然排除，历史引用不悬空，节点重新注册会经 upsert 自动复活（可逆）；执行前 19 行已备份 CSV。真实 E2E：curl nodes 接口仅返回 local-dev-node，浏览器实测项目创建第 4 步仅剩 runtime-local-dev-node（在线）。遗留：接入审批 tab 尚有 3 条 06-21 的待接入 enrollment 测试残留，走 UI"拒绝"即可清；dev 库另有 2 个 smoke_codex_provider_runtime_* 残留 schema 未动。
- 2026-07-19 00:16（Runtime 节点在线状态如实化，用户报告）：项目创建第 4 步与运行节点列表把 7 月初的一次性 smoke 节点全显示"在线"——`runtime_nodes.status` 列只在节点自身心跳时翻转，节点死亡后永远滞留 `online`。修复：服务层 `recordToNode` 读取时按心跳新鲜度推导有效状态（存储 online 但心跳超过 `HeartbeatTimeout`=60s → 报 offline，只降不升不虚报在线），`Node.IsOnline()` 改用 `HeartbeatTimeout` 常量消除硬编码。调度/派发路径（scheduler、node_resolver、planning_profile_adapter）原本就用心跳阈值判活，不受影响；`UpdateHeartbeat` 状态翻转逻辑推演确认行为不变。回归单测（stale online→offline / fresh 保持 online）+ `verify:control-plane` 全过；真实 E2E：重启 control-plane 后 curl `/api/v1/runtime/nodes`，本租户 8 节点仅真在心跳的 local-dev-node 报在线，其余 7 个（含 runtime-enroll-node-disabled、outer-loop/codex smoke 节点）如实报离线。陈旧注册行未删（有 FK 引用风险，且如实显示后已可分辨）；如需物理清理另行拍板。
- 2026-07-19 00:20（分支大扫除收官，用户指令）：仓库分支全收敛至仅 main。①`rescue/shared-tree-20260717` 救援快照核销删除（was 7dccdd5e）：删前逐文件核对 70 文件全存在于 main、604 行"独有"内容均为被取代旧实现或有意删除的端点；唯一真孤本——scenariotemplate Patch 审计 action WIP（name/description-only 应记 update 而非 status）已捞回落 main（89b8dfce，含单测+真实 API 复验 action=update）。②`fix/cli-arg-parsing-exit-code`（平台数字员工执行工作区产出）cherry-pick 入 main（a316059f）：dev-services 非法 action/服务名以非零码失败（修 set -e 下进程替换吞退出码），dev-services.test.sh 全过+负例手验 exit=1+真实服务 status 无扰动；分支与执行工作区 worktree 已删（was 50337412 核对一致）。③`worktree-team-lifecycle-p2` 由其会话自行收尾删除。工程教训：delta pager 劫持重定向输出会污染 `git diff | rg` 管道，脚本化 diff 分析必须用 git diff-tree/--numstat plumbing（首轮误判"零独有行"差点让孤本随快照消失）。
- 2026-07-18 23:59（已合并 main ca6f1aa0 + 孤儿回收补修，P2 GATE 五项+补修全 PASS）：团队删除审核确认制 P2（spec `2026-07-18-team-lifecycle-convergence-design.md` §2，用户拍板滞留策略=永不自动+超时催办）。**状态机**：删除进入 pending_delete（迁移 078——因与并行会话 077_team_description 撞号 renumber；CHECK 重建含 pending_delete、delete_requested_by 列、队列部分索引），全站不可见；P1 前遗留软删终态不入队列。**管理员两动作**：POST /teams/{id}/restore（回 active、发起人清空、员工归属不回填、审计 team.restore）；POST /teams/{id}/confirm-delete（级联物理清理归属型 9 表+projects/员工引用置 NULL+物理删行+审计 team.delete.confirmed 带团队名/slug 快照，仅限 pending_delete 态）。**队列**：GET /teams/pending-deletes（读与团队列表同权、决策动作 team.delete 同权、不走 approval_requests）；Web 团队管理页"待确认删除"区（滞留天数/恢复/确认 danger 弹窗，非数组响应防御），删除弹窗文案改两阶段说明。**滞留催办**：app 层周期任务（启动即扫+24h 巡检），pending 超 7 天向删除发起人 upsert 收件箱条目（inbox 新增 team_pending_delete 类型，(tenant,source_type,source_id) 唯一键幂等不刷屏）。**E2E 揪出并修 1 缺口**：团队恢复/确认删除后催办条目永滞 open——清扫每轮先跑孤儿回收 SQL（open 条目其团队已非待确认态→resolved），最终一致关闭。真实 E2E（合并后 main，CP/web 重启自动应用 078）：建删团队→pending_delete 不可见+发起人落列→队列可见→restore 复活（审计链 create/delete/restore 完整）→再删→confirm 物理删（行消失、成员级联清零、审计快照可读、队列清空）→造 8 天前 deleted_at 重启 CP→秒级收件箱催办（open、目标=发起人、标题带滞留天数）→浏览器待确认区渲染/滞留 8 天/danger 弹窗→彻底删除→区块消失→孤儿催办随下轮清扫翻 resolved。门禁：verify:control-plane（含契约）、verify:contracts、migrate-validate（podman）、web teams 41 例全绿。合并冲突仅生成物+atlas.sum（迁移 renumber 后 regen 解决）。
- 2026-07-18 22:40（已合并 main e021fce8，合并后真实 API 三连 PASS）：场景模板 spec_version 缺失护栏落地（spec 2026-07-18-scenario-template-spec-version-guardrail，当日 G4 踩坑立项）。写入侧 Create/CreateVersion 在 ParseSpec 前判定：`spec_version < 2` 且带 v2 专属顶层键（constraints/exits/collapse_rules）或对象形态 default_acceptance_criteria → 400，中文可执行消息点名违规字段并指示补 `"spec_version": 2`；真 v1 形态照常通过（含 independent_from → role_independence 归一化回归断言），ParseSpec/normalizeV1 读路径零变化，存量已审计无违规无需迁移；Patch 实测不收 spec 无需拦截。测试：scenariotemplate 包 +4 例全绿 + Go 全量 sweep 通过。真实 E2E（dev CP 重启后 API 三连）：无版本号 v2 形态 400（消息点名 constraints、exits）/ 补版本号 201 / 真 v1 形态 201；两个 E2E fixture 模板已置 disabled。
- 2026-07-18 23:38：团队增加可持久化的「团队说明」（最长 280 字符）：迁移 077、团队 Create/Update/Get/List API 契约与生成客户端、仓储和服务校验全链接入；新建团队画布新增带字数计数的说明输入，并在创建确认页预览。团队管理首页卡片将「治理关注」替换为「团队说明」，存量空说明显示明确空态。验证：`verify:control-plane`、团队定向 Web 测试 24/24、Web typecheck/production build 通过；Control Plane 重启已应用迁移，真实浏览器完成说明填写→确认预览→创建→首页卡片回显→删除临时团队。全量 `verify:web` 仍受既有 Vitest/Playwright `route.fulfill` 堆回收异常阻断；`migrate-validate` 因本机无 Docker 无法创建一次性校验库。
- 2026-07-18 23:38：修复技能上传 SKILL.md 描述解析缺陷：带 YAML frontmatter 的 SKILL.md 会把分隔线 `---` 当作描述首行落库（上传页承诺"发布后从 SKILL.md 读取补齐描述"因此失真）。`extractSkillMarkdown` 下游新增 `parseSkillMarkdown`：剥离 frontmatter 并优先取其 `name`/`description` 字段（yaml.v3 解析，解析失败仍剥离该块保证兜底启发式不吞 `---`），无 frontmatter 时维持原 `# 标题`/首段启发式不变。存量数据修复：`evidence-e2e-probe` 描述按其 SKILL.md frontmatter 原文回填；`ux-probe-skill` 同病但本已软删且归档对象 404，描述清空即止。验证：新增 frontmatter 两例单测+`verify:control-plane` 全绿；真实链路：CP 重启后经真实登录 API 上传含 frontmatter 测试包 → 响应 name/description 均取自 frontmatter，真实浏览器技能市场卡片确认新包与 evidence-e2e-probe 描述均正常显示（非 `---`），测试包已删（204）。
- 2026-07-18 23:38：团队管理列表重构为“团队名片”卡片：2.5D 团队图标升为 76px 主视觉，团队名/活跃状态/角色标签形成头部层级，风险摘要作为两行治理关注区，数字员工数与治理状态收敛为可扫描指标，负责人和“进入团队”作为底部行动区；列表改为按内容容器 1/2/3 列响应，整卡通过应用内路由进入详情。移除为展示代表头像而逐团队请求数字员工的 N+1 查询，改用列表已有汇总字段；卡片同时从不适用的 Glass Card 收敛为 v3 Soft Card。验证：定向 teams 16/16、typecheck、生产构建；真实浏览器在重启后的 Web + Control Plane 上确认 2 张真实团队卡片、无横向溢出，并点击卡片进入详情再返回。全量 `verify:web` 仍受既有 Vitest/Playwright `route.fulfill` 堆回收异常阻断。
- 2026-07-18 23:05：团队管理图标改为本地 2.5D 团队角色资产库。新增 31 张透明 WebP（含通用团队与前后端、测试、安全、实施、运维、产品、数据、AI 等 30 个细分角色），创建页选择器支持中文检索；团队卡片、详情页与管理页统一使用该资产。历史 Lucide 图标键与未知键安全回退为“通用团队”，不再渲染旧团队图标。验证：定向 Web 测试 27/27、typecheck、生产构建，以及真实浏览器创建“应用安全”团队→详情和列表回显→删除清理；全量 `verify:web` 仍受既有 Vitest/Playwright `route.fulfill` 堆回收异常阻断。
- 2026-07-18 22:15（G4 补员浏览器腿补验 PASS，真实构造 planning_gap 全闭环）：团队归属参与门禁的 G4 遗留腿完成。真实触发链：新建团队"G4补员E2E团队"+单数字员工项目+自建场景模板 g4_sod_staffgap（**非 review 形状**职责分离 role_independence[requester,approver]，能力键 change_request/change_approval 先注册词汇表）→ 发需求要求 approval_record 出口 → 真实 LLM 规划+治理 enforceRoleIndependence 单员工池结构缺口 → demand failed + planning_gap 决策 + coordination.blocked。浏览器 /workflows 缺口面板（角色/池数/所需能力齐全）→"从标准模板补员"→ 创建并补员：**新员工带项目团队落库**（审查员-egm7 team_id=项目团队，本次改动的核心断言）→ 成员写入过参与门禁 → resolve restaffed → `demand.replanning_reopened` → 重规划把 approve 分派给补员员工（request→G4单兵开发/approve→审查员-egm7，独立性真实满足）→ plan_review 待人类确认（plan 模式正常终点）。过程中三个环境发现：① software_delivery 的 reviewer 独立性已迁 adversarial_review（Phase B Task5 语义），单员工池不再触发缺口——补员触发必须用非 review 形状 SoD 约束；② 自建模板 spec 不写 `spec_version: 2` 会被 normalizeV1 归一化、治理约束**静默失效**（首轮同员工双角色计划直接通过），易踩坑值得立项加护栏；③ 飞书 connector 联调进程会以 admin 身份自动回写决策（曾把 planning_gap 秒决议为 rejected 致 resolve 400），本次以 SIGSTOP/CONT 临时暂停后恢复，dev E2E 涉及人类决策时需注意。fixture 留存可复用：G4补员E2E团队/项目/模板 g4_sod_staffgap v2/词汇两键/员工三名。
- 2026-07-18 20:50（已合并 main 95af67ed + 审计补修，GATE 四项全 PASS）：团队生命周期收敛 P1（spec `2026-07-18-team-lifecycle-convergence-design.md` §1，用户拍板：撤归档/停用只留删除+日志，P2 审核确认制另立）。**API**：下线 `POST /teams/{id}/archive|disable|restore` 三端点（openapi+authz.yaml 契约同步+regen），handler/service/repository/sqlc 全链删除 ChangeTeamStatus/SetTeamStatus 写入路径；**authz**：撤 team.archive/team.disable/team.restore 三 action（types/authorizer/openfga_mapping/allowed_actions 下发）；**TeamStatus 收敛唯一 active**（Go IsValid+openapi 枚举+web 类型+状态词表）；**Web**：团队详情撤归档/禁用/恢复按钮只留删除确认弹窗，权限诊断同步。**迁移 076**：存量 status 残留刷 active（26 个已删团队尸体残留+归档态）、CHECK 约束禁新写入、悬空员工按 DeleteTeam 口径置 NULL 入候岗（增量已由删除事务解绑兜住——运行总览"不可见团队员工"缺口就此闭环）。**E2E 揪出并修 1 个既有缺口**：DeleteTeam 事务只清理不落审计（create 有 team.create，delete 无任何记录），与"删除必有日志"拍板相悖——修复为软删同事务写 team_management/team.delete 审计事件（actor=会话用户，失败即回滚）。验证：verify:control-plane 全绿、verify:contracts 过、migrate-validate（podman 一次性重放）exit=0、web typecheck+teams/permissions 48 例全绿；真实 GATE（合并后 main，CP+web 重启自动应用 076）：27 团队 status 全 active+约束在+悬空 0、三旧端点 404、浏览器详情页仅"删除团队"按钮（归档/禁用/恢复文案全无）、真实建删团队 204→列表消失→软删→team.delete 审计落账。合并途中与并行会话"团队归属参与门禁"（9d6611a9）同域冲突经 regen 解决，其测试 fixture 的 TeamStatusDisabled 常量改非 active 字面量（语义不变）。
- 2026-07-18 20:45（已合并 main 9d6611a9，合并后基于 main 真实 E2E 全 GATE PASS）：数字员工团队归属参与门禁（用户裁定：无归属员工不得参与项目；候岗态本身合法）。服务端三门禁：①项目成员写入（CreateProject/ReplaceProjectMembers 经 MemberTeamAssignmentResolver 批量核实 digital_employee 成员团队归属，候岗/不存在 → ErrTeamlessProjectMember 400 中文报错含员工名）；②协调器派发池 teamless 从"always eligible"（原注释显式相反语义）翻转为 skipped + `project.teamless.employee_skipped` 事件（借调闸门语义不变，解析失败 fail-open，成员写入层为权威门禁）；③chat 锚定校验项目成员资格（ChatParticipantValidator 可选接口，非 active digital_employee 成员 400"不是该项目的成员"）。归队管理能力补齐：POST /teams/{teamId}/digital-employees 收编候岗员工（仅无归属，SQL 自带防抢人守卫，审计 team.digital_employee.bind）+ PUT /digital-employees/{employeeId}/team 换队/首次归队（新 authz employee.team.update，目标团队须 active，审计含 from/to；副作用注记：home dir 按 (team,employee) 键+团队技能/MCP 继承切换）；openapi 两 path + TS 客户端。前端：补员对话框创建员工带项目 team_id（项目无团队前置报错）；ChatPanel 先选项目、员工按项目 active 成员过滤（未选项目禁用+空态）；团队总览新增收编候岗员工面板。测试：Go 五包全绿 + web 825 例串行全绿（chat/staff-gap/index 测试按新语义改造）。真实 E2E（dev CP+PG+Temporal+浏览器）：G1 候岗员工塞成员 400→换队 API→重试 200；G2 直插 DB 造存量 teamless 成员→发需求→真实协调线程落 teamless skip 事件且规划继续；G3 非成员 chat 400/成员 201 真实派发+浏览器员工下拉仅出项目成员；G5 收编 API（重复收编 400）+浏览器收编面板真实收编（候岗 3→2）+审计双事件 DB 复核。G4 补员浏览器腿因 dev 无 planning_gap fixture 未走（组件测试断言 team_id payload+无团队前置报错，API 同链已在 G1 验证），触发式补验。存量：dev 库 7 名无归属员工此前已归默认团队；删团队解绑产生的悬空员工（审查员-csjw/报告员小王）留候岗，归队入口已具备（另见同日团队生命周期收敛 spec）。计划：docs/superpowers/plans/2026-07-18-team-affiliation-participation-gate.md。
- 2026-07-18 20:26（浏览器真实 E2E PASS）：归档项目补确认弹窗 + 队列"当前处理者"归档显示修复（用户体验走查报告）。此前详情页"归档项目"点击即生效——归档是收口级不可逆动作（无取消归档入口），对齐团队删除模式加 ConfirmDialog（danger 确认，文案明示"归档后停止推进、无取消归档入口"，失败错误在弹窗内展示）；项目队列"当前处理者"列对 archived 项目原兜底显示"待调度"（误导为等待调度），改为显示"已归档"。测试：归档确认（取消不发请求/确认才 POST）+ 队列 archived 行断言，web 全量 827 例串行全绿（并行跑撞已知堆崩 flake）。真实浏览器 E2E（dev web+CP+PG）：临时项目走"归档→弹窗→取消（API 复核仍 running）→确认归档→状态已归档、弹窗关闭"，队列中两个归档项目处理者列均显示"已归档"、running 项目仍"待调度"；临时项目已经 DELETE 清理。走查澄清：项目删除能力已存在（07-11 spec 已落地，详情头部"删除项目"+输名确认），归档项目"体验走查测试项目"实测 delete-preview can_delete=true，可随时从详情页删除。
- 2026-07-18 20:18（已合并 main，20:17 浏览器真实 E2E PASS）：透镜 demand 显式化 + 切换器 + 钉住防抢位（用户立项,根治两次真实发生的"最新 demand 抢位换链"）。透镜按 demand 取链的隐式选择显式化：摘要卡常驻"需求"行——单 demand 静态标注,多 demand 下拉切换（shadcn Select,demand 列表 limit 1→20）;**钉住语义**：默认选最新,选定后新 demand 到达不再抢当前视图,仅当前 demand 从列表消失才回落最新;切项目重置;刷新按钮与 SSE 失效补拉 demand 列表。组件测试 +2（切换换链/钉住防抢位——fetcher 造"首拉单 demand、刷新后头部插新 demand"场景断言选中不动）,run-overview 67 例全绿。真实浏览器 E2E（合并后 main,web 重启）：锚点项目现成 3 个 demand——默认标注"透镜E2E链路需求"(最新)且链路 2 边正常;切"终验-任务路径-2"→摘要/链路随切（参与 1 · 交接 0 · 阻塞 1,有需求标注不再是"莫名变了"）;切回→链路完整恢复。截图 gate-demand-switcher.png。
- 2026-07-18 20:05（已入 main，20:00 浏览器终验 PASS）：大厅底图接入 + 候岗动态承载区 + 透镜锚点回归修复。用户提供专属大厅底图 `floor-lobby-office-v4.png` 并把候岗区改为**动态承载区**（无固定 seatId/容量置 null,12 个 3×4 宽间距展示锚点由 LobbyWorkspaceRenderer 按在场顺序排布,超出 +N;文案"待编组 N 名"）。终验揪出并修 **1 个重构引入的回归**：透镜连线端点解算依赖 `employee.seatId`,候岗员工改为无 seatId 后其参与的交接边全部 unlocated——跨层出口徽标消失（G3 场景退化）。修复：12 锚点提升为 `runtime-overview-layout.ts` 导出的 `runtimeOverviewLobbyPositions`,渲染器与透镜端点解算**共用同一组锚点同序对应**（大厅在场第 N 人 → 锚点第 N 位）,溢出员工保持无定位;新增锚点对应单测（run-overview 65 例全绿）。浏览器终验（真实 CP+PG）：新底图生效、大厅 tab"大厅· 1"、"转 大厅"/"自 1层"跨层徽标恢复且与头像同锚点（+26,-90 偏移核对）、participant 高亮正确。环境事实：另一会话按用户新规则（员工必须归属团队才能进项目）已把存量员工全部归队——常态下大厅层隐藏,终验经临时置空一员工 team_id 构造候岗态,验后已还原;e2e demand 二次被并行 demand 抢"最新"位（透镜按最新 demand 取图的收窄语义）,时间戳已顶回。
- 2026-07-18 19:40：团队删除技能/能力绑定悬空修复——用户报缺陷：删除团队后技能详情仍显示幽灵团队（"数据库处理"3 个已删团队）、KPI"团队绑定"计入幽灵、删除确认弹窗未提绑定处置。根因：`DeleteTeam` 事务只解绑数字员工+软删团队,`team_skill_bindings`（无软删列）与 `team_mcp_bindings` 未处置,读侧 `listTeamBindings` LEFT JOIN 不过滤 `deleted_at`。修复三件套：① 增量路径——`DeleteTeam` 事务内新增 `DeleteTeamSkillBindings`（物理删,与该表 Unbind 口径一致）+`SoftDeleteTeamMCPBindings`（软删,与 MCP 绑定自身删除口径一致）,顺序在软删团队前,单测断言执行顺序与失败回滚;② 存量回收——迁移 075 按同口径清理团队已软删/缺失的悬空绑定（dev 库实测技能悬空 7→0、MCP 悬空 2→0,合法绑定 2 条保留）;③ 弹窗文案补充"技能绑定与能力（MCP）绑定将一并解除"。确认软删团队不可恢复（`SetTenantTeamStatus` 带 `deleted_at IS NULL` 守卫,"恢复团队"仅针对 archived/disabled）,物理删技能绑定安全。真实链路验证：control-plane 重启自动应用 075 后,真实 API 全链——skills 列表/详情 team_bindings 清空;增量流程建团队→绑技能+MCP→DELETE 204→技能详情实时归空、DB 复核技能绑定行消失+MCP 绑定 deleted_at 置位;浏览器实测技能市场 KPI"团队绑定"8→0、"数据库处理"详情"团队（0）/未绑定团队"、删除弹窗新文案渲染。门禁：verify:control-plane 全绿、web teams 定向 28 例全绿、atlas migrate validate（一次性 podman postgres 重放）通过。
- 2026-07-18 15:29：目录与能力投影工作线终验——全链综合真实 E2E 一轮通过（三服务基于当前 main 重启后执行,覆盖本工作线全部交付面）。① claude chat 双轮：技能软链为**绝对路径**指向稳定员工缓存（相对路径修复实证）、SKILL.md 直落 key 目录（zip 根前缀修复实证）、HEAD=fixture 仓库最新提交（origin/base 旧检出修复实证）、readonly worktree/线程键 CWD/文件跨轮连续/resume 记忆延续、**全程 presign=0**（技能物化跨会话命中稳定缓存零下载）;② 任务路径：低风险需求经真实 deepseek planner 派发、run 真实执行完成、attempt 工作区被终态清理消亡（仅留空 task 壳）、一次性 project-tasks 目录零新增;执行后需求停在 waiting_human(clarification)=Phase A 已立案的判据结构化自报边界（正交,非本工作线）;过程顺带清掉有害遗留 fixture（team 101 的 example-browser-upload-skill 团队绑定 env 永不可满足致全团队不可派发——07-16 Phase A E2E 观察到的 waiting_human 同源）;③ opencode 腿：readonly worktree+新提交检出 ✓,存量嵌套技能布局按 §4 声明行为确认后删缓存重物化即平铺 ✓;④ 门禁：verify:foundation 除 web 段全绿,web 段失败确证为已知并行 route.fulfill 堆崩 flake——串行复跑 **820/820 全绿**;⑤ 残债 §5 上游检查记录：opencode 1.17.16 无禁用项目配置开关,绕行维持。E2E 现场：锚点项目 4be304f7 已绑 repo+MCP+三员工成员留存复用;vcan 临时技能绑定已还原。
- 2026-07-18 15:10（已合并 main，15:05 浏览器复验 PASS + **G3 补验 PASS**）：候岗区形态修订——独立"大厅"楼层（用户裁定：floor-1 右侧空带放法不成立,底图上无对应办公区语义且仅 3 座,多数候岗员工仍进 +N 不可见）。改为独立大厅层：仅有未归属员工时出现在楼层 tab（带人数徽标"大厅 · N"）,层内一个 10 座开放区候岗工位（几何复用已被布局测试覆盖的 floor-1 开放办公区）,溢出仍 +N;底图临时复用 floor-1,**专属大厅底图（floor-lobby-office-v4.png,1672×941,风格对齐 v4 系列,中下部留 2×5 开放座位区）等用户提供素材后替换**。候岗员工与团队员工的透镜交接链自然跨层。真实 E2E（合并后 main,web 重启）：大厅 tab"大厅· 7"、7 名候岗员工**全部落座**（改造前仅 3 座+4 溢出）、层内无团队工位;**G3 跨楼层出口徽标补验 PASS**（上一条记录中环境阻塞项）：选中透镜项目→大厅层参与者高亮+"自 1层"入口徽标→点击切至 floor-1→同层边 A→B primary+"转 大厅"出口徽标,楼层 tab 双参与点。E2E 中并行会话在锚点项目新增 demand 抢占"最新 demand"位导致透镜短暂空链——行为符合 spec §7 收窄语义（多 demand 项目透镜只显示最新 demand）,fixture demand 时间戳已顶回;该场景确认了"透镜 demand 选择"作为潜在 P2 增强点（如 demand 切换器）。
- 2026-07-18 14:31（已合并 main，14:30 浏览器 GATE 六项 PASS + 一项环境阻塞）：运行总览维度裁决落地——候岗区 + 项目透镜（spec `2026-07-18-run-overview-project-overlay-design.md`）。裁决：地图底座保持团队维度（员工唯一、一人一座），项目建模为**透镜/覆盖层**而非分组容器（员工可属多项目,容器必产生重复头像;透镜=画在座位间的"线"）。**P1 候岗区**：floor-1 右侧固定候岗工位（不占团队 slot、不参与楼层分配、不计任何容量口径）,未归属团队员工落座可见,溢出聚合 "+N" 徽标,详情卡显示"未归属团队"——修复 unassigned 员工地图不可见、画面与汇总数字不一致。**P2 项目透镜**：侧栏项目选择器（员工项目摘要反向聚合,零新查询,默认只列有活跃任务项目+展开全部）,选中后参与者高亮/非参与者降暗/当前停留环节（运行中或阻塞任务的 assignee）脉冲标记,座位间按任务图依赖边绘制有向交接连线（下游完成/未开始 muted、活跃 primary、阻塞 warning,同端点对合并计数,悬停聚焦其余降暗,装饰呼出线透镜态退后）,跨楼层出口徽标点击切层,楼层 tab 参与点,`?project=` 深链消费+选中回写 URL,透镜态轮播强制暂停退出即恢复,项目卡"查看项目详情"深链——任务级操作一律跳项目详情,总览不承载。连线语义=任务依赖/交接,不承诺执行时序。数据面**零新后端**：task-graph 端点强制 demand 域,透镜与项目详情页同规则取最新 demand（`listProjectDemands limit 1`→`getProjectTaskGraph(demandId)`,仅选中时拉取,10s 轮询+SSE invalidate）。路由 `/run-overview` 增加 `validateSearch`（employee/project）。验证：透镜投影纯函数单测（边合并/色调优先级/自环与未派发过滤/跨层 portal/参与楼层）+ layout/adapter 候岗单测 + index 组件测试（候岗可见/透镜激活/退出恢复/深链）共 64 例全绿,全量 web 五分块串行 787 例全绿（chunk1 首跑 route.fulfill 堆回收 flake,复跑绿）。**真实 E2E（合并后 main,web 重启,chromium isolated,真实 CP+PG）**：dev 库天然 8 名无团队员工——候岗区 3 座+"+5"徽标可见、计数一致、容量 2/3 不含候岗;透镜链数据直插锚点项目 4be304f7（demand+4 任务+2 依赖+3 成员,assignee=真实员工）经真实 API 全链回读;选中项目→2 条有向边（完成段→运行段 primary、运行段→待开始段 muted）、3 参与者高亮、运行中 assignee stop 脉冲、其余降暗、摘要"参与 3 人·交接 2 段·待派发 1"、轮播互斥;DB 置 blocked→10s 轮询自动翻 warning+摘要"阻塞 1";退出→边/降暗清除+轮播恢复+URL 清空;`?project=` 直开与项目详情页"在运行总览查看"反向链入均进透镜。**G3 跨楼层出口徽标真实 E2E 环境阻塞**（并行会话清库致 teams 授权可见性残缺,admin 仅见默认团队,凑不出 9+ 团队的跨楼层布局）,portal 投影与切层逻辑由单测覆盖,记残留。**E2E 新发现既有缺口（非本次回归）**：员工归属的团队若对当前用户不可见/非 active（teams API 不返回）,该员工既无工位也不入候岗区,地图仍不可见——候岗区只覆盖 team_id 为空者;待立项。E2E fixture 留存锚点项目内（id 前缀 e2e1/e2e2,task-c 已还原 pending;两员工 5ad34075/17e3a6ee 移入默认团队,17e3a6ee 原属 38c325cf）。

- 2026-07-18 14:07（已合并 main，14:10 浏览器 GATE PASS）：项目 MCP 绑定 Web 管理页（残债交接 `2026-07-18-directory-projection-residual-debts-handoff.md` §2,后端 API 已于 P2 落地）。项目配置页 `/projects/{id}/config` 新增「MCP 绑定」tab（与成员同级,归档项目 disabled;落点选配置页而非运营详情——写入面归配置页与既有信息架构一致,运营详情经配置 tab 一跳可达）：WorkSurface+V3Table 绑定列表(服务名/server_key/transport/credential_env_var/行内移除)、注册表 Select 添加(排除已绑定)+可选凭据 env 输入、本地草稿态("未保存" pill+全量替换语义 Alert)、「保存绑定」声明式 PUT `{items:[...]}` 全量提交、常驻冲突语义文案"同 server_key 时项目绑定覆盖员工绑定"(spec §3.2)、四态齐(loading/empty/error/保存失败 Alert)。api client 新增 list/put 两函数复用既有类型与 helper;零新依赖、无原生跳转、设计系统零改动。验证：面板定向测试 5 例+邻近 18 例串行全绿、typecheck 绿。浏览器真实 E2E（合并后 main,web 重启,chromium isolated）：登录→项目配置页 MCP 绑定 tab→注册表 Select 选 e2e server→添加(草稿态"未保存"pill+全量替换 Alert 出现)→保存(草稿态消失,计数 1 个绑定,保存回落禁用)→服务端 GET 回读 server_key 一致;截图 project-mcp-bindings-saved.png 留证。
- 2026-07-18 14:04（已合并 main，14:06 GATE PASS）：CP 超时看门狗——预确认态 run 周期清扫（残债交接 `2026-07-18-directory-projection-residual-debts-handoff.md` §1 第 2 层，调度韧性家族）。runtime 层零事件早退修复(同日 8bc8304f)覆盖不到"runtime 进程整个死掉/命令未送达/员工此后无新派发"——既有 stale reap 只在同员工下一次派发时按需触发,占位 run 可无限期滞留。新增：sqlc 查询 `ListStalePreConfirmationDigitalEmployeeRuns`（跨租户,queued/dispatching 超 `staleDispatchTTL`(5 分钟),单轮限 100）+ `SweepStalePreConfirmationRuns`（复用 reapStaleRun 三件套:failed+dispatch_stale/run_reaped_stale 事件/审计,系统清扫审计主体记零值 UUID,二次核验防清扫窗口内被 runtime 推进的 run）+ app 启动 1 分钟周期 `StartStaleRunWatchdog`（ctx 取消即停）。可选接口断言接入(StalePreConfirmationRunLister),既有测试 fake 零改动。验证：看门狗单测两例(超时清/活跃不清/无 lister 零操作)+employee 包全绿+`verify:control-plane` 绿。真实 E2E（合并后 main,CP 重启）：直插 10 分钟前的 dispatching 行→部署态 CP 周期清扫在 1 分钟窗口内将其翻 failed（error_code=dispatch_stale）+`run_reaped_stale` 事件落表;fixture 行已清。
- 2026-07-18 13:15（已合并 main，13:23 GATE 三项 PASS）：目录与能力投影残债三项落地（残债交接 `2026-07-18-directory-projection-residual-debts-handoff.md` §1/§3/§4）。① **§1 runtime 层 run 永滞看门狗**（承重,调度韧性家族）：`drain_provider_events` 尾部对"流结束却从未出现 TurnCompleted/TurnError"的异常早退（provider exit 0 零输出、输出格式漂移被解析层全量丢弃）按失败收尾——本地 run 置 failed + 失败回写携诊断"provider exited without a terminal event"，不再永滞 dispatching 等 stale reap；TurnError 既有路径与取消路径不受影响；集成测试用零输出假 provider 全链断言(run Failed+fail 回写)。CP 层 dispatching 超时看门狗仍留家族级立项（§1 第 2 层）。② **§3 PUT mcp-bindings 缺 items 键 400**：字段缺失(nil)不再被宽容为清空全部绑定,显式 `[]` 仍为合法清空;handler 测试表补缺键/空对象两用例。③ **§4 技能 zip 根前缀剥离**：`common_root_prefix` 不再被显式根目录条目("root/")否决——带目录条目的归档(macOS zip 常见)此前整层嵌套落盘(SKILL.md 不在 key 目录直下);平铺 zip 与多根 zip 语义不变(不剥);存量已装技能 checksum 未变不自动重排(交接文档已注)。验证：runtime 全量 113 lib+集成测试与 `verify:runtime-agent` 绿、capability 包测试绿。合并窗口已执行（同日 13:23，双端基于合并后 main 重启），**GATE 附录**：①看门狗——`RUNTIME_AGENT_PROVIDER_OPENCODE_BINARY=/usr/bin/true` 真实派发 chat → run 秒级 failed（error_family=provider），不再永滞 dispatching；②curl 三态——错键 body 400 且既有绑定未被清、显式 `[]` 仍清空；③探针技能删稳定缓存后经真实 claude chat 重物化，`SKILL.md` 落 key 目录直下（修复前嵌套 zip 根一层）。附注：并行会话的 E2E 清库把共享 dev 库项目表清空（本 E2E 全天所用锚点项目均没）,新建锚点项目「目录投影残债E2E锚点」(4be304f7)留存复用;vcan 临时技能绑定已还原。
- 2026-07-18 11:40：review_gate P1.1 follow-up——竞态修复 + 收件箱实时刷新（交接 spec `2026-07-18-review-gate-p1-followups-handoff.md`，已合并 main，三场景真实 E2E 全 PASS）。**§1 承重竞态**（review_gate-only 需求在检测器出结果前 auto-complete 绕门）修法三件套：① 完成写回**事务内**同步写 review_gate `pending` 占位 verdict（`reviewGatePlaceholderVerdictRequests` 经 writeback 请求传入，pg+memory fake 同构；事务化=写回失败零孤儿、提交成功 recompute 必见占位），匹配规则=自身 planned key + **revision-root key**（镜像触发器 `listCriteriaForTaskByMethod`，堵修订腿绕门）；② 收敛闸 review_gate 分支改"**有 verdict 且非 satisfied 一律 HOLD**"（pending/unsatisfied/未知值都留人类；完全无 verdict 仍默认放行，反转精神不变）；③ `RunReviewGateForTask` 持久化完 verdicts 后**每 distinct demand 一次 recompute**（异步放行的另一半——没有它干净产出永滞 held；Activity 错误传播→重试幂等，永久失败=held 留人类）。失败姿态更新：门被触发但未出结论=占位 held 留人类（fail-toward-oversight），未触发仍默认放行；时序保证：检测门 Activity 在 signal handler 内 await、`ensureDemandAcceptanceDecisionForTask` 之后才跑——干净场景瞬时 hold 不开悬挂验收决策（E2E 实证：违规开 1 条、干净 0 条）。**E2E 揪出并修 2 个真缺陷**：⑴ `gatedCompletionStatus` 判据/verdict 读取走连接池 r.q 看不到写回事务内未提交占位→默认放行照旧绕门（memory fake 无事务隔离,单测全绿盖不住）——三个 List 读取抽 q 参数化、闸改 `gatedCompletionStatusWithQueries` 全程走调用方 q；⑵ 收件箱列表默认无 status 过滤,服务端返回全部状态,几天前 resolved 旧项仍标"待处理"且外部渠道 resolve 后永不消失——默认 `status=open`（审批中心本就如此,重置回落 open）。**§2 实时刷新**：inbox 列表/审批中心 5s 轮询 + 侧栏 badge 30s 轮询。配套：迁移 074（纯列注释 pending 口径）；openapi verdict/judge_type 枚举补齐（not_applicable/pending/escalate_human + adversarial/review_gate,修 P1 前既有枚举滞后,escalate_human 为评审揪出的可达值缺失）+ regen；web 判据面板 pending→"检测中"、escalate_human→"升级人类"、检测门/对抗判定来源标签。**全分支多智能体评审（15 agents）9 发现**：4 项修复入本次（占位事务化/revision根key/每demand重算/过时日志），4 项记录为已知局限——F1 多任务同判据 last-writer-wins+forward-only recompute 可放行仍在检测中的另一任务轮（P1 单 satisfied_by 规避的既有家族,修=worst-wins 持久化 P2）；F3 GetVersion 钉死的 pre-P1 协调线程占位永不翻转（dev 库已清零无此类工作流,人签可解）；F6 检测器放行不 resolve 悬挂 demand_acceptance 决策（需"无人参与的第二轮完成"序列,当前流程不产生）；F7 verdict 写入与 recompute 非原子（经 Activity 重试自愈,永久失败 held 留人类不静默放行）。**真实 E2E（合并 main 后服务重启,直插 fixture+真实 presign 证据工件+真实完成 API+真实协调线程/检测器）**：A 违规=完成瞬间 acceptance_pending+pending 占位→秒级 secret_leak unsatisfied（片段脱敏）维持 held→demand_acceptance 决策开出→人签 satisfied→completed+决策 approved；B 干净=占位 hold 11 秒→satisfied"检测门无命中：默认放行"→自动 completed 零人类触点、零多余决策；C 打开的收件箱页不刷新,外部渠道签署后 ≤6 秒该项自动消失,徽标/统计同步归零。E2E fixture 项目已经官方 API 归档,自铸 runtime session 已吊销。

- 2026-07-18 01:13：违反检测门 P1——审核/门控从"证明正确性"重构为"检测违反明确条件"（spec `2026-07-17-review-gate-violation-detection-design.md`）：**立场转变**——LLM 判官对开放任务"做对了没有"证不了伪（判官与执行者同类模型无独立真值，Phase B 的真实 E2E 已暴露：判官"证伪"带测试的真实产出、抓的是"未经证明"非"错误"）；门控**不证明正确性，只检测是否违反明确定义的条件**（可枚举缺陷类在真实工件上检测），**检出违反才 hold，检不出（证不了伪也证不了真）默认放行**，人类只在最终验收确认。① 条件检测器抽象：规则型（正则/AST/命令黑名单，首个 `secret_leak` 扫 diff 密钥形态、命中脱敏）+ LLM-prompt 型（首个 `code_review`，system 框成"检测违反非打分/非判正确性"，指向被审任务真工件非自述，client 错/解析失败/`detected` 缺→detected=false 默认放行）；② 预置条件注册表 + `coordination_policy.review_gate_conditions` 配置层（开关/调档 block|need_human|record_only/`review_gate_minor_tolerance`；未提及条件默认启用 DefaultAction、未知 action 回落 DefaultAction，误配不会静默关安全条件）；③ 检测门执行：被审任务真工件（Contract diff/deliverables）跑 enabledConditions，聚合（record_only 不 hold、minor 超容忍才 hold、block/major 总 hold、strictest-action）→ `review_gate` verdict 投影（迁移 073 `uq_demand_verdicts_review_gate` 与 human/adversarial partial 索引互斥）；④ **收敛闸对 review_gate 默认反转（承重）**：仅当 review_gate verdict=unsatisfied（检出违反）才 held，`!hasVerdict`（没跑/未检出）或 satisfied→放行——**反转旧"无 verdict=held"默认**；automated_test/human_judgment/adversarial_review 语义**不变**（held-by-default 保留）；criterionEffectiveVerdict 加 review_gate 累加器（human 仍优先覆盖）；⑤ 触发接线：独立 `review-gate-trigger` GetVersion 围栏（replay 安全、与 adversarial 双 fence 独立）在任务完成后派 `RunReviewGateForTask`（复用 judgeClient/model），**不阻塞下游**（违反经持久化 verdict 在验收门 held→人类最终验收 inform）；人类签署白名单加 review_gate（可 waive/确认）；executor 投影跳过 review_gate（检测器独占该通道）。全层 Go 测试（检测器/配置/聚合边界/收敛反转/触发/人类签/executor 跳）+ replay 绿；末尾全分支评审 READY-TO-MERGE，两承重不变式 CONFIRMED（默认放行反转端到端成立、无路径因"没跑"永久 held；违反→held→人类可签→放行无死胡同）。**诚实边界（spec §1/§7.1/§10）**：门"通过"= 配置条件里没检出违反，**不等于正确**；不消除缺陷只降缺陷率；开放任务不判好坏。**P1 未做（后续阶段）**：adversarial_review 仍 Phase B held-by-default（未降级、视为弃用新工作用 review_gate）、C1 auto_rework 未默认关（只影响弃用 adversarial 路径）、安全中途硬阻断（block/need_human 目前都只在验收门 held，无中途拦，需 P2 接命令日志）、对象存储 diff 取回（现只扫内联工件，diff 仅在对象存储时 code_review 退扫 executor 自述 fails-toward-release）、多 distinct-task 判据 worst-wins 持久化（现 last-writer-wins，用单 satisfied_by 规避）、自定义条件槽 + 前端菜单（P3）。**真实 E2E 待合并窗口执行**（密钥 diff→held at acceptance_pending→人类签放行；干净 diff→默认放行零人类）。

- 2026-07-17 21:56（分支 feat/directory-capability-projection-p2 已合并 main，GATE 全项 PASS）：目录与能力投影修订 Phase 2——治理增强收官（spec §3.1/§3.2/§8.3，至此该 spec 三分期全部落地）。① **项目级 MCP 绑定**（迁移 072 `project_mcp_bindings` 镜像 team 绑定；capability 声明式 `PUT/GET /projects/{id}/mcp-bindings`，authz 用已落地的 `project.config.read`/`project.config.edit`(ResourceProject)；运行时投影 = 员工侧(团队继承+个人) ∪ 项目绑定、**同 server_key 项目侧整条优先**；派发链路三路径传项目维度——chat 锚点/项目任务/legacy nil；契约+codegen+verify:contracts 绿；Web UI 记债延后）。② **技能冲突留痕**：项目原生技能压过员工同 key 的冲突清单经 CommandWorkspace→RunSpec 进 attestation metadata `skill_conflicts`（项目任务态落库,chat 态 stderr 留痕,writeback 体单测断言）。③ **opencode 仓库配置屏蔽**：实测 `OPENCODE_CONFIG` 不抑制项目配置、发现跟 `--dir` 走、无官方禁用开关——worktree 物化时对 provider=opencode 用 **skip-worktree+删除** 屏蔽根级 `opencode.json(c)`，git status/diff 零污染、重放幂等、其余 provider 不受影响。**GATE 揪出并修两个既有缺陷**：⑴ CP 按"运行时派生"原则下发的相对 `agent_home_dir` 使逐 key 软链 target 悬空（Phase 1 GATE 只验列出未读穿,漏网）——会话入口绝对化；⑵ 仓库缓存 fetch 只推进 `origin/*` 而 worktree add 解析 clone 时刻的本地旧分支——**repo 绑定项目永远检出不到新提交**,base_ref 改为优先 `origin/{base}`,回归单测覆盖。真实 E2E（合并后 main,服务重启）：PUT/GET 项目绑定落库回读一致；claude chat——项目原生技能目录压过员工软链（模型读出 project-native description + runtime stderr 冲突行）、检出含 fixture 新提交（旧检出缺陷修复实证）、工作区 `.superteam/mcp/claude.mcp.json` 含项目绑定 server（员工零 MCP 绑定,纯项目投影）、opencode.json 对 claude 保留；opencode chat——`opencode.json` 从工作区消失、README 在、`git status --porcelain` 对屏蔽文件零痕迹、run 正常完成。E2E fixture（/tmp cli 仓库冲突技能+opencode.json）已 revert,vcan 技能绑定与项目 MCP 绑定已还原。验证：runtime 111 lib 测试+CP capability/employee/api/app 包+verify:control-plane/runtime-agent/contracts 全绿;迁移经 atlas hash+库内临时 schema validate。残余：PUT 请求体缺 `items` 键被宽容为清空（缺 400 校验,小债）;attestation 落库仅项目任务态,chat 冲突只有 stderr(设计使然);Web UI 项目绑定管理页待立项。
- 2026-07-17 20:34（合并 main @ 7f7b9b19，20:41 GATE PASS）：目录与能力投影修订 Phase 3——清理闭环（spec `2026-07-17-directory-capability-projection-revision-design.md` §5，还清"cleanup_policy/max_retained 死配置"断点 3）。新模块 `workspace_cleanup.rs`：① **终态清理**——run 事件流收尾在 artifacts/attestation 采集与终态回写全部落地后按 `workspace.cleanup_policy` 处理该 attempt 任务工作区（on_success 缺省成功清失败留排障/always/never），删除后 `git worktree prune` 清悬空注册；安全闸=仅 `{base}/workspaces/{proj}/{task}/{attempt}` 恰好三段形态可清，chat 线程目录与未知路径在 plan 层即拒绝；drain 早退 `?` 路径跳过钩子由后台清扫兜底。② **后台清扫（janitor）**——命令循环启动时挂起,60s 首扫此后每 30 分钟：任务工作区按项目 LRU 裁剪至 `max_retained`（空壳 task 目录顺手清）,chat 线程目录 TTL（新配置 `chat_ttl_days` 缺省 7 天,活动时间=目录+一级子项 mtime 最大值）+ 每项目条数兜底（`chat_max_retained` 缺省 20;spec 原文"每(项目,员工)5"因路径不编码员工调整为每项目口径,spec §5 同步回写）;活跃 run 引用目录一律跳过（`RuntimeRunStore::active_workspaces` 按 Running 状态取）;清扫在 spawn_blocking 中执行不阻塞事件循环。配置新增 env `RUNTIME_AGENT_CHAT_TTL_DAYS`/`RUNTIME_AGENT_MAX_RETAINED_CHAT_THREADS`。验证：新单测 6 例（策略三态/chat 与异形路径拒绝/LRU+活跃跳过/TTL+条数兜底/一级深度活动时间）+ 全量 109 lib 测试 + `verify:runtime-agent` 绿。合并窗口已执行（同日 20:41，服务基于合并后 main 重启），**GATE 附录**：① janitor 首扫在真实历史数据上生效——项目 46e7206f 的 15 个历史 attempt 工作区被 LRU 裁至 max_retained=10（removed 5），chat 零误删；② 终态清理实证——低风险需求（pwd 采集）经真实链路零人类触点跑完，任务 attempt 工作区在终态回写后自动消亡（仅留空 task 壳归后续 sweep），无 cleanup failed 日志，稳定家目录/chat 目录未受影响；③ chat TTL 实证——旧一次性布局残迹目录 mtime 置 8 天前后，重启触发首扫即被移除（removed 1 chat threads），同项目当日活跃线程目录保活。**失败保留分支仅单测覆盖**（未真实诱导失败任务），always/never 策略同。E2E 现场：项目 69510a02 coordination_policy 已还原。
- 2026-07-17 17:45：自治姿态 Phase C1——对抗 verdict → 自动返工自迭代（外环闭合第一步，spec `2026-07-16-autonomy-posture-calibration-design.md` §4/§8 + 外环 `2026-06-30-autonomous-outer-loop-iteration-attestation-budget-design.md` 支柱 A）：闭合"开发↔判官无人自迭代"——判官证伪（held）且返工预算剩余时，协调线程**自动**把判官逐 lens 驳斥理由合成返工输入 → 同员工重跑被审任务 → 完成**重触发判官** → 收敛（satisfied 放行+释放下游）或耗尽（→ acceptance_pending 人类兜）。Phase B 只是 held 死等，C1 让它自迭代。① `ListAdversarialJudgements` Go 仓储读取器（逐 lens 判官驳斥从只写变可读）；② `listAdversarialCriteriaForTask` 匹配沿修订链解析到**修订根 key**，使返工任务（新 key）完成时判据仍命中、判官重点火；③ `synthesizeAdversarialRevision`（仅 refuted lens）+ 抽出共享 `buildRevisionTask` 构造核心（自报与对抗两路径共用，自报路径逐字节保真）；④ `CreateReworkTaskFromAdversarial` 任务级聚合多判据 refuted lens 成一个返工，`revisionBudgetExhausted` 有界（默认 3 轮）、确定性幂等 key（Temporal 重试不重复）；⑤ workflow 触发 GetVersion **v3**（v1 阻塞下游/v2 放行至验收/v3 返工循环）：held+预算→派返工（下游等收敛）、held+耗尽/escalate/error→放行至 acceptance_pending 转人类，绝不静默自动通过。**末尾全分支评审揪出并修 2 个 per-task 单测漏掉的真实缺陷**（证明"绿单测≠loop 真能跑"）：⒜ CRITICAL——`buildRevisionTask` 漏 `AcceptedPlanRevisionID`，返工任务该字段 NULL 致 `listAdversarialCriteriaForTask` guard 短路、判官永不重点火、loop 不闭合（单测手设该字段盖住了真实形态；一行修 + 真实往返测试，顺带修自报路径同源隐患）；⒝ IMPORTANT——收敛/耗尽的返工不释放**根任务下游**（下游锚根 T0、resolveReadyDownstream(返工) 找不到）致非叶被审任务死锁、人类杆够不着（`ResolveReadyDownstreamInput.ResolveRevisionRoot` gate 到 v3、重锚 `ListDependentsOfTask` 到修订根，收敛/耗尽全释放；旧版本 false 不变 replay 安全）。全层 Go 测试 + replay 绿。**真实自迭代 E2E 三项全 PASS（全真链，真实 coordinator + runtime-agent/claude 跑原任务与返工，无模拟）**：A held→**自动**返工派发（日志 `adversarial_version 3`/`held_criteria 1`/`CreateReworkTaskFromAdversarial`/"auto-rework dispatched" 零人工；返工任务带 revision_of_task_id + accepted_plan_revision_id 非 NULL + input_requirements 3 条逐 lens 驳斥；下游 held 期 blocked）；B 返工完成→**判官重点火**（第二次 AdversarialReviewForTask ActivityID 151 vs 109 + 新判官行，loop 闭合）；C 收敛→释放根下游（返工照反馈**真补 stringutil_test.go 4 边界+100% 覆盖**→重判全 ACCEPT→verdict 翻 satisfied→解析修订根下游→依赖 blocked→completed；agent 真照结构化反馈改对=spec §4 无人自迭代真跑通）。遗留（诚实，均归后续阶段）：C2 无进展**语义**升级（失败指纹/驳斥或 diff 未变即升级，防无人处自旋烧 token——现仅计数型耗尽）、C3 问责层翻转为 pull 记录仪（保守版：只改"verdict 未产出"处理）、外环支柱 D 结论回写复利，均未做；transient DispatchProjectTask "project conflict"（runtime active-run race，既有）未阻断 E2E。

- 2026-07-17 11:28（合并 main @ c3fe0a8f，17:09 GATE 全项 PASS，附录见文末）：目录与能力投影修订 Phase 1——修三处闭环断裂（spec `2026-07-17-directory-capability-projection-revision-design.md` §1/§2/§4，断点勘察经源码+节点目录残迹取证定罪：全节点任务工作区零技能软链、探针技能安装后员工从未被派发、会话路径不消费 `payload.skills`）。① **稳定员工能力缓存**（spec §1）：控制平面删除一次性家目录派生——项目任务派发的 `project-tasks/{proj}/{task}/{attempt}/employees/{emp}` 与 chat 的 `chat/{proj}/{emp}/{nonce}` 全部收敛为 `stableAgentHomeDir`（`workspace_base_dir/(teams/{team}/)employees/{emp}`，与 runtime `instances.rs` 派生一致），技能 checksum 缓存与人格物化自此跨任务/跨轮次生效；`chatDispatchNonce` 保留仅用于兼容执行实例 ID。② **会话技能物化 + 逐 key 软链**（spec §2/§3.1）：runtime `ensure_command_instance` 改 async，会话开始把 `payload.skills[]` 按 `archive_checksum_sha256` 增量物化进员工缓存（复用 install_skills 的 checksum 跳过 + presign 下载 + 原子替换；ProvisionInstance 落点同步统一到 `<home>/<provider_root>/skills/`，消除与 InstallSkills 的双路径不一致）；`link_provider_skills` 从"整目录软链、target 存在即整体放弃"改为**逐 skill key 软链**：工作区已有同 key（项目仓库原生技能）→ 项目侧优先、跳过并留 stderr 痕迹返回冲突清单（attestation 落库归 Phase 2）；已链 key 重放幂等；**声明了技能但物化/软链失败一律 run 级报错，不再静默**。③ **chat 目录按线程键**（spec §4，改写主 spec §13"被动锚点"）：chat 派发 metadata 补齐 `project_id`/`workspace_mode`/`base_ref`/`project_git`（经 `ChatAnchorProjectGitResolver` 可选接口从锚点项目 repo 绑定解析）+ payload 构建期注入 `chat_thread_id`（根轮次=run.ID，续聊继承）；runtime 工作目录从全局共享缺省 `workspaces/unscoped/manual/attempt` 改为 `chat/{proj}/{thread_id}`——线程内 CWD 稳定（claude 会话文件落点不变保 resume）、新对话新目录；repo 绑定项目 chat 物化 **readonly worktree**（复用 repos 缓存），未绑定为空目录。**迁移注记**：既存 chat 线程一刀切断链重开（人类拍板，不做旧共享 CWD 兼容期）；idempotency fingerprint 含 `agent_home_dir`，跨本次部署的重试不再命中旧幂等指纹（按新派发处理）。验证：CP 单测（稳定家目录断言反转、chat metadata 无绑定 none/绑定 readonly+project_git+base_ref、根轮次 chat_thread_id=run.ID）+ RT 单测（chat 线程目录稳定复用/不安全段拒绝/readonly worktree 物化/逐 key 软链+项目原生冲突+重放幂等/未物化技能报错）全绿，`verify:control-plane`/`verify:runtime-agent` 绿。**真实链路 E2E 未跑**（隔离 worktree 实现，dev-services 须主 checkout；按 CLAUDE.md 为默认完成条件，列为合并窗口 GATE：claude 全链技能进工作区被发现调用/二次任务缓存命中零下载/chat 同线程多轮文件连续+resume 不破/codex CODEX_HOME 技能重定位 smoke/opencode 全链 smoke 含项目根 opencode.json 合并行为实测）——合并窗口已执行，**GATE 附录（2026-07-17 17:09，服务基于合并后 main 重启）**：① claude chat 全链 PASS——稳定家目录首次物化探针技能（presign 1 次），`chat/{proj}/{thread}` 工作区现 `.claude/skills/evidence-e2e-probe` 软链→`employees/{emp}` 稳定缓存、readonly worktree（README/cli/.git detached）、provider 真实发现技能并列出；② 同线程二轮 resume PASS——`e2e-thread-marker.txt` 原样读回、provider 复述上一轮三件事、**presign 仍为 1（checksum 缓存命中零重复下载）**、无新增一次性 nonce 目录；③ 项目任务路径 PASS——低风险需求（date 采集）经 deepseek planner 零人类触点直达 completed，`workspaces/{proj}/{task}/{attempt}` 现真实产物 gate-timestamp.txt+技能软链→稳定家目录，一次性 `project-tasks/` 目录零新增（基线 2→2）；④ codex smoke PASS——`.agents/skills` 软链被 codex 发现且自述"按技能指引核对"；⑤ opencode smoke 首轮 FAIL→**顺手修两处既有缺陷后 PASS**（de77ab6d spawn 未关 stdin 致继承守护进程管道时永挂——`tail -f /dev/null |` 管道现场复现；852ff0d6 事件 schema 漂移——1.17 实际输出 step_start/text/step_finish 而 parser 只认 session.updated 族，全部静默丢弃致 run 永滞 dispatching，补映射+现场真实 JSON 样本单测 4 例，定罪路径：包装器截获 argv/exit=0/stderr 空 + CP 日志该命令零回写）；⑥ opencode 项目根 `opencode.json` 合并行为实测**确实加载**（坏配置直接报错）——三 provider 中唯一裸奔者，屏蔽方案归 Phase 2 与注册表项目 MCP 绑定一并设计（spec §8.3 已回写）。E2E 现场：员工"审查员-vcan"/"P2a调研员"临时绑探针技能已解绑、项目 69510a02 coordination_policy 已还原、smoke 员工"目录投影smoke-codex/opencode"保留可复查。附带发现（既有，待立项）：provider 早退/零事件时 run 永滞 dispatching 无看门狗，靠 stale reap 兜底；本机周三遗留 `opencode upgrade` 僵尸进程未动。Phase 2（冲突留痕入派发记录与 attestation、注册表项目级 MCP 绑定）、Phase 3（cleanup_policy 真执行/max_retained/chat TTL）未动。
- 2026-07-17 15:25:飞书集成 P1——IM 投影通道(绑定/发起/审批/通知)+ 外部服务凭据切片(spec `2026-07-17-feishu-integration-design.md`,计划与实施记录 `plans/2026-07-17-feishu-integration-p1.md`,分支 feat/feishu-integration-p1):① 前置清理:剃除 leader/验收人残留(迁移 070,产品层已弃用;项目人类成员同等身份,human_owner=必绑锚点+兜底路由,CLAUDE.md 同步);② 决策 any-of-N:签署/resolve/项目验收授权从单 TargetUserID 扩为合格处理人集合(active human 成员∪owner),inbox 项目决策类事项对全体人类成员可见,存量零迁移;③ 迁移 071 四表:飞书应用配置(secret AES-GCM 加密)/身份绑定(双 UNIQUE)/外部服务凭据(bcrypt 哈希)/出站 outbox;④ 外部服务凭据切片:ServiceAuth 中间件(Bearer+X-Service-Name)+ on-behalf-of(X-On-Behalf-Of+X-Feishu-Open-Id 经绑定表反查核验,判权不扩 OpenFGA 一律以绑定用户为 ActorUser)+ 签发/吊销管理端点;⑤ 身份绑定双路径:通讯录批量反查 + OAuth(一次性 state、防开放重定向、换绑删旧建新),Console 用户页绑定状态列+同步/绑定按钮;⑥ outbox:决策创建/resolve/需求终态三钩子与业务写同事务入队,收件人=合格处理人×绑定表(全员未绑定单行 skipped_unbound 留痕),resolve 后 pending 作废+已发送卡入队 card_update,消费端点轮询+ack(3 次失败终态、重复 ack 幂等);⑦ connector 独立进程(apps/feishu-connector,oapi-sdk-go v3 长连接):集群模式/at-least-once 按 event_id LRU 去重/3 秒窗口只入队、私聊显式意图四步提需求流(普通消息零副作用)、审批卡分级渲染(plan_review/planning_gap 卡内可操作,判据签署仅富信息+深链防橡皮图章,结果只读)、resolve 点击瞬间同步整卡置换。**真实 GATE(真人真飞书)已过**:OAuth 绑定落库、私聊四步流提需求(source_type=feishu)、审批卡送达+批准生效+card_update 置灰、长连接断线自动重连、outbox 全生命周期(真实飞书 API 错误驱动 failed×3);实测揪出并修复 3 缺陷:决策卡按钮时间窗内可重复点击(同步置换修复)、**韧性缺陷家族#4 真实复现并修复**(object 型 deliverables.value 写回 400 致会话完成但任务永久卡 running——UnmarshalJSON 类型宽容)、事件 handler 噪音。门禁:双 Go 模块全绿+replay PASS+契约 guard+web 104 文件 799 例串行全绿+typecheck/build。**联调按用户指示中断**:完整快乐路径复跑/any-of-N 双人/投影不阻塞/通讯录反查等待手动联调清单与遗留缺陷(写回失败无持久重试等)见计划文档实施记录。

- 2026-07-17 12:02：自治姿态校准 Phase B 收尾——全栈真实 E2E 通过 + held 活性修复 + 模板迁移（承接 01:29 档2 机制）：① **provider 腿全栈真实 E2E PASS**（合并后主 checkout 真实栈）：全新项目 coordinator（GetVersion trigger v1）→真实 deepseek planner 出 4 任务→**runtime-agent 真实认领 + claude 在 worktree 真跑写代码+测试**→提交结果契约→协调线程 `AdversarialReviewForTask` live 触发（日志 ActivityID 119 ~46s）→**3 个真实 deepseek 判官 3/3 证伪**→聚合 unsatisfied + 3 明细行落库→gate HELD（demand executing/下游 blocked/API verdict=unsatisfied judge_type=adversarial）；比隔离 E2E 更强——claude 真实产出**带了测试文件**（非 trivial 空），判官仍以"边界覆盖无法证明、测试无法独立复现"证伪，证伪不确认对貌似合理真实产出也守住。② **held 活性修复（GetVersion 围栏 v2）**：E2E 揪出 held 的 demand 困在 `executing`（触发 held 不解锁下游）、Task4.5 人类覆盖需 acceptance_pending 却够不着=死等。改 v2：held/error 仍持久化 verdict 但**放行下游**→demand 走完任务图到 acceptance_pending→gate 在该判据 held→人类档3 杆可达；v1 保留（replay + 已 ship 的 dev workflow），minSupported=DefaultVersion 旧历史安全，replay 绿。（产品拍板：held 不挡任务图，到验收闸由人类兜；返工态归 Phase C。）③ **模板 reviewer 角色迁入 adversarial_review（替换）+ exit_evidence 近似校验（Task5）**：治理层 `EnforceScenarioTemplateGovernance` 对声明独立审查的 role_independence（reviewer 角色带 review 类能力 + 依赖边判定被审任务）注入 blocking adversarial_review 判据 satisfied_by 被审任务（AI 判官即审查者，四眼由判官天然独立满足→该约束不再因共享员工 reject），幂等；exit_evidence：非 human_gate 的自治阶段其出口判据不得全 human_judgment（至少一条 automated_test/adversarial_review），否则 replan（human_gate 为键近似，per-stage checkpoint 落地后精化）。**评审揪出并修 Critical-latent 洞**：原迁移判别子纯结构（2 角色+依赖边）无审查信号→任何 role_independence 带依赖边都误迁移+关掉职责分离强制（如 approver≠requester 四眼失守）；修=判别子加 review 信号（reviewer 须带含"review"子串能力）+负向测试锁定，fail-safe（AND 收窄最坏退回 enforceRoleIndependence）。全层 Go 测试 + replay 绿。遗留：④ held demand 的自动返工/重评 + 前端签署 UI + 进度计数器纳 adversarial 归 **Phase C 外环**；⑤ 迁移判别子 review 信号是子串启发（robust 版=显式约束字段/kind，代码 TODO）；⑥ Task5 template→注入→judge 全栈 live 未单独跑（注入判据流经已 E2E 验证的 adversarial 路径）。

- 2026-07-17 02:07：证据地基——artifact 采集/内容寻址/可取回 + runtime 全面 presign 归零对象存储凭证（spec `2026-07-09-evidence-grounding-artifact-collection-design.md`,§8 实施修订含人类拍板"presign 一步到位"）：让「证据」真正成立——每个完成的项目任务至少产出一条工具产生的真证据,人类可经平台取回数字员工无法伪造的事实。① 迁移 068:`project_artifact_refs` 加 attempt_id/digital_employee_id 血缘列 + attempt 内幂等部分唯一索引 + checksum CHECK;`project_evidence_refs` 空串状态回填 submitted + 非空 CHECK;DROP 零引用遗留表 task_artifacts(连清 4 个 sqlc 死查询)。② 控制平面 presign 三端点(RuntimeSessionAuth,tenant 一律取自 runtime 身份不信请求体):`POST /runtime/artifacts/presign`(key=artifacts/{tenant}/sha256/{hex},Exists→already_exists 跳传)、`POST /runtime/raw-logs/presign`(attempt 租户归属校验+服务端派生 key)、`POST /runtime/skills/presign`(skills/{tenant}/ 前缀闸);S3ObjectStore 增 StatObject/PresignPut/PresignGet;`GET /artifacts/{id}/content` 项目读权限校验后 302 presigned GET,字节不穿控制平面。③ 物化路径合一:删除 extract/materialize 双写(28 行空状态行的病根),`materializeAttemptEvidenceWithQueries` 进 attempt writeback 事务——对象存在性+大小核验→artifact 行(血缘+attempt 内幂等 upsert)→证据行按类型映射(transcript→execution_transcript/submitted、diff→code_change、conclusion→self_report/**unverified**,自述不冒充证据)带 artifact_ref_id;**零真证据→完成回滚**(数字员工不能只交自述结题)。④ Runtime 归零凭证:删 aws-sdk 依赖与 S3 配置段整体;raw 分段/manifest 上传迁 presign(PresignRawLogUploader,重试单元含换签);skills 下载迁 presigned GET(SkillArchiveFetcher,sha256 复核不变);artifacts.rs 重写为采集器——execution_transcript(本地 raw 缓冲**脱敏后**哈希上传,截断保尾)/diff(git diff HEAD)/conclusion(is_evidence=false),采集 refs 前插合并进 complete writeback 与 result contract(TaskResultRef 契约扩展 name/sha256/size_bytes/content_type/truncated/is_evidence,Go 侧 taskResultRefsToAny 透传)。⑤ 顺带修三缺陷:dispatch-conflict 事件静默丢 ledger(started 回写带 command_id,按 task_runs.command_id 回填 NULL 的 attempt run 关联)、模型 prose 不脱敏(text_delta/turn_completed/turn_error 事件 + 终态 summary/conclusion 在离开执行机前统一 redact,raw 保持原样)、conclusion 全下游(run_completed 事件/conclusion.md/attempt.completed/summary.created/execution_summaries)明文泄漏。⑥ Web:工件面板加"下载"(302 取回,非内容寻址对象标"不可取回"),evidence 面板 unverified→"自述·不构成证据"标签。验证:Go 单测(presign 租户强制/sha 校验/already_exists/物化解析映射/skills 前缀闸)+verify:control-plane/contracts 绿;Rust 36+ 集成测试(mock presign 桩)+采集器单测(脱敏先于哈希/保尾截断/自述非证据)绿;web 797+typecheck+build 绿;**真实链路 E2E(spec §6,runtime 配置零 S3 凭证)**:真实 claude 任务跑 `git status`→artifacts 现 execution_transcript(sha256=DB checksum=取回后本地重算)→302 取回 JSONL 内含 2 条真实 tool_result(git 真实输出,is_error 为进程所写)→evidence 每 ref 恰一行不再双写→transcript submitted+artifact_ref_id 非空/conclusion self_report+unverified→TOS 对象 200;脱敏取证任务(echo 假 sk- token)首轮揪出 conclusion 链明文泄漏,修复后复验 ledger 0 残留/9 处 REDACTED、conclusion.md 与 summaries 全净;零凭证下 raw 分段+manifest 照常落 TOS(attempt log_ref 完整)。残余:§7 后续 spec(判据消费 transcript 指针、人类待办机制反转、哈希 attestation 强校验)未动。补记(2026-07-17 09:29,skills presign 残余项闭环):上传零依赖探针技能 evidence-e2e-probe → 直插 DB 造执行实例(install 预检需 `digital_employee_execution_instances` ready 行,standalone 实例 designed-off 后的既有 E2E 惯例)→ `POST /skills/{id}/install` 真实走通:control-plane 日志 `POST /runtime/skills/presign 200`、SKILL.md 物化到员工 home `.claude/skills/evidence-e2e-probe/`、`.skill-checksum` 与 API 报 sha256 一致(复核通过才落盘)——runtime 零 S3 凭证下技能安装全链实证。既有小瑕疵(非本次引入):zip 含目录条目时 `common_root_prefix` 不剥根前缀,SKILL.md 落在嵌套子目录。
- 2026-07-17 01:29：自治姿态校准 Phase B——对抗式 AI 评判（信任三档之档2，spec `2026-07-16-autonomy-posture-calibration-design.md` §2/§8）：把"落不到执行事实（attestation）的验收判据"从"一个 reviewer 说 OK"升级为 **N 个独立 LLM 判官"证伪不确认"评审 + 多数证伪即杀**——HiClaw/paperclip 共缺、SuperTeam 差异化的那一层。① 新 `adversarial_review` verification_method（注册 + satisfied_by 必填校验，Task1）；② 迁移 **069**（`demand_adversarial_judgements` 逐判官明细表 + `demand_criterion_verdicts` 的 adversarial 聚合行唯一索引 + 收紧 `uq_demand_verdicts_human` 加 `judge_type='human'` 谓词使 human/adversarial 两 NULL 行互斥，Task2）；③ 判官引擎（纯函数 `runAdversarialReview`：3 固定视角 correctness/security/reproducibility 各一次证伪式直连 LLM 调用、解析失败保守判 refuted、`RefutedCount>=ceil(N/2)`→unsatisfied、判官数默认 3 策略 `adversarial_review_judges` 可调硬顶 7、预算触顶短路 `escalate_human`；诚实标注 N 判官=同模型多视角非模型独立，档2 已知局限 §7，Task3）；④ 投影+触发+收敛闸（协调线程在被评审任务完成后经 `GetVersion` 围栏派 `AdversarialReviewForTask` Activity——旧历史无 adversarial 判据故围栏从诞生即正确、replay 绿；证据取被评审任务**自身**最新结果 Contract 非上游；`criterionEffectiveVerdict` 加 adversarial 分支（human>adversarial>executor）**并修缺陷**——原将非 human 行全当 executor 会误计 adversarial 聚合行；executor 自报对 adversarial_review 判据跳过投影，双重防御"员工无法自放行判官应评的判据"；判官 Activity 返回错误（LLM 传输故障重试耗尽）→升级人类档3 hold 绝不自动放行，Task4）；⑤ **打通档3落地杆**（Task4.5）：`SignDemandCriterionVerdict` 扩为可签署 adversarial_review 判据——人类档3 可覆盖被判官 held（unsatisfied/escalate_human）的需求（gate 已有 human 优先），使我方 escalate_human 路径有真实人类落地杆、契合三档"档3 解决档2 升级"骨干。承重安全属性经末尾全分支评审 CONFIRMED：executor 永不能自放行 adversarial 判据、escalate_human 在 Go 与 SQL 两层均 held（`CountUnsatisfiedBlockingCriteria` 走同一 Go 解析器无 SQL 旁路）、key/UUID 匹配失败 fail-closed（判官没跑则 hold 不放）。全层 Go 测试（判据/迁移/引擎多数投票与解析边界/投影与收敛/人类覆盖）+ replay 绿。**遗留（诚实）**：⑴ 真实全栈 E2E ——**已于 2026-07-17 12:02 条目 PASS**（真实 claude→协调线程 live 触发→真实 deepseek 判官 3/3 证伪→gate HELD）；⑵ 场景模板 reviewer 迁入 adversarial_review + exit_evidence（Task5）——**已于 12:02 条目完成**（含 Critical 洞修复）；⑶ 迁移已 renumber→**069**（并发 `068_evidence_grounding` 先落 main，集成时避号 + 重算 atlas.sum，无表重叠）；⑷ 前端签署 UI + 进度计数器纳入 adversarial 判据归 Phase C；⑸ 每 lens 唯一键不含 reviewed_task_id（一判据两 satisfied_by 任务时末者胜，可接受）。

- 2026-07-16 22:36：Chat 会话持久化——切页恢复当前会话 + 新对话断链（spec `2026-07-16-chat-session-persistence-design.md`，评审决议 §6 已收口）：修复任务中枢对话模式切菜单/刷新即丢线程的问题；数据侧每轮 chat 本就持久化（run 链 + resume_of_run_id），本次补"视图重建 + 会话边界"。① 迁移 067：`tasks.chat_thread_id`（可空 UUID + `(tenant_id, chat_thread_id)` 局部索引），存储约定根轮存 NULL、追问轮继承前序有效值，读侧 `COALESCE(chat_thread_id, run_id)` 投影（Go mapper 单点 `effectiveChatThreadID`），atlas.sum 已更新并经一次性 podman postgres:16 `migrate-validate` 全量 replay 通过；② 控制平面：CreateRun 的 chat resume 分支继承 prior 有效 thread id（legacy 无存储值回退 prior.ID，字段服务端独占解析、包内 unexported 防调用方注入），runs 列表/Get 投影 `chat_thread_id` 并新增列表过滤参数（root 按 tr.id 命中、追问按索引命中），**顺带修复** chat 锚点盲区：列表 `project_id` 过滤原 join project_tasks 对 chat run 恒空，现兼容 §13 审计锚点 `params->metadata->>anchor_project_id`；③ 契约：run schema + 列表 `chat_thread_id` 参数，`generate:control-plane` + `verify:contracts` 绿；④ ChatPanel：挂载/换锚点两查恢复最新链（最新 chat run 取 thread id → 拉全链倒序重建，question=task_title、answer=extractAnswerText，limit 100 截断；水合仅认 `isFetchedAfterMount` 新鲜数据防旧缓存丢最后一轮），进行中轮恢复后自动续接既有轮询,过期/被清理 result 降级"（内容已过期或无结果）"（§6 决议 2），恢复失败非阻断提示可直接开新会话；新增「新对话」按钮为断链唯一主动入口（下一条不带 resume 即服务端冠新根）；**顺带修复** retry 孤儿链：失败重试原不带 resume 每次开新链，改为与发送一致续 lastCompleted。验证：Go 单测（thread 继承/legacy 回退/root 置空/task 屏蔽 + 过滤参数解析）+ verify:control-plane 绿；web chat-panel 97 例（新增恢复/过期降级/新对话断链/进行中续轮询 4 组）+ 全量 797 例 + typecheck + build 绿；真实链路——Web(3000)→Control Plane(8081)→远程 PG→Runtime Agent→Claude provider 六轮真实对话：两轮问答后切项目管理页返回完整恢复且追问上下文延续（"乘以3"答6、恢复后"加10"答16）、整页刷新恢复、发问后立即切走 run 后台完成返回全链在列、换项目锚点显示各自链且互不串、「新对话」后 provider 自证无历史（"没有历史——这是全新会话"）且 DB 断言根轮 chat_thread_id=NULL/四追问全归组根 run/新会话独立成链、恢复两查经 curl 带真实鉴权复核（老链 total=5 / 新链 total=1）。遗留：会话列表/历史回溯 UI 另立项（thread_id 已铺路）；spec §5-6（真实失败重试续链）以组件测试覆盖，未在真实链路诱导 provider 失败。——SSE 秒级推送 + operational_status 服务端过滤 + task_events 索引：① 迁移 066 `idx_task_events_tenant_created`(tenant_id, created_at DESC, id DESC)，activity 端点/流的排序与游标增量走索引（EXPLAIN 实证 Index Scan；atlas.sum 已更新，本机无 docker 仅做 checksum 校验 + 真实 dev 库 apply 验证，dev-url 全量 replay 留给 CI）；② overview 新增 `operational_status` 逗号分隔多值过滤：计算态无法直接下推 SQL，在既有 operational facts 全量行上由 Go 状态机裁决后回填命中 ID 集合作为 items 查询过滤条件（空命中传 uuid.Nil 哨兵区分"不过滤"），pagination.total_count 同步改为命中数，非法值 400——轮播队列规模化时可只拉非空闲员工；③ 新增 `GET /digital-employees/activity/stream`（SSE）：服务端 2s 游标增量轮询，新事件按时间正序逐帧 `event: activity` 推送（数据面复用 GetActivity 同一查询），空闲期注释行保活，未带 since 从"现在"起播不回放历史，查询失败即断流交给 EventSource 自动重连；④ Web：EventSource(withCredentials) 订阅流，事件到达即节流(2s)失效 activity+overview 查询——动态流秒级上屏、状态变化经既有轮询 diff 通道同步提速轮播插队，10s 轮询保留为断流兜底（组件测试注入 fetcher 时默认不开真实流防重连噪音，测试经 eventSourceFactory 注入假源）。验证：Go 单测（SSE 时序/游标推进/保活/非法游标 400）+ employee/api + verify:control-plane/contracts 全绿；web run-overview 48/48 + typecheck + build 绿；真实链路——curl `operational_status=working,error` 精确 7 命中+400、SSE 抓包注入事件 2s 内带全量上下文到帧、浏览器动态流秒级刷新（快于 10s 轮询）、control-plane 重启断流后 EventSource 自动重连恢复推送（现场实测）。
- 2026-07-16 22:52：自治姿态校准 Phase A——默认翻转+rollout迁移注记（分支 feat/autonomy-posture-a，commit aebb4b47..fc415b5b，Task 1-6 收尾）：把需求验收的"是否需要人类判据"从**每需求兜底强制注入**翻转为**自治默认**——不再无条件给每个需求注入 `human_final_confirmation` 兜底判据，只在两种条件下注入：① 项目 `coordination_policy.require_human_acceptance=true`；② 需求触达高风险（`planTouchesHighRisk`）。高风险判定补齐中文风险标签集（原只认英文标签致高风险活逃过人类监督，ff0574f6），且高风险路径**宪法级不可豁免**——不受 `acceptance_human_judgment_exempt` 影响。注入时序移到治理判定之后（fc415b5b）：治理阶段（`role_independence`/`human_gate` 等约束求解）派生出的高风险同样能在验收期拿到人类判据，不会因为注入发生在治理之前而漏判。确认密度按出口深度缩放（350ca701）：浅出口（如 `raw_metrics` 单步采集）计划自动派发免确认卡，深出口/高风险出口（如 `release_record` 发布上线）仍强制 `pending_review` 确认卡。收敛闸（6119c5e5）：全部 blocking 判据为纯 automated_test 且均已满足时需求零人类触点直达 `completed`；含 `not_applicable` 裁决的判据按非阻断处理不逃逸未决状态，高风险需求即使 automated 判据齐全也仍在验收期受人类监督。**Task 4（模板声明式 `human_checkpoints`）本轮推迟**——并发会话持有 `scenariotemplate/service.go` 未提交改动，为避免撞车未落地，遗留给后续分支。**ROLLOUT 迁移注记（重要）**：存量项目原先依赖的"每需求隐式强制人类验收"行为随本次翻转**默认丢失**——除非项目显式在 `coordination_policy` 设置 `require_human_acceptance:true`，或需求本身判定为高风险，否则计划会跳过确认卡、需求可能零人类触点直达完成；存量项目若业务上仍需要"每单必过人"，必须在升级后主动补写该策略字段，这不是自动迁移的。**Phase A 观察（非缺陷）**：即使兜底强制注入已关闭，planner 仍可能在描述判据时**自主判断该需求需要人类主观确认**并产出措辞上属于 `human_judgment` 的判据（例如"数据需完整可读"这类非纯客观陈述）——这是系统对 LLM 自主判断的如实保留而非注入逻辑漏判，但意味着"低风险零触点"实际上依赖 planner 产出全 automated 判据的稳定性，措辞越机械客观（如"文本非空且含指定表头/关键词"）命中率越高，这一措辞不确定性留给 Phase B 处理。**GATE（Task 5，pid 46640 真实链路，deepseek planner + claude-code）**：低风险需求 `63d2ffae`（df -h 采集）计划自动派发（0 个 decision_request）、判据快照仅 1 条 automated_test、真实执行后需求直达 `completed`（从未 `acceptance_pending`）——端到端零人类触点；高风险需求 `19873ea3`（CLI 发布上线）计划保持 `pending_review`（确认卡+`human_review.required=true`+`human_final_confirmation` 判据注入+`human_gate` 治理说明），未逃逸。验证：`verify:contracts` 绿；`go test ./internal/workflow/projectcoordination/ ./internal/project/ -count=1` 跑真实远端测试库（`TEST_DATABASE_URL`）——4 个失败经与 merge-base（2ef47725）逐用例比对**逐字复现**，确认与本分支改动无关（均为既有环境缺陷）：`TestRecordPreDispatchGateResultReturnsLinkedGateWithoutOverwrite`/`TestListWorkflowInstancesOrdersAttentionBeforePagination`/`TestProjectTaskGraphReadReturnsGraphScopedSidecarsAfterUnrelatedRows` 为本机时区（+0800 CST）与 UTC 断言不一致 + DB 既有数据下 UUID 排序不确定性，`TestRecordPreDispatchGateResultUpdatesDecisionLinkedWaitingGate` 为并发会话未提交迁移 `067_chat_thread_id.sql` 造成的 schema 漂移；其余全绿。**存量项目回归 E2E**（复用 GATE 用的低风险项目 `69510a02`，psql 现改 `require_human_acceptance:true`，浏览器 admin 真实提交新需求"运行 uptime 采集系统运行时长文本快照"）：计划 `b54b1364` 回落 `pending_review`（`human_review.required=true` 原因 `plan_requires_human_review`），`plan_acceptance_criteria` 含服务端注入的 `human_final_confirmation`（method=human_judgment/severity=blocking）——证明策略触点在低风险需求上依然生效未被默认翻转吞掉；浏览器同意确认卡后计划 `decomposed`、任务真实派发执行——P1 计划确认→分解→派发链路在 `require_human_acceptance=true` 下完整走通。执行任务随后因**判据①之外的正交问题**（automated_test 判据 `raw_metrics_not_empty_and_contains_load` 缺少任务结果契约要求的结构化 `acceptance_results` 自报，属 Task 5 判据强收紧机制的既有边界，非本次注入时序/默认翻转改动引入）落 `waiting_human`（reason=clarification），停在验收收敛闸之前，未能在本轮跑通到 `acceptance_pending`→`demand_acceptance`→签署 completed 的完整尾段；已尝试"同意"恢复原任务一次，信号已送达工作流但轮次内未见新派发，判定为超出本任务范围的独立问题，留痕不深挖（低风险项目 `69510a02` 与新需求 `85773901` 现场未清理，可供后续复查）。

- 2026-07-16 16:44：运行总览大屏二期——跨员工 activity 端点 + 动态流事件驱动化：新增 `GET /api/v1/digital-employees/activity`（契约+oapi 再生成）：task_events 联 task_runs/digital_employees/tasks + LATERAL 反查 project_tasks 带出所属项目，时间倒序，`since=<created_at|event_id>` 游标增量拉取（响应带 `next_since`，无新事件回显请求游标），limit 默认 20 钳 100，employee-read authz 同 overview。②事件标签单一来源：新建 `employee.ActivityEventPresentation`（13 种已知 event_type 中文标签+状态语义，fail/cancel/complete/provider 启发式兜底），overview `recent_events` 的 SQL CASE 映射删除改为透传原始 event_type 由同一函数映射——还清"每员工 top3 拼动态+text_delta 裸露"两笔债（text_delta→输出内容更新、tool_started→开始调用工具等）。③Web：动态流改走 activity 端点（10s 轮询、失败回退 overview 聚合），条目升级为"员工 + 标签 · 任务标题 + 时间"。验证：Go activity 单测（标签映射/游标往返/服务钳制与 NextSince）+ employee/api 包及 verify:control-plane 全绿、verify:contracts 绿；web run-overview 47/47 + api client 16/16 + typecheck + build 绿；真实链路——curl 全量/增量（0 新事件回显游标）/非法游标 400，浏览器动态流与员工卡日志均显服务端映射标签与任务标题。记债：task_events 无 (tenant_id, created_at) 索引，当前数据量顺序扫描可接受，规模化时随三期补迁移。：运行总览轮播队列滞留时长加权（用户拍板以加权取代固定间隔配置思路）：同紧迫度层内按 `statusSince` 升序——进入状态越早（等待/运行/滞留越久）越靠前，无时长信息垫底，楼层/ID 退为末级决胜；紧迫度分层（异常 > 待确认 > 工作中 > 排队）仍是第一优先级。验证：hook 加权单测 + run-overview 46/46 + typecheck + build 绿；真实浏览器队列头从按 ID 排序的异常员工变为滞留 5 天 15 小时的最久异常员工。

- 2026-07-16 16:15：运行总览员工卡改造为"运行快照"+工具栏腾挪（大屏轮播定位下重新分配卡片注意力，10 秒驻留内回答"什么状态/为什么/多久了/为哪个项目干什么"）：① 新增「运行状态」区块——状态 + 近似持续时长（working 取运行开始时间，其余取最近活动时间；"已运行/已等待/已排队/已持续"按状态措辞）+ 结构化状态原因（`operational_state.reasons` 首次上卡）+ 异常时最近运行错误信息（danger 色两行截断）；② 关联项目列表（最近 5/共 N）降级：当前任务块改带**所属项目**单链（取 working>active 项目），整体关联情况压成身份行"关联 N 个项目"计数，完整列表留给员工详情页（数据层 project_summary 原样保留）；③ 任务 UUID 从卡面移除、Runtime 压缩为一行元信息；卡片网格改宽三列/中两列/窄单列；④ 工具栏腾挪（用户指定）：删"当前楼层"文案行（楼层按钮选中态已表达）、状态图例移顶部居中、轮播指示器+暂停按钮移顶部右侧。模型/adapter 新增 statusReasons/statusSince/latestRunErrorMessage，formatters 新增 formatDurationSince。验证：run-overview 45/45 + typecheck + build 绿；真实浏览器——报告员小王卡显示"待确认 · 已等待 17 小时 39 分钟 + 等待人工确认后继续执行 + 所属项目 Handoff-E2E + 关联 17 个项目"，审查员-csjw 显示"异常 · 已持续 16 分钟 + Runtime 离线，已有任务受影响"，项目列表/楼层文案已不在页面，指示器落位工具栏右侧。

- 2026-07-16 16:04：意图与验收标准 P1 收尾——判据语义化+verdict投影+收敛闸+签署闭环（分支 feat/intent-acceptance-p1，commit 2afa8c3b..4e8eaa35，spec `2026-06-30-intent-acceptance-criteria-design.md`）：把"什么算做对了"从散文需求文本升级为可持久化、可核验、可回溯的结构化判据链。① 判据语义化：planner 产出的 `plan_acceptance_criteria` 补齐 `verification_method`（automated_test/human_judgment）/`severity`（blocking/non_blocking），无 human_judgment 判据时兜底注入需求级人类确认判据（`ensureHumanJudgmentCriterion`，可被项目 `coordination_policy.acceptance_human_judgment_exempt` 豁免），启发式歧义标注（陈述 <8 字或含"尽量/适当/合理/优化一下/等等/大概"等模糊限定词 → `ambiguity_flag`，仅标注不拦截）；确认卡渲染 method/severity 徽标 + 歧义黄旗。② 分解落库：迁移 064 新增 `demand_acceptance_criteria`（判据快照，按 plan_revision 版本化）+ `demand_criterion_verdicts`（逐条裁决）；分解时判据快照连同 `criterion_id` 一并注入对应任务的 handoff_contract，使执行者产出与判据的映射在派发时就锚定。③ executor verdict 投影：任务结果按 `SatisfiedBy` 限定归属判据，automated_test 判据强制要求 `evidence_refs` 含服务端签发的 `attestation:` 前缀引用（由 attempt 的 runtime attestation 事实核实后自动附挂，员工不自报也不可伪造），human 平票按 id 次键确定化裁决顺序。④ 收敛闸：全部 blocking 判据有已裁决 verdict 前，需求停在 `acceptance_pending`（不进 completed），同步创建 `demand_acceptance` DecisionRequest 三件套并入 inbox；判据面板按 planned_task_key 解析出真实任务结论（反橡皮图章）。⑤ 人类签署端点：逐条判据签"满足/不满足"（`POST criterion-verdicts`），human verdict 写入后全部 blocking 判据满足则需求 `completed`+`demand.acceptance_completed` 事件，任一不满足则 `failed`+`demand.acceptance_rejected`（payload 含 criterion_id/statement/reason）；签署收敛路径支持重试恢复，项目验收随之重评。⑥ Console：判据血缘面板（method/severity/存证 chips/任务结论摘要）+ 签署 UI（理由框+满足/不满足）。**真实 E2E 门禁**（Task 9，两轮）：首轮 FAIL——3 个 GATE-blocking/major 集成缺陷经真实链路复现：automated_test 判据的 attestation 引用在真实链路上无生产者（guard 只认 `acceptance_results[].evidence_refs`，runtime 只回填 `verification[]`）、`demand_acceptance` 决策行缺 `plan_revision_id` 致签署端点查询恒 404、判据面板按 UUID 解析 `satisfied_by` 但快照存的是 planned_task_key 致任务产出恒空；修复（commit 4e8eaa35：服务端按 attempt 的 runtime attestation 事实自动核验并附挂 evidence_refs、补 `plan_revision_id` 列回填、面板改按 planned_task_key 解析）后复验 **PASS**（完成径+驳回径全链路真实走通，attestation 引用可回溯至真实 provider 会话）。Task 10 收尾复核三条残余场景：(a) 歧义黄旗——本轮 3 次真实提交模糊需求（"尽量优化登录接口的性能"/"让CLI合理变快一点，大概优化一下"）planner 均重写为具体判据未触发标注，代码路径（`isAmbiguousCriterionStatement`，8 字阈值+模糊词表）未变、Task 9 复验已用真实数据实测触发（截图 `rv-step1-confirmation-criteria.png`）、组件测试 `project-operational-detail.test.tsx` 断言仍在 777/777 绿名单内——判为 planner 稳健而非回归；(b) exempt 护栏——项目 `coordination_policy.acceptance_human_judgment_exempt=true` 后，含审查/合入语义的需求因 planner 自然产出 human_judgment 判据仍正确收敛至 `acceptance_pending`（豁免不豁免计划自带判据，符合设计），而 planner 未产出任何判据的需求（豁免生效前本应被兜底注入）在豁免下 `plan_acceptance_criteria` 全程为空、真实执行完成、demand 直接 `completed`、全程未创建 `demand_acceptance` 决策——真实验证 exempt 只豁免注入、legacy 无判据路径行为不变；(c) automated_test attestation 强制——未重新诱导，依赖 Task 9 FAIL→修复→复验证据链（伪造/缺失 attestation 引用被 guard 拒绝，含 Go 单测覆盖）。验证：`verify:control-plane` 绿；web 测试 103 文件/777 例串行全绿；typecheck/build 在共享工作树受**并发会话**未提交改动（`run-overview` feature 的 `use-runtime-focus-carousel.ts` 等）污染直接报错，经隔离 worktree 核对 committed HEAD（4e8eaa35）本身 typecheck/build 均**干净**，污染与本分支代码无关。残余债：human verdict 写入时未合并 evidence_refs 到已满足的 automated verdict（仅新增而非 merge）；attestation 存在性核验止于 attempt 级别（未逐条命令级绑定）；预存量 clarification 型 waiting_human 无 UI/协调器恢复通路（缺陷#0）与 dispatch-conflict 事件静默丢失家族未在本迭代修复。

- 2026-07-16 15:51：运行总览大屏轮播一期——焦点轮播引擎（纯前端，后端零改动，定位从"点击查看"转向"大屏被动感知"）：新增 `use-runtime-focus-carousel.ts`。① 轮播队列 = 非空闲员工（异常 > 待人工确认 > 工作中 > 排队，同级按楼层聚簇减少切层），空闲/待配置/不可用不进队；② 驻留 10s 自动轮转，焦点即选中（地图高亮 + 底部横向卡跟随），跨楼层焦点自动切楼层；③ 状态变化插队：前端 diff 相邻两次轮询的员工状态快照，出现进入队列态的变化立即抢占焦点并重新计时；④ 交互暂停：点头像/切楼层/带 `?employee=` 深链进入均暂停轮播（用户选择优先），45s 无交互自动恢复，暂停期间不被插队打断；工具栏新增轮播指示器（`焦点轮播 n / N` / `已暂停`）+ 暂停/恢复按钮。验证：hook 单测 7 例（队列排序/空队列/轮转/插队/暂停不打断+自动恢复/深链初始暂停/焦点出队回退）+ 页面测试 2 例，run-overview 42/42 + typecheck + build 绿；真实浏览器：15 人队列自动轮转（1/15→5/15），DB 现场翻转任务状态制造 error→working 变化，下一轮询焦点即抢占至该员工、驻留后继续轮转（逐秒采样取证），点击头像即暂停并显示所点员工。二期待做：跨员工 activity 端点（事件驱动主叙事+插队信号源）；三期：operational_status 服务端过滤与 SSE。：运行总览员工卡全宽度横向化（用户反馈笔记本下右栏员工卡仍不可见，撤销 15:12 的 1500px 阈值方案）：员工详情卡任何宽度都平铺在地图正下方，右栏永远只放运行概况；删除页面级 ResizeObserver 判宽与 vertical 方向，卡片自身 `@container` 容器查询按卡宽升列——窄单列 → `@xl`(36rem) 两列 → `@5xl`(64rem) 四列。教训入档：vitest browser 装置不加载全局样式，容器查询/计算样式断言无效（失败截图为无样式裸 HTML），测试改断言类名声明，列数行为走真实浏览器。验证：run-overview 33/33 + typecheck + build 绿；真实浏览器 1510px 视口卡宽 878px 呈 2 列贴地图下方无溢出（scrollHeight 1158），DOM 内联宽度探针 1200px→4 列 / 500px→1 列全部生效。：运行总览「今日消耗 tokens」大数值紧凑显示：新增 `formatCompactTokens`（≥100 万转 M、≥10 亿转 B，最多两位小数去尾零，百万以下保持千分位），应用于运行概况指标卡。验证：formatters 单测 3 例 + run-overview 33/33 绿；真实浏览器指标卡显示 `3.51M`。

- 2026-07-16 15:12：运行总览大屏自适应布局（工作树改动，未建分支，续 14:46 条目）：修复大屏下地图底部大片留白 + 员工卡右栏需滚动才能读完的问题。员工详情卡抽为独立组件 `employee-detail-card.tsx`（vertical/horizontal 双方向，分区块复用同一套 JSX），页面容器 ResizeObserver 实测宽度 ≥1500px 时切宽画幅模式：员工卡横向四列（身份+操作 | 当前任务+Runtime | 已关联项目 | 日志+消耗）平铺到地图正下方填补留白、右栏只留运行概况（高度与地图匹配）；低于阈值维持右栏纵向堆叠（笔记本现状不变）。选中态与 URL `?employee=` 深链在两种模式间共享。侧栏组件瘦身为纯运行概况，状态 tone/格式化函数收敛到 `status-maps.ts`/`formatters.ts` 单一来源。验证：run-overview 30/30（新增宽窄两模式断言：横向卡在 master 列内、窄容器回落右栏）+ typecheck + build 绿；真实浏览器 2000×1030——横向卡贴地图下方、整页 scrollHeight 1097（此前 >1900 需滚动）、17 项目员工卡"最近 5/共 17"完整可读、1440px 回落右栏纵向无横向溢出。

- 2026-07-16 14:46：运行总览真实数据流展现（工作树改动，未建分支）：把 /run-overview 从静态状态快照升级为可感知"谁在干活、干什么、为哪个项目"的实时展现窗。① Overview 读模型扩展（复用底层数据，零新表零迁移）：`ListDigitalEmployeeOverviewItems` 新增 `employee_projects` CTE——员工↔项目关联口径 = project_members 活跃 digital_employee 成员 ∪ project_tasks 分派（UNION 去重 + BOOL_OR 合并 is_member），按项目聚合 active/working/total 任务数与 `GREATEST(任务,项目).updated_at` 最近活动，每员工按活动倒序取前 5 + 总数（`project_count` 不截断）；契约新增 `DigitalEmployeeProjectSummary`/`DigitalEmployeeProjectLinkSummary`（overview item 必填 `project_summary`），oapi-codegen/sqlc 再生成；② 头像运行态差异化（DESIGN.md 动效仅服务状态反馈口径）：状态色 border ring + 右下角状态小圆点双编码，光晕仅活跃态——working 绿色 ping 2.2s 呼吸、error 红色 ping 1s、waiting_human/queued 琥珀/蓝 pulse，全部 `motion-safe:` 尊重减弱动效偏好，idle/待配置/不可用保持静态；③ 员工信息卡新增「已关联项目」区块：`最近 N / 共 M` 口径 + 项目名 + 活跃任务数 + working/active/静默三态圆点 + TanStack Link 深链项目详情，空态文案；④ 运行概况卡公司级增强：新增「关联项目」（跨员工去重计数）与「今日消耗 tokens」（求和 usage_tokens_today）指标、跨员工「最新动态」信息流（recent_events 按时间倒序聚合前 5，员工名+事件+时间）；状态映射抽出 `status-maps.ts` 单一来源（头像节点/侧栏共用）。验证：verify:contracts/control-plane 绿；web 测试全量分块串行全绿（全量单发命中预存量 route.fulfill 堆崩溃 flake，与改动无关）+ typecheck + build 绿；真实链路——curl 带 auth 打 overview API 确认 27 员工 project_summary 真实回填（报告员小王 17 项目截断前 5），浏览器实测三种光晕 computedStyle 动画在跑（ping 1s/ping 2.2s/pulse 2s）、关联项目深链真实跳转项目详情、概况卡 17 项目/3,234,767 tokens/动态流真实数据；working 态经直插 DB fixture（团队挂载+running project_task+ready 执行实例绑在线 local-dev-node）走真实状态机收敛为 working 后浏览器确认。记债：无团队归属员工不出现在楼层地图（预存在缺口，本次借 fixture 暴露）；「最新动态」事件标签沿用后端既有 event_type 映射，未映射类型（如 text_delta）原文透出。

- 2026-07-16 04:10：场景模板 P2b 选角与补员（分支 feat/scenario-template-p2b，spec 2026-07-15-scenario-template-p2-contract-governance-design.md §7 P2b，计划含双闸门实施记录同名 plan 文件）：把 P2a 的"规划期拒绝"升级为可操作的补救闭环。① 迁移 063：`capability_vocabulary` 能力词汇注册表（5 种子键）+ `project_demand_constraint_exemptions` 豁免记录表 + 两个 `is_system` 标准员工模板种子（标准代码审查员/标准测试员）；② 事实性可行性三档：删除 LLM 置信度硬门（07-13/07-15 两轮 E2E 的假阴性来源），服务端 `SelectionScore` 裁决（`selection_score_threshold` 默认 40，低分产生 `low_feasibility` 降级注记+人类确认），结构性缺口仍终局；③ 结构化缺口贯通：`PlanningGap` 经 Temporal ApplicationError details 跨界 → coordination.blocked payload.gap + `decision_request_id`；④ `planning_gap` 决策请求进 inbox（自定义动作 已补员，重新规划/豁免约束并重规划/关闭），resolve 词汇类型域收窄（restaffed/exempted/rejected 仅限 planning_gap），补员/豁免后需求经专用 `ReopenProjectDemandForReplanning`（failed→planning_pending+事件）原地重规划；⑤ 豁免一等决策记录：exempted → 记录落库（绑约束+角色集+决策人+决策关联）→ 重规划时治理跳过匹配 role_independence 并落 exemption 注记；⑥ workflows 直链按 id 拉取修复（顺带修活预存在缺陷：`GetProjectDemand` 裸抛 ErrNoRows 致 launch-detail 500）+ 阻塞面板按需求 failed 状态门控；⑦ 缺口面板三动作 + 补员对话框（标准模板预选/重试幂等/成员读改写保留 settings）；⑧ 场景模板管理 API（建模板=v1/升版 MAX+1 派生防宕机楔死/版本历史/状态 PATCH，ParseSpec+词汇校验+manage authz 接线+domain 审计 diff）+ Console 管理页（新建/升版/状态/版本历史对话框化）。双闸门真实链路：**补员闭环强形态 PASS**——单员工项目缺口→inbox 决策→一键从标准模板实例化审查员→真实调度就绪→重规划绑定新员工→批准→审查任务真实派发新员工执行完成（claude-code 完整会话）；豁免路径同验（同员工身兼两角+确认卡豁免注记+真实派发）；管理页 E2E 六场景全过（词汇 400 点名缺键/升版隔离 pending 计划钉版/disabled 回落 generic+事件）。验证：verify:control-plane/contracts 绿、web 串行 760 全绿+typecheck+build、零 TMPRL1100/workflow 死亡。新暴露待立项：deliverables.value object 写回 400 致 attempt 永滞（handoff 契约类型宽容，韧性家族第 4 例）；generic 回落 payload 保留 planner 幻觉 template_key（P1 标签语义归因债）。

- 2026-07-16 02:05：场景模板 P2b Task 8——规划缺口面板 + 一键补员对话框 + 豁免动作（分支 feat/scenario-template-p2b）：① Go 侧透传 `coordination.blocked` 事件的结构化 `gap`（constraint_kind/roles/required_capabilities/active_executor_count/options）与 `decision_request_id` 到 task-graph blocking fact（域结构体+响应结构体+OpenAPI+codegen）；`decision_request_id` 来源于 `RejectDemandPlanning` 内同一流程创建的 planning_gap 决策，决策创建提前到阻塞事件构建前，幂等重放路径用既有 `ListDemandLaunchDecisionRequests` best-effort 补查（不新增仓库接口/查询）；② workflows 详情页阻塞横幅下新增规划缺口面板：约束/角色/所需能力/员工池摘要 + 三动作——「从标准模板补员」（新组件 `staff-gap-dialog.tsx`：按 `gap.required_capabilities` 预选系统模板 code_review→standard_code_reviewer/test_execution→standard_tester，提交链 createDigitalEmployee→项目成员读改写 PUT→resolve restaffed）、「豁免并重规划」（ConfirmDialog 说明后果→resolve exempted）、「发起借调」（链接到项目配置页成员管理，不实现新借调流程）；③ 项目页「当前需求」卡对失败需求增诊断摘要行 + 缺口处理深链（复用已按需求 id 取数的 taskGraph.blocking_facts，无需新请求）；④ 顺手补 `stringSlicePayload` 默认返回空切片而非 nil，避免 gap 数组字段序列化为 null 撞 Web 侧无空值防护的数组访问；顺手补齐 `ProjectDemandStatus` TS 类型缺失的 planned/executing/completed/failed 四值（原本已被 demandStatusLabel 字典处理但类型未声明）。验证：Go `go test ./...`（control-plane 全量）绿、gofmt 干净、verify:contracts 绿；Web 定向测试（workflows/staff-gap-dialog/project-operational-detail）36/36 绿、全量串行 747/747 绿、typecheck 绿、verify:design-system 绿。范围声明：本任务验证止于组件级+Go 单测，真实浏览器端到端补员/豁免/借调全链路验证是 Task 9 的收尾门禁，尚未执行。

- 2026-07-15 23:09：MCP 管理页布局重构（分支 fix/mcp-page-layout）：① 注册表单从零内边距 WorkSurface 内联块改为 Dialog（修复标签/标题贴卡片边缘被裁切与"裸奔"观感——WorkSurface 是给自带单元格 padding 的 V3Table 用的脆数据面容器，不适合直装表单），表单补上遗失的「描述」字段输入（原 EMPTY_FORM 有 description 但无输入框，注册的 MCP 永远空描述）、必填星标、server_key 正则提示移入 placeholder/title、页脚按钮右对齐、环境变量输入限宽；② 「注册 MCP」按钮移入 ShellPageHeader actions 槽，消除 KPI 上方孤行；③ KPI 带迁移 MetricGrid 基元（布局宪法收官漏网页）并补三卡图标；④ 表格名称/server_key 合并为双行列，传输/鉴权/状态定宽收紧，URL 弹性吸收余宽；⑤ 删除错误从裸 <p> 改 Alert destructive（还清终审 UX 债）；⑥ 修复 pattern 属性正则 `[A-Za-z0-9_-]` 在现代 Chrome v 模式下非法导致 server_key 校验静默失效（预存量缺陷，连字符改转义）。验证：mcp 定向测试 5/5（注册流重写为对话框交互含关闭断言）+ typecheck 绿；真实链路——浏览器经对话框注册真实条目落库并实时上表、删除清理、非法 key 被原生校验拦截（气泡中文提示）、2000px 截图核对页头/KPI/表格三区布局。
- 2026-07-15 22:36：技能↔MCP依赖校验 + 家目录MCP会话级自动卸载（分支 feat/skill-mcp-dependency-unload，spec/plan docs/superpowers/{specs,plans}/2026-07-15-skill-mcp-dependency-and-unload*）：① 迁移 062 `skill_mcp_dependencies`（FK+双向索引；软删下删除保护在应用层）；② capability 依赖读写 API：GET/PUT `/skills/{id}/mcp-dependencies`（PUT 全量声明式，校验 skill 租户归属，404/400/409 语义完整）、GET `/mcp-servers/{id}/dependent-skills` 反查、DELETE 被依赖时 409（列依赖技能 slug）、GET `/digital-employees/{id}/skill-mcp-dependency-status`（satisfied/missing_binding/blocked_missing_env 三态）；③ 派发闸门**只校验不授权**：`run_service` 在既有技能 tools/env 闸门旁校验技能依赖的 MCP 是否在员工 env-satisfied 生效集合内，缺失即阻断（`skill_mcp_dependencies_not_satisfied` + 技能/MCP 名结构化消息），绝不因依赖扩集 payload；④ runtime-agent 家目录 MCP 配置**会话作用域化**：ProvisionInstance 不再写 MCP，会话开始经 `inject_session_mcp_config` 注入（先回滚残留→整文件快照→manifest 先行原子落盘→物化），会话收尾四层回滚（drain 尾钩/spawn Err 兜底/注入后早退统一回滚/下次注入残留自愈），用户自有配置逐字还原；⑤ Web 三处：技能详情「依赖 MCP」区块（注册表 Select 添加/移除、全量 PUT）、MCP 删除确认对话框展示被依赖技能并禁用误删、员工能力面板技能行缺依赖警示（warn pill + 阻断文案，绑定操作后自动刷新）。全流程 subagent-driven（12 任务逐任务评审 + 全分支终审两轮，修复含跨租户校验缺口、drain 早退回滚泄漏、面板不自刷新等 8 处评审发现）。验证：verify:contracts/go/rust 绿、web 串行 737/737+typecheck+build（并行模式预存量堆崩溃基线复现在案）；合并 main 后真实链路 E2E——闸门阻断→绑定放行→重派成功（真实 claude-code 会话）、会话期 agent_home 注入+manifest 在场证据与 workspace 投影 provider 侧读出、会话后卸载/用户配置快照还原/残留 manifest 兜底三场景全过、删除保护 UI+API 409、三处 UI 浏览器实测。记债：存量 provision 时代家目录残留为会话间休眠内容（会话内受管键整键替换不受影响）、start_run Err 回滚位于 writeback 之后（残留自愈兜底）、note 字段无 UI 入口时被全量 PUT 抹除。
- 2026-07-15 18:57：场景模板 P2a 契约化地基（分支 feat/scenario-template-p2a，spec 2026-07-15-scenario-template-p2-contract-governance-design.md，计划含实施记录同文件）：模板从 prompt 建议升级为服务端契约。① 迁移 061：`scenario_template_versions` 版本表（主表 spec 镜像 active 版本）+ 五种子 spec v2（constraints/defaults 分离、exits 出口标注、software_delivery 新增 release 步）+ `project_demands.scenario_template_key`；② `scenariotemplate.ParseSpec` 类型化解析（v1 归一化、constraint kind 注册表）；③ 需求级模板解析（需求显式 > 项目默认 > generic，解析失败落 `scenario_template.resolution_failed` 项目事件）；④ 出口=交付物剪枝：planner 输出 `exit_deliverable`（提示词偏最浅出口、pinned 必从），服务端校验合法出口 + 剪枝集内骨架步骤 produces 逐字遵循；⑤ 约束评估器服务端强制：`role_independence`（四眼，池结构缺口走 ErrNoSuitableEmployee 终局防重规划自旋）/`stage_required`/`human_gate`（强制人类审批+触发说明）/collapse 折叠标注服务端生成，TemplateVersion+AvailableExits 回填 payload（available_exits/constraint_notes 不入指纹）；⑥ Plan 模式计划确认全量强制（loop/chat 暂保现状待 Loop 信封 spec）；⑦ 确认卡改选出口：`request_changes` 携 `target_exit_deliverable` 钉住重规划（顺带修活预存在缺陷：控制台「要求修改」按钮三闸门 400——project 白名单/approval validDecision/inbox 投影，并将 request_changes 收窄至 plan_review 类型）；⑧ Web：需求表单模板 Select、确认卡 `key@v{version}`/交付出口/约束说明块/出口改选 Select（按 template_version 门控防幻觉行）。收敛闸门（真实 deepseek planner）：双员工审查合入需求 1 轮过全部新校验至 pending_review；单员工结构缺口 1 次 LLM 调用即终局 failed+诊断；批准后真实派发执行。E2E 揪出并修 6 缺陷（判据②三连修 a9a4b8a9、幻觉出口剥离 4fb58cdf、replan 驳回通道 a310259a、Temporal 事后围栏事故 3e963833→91e492cc 撤销——错误类型即重放判别器，新增真实历史 WorkflowReplayer 回归测试）。验证：verify:control-plane/contracts 绿、web 串行 726 全绿+typecheck+build、四场景浏览器真实链路（研发/调研/generic/出口改选）+ 协调线程死亡运维恢复实测（SignalWithStart 自愈+CLI 补发信号）。新暴露待立项：coordinator 死亡零告警无自动重拉、workflows 详情页重定向致阻塞横幅不可达（预存在）、replay 测试纳入 CI 门禁。
- 2026-07-15 16:33：协调规划 ErrNoSuitableEmployee 终局化（分支 feat/scenario-template-p2a，修 Task 10 判据②）：单员工项目提「合入」需求时，planner 如实给审查任务打低置信，触发 `ErrNoSuitableEmployee` 家族终止。三处修复：① `Activities.PlanDemandRoute` 对该家族错误包装为 `temporal.NewNonRetryableApplicationError`（Type=`NoSuitableEmployee`），Temporal 不再按 `defaultRetryPolicy` 重试 3 次白烧真实 LLM 调用——一轮即终止；② 新增 `Activities.RejectDemandPlanning`/`ProjectStore.RejectDemandPlanning`：需求从 `planning_pending` 推进到 `failed` 终态（UI 渲染「失败」），并落一条 demand 域 `coordination.blocked` 事件（`reason_code=no_suitable_employee` + `recommended_action` + 诊断 summary），空任务图下由既有 `projectTaskGraphBlockingFacts` 渲染为 workflow 详情页红色 `WorkflowBlockingBanner`（message + 出路建议），替代原先不可读的项目级 `workflow.signal_failed`；`workflow.handleDemandSubmitted` 检测家族错误后改走驳回通道；③ `template_governance.go` 新增 `structuralGapForPlan`，在 `openai_compatible_planner.go` 两处治理调用点当校验返回家族错误时优先返回结构缺口文案（「…可改选更浅出口、为项目补充员工、或换用模板」），使低置信旁路也能给出「补充员工/改浅出口/换模板」出路提示。未放宽置信阈值、未改 prompt、未重构工作流。TDD：`activities_test.go`（不可重试+非家族透传）、`template_governance_test.go`（structuralGapForPlan 四例）、`openai_compatible_planner_test.go`（低置信优先结构文案、不含 scored）、`project_store_test.go`（驳回推进+诊断可见+幂等）、`workflow_test.go`（一轮驳回、planner 仅调 1 次、不落 signal_failed）先红后绿。验证：`go test ./internal/workflow/projectcoordination/ ./internal/project/ -count=1` 全绿。真实链路 E2E 由 Task 10 验证者复跑（诊断落点：workflow 详情页 `/workflows/{demandId}` 顶部红色阻塞横幅 + 需求状态「失败」）。
- 2026-07-15 01:16：布局宪法全站收官（分支 feat/layout-constitution-rollout）：① `Main` 默认档位翻转为 contained（安全默认），全站 30 处调用显式定档（wide=主从工作台/密集表格 21 处、canvas=创建画布/日志/任务发起 4 处、contained=设置/占位页 5 处），legacy `contained`/`fluid` prop 映射保留；② 页面级手写主从栅格 13 处全部迁移 `MasterDetailLayout`（employees 详情/目录/模板、runtime、run-overview、users、permissions 诊断、skills 上传/详情、teams 宪法/概览 tab、projects 运营详情×2），右栏从 7 种魔法宽度收敛到 340/420 两档、视口断点改容器断点，右栏 sticky 改容器变体（upload 页 sticky 迁移到 detail 根修正 items-start 下失效问题），team-overview 条件右栏改按需 detail；③ 两处测试 Main mock 从 `fluid` 断言迁到 `data-width`；宪法文档两条已还清债务改为现行规则。验证：tsc 绿；全量串行 720/720 绿；真实浏览器 2000px 抽查 /employees(wide 1680+MD 1288+340)、/settings/account(contained 1280 对称居中)、/skills/upload(MD 1208+420+右栏 sticky)、/run-overview(region 语义保留)、/logs(canvas 1760)，1280px 窄容器 648+340 无溢出无回归。
- 2026-07-14 17:50：项目队列每页默认条数 5→10：修复分页器名不符实（默认值 5 不在 [10,20] 选项内，下拉显示 10 实际渲染 5），并让队列在常规桌面高度下填满首屏、与驾驶舱右栏高度匹配。验证：projects 48/48 绿；真实浏览器 10 行渲染、队列底部与右栏齐平、无溢出。
- 2026-07-14 17:43：项目驾驶舱打磨（分支 fix/projects-dashboard-polish）：① `MetricGrid` 从"卡片有界+间距分布"改为"卡片弹性填满整行+恒定 12px 间距"（用户反馈间距过大；宽屏由卡片变宽吸收空间），layout-density.md 指标带规则同步且保持通用口径；② 队列表格列宽重分配（项目 34/待处理 20/处理者 20/时间 13/操作 13），待处理状态 pill 改 `items-start` 不再拉伸占满整列；③ 风险信号层新增 principal 名称回退链（成员快照 → 全局数字员工目录 `listDigitalEmployees` → id），「当前处理者」显示数字员工真名。验证：layout/projects/project-risk 62/62 绿（含名称回退 2 个新单测）、typecheck 绿；真实浏览器 1887px——KPI 5 卡 448px 等宽、间距恒定 12、末卡贴右缘；pill 68px 收紧；处理者列显示「报告员小王」「压测员小李」真名、未分派显示「待调度」。
- 2026-07-14 17:17：数字员工详情页遗留跟进（分支 fix/employee-detail-followups）：① 事件流恰好 50 条时不再误显「仅显示前 50 条」提示——抽屉改为查询 51 条判断是否真截断,展示层固定切前 50 条;② 抽屉切换到另一条运行时,事件/结果的「原始 JSON」折叠展开态按 run.id 重挂载重置,不再残留上一条的展开状态;③ 空 text_delta(无前置正文块)不再渲染空白正文卡。验证：TDD 新增 4 个回归用例(先红后绿),employees 套件 129/129 串行通过,typecheck 绿。④ 通道警示条**正向路径**补真实验证：dev 库自造带 `e2e_fixture` 标记的执行实例行(绑 local-dev-node)后停 runtime-agent,真实浏览器确认页面顶部出现「Runtime 命令通道未连接」警示条(截图留存),恢复 agent 后警示条消失;fixture 行已删,实例接口恢复 404,runtime-agent 已由主 checkout 正常接管。
- 2026-07-14 17:12：项目管理首页升级为运营驾驶舱（spec 2026-07-14-projects-dashboard-design.md）：信息架构从 1.5 维（队列+从属 triage）扩展为多维铺满——列表态改 canvas 档铺满全屏（宪法 canvas 档适用范围同步扩展为"画布 + 多面板驾驶舱"）；右栏变可切换槽位：默认装「待我决策」（`listInboxItems` mine/open 跨项目审批+人工决策流，行深链项目审批 Tab/收件箱，首页只导流不承载审批操作）+「最近运行动态」（复用已请求的 workflow instances，零新增请求），选中项目切换为 triage 面板（新增关闭钮返回，Sheet 模式下用容器变体隐藏避免与抽屉关闭钮重叠）；KPI 带新增第 5 卡「待我决策」真值（inbox badge，>0 warn）；`MasterDetailLayout` 新增 `narrowDetail: sheet|stack`（常驻面板窄容器堆叠、按需上下文走抽屉）。验证：layout/projects 52/52 绿、typecheck、verify:design-system 绿；真实浏览器 1887px——canvas 铺满（main 1647 无上限）、KPI 5 卡两端对齐、决策流真值 49 条与侧栏收件箱一致（8 行展示+高危 pill+相对时间）、运行动态 8 行、选中→triage 换入/关闭→驾驶舱换回、无横向溢出。Runtime 健康/成本面板留运行总览（P2 可选）；队列加列消化超宽为待办。
- 2026-07-14 16:44：布局宪法 P1.1 大屏细化：① 选中态右栏加视口高度上限 + 内部滚动（`@5xl` 下 `max-h-[calc(100svh-2rem)] overflow-y-auto`），长待办/证据列表不再把页面撑长、sticky 恢复有效；待办标题两行 clamp + title 提示（runtime-command 长 URI 不再折三行）。② `MetricGrid` 卡片到宽度上限后剩余空间摊进卡间距（`has-[>:nth-child(3)]:justify-between`，gap 为最小间距），指标带首尾与下方内容块两端对齐；1–2 张退化左对齐防中间大洞。验证：layout/projects 52/52 绿；真实浏览器 2000px——12 条待办右栏 1068≤视口 1100 内部滚动、页面高度由队列主导；KPI 卡 336×4、间距均摊 101×3、首尾与内容块 0 偏差对齐、无溢出。
- 2026-07-14 16:32：布局宪法 P1.1（分支 feat/layout-constitution，spec 同文件追加章节）：主从右栏改为**按需详情层**——`MasterDetailLayout.detail` 可选，未选中不渲染空态占位栏（队列独占全宽）；选中后宽容器 in-flow 右栏、窄容器自动改右侧 Sheet 抽屉（关闭经 `onDetailDismiss` 清除选中态；JS ResizeObserver 阈值与 @4xl/@5xl 容器断点同源）；rail 列宽防御性 `minmax` 可压缩；项目管理页删除「选择项目查看待办」常驻空态卡。多视口门禁提前落地：projects 页 1280/1536/1920 断言无横向溢出且右栏完整可见。背景：用户环境反复出现右栏被裁剪/右侧空白，规范渲染六档宽度+根字号18px+侧栏收起+窗口拉大均无法复现（fr 栅格无法超出父容器），判定环境侧视口异常，但按 Linear/GitHub 惯例移除结构上脆弱的常驻固定宽度右栏使页面对异常环境免疫。验证：layout/main/projects 单测 56/56 绿；verify:design-system 绿；真实浏览器 E2E——1778 宽未选中队列全宽、选中 in-flow 1066+420 右栏完整；选中态缩到 1100 面板自动迁入 420px Sheet、关闭清除选中；全程无横向溢出。
- 2026-07-14 16:27：数字员工详情页优化（分支 feat/employee-detail-refinement，spec 2026-07-14-employee-detail-page-refinement-design.md）：① 运行详情抽屉事件流改为语义化时间线（新组件 `RunEventTimeline`：中文生命周期节点、连续 text_delta 合并正文、tool_started/completed 按 tool_id 配对为可折叠工具行、孤儿/未知类型回退、每条事件原始 JSON 惰性折叠、50 条截断提示）；② 抽屉「更新时间」本地化格式、「结果」提取结论正文+原始 JSON 折叠、命令/节点等宽；③ 指标条删「Runtime 执行位置」「当前状态」两卡（剩 8 卡），命令通道断开时改为页面顶部警示条；④ 员工状态中文化（头部/运行表接入 status-labels）；⑤ 删除「下次任务会注入的上下文包」流程图;⑥ 生效上下文删重复「状态」行,技能入口由 /skills 改为打开「管理技能与 MCP」抽屉；⑦ 底部快照卡仅留「人格记忆」「预算策略」并改可读排版，删「能力绑定」「运行与缓存状态」JSON 卡；⑧ providerDisplayName 五处副本收敛至 `features/employees/provider-label.ts`,抽屉 runStatusLabel 统一走 lib（dispatching 文案调度中→分派中）。验证：web 串行测试 705/705、typecheck/build 绿（并行模式仍存在预存在的 vitest browser route.fulfill 堆回收崩溃）；真实 E2E（worktree Vite :3100 + 运行中 Control Plane :8081,员工「报告员小王」真实历史运行）：详情页 13 项断言 + 含 WebSearch 工具调用的运行抽屉时间线 5 项断言全过,截图留存;通道警示条正向路径因 dev 数据无绑定执行实例的员工无法真实触发,已验证负向门控（实例 404+通道断开→正确不显示）与信号源真实翻转,正向渲染由集成测试覆盖。
- 2026-07-14 15:19：布局宪法 P1（分支 feat/layout-constitution，spec 2026-07-14-layout-constitution-design.md）：`theme.css` 新增 `--v3-layout-*`/`--v3-metric-*` 宽度 token（三档：contained 1280 / wide 1680 / canvas 不限）；`Main` 加 `width` 档位属性（默认行为不变，存量 32 处调用零影响）；superteam 新增布局基元 `MasterDetailLayout`（rail 340/420 两档、`@container/master-detail` 容器断点折叠）与 `MetricGrid`（KPI 卡 208–336px 有界，flex-wrap 先压缩后换行——grid auto-fit 按 max 计列数会提前孤行，实测弃用）；`layout-density.md` 升级为可执行规范（宽度档位/断点口径/主从布局/指标带四章），DESIGN.md 概念表登记；试点迁移项目管理页（`Main width="wide"`、主从 420 栅格与 KPI 带换基元、右栏 sticky 改容器变体）。验证：layout/main/projects 单测 54/54 绿；verify:design-system 绿；真实浏览器（admin 登录真实 Control Plane）1280/1536/2000 三视口实测——2000 下内容 1680 居中、双列 1208+420、KPI 336×4、右栏 sticky；1536 下 KPI 307×4 单行；1280 下容器 1008<@5xl 正确落单列；三档均无横向溢出。
- 2026-07-14 12:26：新建团队页数字员工库分页调整为每页展示 5 位；分页回归覆盖第 6 位员工进入下一页。验证：`create-team-page.test.tsx` 8/8。
- 2026-07-14 12:24：移除新建团队页底部操作栏的半透明底色与背景模糊，操作栏现在直接融入页面极光背景，同时保留顶部边线与按钮层级。验证：`git diff --check`。
- 2026-07-14 12:21：新建团队页改为受限视口工作区：页头与底部操作栏保持可见，配置画布与确认内容在中间区域滚动；因此无论桌面屏幕高度如何，取消、上一步和下一步/确认创建都固定在内容区底部。验证：`create-team-page.test.tsx` 8/8、串行 Web 测试 696/696、Web typecheck/build；Web 服务重启后 `/teams/new` 返回 200。标准 `verify:web` 的并行测试仍在 `route.fulfill` 内存回收异常处中断；浏览器真实 UI 检查仍受本机浏览器控制运行时初始化 `process` 冲突阻断。
- 2026-07-14 12:16：新建团队「团队画布」右侧数字员工库固定为 42rem 高度，并改为每页 4 位的本地分页；搜索时自动回到第一页，已选员工仍会从候选项中移除，避免员工多时将下一步与取消操作推到首屏外。验证：`create-team-page.test.tsx` 7/7、串行 Web 测试 695/695、Web typecheck/build；浏览器真实 UI 检查仍受本机浏览器控制运行时初始化 `process` 冲突阻断。
- 2026-07-14 12:10：新建团队页改为「团队画布」工作面：左侧为团队名片与人类负责人，中部用负责人→数字员工的关系画布呈现已选结构，右侧以实底员工库搜索并加入未归属员工；保留既有两步确认、多人负责人、创建接口与元数据契约。新增回归覆盖负责人和数字员工分别进入画布及 POST 负载。验证：`create-team-page.test.tsx` 6/6、Web typecheck；Web 服务重启后 `/teams/new` 返回 200 并确认 Vite 正在加载新组件。浏览器真实 UI 检查待本机浏览器控制运行时恢复（初始化受 `process` 冲突阻断）。

- 2026-07-13 17:50：新建团队 UI 打磨——数字员工列表改用 `EmployeeAvatar`；向导收敛为两步（配置团队=身份+负责人/数字员工同屏 → 确认并创建），去掉空壳权限步与单独身份页的稀疏布局。验证：`create-team-page.test.tsx`；浏览器 `/teams/new`。

- 2026-07-13 12:33：场景模板注册表 P1（分支 feat/scenario-template-p1，spec 2026-07-13-scenario-template-registry-design.md）：迁移 058 建 `scenario_templates`（5 种子含 generic 兜底）+ `projects.scenario_template_key`；只读 API GET /scenario-templates(+/{key})（authz scenario_template.read）；项目创建绑定 key（注册表校验 active）；规划快照装载模板内容注入 planner prompt（骨架实例化指令），`ValidateRouteDecisionPlan` 强制 plan.template_key == 绑定 key（template_key 首次有消费方）；template_key 经 PlanRevisionPayload 落库并在计划确认卡显示；Web 新增「场景模板」目录页（流程能力组）与项目创建模板下拉。顺手修复：web typecheck 两处预存在破损（workflows fixture 缺 execution_summaries、constitution tab unknown 取长）；五处过期测试断言对齐现实（5a8804fa 侧栏软化样式值、未提交的嵌套日志菜单期望、runtime/trace-panel 卡片数）。验证：verify:contracts/control-plane/runtime-agent 绿；web 679/679 绿（--no-file-parallelism；并行模式存在预existing 的 vitest browser route.fulfill 堆回收崩溃，待另行排查）；真实 E2E（2026-07-13 14:20 回填）：注册表 API/页面 5 种子可见可展开；绑 ops_analysis 项目的规划骨架逐字实例化（produces/required_inputs/depends_on）并全链执行（collect 交付真实 df -h 输出、analyze 经 upstream_results 消费、handoff.verified×2）、payload template_key 落库且计划卡显示；未绑项目回归正常；创建表单下拉列全模板。首轮规划暴露两问题已修（骨架任务必填字段 prompt 加固；置信度阈值用项目级 policy 旋钮）。附带发现调度韧性缺口：CP 重启瞬断命令通道时 DispatchProjectTask 超时放弃、queued 孤儿 attempt 无恢复（项目 12579692，待立项）。

- 2026-07-13 10:59：交接契约执行闭环 P1（分支 feat/handoff-loop-p1，spec 2026-07-13-handoff-contract-execution-loop-design.md）：① 派发注入——两条派发路径把**直接 blocker** 的最新 result（summary 4KB 截断 + deliverables + evidence/artifact refs）作为 upstream_results 注入员工 prompt 与 execution_context_packet，prompt 同时携带本任务 produces 与 deliverables 义务；② 图校验收紧——required_inputs 必须由直接 blocker 生产（原为任意祖先，一条边=一份交接），fan-in 靠显式建边；③ result_contract 新增 deliverables（Go+Rust 双侧，Rust 强类型 struct 补字段防静默丢）；④ 生产者侧履约核对——completed 结果缺 produces 声明项即校验失败走既有 rejected+waitHuman，通过则平台追加 handoff_fulfillment verification 并写 handoff.verified/unfulfilled ledger 事件。顺手修复 main 上 api 包测试红（5488e39c 验证码默认关闭后 routeLogin 仍强制解码验证码图）。验证：verify:control-plane + verify:runtime-agent 全绿；真实 E2E（项目 46e7206f「Handoff-E2E 20260713」，真实 claude-code×2 员工 + local-dev-node）三场景全过——①链式：任务 A 交付 secret_code=1783911737，任务 B 派工单 packet 含 upstream_results，B 复用该值译出中文大写且未重跑命令；②缺项拒绝：员工按测试指令交付错名 deliverable → validation_errors=[handoff_deliverable_missing:handoff_test_result]、任务停 waiting_human 未判 completed、handoff.unfulfilled 事件+澄清决策生成；③fan-in：汇总任务 packet 含 2 条 upstream_results（kernel_name+work_dir）并合并成结论；handoff.verified 事件 6 条，contract verification 首次有平台核对数据。附带观察（预存在，非本次回归）：并行任务撞"单员工单活跃 run"限制时派发失败进 recovery，人批准恢复后重试仍撞同一冲突，任务滞留 waiting_human（4dee7323，待另行立项）。

- 2026-07-13 01:46：Provider transcript 捕获 spec 收尾（分支 `feat/transcript-capture-completion`）：① env 值脱敏接线——`redact_with_environment` 原为死代码，ledger excerpt 现携带 provider 进程环境快照脱敏；② 本地 `raw.jsonl`/`events.jsonl` 创建 mode 0600；③ raw 分段新增 30s 时间轮转（原只按 8MB，安静 run 崩溃即丢全部已缓冲 raw）；④ 修复 main 上 `provider_exit_test.rs` 编译失败（8cd076c4 漏改 raw_sink 参数）。验证：`corepack pnpm verify:runtime-agent` 全绿；**Phase 2 真实 E2E 首次通过**（项目 `febd08af`「Transcript-E2E 20260713」真实 claude-code + 真实 TOS）：ledger tool_started/tool_completed 齐备、真实失败命令 `is_error=true`（attempt `25d17c60`）、TOS `raw.part-0001/0002 + manifest` 逐段与总量 sha256 三方一致（分段实算=manifest=attempt 行回写 `61d787b1…`）、raw 91 行全可解析含 3 个 tool_result、`sk-test…` 假 token ledger/Web 已脱敏而 raw 原样 9 处、Web「执行证据链」渲染工具调用/结果节点且失败标红。TOS Object Lock/版本控制核实为**支持但 bucket 未开启**。E2E 发现两缺陷（spec §8 已记录，待立项）：派发 `project conflict` 后 attempt 缺 run 绑定致 provider 事件静默不入 ledger（attempt `53809434`）；模型 prose（text_delta/summary）不脱敏。

- 2026-07-11 16:09：项目管理新增项目软删除（`GET .../delete-preview` + `DELETE .../projects/{id}`、活跃执行硬阻断、待审批警告后清理、先终止 Temporal coordinator 再同事务级联软删与审计、详情头归档+删除）。验证：迁移 057 已 apply；真实 API smoke（项目 0febfc92-1c6b-4588-96f9-299a69151017）——allowed_actions 含 project.archive/project.delete、preview can_delete=true、DELETE 204、GET 404、列表不可见；Temporal `WORKFLOW_EXECUTION_STATUS_TERMINATED`；`audit_events.action=project.delete`。证据 `.scratch/smoke/project-delete-summary.txt` / `.scratch/smoke/project-delete-preview.json`；浏览器详情头可见「归档项目」「删除项目」，删除确认框含名称确认与 preview 提示；并修复 overview 覆盖导致 allowed_actions 丢失。

- 2026-07-11 16:01：Console 状态文案通用化：扩展 `apps/web/src/lib/status-labels.ts` 共享码表与域包装（`teamStatusLabel`/`governanceStatusLabel`/`employeeStatusLabel`/`projectStatusLabel`）；团队管理去掉裸英文 status 与本地中文 map；项目生命周期三处本地表收敛到共享导出。验证：定向 Vitest status-labels+teams 30/30、touched project components 4/4；worktree Web(:3000)+CP(:8081, captchaEnabled=false) 浏览器确认团队「活跃/就绪/已禁用」、项目「运行中/验收中」中文展示。

- 2026-07-11 15:56：登录图形验证码默认改为关闭：`auth.captchaEnabled` 与 `auth.Service` 默认 `false`，仅当配置或 `AUTH_CAPTCHA_ENABLED=true` 时开启；避免各分支未携带本地 `config.yaml` 时端到端登录被验证码阻断。验证：`go test ./apps/control-plane/internal/auth ./apps/control-plane/internal/config`；重启 Control Plane(:8081) 后 `GET /api/auth/captcha` 返回 `{"enabled":false}`，`POST /api/auth/login` 不带验证码以 `admin/admin` 返回 200 且 `/api/auth/me` 有效。

- 2026-07-11 15:10：工作对象列表时间字段与排序：`DESIGN.md` 新增「时间可见 / 默认新近优先」规则；OpenAPI 与 Control Plane 暴露 `ProjectTask.created_at/updated_at`；项目任务/审批与流程河道组内按时间倒序并展示相对时间。验证：`verify:contracts`、`go test ./internal/project`、定向 Vitest 29 通过；重启 CP(:8081)+Web(:3000) 后 cookie 登录 API 确认 tasks 含时间且 `updated_at` 倒序、decisions 含 `created_at/resolved_at`；浏览器确认任务 Tab「更新」列、审批「决议 …前」、流程编排「创建 …前」与组内新近优先。

- 2026-07-11 12:32：Plan 6 会话血缘续接修复：FindProviderSessionForTaskRoot 放宽为可续接 recoverable 的 active/idle/completed 会话（上游完成后补做/返工派发仍能命中同一 provider_session_id）。验证：定向 go test TestPgRunRepositoryFindProviderSessionForTaskRoot 通过；真实 E2E 证据 .scratch/smoke/plan6-t5-resume-fix-20260711-121427/evidence.json（supplement 派发 metadata.provider_session_id == owner 已完成会话）。

- 2026-07-11 12:02：Plan 8 删除派发闸门中结构上不可能失败的 `tool.binding` / `tool.authorization` / `tool.available` 检查及整条死代码链（`PreDispatchToolSnapshot`、adapter `requiredTools`/`effectiveMCPServerNames`/`gateCapabilityReader`、workflow `mergePreDispatchToolSnapshot` 与 metadata 读取）；`capability.Service.ListEffectiveMCPServers` 与 `/effective-mcp-servers` 保留。验证：`corepack pnpm verify:control-plane` 通过；本 worktree 启停 Control Plane(:8081) 后对真实任务 `da4afe31-…` 跑 `RunPreDispatchGate` 落库，最新 `project_task_dispatch_gate_results.checks` 不含任何 `tool.*` key（证据 `.scratch/smoke/plan8-gate/evidence.json`）。

- 2026-07-11 11:40：Plan 6 会话血缘范围收紧分层门禁 + 真实 E2E（Task 5）。迁移 056 为 `provider_sessions` 加 `project_task_root_id`（血缘根，nullable 向后兼容）+ `FindProviderSessionForTaskRoot(tenant, employee, root)` 查询（仅命中 `status='active'`）；派发路径 `StartProjectTaskRun` 解析血缘根（`revision_root_task_id` 一跳，否则任务自身）并在可复用时把命中的 `provider_session_id` 注入 runtime payload metadata；runtime `non_empty_session_id` 改为优先读 `metadata.provider_session_id`（此前只读 `session_policy`，Task 3 的注入是死码）；写回路径 `UpsertProviderSessionByExternalID` 补齐 `project_task_root_id` 落库（`EXCLUDED` 非空则覆盖，回填历史 null）。验证：`corepack pnpm verify:control-plane` 通过；`corepack pnpm verify:runtime-agent` 中 `cargo test`（含预置 `tests/provider_exit_test.rs`）在本分支与 `main`/`main-integration` 上均因该测试文件未随 `run()` trait 新签名更新而编译失败（`git diff main...HEAD` 对该文件与 `providers/mod.rs` 均为空，确认为既有债务、非本任务引入）；范围内 `cargo test --lib` 79/79 通过。真实 E2E（本 worktree 独立重启 Control Plane :8081 + Runtime Agent，均加载迁移 056 与本分支代码）：创建项目+需求，LLM 规划出 owner→downstream（produces/required_inputs=load_test_report_t5），owner 由真实 Runtime/Claude Code dispatch 后组织完成；downstream 真实 claim 后通过 `POST .../project-task-attempts/{id}/result` 注入 blocked{missing_inputs}，触发 `CreateUpstreamSupplementTasks` 派生补做任务（真实 dispatch 并完成）。DB 确认 `project_tasks`：补做任务 `revision_of_task_id`=owner 任务、`plan_iteration=1`、`assigned_digital_employee_id`=owner 员工；`provider_sessions.project_task_root_id` 在补做任务的新会话行上正确等于 owner 任务根 id（Task 1/2 血缘归属基建验证通过）。**但会话级复用未在本次真实链路触发**：owner 任务与补做任务各自产生了不同的 `provider_session_id`（未共享同一会话）——根因是 owner 的原始会话在补做任务派发前已经因 `Complete()` 写回把 `status` 更新为 `completed`（`ON CONFLICT` 按 `last_sequence_number` 更新 status，completed 序号更高会覆盖 active），而 `FindProviderSessionForTaskRoot` 只匹配 `status='active'` 行，导致标准"上游先完成、下游后发现缺失"路径下复用查找必然落空；`recoverable` 列因 `ON CONFLICT` SET 列表未覆盖该字段停留在初始 `true`，不影响上述判断。此发现已如实记录，留待人类判断是否需要放宽复用窗口（如接受 completed-but-recoverable 会话）或调整设计预期。证据 `.scratch/smoke/plan6-t5-20260711-112321/evidence.json`。

- 2026-07-11：删除返工/延展的「失败指纹熔断」（`revisionFailureFingerprint` + `repeatedRevisionFailure`），终止性统一由与内容无关的计数上限承担（返工 `revisionMaxAttempts`、延展 `max_plan_iterations`）。理由：指纹能被任意内容扰动击穿，天然不能承担 liveness 保证——只在封闭词表字段（`missing_inputs`）上可靠，而那里计数上限本就必然触发；在模型自由列表字段（`requested_changes`）上换措辞即失效。指纹是伪保证，保留只会让人误以为它是安全阀。`repeatedRevisionFailure` 与 `revisionBudgetExhausted` 返回值完全相同（都是 `Exhausted: true` → 同一条人类评审路径），故删除只改变升级时机（一律走到计数上限），不改变升级类型或去向。改动：`project_store.go` 删两函数 + `revisionPlannerMetadata` 不再写 `revision_failure_fingerprint`（`priorRevisionTasks` 保留，`revisionBudgetExhausted` 仍用）；删 5 个相关测试；spec §4.8 加勘误（2026-07-11）并同步 §4.6/验证清单引用。验证：`go build ./internal/...` + `go test ./internal/project/ ./internal/workflow/projectcoordination/` 通过。删除的仅是既有升级路径的早退分支，存活路径（计数上限 → 人类）在 Plan 5 Task B4 已真实 E2E 验证且行为不变，未引入新运行路径。

- 2026-07-11 02:12：Plan 5 图扩展（A1–B3 上游补做/plan_iteration 熔断）分层门禁 + 真实 E2E 补测（Task B4），并修复三处只有真实链路才会暴露的缺陷：(1) `routeNonCompletedProjectTaskAttemptResult` 缺少 `blocked_resolvable_upstream` 分支，真实 HTTP 结果提交永远走不到 `CreateUpstreamSupplementTasks`（单测用内存仓库绕过了信号路由，未覆盖）——补上无需人类等待、直接信号协调工作流的分支；(2) `project_task_results.chk_project_task_results_decision` check 约束建表时（034）早于 `blocked_resolvable_upstream` 决策值引入，导致该决策的每次真实落库都违反约束——新增迁移 055 补齐约束值；(3) `CreateUpstreamSupplementTasks` 把补做任务的 `planned_task_key` 直接复制 owner 任务的 key，与同一 `coordination_job_id` 下的 `uq_project_tasks_coordination_planned_key` 唯一索引冲突——改为按补做轮次派生唯一 key（`upstreamSupplementTaskKey`，仿照既有 `revisionTaskKey` 模式）。验证：`corepack pnpm verify:control-plane` 通过（含新增单测 `TestSubmitProjectTaskAttemptResultBlockedResolvableUpstreamSignalsCoordinatorWithoutHumanWait`）；`atlas migrate apply` 在联调库应用迁移 054/055，`information_schema.columns`/`pg_constraint` 确认 `project_tasks.plan_iteration` 列与新 `chk_project_task_results_decision` 约束值均生效；`make -C apps/control-plane migrate-validate` 因本地沙箱 podman machine 无法建立 SSH 连接未能跑通（尝试重启 podman machine 两次均在 socket 连接阶段失败），以上真实 `atlas migrate apply` + DB 内省作为替代证据。真实 E2E（本 worktree 独立启停 Control Plane :8081 + Runtime Agent，均从本 worktree 加载迁移 054/055 与代码）：通过真实 API 创建两位数字员工、项目（`coordination_policy.max_plan_iterations=2`）与需求，LLM 规划出 a→b 依赖（a.produces=[load_test_report]，b.input_requirements.required_inputs=[load_test_report]）；a 由 Runtime 真实 dispatch/claim 后通过 `POST .../project-task-attempts/{id}/result` 提交 completed，b 同样真实 dispatch 后提交 blocked{missing_inputs:[load_test_report]}；DB 确认新增补做任务 `assigned_digital_employee_id`=a 的员工（非 b 的）、`revision_of_task_id`=a、`plan_iteration=1`。熔断轮次单独起一组 c→d 需求复现：对同一下游任务 d 连续 3 次提交同一 `missing_inputs` 的 blocked 结果，第 1/2 次分别产出 `plan_iteration=1/2` 的补做任务，第 3 次 `CreateUpstreamSupplementTasks` 返回 Exhausted 并触发 `RequestProjectTaskIterationExhaustedReview`，产生 `decision_type=project_task_iteration_exhausted` 的人类决策请求（id 见证据文件）。清理：smoke 项目已归档，遗留的真实 Claude Code 补做子进程已确认父进程为本 worktree runtime-agent 后终止。证据 `.scratch/smoke/plan5-b4-20260711-021230/evidence.json`。

- 2026-07-11 00:54：计划级验收判据：PlanRevisionPayload/RouteDecisionPlan 新增 plan_acceptance_criteria（id/statement/satisfied_by→task_key）；落库校验拒绝空/未知 satisfier（acceptance_criterion_has_no_satisfier / satisfied_by_task_not_found）；planner 提示词产出判据；评审上下文透传；计划确认面板展示「调度顺序」与「验收判据」。验证：corepack pnpm verify:control-plane；focused web test project-operational-detail.test.tsx；真实链路证据 .scratch/smoke/plan4-acceptance-criteria-retry-20260711-005223-10714/evidence.json（判据 satisfied_by ⊆ 真实 task keys）。浏览器 UI 已在本 worktree Web+CP（captchaEnabled=false + VITE_CONTROL_PLANE_URL=:8081）验证「调度顺序」「验收判据」可见；证据 .scratch/smoke/plan4-ui-browser-20260711-010058/meta.json。

- 2026-07-11 00:12：计划内部引用完整性：任务新增 produces 与 input_requirements.required_inputs；落库前校验祖先可达与 produces 键唯一；自由字段迁入 planner_metadata.planner_notes。验证：corepack pnpm verify:control-plane；拒绝路径 go test -run TestValidateRouteDecisionPlanRejects；真实 E2E 证据 .scratch/smoke/plan3-referential-integrity-20260710T161026Z/evidence.json （上游 produces=[cpu_memory_metrics]，下游 required_inputs 引用之，input_requirements 仅含 required_inputs）。

- 2026-07-10 23:09：计划期能力控制流退役并引入 selection_confidence：scoreCapabilities 不再因虚构能力名 HardFail/归零；ApplyPlanningProfileScores 不再因 MissingCapabilities 强制审批；ValidateRouteDecisionPlan 删除空 required_capabilities 与能力拒计划规则；planner 输出 selection_confidence，低于 coordination_policy 阈值返回 no_suitable_employee；另退役计划期 tool_requirements HardFail（MCP 由 Runtime 物化为项目 mcp.json，不经计划词表选人）。验证：corepack pnpm verify:control-plane；真实 E2E 证据 .scratch/smoke/plan2-retire-capability-toolfix-20260710T150607Z-2072afb0/evidence.json （missing_capabilities 非空、selection_score=80、low risk 且 requires_human_approval=false）。

- 2026-07-10 17:48：拆除派发假闸门与散文熔断指纹：EvaluatePreDispatchGate 不再产出 capability.match / capability.hard_missing；能力快照构造器停止从 planner 输出取能力；PreDispatchCapabilitySnapshot 收缩为 Required/Matched；revisionFailureFingerprint 只取 status + 排序后的 RequestedChanges；planner 提示词不再把 missing_capabilities 绑到人类审批。验证：corepack pnpm verify:control-plane 通过；真实链路用本 worktree Control Plane(:8081) + 既有 Runtime/Web，创建空 external_capabilities 员工并批准计划后，任务 d97bd715-91f1-4732-a576-636ced5a7cf4 闸门 passed 且 checks 不含 capability.match，missing_capabilities 非空，任务越过能力闸门完成执行后进入 waiting_human(acceptance_required)；证据 .scratch/smoke/task6-remove-fictional-gate-20260710T094031Z/evidence-final.json。

- 2026-07-10 02:08 完成数字员工配置破坏性重构：删除旧个人治理配置字段，改为人格记忆、能力绑定和预算策略，并通过真实创建与 Runtime 投影链路验证。

- 2026-07-09 04:11：新增数字员工可审计删除：Control Plane 提供 `DELETE /api/v1/digital-employees/{employeeId}`，删除前阻断 `queued/dispatching/running/cancelling` 运行与 `queued/running/in_progress` 项目任务；成功路径软删除员工、当前执行实例、环境变量、MCP/技能/配置/工作目录投影并清理项目员工节点亲和，保留历史运行/项目任务/工件/审计，同时写入 `digital_employee.delete` 审计和清理候选。Web 员工详情页按 `allowed_actions` 展示危险删除按钮，要求输入员工名称确认，409 时展示阻断项。验证：`go test ./apps/control-plane/internal/authz -run 'TestDBAuthorizerEmployee(ActionsUseBusinessActionSurface|OwnerCanUsePersonalEmployeeActions)' -count=1`、`go test ./apps/control-plane/internal/employee -run 'TestServiceDeleteDigitalEmployee' -count=1`、`go test ./apps/control-plane/internal/api -run 'Test(DeleteDigitalEmployeeRoute|GetDigitalEmployeeIncludesAllowedDeleteAction|EmployeeRoutesUseAuthzActions)' -count=1`、`go test ./apps/control-plane/internal/employee ./apps/control-plane/internal/api ./apps/control-plane/internal/authz -count=1`、`corepack pnpm generate:control-plane`、`corepack pnpm verify:contracts`、`corepack pnpm --filter ./apps/web run test -- src/lib/api/client.test.ts src/lib/api/employees.test.ts src/features/employees/components/employee-detail-header.test.tsx src/features/employees/detail.test.tsx`、`make -C apps/control-plane generate-sqlc`、`git diff --check` 通过；真实链路重启 Control Plane(:8081)+Web(:3000) 后，`admin/admin` 登录真实接口，插入临时数字员工后 `GET` 返回 200、`DELETE` 返回 204、删除后 `GET` 返回 404，DB 确认 `status=disabled`、`deleted_at` 非空且 `audit_events.action=digital_employee.delete` 含 `cleanup_candidates`，Chrome 打开临时员工详情页确认真实页面显示“删除员工”按钮和二次确认弹窗且确认按钮默认禁用。已知：`corepack pnpm --filter ./apps/web run typecheck` 当前被既有无关错误阻断（`team-constitution-tab.tsx` 的 unknown hard_rules、`workflows/index.test.tsx` fixture 缺 `execution_summaries`），本次未将 typecheck 作为通过项。

- 2026-07-08 02:13：重构数字员工创建页：删除独立“配置预检”步骤，将创建流程收敛为“创建方式 / 完成配置 / 确认创建”；空白自定义不再展示员工类型和描述输入，前端内部提交 `custom_agent`；团队治理缺失或团队未允许 `custom_agent` 时显示业务阻断，不暴露 422；风险等级显示为 `低 / 中 / 高 / 严重`，Provider 类型改为必选项并使用 Codex/OpenCode/Claude Code 标签；创建请求移除 Runtime 节点必填语义，团队继承能力只读展示且不会作为员工扩展能力重复提交。验证：`go test ./apps/control-plane/internal/employee ./apps/control-plane/internal/api`、`corepack pnpm verify:contracts`、`corepack pnpm --filter ./apps/web run test -- src/lib/api/employees.test.ts src/features/employees/create.test.tsx`、`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；真实链路使用当前 worktree Control Plane(:18082)+Web(:3002) 连接真实开发库并允许 3002 CORS，`admin/admin` 登录成功，`GET /api/v1/digital-employees/create-options?team_id=00000000-0000-0000-0000-000000000101` 返回 200 且 Provider 候选可用，浏览器打开 `/employees/new` 确认无独立“配置预检”节点、风险中文展示、身份页不展示员工类型/描述、空白自定义选择未治理团队显示“团队治理未启用/先不归属团队创建”业务阻断且无 422。

- 2026-07-07 16:39：内置数字员工头像库新增 12 张 AI 生成头像（8 男 4 女）：补齐 `engineer-m-11` 至 `engineer-m-18`、`engineer-f-11` 至 `engineer-f-14` 的 512/256 WebP 资产、设计源图归档、Control Plane 内置头像定义和 Web 本地头像库。验证：`go test ./apps/control-plane/internal/avatar ./apps/control-plane/internal/employee ./apps/control-plane/internal/api`、`corepack pnpm --filter ./apps/web run test -- src/lib/api/employees.test.ts`、`corepack pnpm --filter ./apps/web run typecheck` 通过；重启 Control Plane(:8081) 与 Web(:3000) 后，`admin/admin` 真实登录，`GET /api/v1/digital-employee-avatar-assets` 返回 200 且 32 个头像，新增 8 个 male 与 4 个 female ID 均存在，Web 静态路径 `/images/digital-employee-avatars/engineer-m-18-256.webp` 返回 `200 image/webp` 且为 256x256。已知：`corepack pnpm --filter ./apps/web run test` 全量 Web 测试在 Vitest/Playwright harness 层抛 `route.fulfill: The object has been collected to prevent unbounded heap growth`，未进入头像相关断言。

- 2026-07-07 04:57：补齐数字员工创建到项目调度就绪的闭环：Control Plane 新增员工调度就绪 read model 与 `GET /api/v1/digital-employees/{employeeId}/scheduling-readiness`，聚合员工状态、生效配置、技能/MCP/环境变量事实，并保持项目 Runtime readiness 作为真实执行来源；OpenAPI、生成代码与 Web API client 同步；创建页将运行配置文案收敛为 Provider 偏好，明确 Runtime 节点在项目运行准备中决定；员工详情页新增“项目调度就绪度”面板、展示检查项/能力计数/缺失项和项目入口，并降低直接“开始任务”入口视觉优先级。验证：`go test ./apps/control-plane/internal/employee ./apps/control-plane/internal/api`、`corepack pnpm verify:contracts`、`corepack pnpm --filter ./apps/web run test -- src/features/employees/create.test.tsx src/features/employees/detail.test.tsx src/features/employees/components/employee-detail-header.test.tsx`、`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；真实链路重启 Control Plane(:8081) 与 Web(:3000) 后，`admin/admin` 登录真实接口，`GET /api/v1/digital-employees/6ac6e7b8-3111-400e-926e-5cda9ed9cd14/scheduling-readiness` 返回 200 且 `ready_for_project_scheduling=true`、`project_execution_source=project_runtime_readiness`，Chrome 打开 `/employees/6ac6e7b8-3111-400e-926e-5cda9ed9cd14` 确认“项目调度就绪度”面板、项目运行准备文案、`进入项目` 链接和 outline “开始任务”按钮均为真实页面渲染，浏览器 error 日志为空。

- 2026-07-06 16:41：落地 UX 架构重设计三批 Web 改造：项目详情升级为 `概览 / 任务 / 工件 / 审批 / 预算 / 验收 / 配置` 工作中枢并补齐需求、员工、成员、运行总览链接；运行总览支持 `employee/project` query 与员工/Runtime 跳转，Runtime 页支持 `node` query；收件箱和审批中心统一项目决策深链为 `/projects/{projectId}?tab=approval&focus={decisionId}`，审批弹窗展示风险、来源项目/任务/审批请求和上下文摘要；`/approvals` 从占位页改为真实 inbox 驱动审批中心；侧栏重组为工作台、协作对象、流程能力、治理平台四组并保持 `/tasks` 不暴露。验证：`corepack pnpm --filter ./apps/web run test -- src/features/inbox/index.test.tsx src/features/approvals/index.test.tsx src/features/projects/components/project-operational-detail.test.tsx`（23/23）通过；真实链路使用当前 worktree Web(:13001)+临时 Control Plane(:18082，连接真实开发库且允许 13001 CORS)，登录 `admin/admin` 打开 `/approvals` 确认真实 inbox 审批数据而非占位页，点击项目审批链接进入项目详情审批 Tab 并高亮目标 decision，`/run-overview?employee=f4bd1d90-bb7a-4d16-9bd1-78c76274ea08` 默认选中员工且显示 `/employees/...` 与 `/runtime?node=local-dev-node` 链接，`/runtime?node=local-dev-node` 展示该节点真实事件；对低风险 smoke 审批 `Create smoke-note-success.txt` 提交同意后真实 API 返回 `resolved`，审批中心开放数从 13 变 12 且列表移除该项。

- 2026-07-06 14:58：数字员工创建页开放「空白自定义」路径：创建入口支持选择现有员工类型生成空白草稿，模板模式继续使用模板默认能力/治理，空白模式不注入推荐技能、MCP、外部能力、上下文覆盖或审批覆盖，并在提交时写入 `metadata.creation_mode=blank_custom`；预检对空白模式放行真实 `capability_policy`/兼容 `capability_boundary` 的零能力边界阻断，但保留模板模式和其他 blocked checks 的阻断语义。验证：`corepack pnpm --filter ./apps/web run test -- src/features/employees/create.test.tsx`（38/38）、`git diff --check` 通过；真实链路使用 feature worktree Web(:3000)+Control Plane(:8081，`--config apps/control-plane/config/config.yaml` 且 captcha disabled) 和真实 admin 会话打开 `/employees/new`，确认左侧「空白自定义」可用、选择「数据库管理」后真实 `GET /api/v1/digital-employees/create-options` 仍返回 `capability_policy` blocked/`技能 0 · MCP 0 · 外部能力 0`，但空白模式预检按钮可继续并进入「员工画像蓝图」；3002 无监听。

- 2026-07-06 10:03：收件箱首页从「表格+右侧详情」两栏重构为「左列表+中详情+右操作面板」三栏决策控制台，对齐 `docs/prototypes/inbox-decision-console.html` 原型。InboxShell 拆为三栏：左栏 WorkSurface 紧凑列表（风险 accent bar + IconTile + 标题 + 风险 pill + 两行摘要 + 来源·节点 + 相对时间）、中栏 InboxDetailPanel（KI 编号 + 标题 pills + 为什么需要处理 dl + 过程记录时间线 + 关联引用）、右栏 InboxActionPanel sticky（已等待时长环 + 动作矩阵 + 快速跳转）；顶部指标从 3 卡扩为 4 卡，新增「等待最久」基于 `items[].created_at` 前端计算。数据真实性审计修正 5 处装饰占位：第 4 指标由「平均响应」改为「等待最久」(max(now-created_at))、KI 编号由编造业务号改为 `item_type`+`source_id` 前 8 位、过程记录由 3 步编造时间戳改为 created_at/last_activity_at 两真实点+当前状态、证据文件列表由编造文件名改为 `source_*_id` 关联引用跳转、右栏 SLA 倒计时由无来源改为基于 created_at 的已等待时长（InboxItem 无 sla 字段）。验证：`corepack pnpm --filter ./apps/web run typecheck`、`corepack pnpm --filter ./apps/web run test -- src/features/inbox/index.test.tsx`（17/17）、`git diff --check` 通过；源码核对 5 处修正全部落实（grep 确认无 DB_MIGRATION_PROD/平均响应/SLA 倒计时/artifacts[]/process_records 残留）；真实链路 curl Web(:3000)/inbox 返回 200 + 正常 SPA HTML、inbox-shell.tsx 与 inbox-item-list.tsx 经 vite 即时编译返回 200（无编译错误）、Control Plane(:8081)/api/v1/inbox/items 与 /inbox/badge 返回 401（接口真实存在，认证拦截非 mock）；浏览器视觉渲染验证因环境无 Playwright/codex chrome plug 未做，需人工浏览器复核三栏布局渲染。

- 2026-07-06 00:09：运行总览落地为真实数据驱动的三层地图：Web 通过现有数字员工 overview 与团队列表接口轮询渲染 2.5D 团队办公区、固定 10 工位、数字员工头像、楼层切换、侧栏详情和表格视图；Control Plane 在团队创建初始数字员工和数字员工创建入队时增加每团队 10 人服务层容量守卫。验证：`corepack pnpm --filter ./apps/web run test -- run-overview runtime-overview-adapter`、`corepack pnpm --filter ./apps/web run typecheck`、`go test ./apps/control-plane/internal/tenant ./apps/control-plane/internal/employee`、`git diff --check` 通过；真实 Chrome 连接当前 worktree Web(:3000)+Control Plane(:8081) 打开 `/run-overview`，确认真实接口数据渲染 6 个团队 workspace、60 个工位和员工头像，页面不是占位页。

- 2026-07-05 23:32：落地 v3.1 语义色板并按新词表重构流程编排首页。theme.css 语义色改为 OKLCH 公式派生（浅端 solid=oklch(0.60 0.15 H)，深端 L=0.75；danger 唯一例外 C=0.175），info 从撞品牌蓝的 H263 迁到青色 H215，artifact 迁 H305 仅作类别色，新增 `--v3-*-text` 文字层专用于 soft 底文字（对比度从 2.89~4.48:1 提到 ≥6.3:1），ink-2/ink-3 加深；StatusPill 文字改用 text 层 + semibold、圆点用 solid。流程编排首页删除 signature/指标卡带，最终形态为"流水线运行流"：头部可点击统计筛选条（全部/进行中/等待人工/阻断/已完成）+ 分诊分组运行列表（需要介入/进行中/已完成/其他），每行含状态 pill、mini 节点链（已完成 brand/运行 info/等待人工 warn/阻断 danger/待执行空心，>14 节点退化分段条）、非零计数与摘要，阻断行左 danger accent bar，整行可点进详情；行级 warn 染色、状态换色 IconTile、零值计数芯片、mute 档 badge 全部移除；共享 workflowStatusTone 的 planning 从 warn 降为 mute。详情页同批词表清理：编排头 IconTile 固定 brand、去重复状态 pill、零值计数 pill 不渲染、任务节点"等待人工决策"从 ok 绿改 warn、风险 pill 按级别取色、决策附件节点去绿色系、阶段标签修 artifact 紫与硬编码白底（深色模式 bug）；修复 `v3-warning` 不存在 token 的静默失效。同步 `DESIGN.md`（状态色/类别色分家、彩色文字预算）与 `docs/design-system/tokens.md`；色板对照原型存 `docs/prototypes/palette-v3-semantic-comparison.html`，设计 spec 存 `docs/superpowers/specs/2026-07-05-workflows-entrance-v31-refactor-design.md`。验证：`node docs/design-system/verify-design-system.mjs`、`corepack pnpm --filter ./apps/web run test -- v3-components`（18/18）、`-- workflows`（42/42）、`git diff --check` 通过；真实浏览器连接 Web(:3000)+Control Plane(:8081) 打开 `/projects`、`/employees`、`/workflows`（浅+深色）确认新色板与新首页渲染真实数据，筛选 chip 过滤（阻断=2 行）与整行点击 TanStack 路由跳转 `/workflows/{demandId}` 正常；流水线版复验 24 实例按需要介入 13/进行中 5/已完成 4/其他 2 分组、节点链与"待规划"态渲染正确、详情页画布节点卡语义色正确。

- 2026-07-05 22:34：项目管理页面改为更紧凑的企业级控制面风格：首页保留真实项目队列与风险识别逻辑，压缩风险汇总、筛选条和表格行距，风险行改为左侧语义条而非整行大面积染色；新建项目页改为“新建项目工作台”，步骤条和主表单收紧，创建前审阅改为字段表格。验证：`corepack pnpm --filter ./apps/web run test -- src/features/projects/index.test.tsx`、`git diff --check` 通过；真实浏览器连接 Web(:3000)+Control Plane(:8081) 打开 `/projects` 与 `/projects/new`，确认紧凑队列、审阅表、真实接口数据加载正常，新建页不渲染项目队列。
- 2026-07-05 21:27：项目管理首页队列表格补充固定列宽与长文本溢出策略：表格改为 `table-fixed` + 7 列 `colgroup`，项目名称、当前任务、当前处理者最多两行换行截断，UUID/需求 ID/能力 key/时间使用省略或稳定不换行，避免长项目名和长数字员工名称撑宽首页队列。验证：`corepack pnpm --filter ./apps/web run test -- src/features/projects/index.test.tsx`、`git diff --check` 通过；真实 Chrome 打开 `/projects` 复测 table 从 1893px 收敛到 1454px，与容器同宽，页面无横向溢出。
- 2026-07-05 16:02：项目管理首页队列的“当前处理者”改为从项目成员快照解析显示名：当前任务分配数字员工时优先展示对应数字员工名称，人类待处理任务优先展示负责人名称，缺少成员快照时才回退内部 ID；风险信号读取补充项目成员接口，首页仍不拉取详情 overview/events。验证：`corepack pnpm --filter ./apps/web run test -- src/features/projects/index.test.tsx` 通过。
- 2026-07-05 04:42：将项目 Runtime Placement 做成一等配置与诊断事实，拆分规划资格和派发就绪：项目可绑定/释放 Runtime 节点并聚合执行就绪状态，协调规划不再因 Runtime 未就绪丢失可规划员工，派发前置 gate 会持久化缺 placement、Runtime/Provider/工作区等阻塞事实；项目 task graph 与流程编排页在无任务节点时显示结构化阻塞原因和下一步动作，项目详情页新增 Runtime placement/readiness 面板。验证：`go test ./apps/control-plane/internal/project ./apps/control-plane/internal/api ./apps/control-plane/internal/workflow/projectcoordination -count=1`、`corepack pnpm --filter ./apps/web run test -- src/lib/api/projects.test.ts src/features/projects/index.test.tsx src/features/workflows/workflow-graph-adapter.test.ts src/features/workflows/index.test.tsx`、`corepack pnpm verify:contracts` 通过；`make -C apps/control-plane migrate-validate` 因当前环境缺少 `docker` 可执行文件，Atlas 无法启动 `postgres:16` dev 容器，未完成迁移可重放校验。
- 2026-07-05 15:10：项目管理首页重构为全宽运行队列：移除首页右侧选中项目上下文栏和“最近事件/最后事件”口径，队列改为展示项目、任务发起描述、当前节点/能力、当前处理者、执行状态与“最后运行时间”，行操作直接进入对应流程编排；首页不再因为默认选中项目而拉取详情 overview/events，避免把不确定事件或假阶段放到首页。验证：`corepack pnpm --filter ./apps/web run test -- src/features/projects/index.test.tsx`、`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；真实 Chrome 连接 Web(:3000)+Control Plane(:8081)，登录 `admin/admin` 打开 `/projects`，确认项目队列表头为“项目 / 当前任务 / 当前节点/能力 / 当前处理者 / 执行状态 / 最后运行时间 / 操作”，无 `project-selected-context-panel`、无“最近事件/最后事件”，行操作链接到 `/workflows/{demandId}`，浏览器 error 日志为空。

- 2026-07-03 23:09：将数字员工 Provider 提升为创建时固定身份事实，创建数字员工不再要求 Runtime 绑定，旧员工级 execution-instance 绑定写入口改为拒绝并提示 Runtime 应绑定到项目；ProjectTask dispatch 改为按项目 Runtime placement 与员工 Provider 解析执行节点，并在任务 attempt 上记录项目、数字员工、Provider 与 Runtime 执行事实。验证：`go test ./apps/control-plane/internal/employee -run 'TestBindExecutionInstance|TestCreateDigitalEmployeeDoesNotRequireRuntimeBinding|TestDigitalEmployeeRun' -count=1`、`go test ./apps/control-plane/internal/project -run 'TestQueueProjectTaskWithAttempt|TestProjectTaskAttempt|TestExecutionTrace' -count=1`、`go test ./apps/control-plane/internal/workflow/projectcoordination -run 'TestProjectStoreDispatchProjectTask' -count=1`、`cargo test --manifest-path apps/runtime-agent/Cargo.toml project_task --test runtime_command_executor_test -- --nocapture` 通过；真实链路以隔离 Control Plane(:18081)+Web(:13000)+Runtime Agent+Codex Provider smoke 跑通项目 `b2e286be-bd9c-425d-9dee-6f1c96f19764`、任务 `3cb572eb-3898-41fa-a845-cf3404c2a121`、attempt `793bc9ed-5a58-5c2d-b4ad-7b23c25cc3d9`，Chrome 回读项目页和员工页确认员工未绑定 Runtime、Provider 为 `codex`、任务按项目 Runtime placement 执行并回写 `provider runtime smoke completed`。

- 2026-07-03 20:52：修正 Shell 页头迁移后业务主操作位置：数字员工首页“模板管理 / 创建数字员工”、技能市场“上传技能”以及同类“新建项目 / 新建团队 / 新建用户 / 注册 MCP”等入口不再渲染到全局顶栏，改回页面内容区首个无背景操作行；项目管理与数字员工模板 wrapper 的 `actions` 也统一落在 `Main` 内，避免后续调用方再次把创建类按钮顶到 shell。验证：新增员工/技能回归测试断言按钮不在 `header` 且仍在 `main`；`corepack pnpm --filter ./apps/web run test -- src/features/employees/index.test.tsx src/features/skills/index.test.tsx --maxWorkers=1`、`corepack pnpm --filter ./apps/web run test -- src/features/projects/index.test.tsx src/features/teams/index.test.tsx src/features/users/index.test.tsx src/features/mcp/index.test.tsx src/features/employees/templates.test.tsx --maxWorkers=1`、`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；真实浏览器连接运行中的 Web(:3000)+Control Plane(:8081)，在 1280px 与 390px 下打开 `/employees`、`/skills`，确认 shell 顶栏不含这些按钮、内容区按钮链接存在且无横向溢出。

- 2026-07-03 20:34：Web 认证后控制台页面统一 Shell 页头：新增 `ShellPageHeader` / `ShellPageHeaderBack` 作为业务页头入口，标题统一进入全局顶栏，标题模式搜索保持居中窄化，迁移流程编排、项目、技能、数字员工、团队、收件箱、Runtime、MCP、权限、成本、审计、日志、账户等页面，清理旧 `<Header><Search/><ThemeSwitch/></Header>` 死代码和内容区重复主标题；页面内容区默认继承整页背景，不新增 `bg-v3-bg`。验证：`corepack pnpm --filter ./apps/web run typecheck`、`corepack pnpm --filter ./apps/web run test -- --maxWorkers=1`、受影响页面定向测试、`git diff --check` 通过；真实浏览器连接运行中的 Web(:3000)+Control Plane(:8081)，在 1280px 与 390px 下打开 `/workflows`、`/projects`、`/skills`、`/employees`、`/inbox`，确认 shell 页头、main 内无重复页头、main 无新增背景层且无横向溢出。

- 2026-07-03 09:23：Project task graph 可视化补齐桌面编排视图：项目管理详情新增计划任务图画布，复用流程编排任务节点展示数字员工头像、名称、角色、任务描述和中文状态；流程编排与项目详情共享 `PLAN_TASK_GRAPH_LAYOUT` 标准布局，最多 10 个动态任务按稳定网格自动排布并保留连线，避免纵向叠放、折叠和遮挡。验证：`corepack pnpm --filter ./apps/web run test -- workflow-task-node plan-graph-canvas plan-task-graph project-execution-trace-panel src/features/workflows/index.test.tsx src/features/projects/index.test.tsx src/features/projects/config.test.tsx workflow-graph-adapter`、`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；分支内真实浏览器验证打开项目 `bf75f89e-c5fe-4c2f-b77d-71c7b8afe1b0`，确认 10 个任务节点、10 个数字员工头像、连线路径存在、桌面布局无重叠且行分布为 2/2/2/2/2。合并后仍需在 `main` 上重跑真实链路 smoke 后才能删除分支/worktree。

- 2026-07-02 20:12：Project task graph 员工身份补齐：`GET /api/v1/projects/{projectId}/task-graph` 的 assigned digital employee 现在返回专业角色 `employee_role` 与头像 `avatar_asset`，服务层通过注入的 `DigitalEmployeeIdentityLookup` 从数字员工身份读取并复用内置头像资产解析，Repository 不引入跨包依赖；OpenAPI、生成 Go 类型和 Web API 类型同步补齐字段；`verify:contracts` 的 Rust Runtime client guard 对齐当前 RuntimeCommand/ProjectTaskAttempt 写回路径，不再要求已 deprecated 的 legacy `/runtime/tasks/*` polling client。验证：`corepack pnpm test:go`、`corepack pnpm verify:contracts`、`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；真实链路以当前分支 Control Plane(:8081) 登录 `admin/admin`，对项目 `f2e0e0a0-0483-4d8f-9656-5695efe241c7`、需求 `58de5a8f-deb4-4335-8e8e-a2b9a56707f3` 调用 task-graph，确认员工 `Smoke Executor` 返回 `employee_role=general_engineer` 与 `avatar_asset.id=engineer-m-01`、`thumbnail_url=/images/digital-employee-avatars/engineer-m-01-256.webp`。

- 2026-07-02 17:57：ProjectTask dispatch/runtime recovery 首版：dispatch failure 已记录后会转成 retry scheduled 或 waiting-human recovery decision，dispatch 重试按 `dispatch_failed` 事件计数对 `max_attempts` 封顶，coordinator 在 retry 到期后用 workflow timer 自动重派（不再依赖后续 signal）；恢复对 Temporal activity 重试幂等（pending decision 不重复、未到期 retry 不重排）；queued 未 started 和 running lease lost 可通过 Control Plane recovery 终态化旧 attempt 并按 retry policy 推进（带退避，waiting-human 时创建 `project_task_recovery` 决策并投影 inbox）；`ScheduleProjectTaskRetry` 允许从 `queued` 重排以替换 stale attempt；`ListDispatchableTasks` 尊重 `retry_not_before`，避免未到期重试被立即重新分派。验证：`go test ./apps/control-plane/internal/project ./apps/control-plane/internal/workflow/projectcoordination ./apps/control-plane/internal/storage -count=1`（project 包 4 个预存在失败与本变更无关：`TestRecordPreDispatchGateResult*`、`TestListWorkflowInstancesOrdersAttentionBeforePagination`、`TestProjectTaskGraphReadReturnsGraphScopedSidecarsAfterUnrelatedRows` 在未改动的 main 上同样失败，属环境时区/共享 dev 库问题）、`git diff --check` 通过；`node scripts/verify-foundation-contracts.mjs` 在 main 与本分支同样失败（Rust Control Plane client 缺 runtime task 路径，预存在，与本变更无关）。真实链路 smoke：分支代码以临时 Control Plane(:18081，隔离 Temporal task queue `superteam-project-coordination-smoke-dr`)接共享 dev DB/Temporal，创建项目 `1175f901-a93b-4630-a92e-839c90b254d4` 并提交需求 `84899199-b303-4519-b7ff-e7e4c673abba`，批准 plan review 后 coordinator 真实执行 `DispatchProjectTask`（terminal 失败）→ `RecoverTaskDispatchFailure`；落库验证 `project_task.dispatch_failed` + `project_task.recovery_requested`(failure_family=invalid_contract) 事件，任务 `cc7fcdee-355e-486d-a2a3-cd50f46ce0be` 为 `waiting_human`/`plan_invalid` 且 `waiting_request_id` 链接 pending 的 `project_task_recovery` 决策 `426e0e5b-6bc8-4444-9436-4c511a4b2037`。retryable→retry_scheduled+timer 分支由 Temporal 测试环境 `TestProjectCoordinatorRedispatchesAfterRetryBackoff` 与 pg 集成测试覆盖。合并后仍需在 `main` 上重跑真实链路 smoke 后才能删除分支/worktree。

- 2026-07-01 23:01：修复项目管理首页首开风险数据逐项刷新导致的半成品计数和排序抖动：当前页任务、决策、证据信号未全部返回前，风险摘要与队列统一显示“风险识别中”，待当前页信号稳定后再一次性展示真实风险计数、行级标签和排序；后台 refetch 已有稳定结果时不再清空页面。验证：`corepack pnpm --filter ./apps/web run test -- src/features/projects/index.test.tsx`、`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；重启真实 Web 后用 Chrome 打开 `/projects?finalSmoke=1`，页面加载真实 Control Plane 数据，项目管理标题、风险摘要、项目队列均可见且浏览器 error/warning 日志为空。

- 2026-07-01 17:52：修复 Runtime Agent 项目任务完成写回对 Provider `result_contract.verification[].evidence` 别名不兼容的问题：写回前将非后端契约字段 `evidence` 归一到 `summary` 并移除原字段，避免 Control Plane 严格解码返回 `json: unknown field "evidence"`，导致 `task_runs` 已 completed 但 `project_tasks/project_task_attempts` 仍停在 running。验证：`cargo test --manifest-path apps/runtime-agent/Cargo.toml`、`git diff --check` 通过；真实链路在合并后的 main 临时 worktree 上用 Control Plane(:8081)+Temporal+Runtime Agent+Codex Provider 新建项目 `0a90c33e-a1b6-4094-837b-45aca03b62bc` 和需求 `7c60646a-55fa-4d35-bb10-f9de0f5a5820`，Provider 输出刻意包含 `verification[].evidence`，最终 `/api/v1/runtime/project-task-attempts/97c65b42-fd90-5716-a130-718e99beb20f/complete` 返回 202，ProjectTask `c1a7bb58-8f00-41c3-a430-7b7ea1a34a74` completed、attempt succeeded、`project_task_results` 为 `completed/accepted/complete_accepted`。

- 2026-07-01 15:43：Web 侧栏菜单补强字体渲染与深色模式对比度：全局开启字体平滑，侧栏菜单字号提升至 15px，并为深色模式下未激活菜单文字和图标增加可读性覆盖；同步补充侧栏菜单浏览器测试。验证：`corepack pnpm --filter ./apps/web run test -- src/components/layout/sidebar-menu-sizing.test.tsx src/components/layout/sidebar-collapsed-alignment.test.tsx src/styles/v3-shell-background.test.tsx`、`git diff --check` 通过。

- 2026-07-01 15:15：ProjectCoordinator 增加历史安全续跑：工作流在 Temporal 建议时 `ContinueAsNew`，并通过持久化 `project_decision_requests.plan_revision_id` 与 `project_plan_revisions.created_event_id` 在 Continue-As-New 后从数据库恢复人工计划评审路由；新路由用 `workflow.GetVersion("route-human-decision-from-store", ...)` 保护，保留 legacy pending map 回放分支，避免已运行协调线程 replay 非确定性。验证：`go test ./apps/control-plane/internal/project ./apps/control-plane/internal/workflow/projectcoordination ./apps/control-plane/internal/storage -count=1`、`atlas migrate validate --dir file://apps/control-plane/internal/storage/migrations`、`git diff --check` 通过；开发库 Atlas 状态 Current Version 044 且无 pending，确认 `project_decision_requests.plan_revision_id` 与 `project_plan_revisions.created_event_id` 已存在；真实 Temporal/Control Plane smoke 中项目协调线程记录 `route-human-decision-from-store` version marker，并调度 `LoadHumanDecisionRoute`、`ResolvePlanRevisionReview`、`DecomposeAcceptedPlanRevision`、`DispatchProjectTask`、`FinishCoordinationJob`。

- 2026-07-01 14:58：Project Workspace autonomous outer loop 首个可运行切片补齐：项目支持源码仓库绑定与 Runtime 放置事实，项目任务分派向 Runtime 传递 workspace/git/handoff/预算上下文，Runtime 将员工 home 与项目 worktree 分离并用真实 Provider CWD 执行，执行结果写回 attestation、budget heartbeat 与结构化 result contract，成功验收要求 runtime attestation 引用；同时补齐 bounded revision loop、路径脱敏、OpenAPI/sqlc/Atlas 生成物和 Runtime/Control Plane 回归测试。验证：`make -C apps/control-plane generate-sqlc`、`corepack pnpm generate:control-plane`、`corepack pnpm verify:contracts`、`go test ./apps/control-plane/internal/storage -run 'ProjectRepoBindingAndAttestation|ProjectTaskAttestationRuntimeContext' -count=1`、`go test ./apps/control-plane/internal/project ./apps/control-plane/internal/workflow/projectcoordination ./apps/control-plane/internal/employee ./apps/control-plane/internal/api ./apps/control-plane/internal/storage -count=1`、`cargo test --manifest-path apps/runtime-agent/Cargo.toml`、`cargo fmt --manifest-path apps/runtime-agent/Cargo.toml --check`、`git diff --check` 通过；分支内真实链路以临时 Control Plane(:18081)+Runtime Agent+Codex Provider 跑通 repo-bound 项目任务，task `553e3812-6215-41c6-9e77-bfc9456ea0f8` completed、attempt `bd2534c3-d882-5471-aeaa-3a17b0113f14` succeeded、result `accepted/complete_accepted`，receipt/attestation 未持久化 `agent_home_dir`、`workspace_path`、`mcp_config_path` 或 `/Users/tinker`。合并后仍需在 `main` 上重跑真实链路 smoke 后才能删除分支/worktree。

- 2026-07-01 02:01：用户管理页改为表格治理台：从用户主页移除内嵌账号操作、登录日志、授权决策、可选团队和审计日志入口，仅保留人类用户账号状态、控制台访问、成员身份、禁用/启用、重置密码与创建用户；创建用户支持不预选团队，后续可到团队管理分配。新增三版用户管理原型和 GPT Image 参考图。验证：`corepack pnpm --filter ./apps/web run test -- src/features/users/index.test.tsx`、`corepack pnpm --filter ./apps/web run typecheck`、`go test ./apps/control-plane/internal/auth -run 'CreateUser|ManagedUser|ValidateActiveTenantTeamIDs' -count=1`、`git diff --check` 通过；真实链路重启 Web 后用 Chrome 插件登录 `admin/admin` 打开 `/users`，确认用户治理表加载真实用户，`main` 与用户管理 layout 内不再出现可选团队、审计日志或日志类型入口。

- 2026-06-30 11:12：登录验证码新增开发期开关：Control Plane 配置增加 `auth.captchaEnabled`，并支持 `AUTH_CAPTCHA_ENABLED=false` 环境变量关闭；关闭时 `/api/auth/captcha` 返回 `{"enabled":false}`，登录接口不再要求 `captcha_id`/`captcha_code`，Web 登录表单隐藏验证码并直接提交账号密码。默认仍为开启状态。验证：`go test ./apps/control-plane/internal/auth ./apps/control-plane/internal/config ./apps/control-plane/internal/app`、`corepack pnpm --filter ./apps/web run test -- src/lib/api/auth.test.ts src/features/auth/auth-provider.test.tsx src/features/auth/sign-in/components/user-auth-form.test.tsx`、`corepack pnpm --filter ./apps/web typecheck`、`corepack pnpm verify:contracts`、`git diff --check` 通过；真实链路以 `AUTH_CAPTCHA_ENABLED=false` 重启 Control Plane，确认 `/api/auth/captcha` 返回 disabled、`POST /api/auth/login` 不带验证码返回 200 且设置 session cookie，并在真实 Web 登录页确认验证码控件不存在、`admin/admin` 登录进入首页；随后已按默认命令重启 Control Plane 并确认验证码恢复 enabled。

- 2026-06-30 03:55：登录页新增强制图形验证码：Control Plane 生成 4 位数字+字母图片验证码并以 PostgreSQL 一次性 challenge 校验，登录请求必须提交 `captcha_id`/`captcha_code`；Web 登录表单加载验证码、支持刷新，并在登录失败后清空验证码但保留账号密码。验证：`go test ./apps/control-plane/internal/auth ./apps/control-plane/internal/storage -run 'Test.*Captcha|TestLogin|TestAuthCaptchaChallenge' -count=1`、`go test ./apps/control-plane/...`、`corepack pnpm --filter ./apps/web run test -- src/lib/api/auth.test.ts src/features/auth/auth-provider.test.tsx src/features/auth/sign-in/components/user-auth-form.test.tsx`、`corepack pnpm --filter ./apps/web run test -- src/lib/api/auth.test.ts src/features/auth/auth-provider.test.tsx src/features/auth/sign-in/components/user-auth-form.test.tsx src/components/layout/header.test.tsx`、`corepack pnpm --filter ./apps/web run typecheck`、`corepack pnpm verify:contracts` 通过；迁移 `039_auth_captcha_challenges.sql` 已应用到开发库且状态 OK；当前分支 Control Plane(:18081)+Web(:3001) 真实链路验证 `/api/auth/captcha` 返回 PNG data URL、缺失验证码登录 400、错误验证码登录 401 并写入 `captcha_invalid` 登录日志、`admin/admin` 携带新验证码登录成功并进入认证首页。已知：`corepack pnpm --filter ./apps/web run test` 全量 Web 测试在 Vitest/Playwright harness 层抛 `route.fulfill: The object has been collected to prevent unbounded heap growth`，未落到本功能断言。

- 2026-06-30 01:06 项目管理风险首页布局改为队列主导：桌面首页右侧上下文收窄为辅助栏，风险队列表格从 6 列收敛为“项目 / 风险与落点 / 状态 / 操作”4 列，并将负责人和处置落点折入行内次级信息，减少宽屏首屏挤压。

- 2026-06-30 00:08 项目管理首页改为风险优先队列，基于当前页项目补强任务、决策、证据和协调状态风险信号，并保留统一详情跳转。

- 2026-06-30 00:21：Web 默认首页改为“任务中枢”，复用任务发起表单作为首页任务提交入口；侧栏工作区首项同步改为“任务中枢”，并移除重复的“任务发起”菜单项；默认侧栏样式切换为浮动玻璃面板，增加 26px 圆角、渐变背景和激活项左侧锚点。验证：`corepack pnpm --filter ./apps/web run test -- src/components/layout/sidebar-data.test.ts src/context/search-provider.test.tsx src/features/dashboard/index.test.tsx src/features/task-launches/index.test.tsx src/styles/v3-shell-background.test.tsx src/components/config-drawer.test.tsx src/components/layout/sidebar-menu-sizing.test.tsx`、`git diff --check` 通过；真实运行 Web 登录 `admin/admin` 打开 `/`，页面标题为“任务中枢”，侧栏有“任务中枢/收件箱”且无“任务发起”，浮动侧栏计算样式生效，并确认 `/api/auth/me`、`/api/v1/projects` 真实 Control Plane 返回 200。

- 2026-06-29 14:23 项目创建流程收敛为负责人池模型：新建项目使用主负责人 + 额外 owner 成员，多来源团队只用于筛选数字员工；任务发起不再要求选择审核人，后端默认派给主负责人。

- 2026-06-29 00:23: 项目管理新建项目改为分屏配置台，支持授权团队、人类角色、数字员工池、策略预设和创建前审阅。

- 2026-06-28 23:14 完成了「任务编排模板库」的全栈开发，包括 OpenAPI 契约、后台 SQLC 和服务、控制平面 API 路由、前端模板选择器组件和插入表单的对接集成。

### Changed

- 2026-06-28 03:26：项目管理详情默认视图收敛为项目负责人主循环：前置当前需求、当前执行、最新结果、待负责人处理、当前阻塞、项目负责人组和项目服务池；路由决策、协调任务、执行摘要、转派请求、协调线程、Dispatch gate 技术详情、治理和归档入口统一折叠到“高级项目事实”。默认事件流改为负责人可读动态，避免泄漏 `route_decision` 等内部协调对象。验证：`corepack pnpm --filter ./apps/web run test -- src/features/projects/index.test.tsx`、`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；真实 Web 连接真实 Control Plane 打开带 dispatch gate 的项目，默认视图业务标签可见、内部标签折叠隐藏，展开高级事实后内部面板可见且无横向溢出。
- 2026-06-28 02:52：数字员工新增只读模板管理入口与目录页：数字员工首页增加“模板管理”次操作，`/employees/templates` 基于真实 `create-options` 展示内置模板表格，`/employees/templates/$templateType` 展示模板能力、默认注入、治理影响和“用此模板创建数字员工”入口；创建页模板表格统一改为“选择内置模板 / 模板能力 / 默认注入 / 风险等级”，模板列不再重复英文类型，`/employees/new?template=...` 会预选对应模板并进入预检。验证：`corepack pnpm --filter ./apps/web run test -- src/features/employees/create.test.tsx src/features/employees/index.test.tsx src/features/employees/templates.test.tsx src/navigation-rules.test.ts`、`corepack pnpm --filter ./apps/web run typecheck`、`corepack pnpm --filter ./apps/web run build`、`git diff --check` 通过；真实浏览器从 `/employees` 点击“模板管理”进入 `/employees/templates`，查看 `frontend_engineer` 模板详情，再进入 `/employees/new?template=frontend_engineer`，创建页显示“已选择前端开发模板”，预检页由真实后端返回 `Runtime 可用: 1/10 个运行绑定可用` 且无浏览器 error log。
- 2026-06-27 22:49：数字员工创建页改为四步创建流：先用可搜索/风险筛选的模板表格选择专业模板，再进入独立配置预检页，预检通过后完成身份、能力、治理和运行配置，最后在确认页核对团队、模板、名称、角色、能力数量、Runtime、预算和环境变量数量后提交创建；模板表格仅展示模板真实提供的默认角色、推荐技能/MCP、风险触发和默认注入摘要，不把 Runtime/Provider 可用性混入模板列。验证：`corepack pnpm --filter ./apps/web run test -- src/features/employees/create.test.tsx`、`corepack pnpm --filter ./apps/web run typecheck`、`corepack pnpm --filter ./apps/web run build` 通过；真实浏览器登录 `admin/admin` 打开 `/employees/new`，模板页加载 9 个模板、表格不含 Runtime/Provider/运行可用性字段，进入预检页后真实后端返回 `Runtime 可用: 0/10 个运行绑定可用`，继续配置按钮按预期禁用，页面无横向溢出且无浏览器 error log。
- 2026-06-24 14:37：控制台与技能市场背景切换为 v3 Acrylic Shell：全局 Shell 使用柔和底色、低对比渐变与网格，左侧导航、顶部栏、顶部搜索/主题/侧栏开关和左侧激活菜单项改为 token 化毛玻璃与半透明 control 表面，并移除 inset 内容容器的顶部空白和顶部圆角，让顶栏贴边融合；同时保留卡片、表格和表单实底高对比；同步补充 shell/acrylic token 与表面设计规范。验证：`corepack pnpm --filter ./apps/web run test src/styles/v3-shell-background.test.tsx src/components/layout/authenticated-layout.test.tsx src/components/layout/header.test.tsx`、`git diff --check` 通过；真实运行 Web/Control Plane 下打开 `/skills`，页面加载技能市场且 body/shell 为 Acrylic 背景，Header/Sidebar/顶部控件/左侧激活菜单项为 blur 或半透明 acrylic 表面，Header 与内容 inset 的 `y=0`、`marginTop=0`、顶部圆角为 0，无 Vite error overlay。
- 2026-06-23 16:36：任务发起页重构为单张 v3 Soft-Flat 命令面板：保留中枢指令区、项目/审核人/优先级/风险级别和保存草稿/提交需求动作，移除底部“添加附件 / 关联链接 / 导入资料”资料入口及右侧说明栏，提交 payload 不变；同步将任务发起页、命令面板、主题/侧栏/外观设置等可见英文改为简体中文，并在 `DESIGN.md` 增加“简体中文优先”约束。验证：新增任务发起浏览器测试断言资料动作不渲染，`corepack pnpm --filter ./apps/web run test` 449 项通过、命令面板/外观设置/任务发起定向测试 30 项通过、`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；重启真实 Web 后打开 `/task-launches`，真实 Control Plane 数据加载成功且浏览器日志无错误。
- 2026-06-23 15:16：统一 Web 菜单页标题区：`V3PageHeader` 增加受控 `icon`/`iconTone`/`actions` 入口，工作台、技能市场与收件箱页头使用统一图标样式；收件箱移除页头右侧与下方指标卡重复的开放/高风险/阻断摘要。验证：Dashboard/Skills/Inbox 定向浏览器测试 20 项通过，`corepack pnpm --filter ./apps/web run typecheck`、`git diff --check` 通过；真实运行 Web 下打开 `/`、`/skills`、`/inbox` 确认页头图标和统计分布正确。
- 2026-06-23 11:33：SuperTeam Web 全站 v3 Soft-Flat 迁移收口：所有业务页面与 Shell 使用 `--v3-*` token 和 `components/superteam` v3 组件，删除旧液态/玻璃组件导出、组件测试、样式 token、全局旧 class 与过时原型目录；设计系统文档改为 v3 唯一基线。验证：旧组件符号与旧样式前缀 grep 归零，`corepack pnpm --filter ./apps/web run typecheck`、`run build`、`run test`、`git diff --check` 通过；重启真实 Control Plane/Web 后用 Playwright 登录 admin/admin 打开 22 个主菜单路由，均 HTTP 200、无旧 class/slot、无页面 JS 错误。

### Fixed

- 2026-06-28 23:56：修复任务发起「浏览模板库」弹窗布局异常：模板卡片网格原用视口断点 `sm:grid-cols-2 xl:grid-cols-3`，被 ≥1280px 视口 `xl` 断点在窄弹窗内压成过窄多列、正文过早截断，改为 `grid-cols-[repeat(auto-fill,minmax(15rem,1fr))]` 跟随容器宽度；并修复 `DialogContent` 默认 `sm:max-w-lg`（512px）覆盖了组件 `max-w-[900px]`、导致弹窗实际仅 512px 内容区挤压的问题，补加 `sm:max-w-[900px]` 覆盖默认值；同时清理未用的 `useMutation`、`replaceAll`→`split/join`（es2021 lib）与未用的 `Button`。验证：真实 Web（:3000）连接真实 Control Plane（:8081），登录 `admin/admin` 打开 `/task-launches` 点击「✨ 浏览模板库」，弹窗宽度 512→900px、模板卡片 ~321px、正文完整；`corepack pnpm --filter ./apps/web run typecheck` 通过；`git diff --check` 通过。

- 2026-06-28 22:46：Runtime Agent 运行时 session auth 增加自愈：所有 Control Plane 业务请求改为请求时读取共享 `RuntimeAuthState`，临近过期会暂停业务流量等待安全 token，daemon supervisor 会在过期前主动续期，并在 401 fallback 时使用 `bootstrap_key` 重新 enrollment；WebSocket 重连每次重新取 Authorization，并用 generation guard 忽略旧 session 的迟到 401，避免 12 小时 TTL 后长期重复 401；续期 401 后的 re-enroll 会消费对应 reauth event，避免重复 enrollment，active task 的 lease 与 event writeback 遇到 typed auth 401 时暂停等待 auth 恢复，不再因为 token 切换取消或失败任务。验证：`corepack pnpm verify:runtime-agent`、Control Plane runtime/api 定向与完整 Go 测试、`git diff --check` 通过；真实链路重启 runtime-agent 后 `local-dev-node` 建立 active session，强制 DB session 过期后运行中 agent 触发一次 typed 401 并自动 `Runtime session established`，DB 恢复 active session 且 `latest_seen_at` 更新；真实 Codex Provider smoke 输出 `session_started`、`runtime auth self-healing smoke`、`turn_completed`。
- 2026-06-23 16:04：修复技能上传页两处问题：发布链路中“校验依赖”在未声明 CLI/env 依赖时默认置灰并标记 `aria-disabled`，但沿用等待步骤文字 token、不额外降低透明度，声明依赖后才进入完成态；Control Plane 上传技能 zip 时忽略 `__MACOSX`、`.DS_Store`、`._*` 等压缩元数据再识别包根目录，避免包含 `skill/SKILL.md` 的 macOS zip 被误报“不含 SKILL.md”。验证：新增上传页依赖灰态浏览器测试与后端元数据 zip 解析测试，定向 Web/Go 测试通过；真实链路验证见本次任务记录。
- 2026-06-23 03:46：Project coordinator Phase 4 result contract/revision-loop 收口修复：`CompleteProjectTaskAttempt` 对无效 `result_contract` 不再直接 400 丢失执行结果，而是持久化 rejected `project_task_results`、链接 `latest_task_result_id`，并把 task/attempt 路由到 `waiting_human` 澄清；Runtime Agent 对 Codex `item.completed`/`text_delta` 与空 `turn.completed` summary 的真实输出形态做终态摘要兜底，并将 `acceptance_results[].evidence_refs` 规整为字符串数组，避免结构化结果被降级为 legacy fallback。验证：`corepack pnpm verify:contracts`、`go test ./apps/control-plane/...`、`cargo test --manifest-path apps/runtime-agent/Cargo.toml`、`cargo fmt --manifest-path apps/runtime-agent/Cargo.toml --check`、`git diff --check` 通过；真实链路以临时 Control Plane(:18081)+Runtime Agent(:17077)+Codex Provider 跑通，任务 `completed`、attempt `succeeded`、`project_task_results` 为 `accepted/complete_accepted`，receipt summary 保留 Codex 输出的结构化 `result_contract`。
- 2026-06-22 16:19：修复上传技能工作台提交中文技能名称时后端 slug 生成失败的问题：`UploadSkill` 现在保留中文展示名，但当展示名无法 slugify 时，会依次从 `SKILL.md` 标题和 zip 文件名去掉后缀生成包 slug，支持“技能包描述名称/包标识”和“技能中文名称”分离。验证：新增 `TestServiceUploadSkillUsesArchiveFilenameSlugWhenDisplayNameIsChinese`，`go test ./apps/control-plane/internal/skill/...` 通过；重启 Control Plane 后用示例 zip 真实 multipart 上传返回 201，并在技能管理页看到“示例浏览器上传技能”及 CLI/env 依赖声明。
- 2026-06-22 04:54：Project coordinator Phase 3 pre-dispatch gate 收尾修复：高风险项目任务在 gate `waiting_human` 后，人工 `project_task_approval` 批准会通过版本化 Temporal 分支调用 `ApplyPreDispatchGateDecision` 重新评估 gate，并以 `human_resolved` 原因继续 dispatch；非 gate decision 仍只记录 observed 事件，不触发分派。修复 decision-linked `waiting_human` gate 的幂等更新条件，使审批后同一 idempotency key 可从 `waiting_human` 更新为 `passed` 并绑定 attempt，但已绑定 attempt 的 gate 不会被覆盖；Runtime start-session payload 对缺省 `session_policy` 统一补 `mode=new`、`recoverable=true`；Runtime `/started` 写回对“Runtime command 早于 queued attempt 可见”的短暂竞态做有条件重试，真实 lease/node/task 冲突仍立即失败。验证：`make generate-sqlc`、`go test ./apps/control-plane/internal/project ./apps/control-plane/internal/workflow/projectcoordination ./apps/control-plane/internal/employee ./apps/control-plane/internal/app ./apps/control-plane/internal/api ./apps/control-plane/internal/storage -count=1`、`git diff --check` 均通过；真实链路以临时 Control Plane(:18081)+Temporal+Runtime Agent+Codex Provider 跑通，gate `waiting_human -> passed`，attempt `running -> succeeded`，task_run/receipt `completed`，receipt payload 中 `session_policy={"mode":"new","recoverable":true}`。
- 2026-06-21 02:46：执行证据补齐进入证据读模型（金路径证据缺口）：任务完成回写（`CompleteProjectTaskAttemptWriteback` / `CompleteProjectTaskAttemptAcceptanceWriteback`）原本只把 `evidence_refs` 存进 `project_execution_summaries` 行的 JSONB 列，并未抽取到独立的 `project_evidence_refs` 读模型——而 `/projects/{id}/evidence` 读取后者，导致「执行摘要带 evidence_refs 但证据列表空」。新增 `extractExecutionEvidenceRefsWithQueries`：在两条完成回写事务内、`createExecutionSummaryWithQueries` 之后，把每个 `{ref,type,provider_session_id?}` 条目映射为一条证据行（evidence_type/source_type=type、source_ref=ref、execution_summary_id、project_task_id、submitted_by=digital_employee、metadata=原条目）。best-effort：单条畸形跳过、不回滚任务完成（权威 refs 仍在摘要上）。验证：`go build ./...` 通过、`go test ./internal/project/` 通过（无回归）；真实链路待验证——当前开发库被重置后无处于 queued/running 的 task attempt，无法在本次跑通一次「带 evidence_refs 的任务完成 → 证据行落库」的真实回写，故不声明端到端已验证。
- 2026-06-21 02:21：修复批准路由决策后任务分派被永久阻塞（卡死 `dispatching` run 占用单活跃槽位）：数字员工 run 进入 `dispatching`（已向 runtime 节点下发 start-session 命令但 runtime 从未回报启动，如节点失联/回调丢失/进程中途崩溃）后会无限期停留，`GetActiveRun` 一直返回它，单活跃 run 守卫（`ErrConflict: active digital employee run exists`）随之永久挡住该员工后续所有 dispatch——表现即「人类决策队列批准失败、拒绝正常」（批准触发 dispatch 命中冲突重试 3 次全败、任务停在 planned；拒绝不走 dispatch 故无碍）。在 `DigitalEmployeeRunService.CreateRun` 冲突分支新增陈旧预确认 run 回收：当被阻塞的活跃 run 处于 `queued`/`dispatching`（命令已发未确认启动）且 `time.Since(UpdatedAt) > staleDispatchTTL(5min)` 时判定废弃，就地标记 `failed`（error_code `dispatch_stale`/error_family `dispatch_timeout`）并写 `run_reaped_stale` 生命周期事件 + `digital_employee_run_reaped_stale` 审计，释放活跃槽位后继续创建新 run；同幂等重试仍走原有重发路径，`running`/`cancelling`（runtime 已确认在跑）即使久也不回收。验证：`go test ./internal/employee/` 通过（新增陈旧回收/新鲜不回收/running 不回收 3 例 + 既有冲突用例）；Control Plane(:8081) 重启加载新代码并健康后，对真实开发库中卡死超过一天的 run `c612ec8c`（dispatching）触发 `POST /digital-employees/{id}/runs`，确证其被回收为 `failed/dispatch_stale`、新 run 创建并派发到真实节点、该员工活跃 run 集合由「卡死旧 run」变为「单一新 run」，且 `task_events` 写入 `run_reaped_stale`、`audit_events` 写入 `digital_employee_run_reaped_stale`。

### Removed

- 2026-06-21 02:31：删除「任务发起」侧孤儿详情组件：`task-launches/components/task-launch-detail.tsx`（`TaskLaunchDetail`）及 `features/task-launches` 中未被任何路由消费的 `TaskLaunchDetailPage`/`TaskLaunchDetailView`（路由 `/task-launches/$demandId` 实为重定向到 `/workflows/$demandId`，而后者 `WorkflowDetail` 已基于真实 `task-graph` 渲染完整任务节点/负责人/阻塞/输入输出/执行结果，孤儿组件既不可达又与之重复）。统一以 `/workflows/$demandId` 为唯一的计划/详情入口，保留 `/task-launches/$demandId` 重定向作为书签兜底；同步移除对应测试与未用的 `rerenderWithQueryClient` 辅助。验证：`corepack pnpm --filter ./apps/web run typecheck` 通过、`run test -- task-launches` 7/7 通过、Vite HMR 加载 `task-launches/index.tsx` 无报错；发起器（选项目→提交→导航 `/workflows/$demandId`）行为不变。
- 2026-06-21 00:52：删除已废弃的 `runtime_leases` 表：新增 migration 028 `DROP TABLE IF EXISTS runtime_leases` 并已应用到当前开发库；同步移除迁移/查询测试中对该表作为当前表的要求，规范文档明确项目任务执行租约由 `project_task_attempts` 承载，避免后续重建独立 runtime lease 表。

### Added

- 2026-06-28 09:07：新增 MCP HTTP 能力管理（注册表 + 绑定 + 环境变量预检 + Provider 配置投射）：migration 037 建立 `mcp_servers` 注册表与 `team_mcp_bindings`/`digital_employee_mcp_bindings_v2`，仅允许 streamable_http/http 传输与 none/bearer_env/headers_env 鉴权，并按 (tenant,name,url) 从旧 `team_mcp_servers`/`digital_employee_mcp_bindings` 回填（bearer_env 行同步写入 `required_env_vars` 占位保持预检一致），atlas.sum 已重算；Control Plane 新增 `/api/v1/mcp-servers`、`/teams/{teamId}/mcp-bindings`、`/digital-employees/{employeeId}/mcp-bindings-v2`、`/effective-mcp-config` 全套 CRUD 与 `mcp_registry.read/manage` 租户管理员鉴权，绑定按 `mcp_server_id` 而非原始 URL，员工绑定返回 `blocked_missing_env` 预检；start-session payload 投射环境变量满足的有效 MCP（阻塞项排除）；Runtime Agent `mcp_config.rs` 以原子写为 Codex(`config.toml` 读合并保留)、Claude Code(`.mcp.json`)、OpenCode(`opencode.json` 读合并保留) 渲染配置，只写环境变量名不写凭据值；Web 新增一级 `/mcp` 管理页与侧栏入口，团队/员工能力面板改为注册表选择绑定，员工绑定在缺必需环境变量时禁用并提示精确变量名。验证：`go test ./internal/capability ./internal/api ./internal/authz ./internal/employee ./internal/storage`、`cargo test --manifest-path apps/runtime-agent/Cargo.toml --lib`、`corepack pnpm --filter ./apps/web run test`（mcp/teams/employees/capabilities 共 36 项）、`corepack pnpm --filter ./apps/web run typecheck`、`corepack pnpm verify:contracts`、`git diff --check` 通过；真实链路在合并后的 main 重启 Control Plane，admin 会话真实创建 MCP 定义、stdio 传输被 400 拒绝、团队按 `mcp_server_id` 绑定返回 active、员工 v2 绑定返回 `blocked_missing_env missing=['GITHUB_TOKEN']`，upsert `GITHUB_TOKEN` 后 effective-mcp-config 变为 `active missing=None`，Web `/mcp` 与 web→api 代理均 200。
- 2026-06-24 05:08：技能市场新增同步技能安装闭环：安装弹窗支持选择团队或单个数字员工，Control Plane 对目标员工、Provider 与 Runtime 节点做预检并通过 `install_skills` Runtime command 同步等待短超时结果；团队安装当前要求所有目标位于同一个已连接 Runtime node，否则在预检阶段失败且不派发安装命令，单 Runtime node 内由 Runtime `rollback_on_failure` 保障；失败原因写入 `audit_events`，并在当次安装弹窗展示返回原因与 blocked targets。Runtime Agent 从技能 archive 拉取 zip、校验 checksum、按 Provider 写入 `opencode -> .opencode/skills/<skill_key>`、`codex -> .agents/skills/<skill_key>`、`claude-code -> .claude/skills/<skill_key>`，并回写安装 receipt；技能详情展示已安装团队/员工、Provider、路径与 checksum。验证：migration 035 已应用且 pending=0，`go test ./apps/control-plane/internal/storage ./apps/control-plane/internal/skill ./apps/control-plane/internal/api ./apps/control-plane/internal/app -count=1`、`cargo test --manifest-path apps/runtime-agent/Cargo.toml`、`corepack pnpm --filter ./apps/web run test`、`corepack pnpm --filter ./apps/web run typecheck`、`corepack pnpm verify:contracts`、`git diff --check` 通过；临时真实栈 Control Plane(:18081)+Web(:3001)+Runtime Agent 跑通员工安装，`POST /api/v1/skills/{id}/install` 返回 201，DB `skill_installations` 为 installed，Runtime receipt completed，员工 workspace 下生成 `.agents/skills/example-browser-upload-skill/SKILL.md` 与 `.skill-checksum`，真实浏览器 `/skills` 可看到安装记录、Provider、路径和 checksum。
- 2026-06-23 22:24：新增技能详情页 `/skills/{skillId}`：技能市场“查看详情”改为进入只读技能档案，展示已绑定团队、已绑定数字员工、创建与上传数据、归档包元数据、运行依赖与标签；技能详情 API 客户端新增 `getSkill`，绑定名称缺失时回退展示团队/员工 ID，避免真实数据空名导致审计信息不可见。验证：技能 API/列表/详情浏览器测试 14 项通过，`corepack pnpm --filter ./apps/web run typecheck`、`run build`、`git diff --check` 通过；重启真实 Web 后用 admin 会话打开 `/skills/e8f30e51-2202-4db5-8321-76f262e739d4`，Control Plane 详情接口返回 team_bindings/archive 字段，页面渲染技能档案、团队绑定、数字员工空态、创建上传数据且无控制台错误。
- 2026-06-22 16:00：新增技能上传单页工作台 `/skills/upload`，技能管理页上传入口改为跳转独立页面；zip 文件名去掉 `.zip` 后缀生成“技能包描述名称”，并单独填写“技能中文名称”用于市场展示和后续管理；运行依赖仅声明 CLI/环境变量名，移除步骤条、解析结果表、权限勾选和环境变量缺失检查。上传 FormData 不再提交空 `name`/`description`/`tags`/运行依赖字段，保留服务端从 `SKILL.md` 兜底解析。验证：`corepack pnpm --filter ./apps/web run test`、`corepack pnpm --filter ./apps/web typecheck`、`corepack pnpm --filter ./apps/web build`、`go test ./apps/control-plane/internal/skill/...`、`git diff --check` 通过；临时 Vite 服务 `/skills/upload` HTTP 200；真实浏览器点击验证因当前工具未暴露 Chrome/Browser 控制能力未完成。
- 2026-06-22 02:54：Added CLI runtime dependency checks for skills, Runtime tool capability reporting, and encrypted digital employee environment variable injection.
- 2026-06-21 15:32：Project coordinator Phase 1 planning profile 补齐两项 P0 缺口。（1）任务类型默认能力注册表（`task_type_defaults.go`）：为 `database_analysis`/`incident_triage`/`feature_development` 三类任务注册平台级默认 required capabilities（含别名归一化），planner 解码后 `ApplyTaskTypeDefaults` 将默认能力 union 合并到每个 PlannedTask.RequiredCapabilities，使能力匹配即使模型遗漏声明也能生效；默认流入 scoring → hard-failure → human-review 升级完整链路，task_type_defaults_applied 记入 planner_metadata 供审计。planner 系统提示新增 canonical task_kind 引导（database_analysis / incident_triage / feature_development），避免模型自创 generic kind 导致默认不触发。（2）Load / Reliability 真实数据接入：新增 sqlc 批量查询 `CountDigitalEmployeeOperationalSignals`（JOIN project_task_attempts + project_tasks，30 天窗口，按 assigned_digital_employee_id 聚合 in_flight/success/failure/human_reject 计数），employee.PgRepository 新增 `GetDigitalEmployeeOperationalSignals` + `OperationalSignals` 类型并加入 Repository 接口，planning_profile_adapter 在 `PlanningProfileRecords` 中调用并填充 `LoadState`（in_flight_tasks/available_slots/lendable）与 `ReliabilitySignals`（status/success_rate/recent counts），使 scoreLoad(10) + scoreReliability(5) 不再对全体员工恒定。验证：`go build ./...` 通过；`go test ./internal/workflow/projectcoordination/... ./internal/app/... ./internal/employee/...` 全绿（含 §9 默认补齐/unknown kind 不补/union 保留 5 例 + 反例集成测试「无 DB 能力员工被升级 human review」+ adapter LoadState/ReliabilitySignals 填充断言）。真实链路验证：对运行中 Control Plane(:8081) 用 admin 会话提交 demand 到含 2 DE 成员的项目，deepseek planner 端到端跑通——route decision 生成、task_kind=`database_analysis`、`task_type_defaults_applied=["db_slow_query_analysis"]` 记入 planner_metadata、`required_capabilities` 含 4 个默认能力（database.read/sql.analysis/data.quality.check/business.metric.interpretation）与模型自定义 union、selection evidence 完整持久化、缺能力任务正确升级 requires_human_approval；新 SQL 查询对真实开发库验证跑通（全窗口返回 1 员工 2 成功 1 失败）。
- 2026-06-21 11:37: Project coordinator Phase 1 real-chain validation hardened OpenAI-compatible planner output handling: normalized `selection_score` floats are accepted, platform-side planning profile scoring now overwrites model-provided selection evidence, and missing capability/profile gaps automatically route tasks and route decisions to human review instead of failing the plan.
- 2026-06-21 11:10：Runtime Agent 在执行项目任务 Provider 前先回写 `project_task_attempts/{attemptId}/started`，使 execution ledger 与项目执行 Trace 在真实 Runtime/Provider 链路中包含 `attempt.started`、Provider session 事件、终态与证据引用的完整顺序；补充 Runtime Agent 回写单测并修正 provision payload 测试中的技能 archive contract fixture。
- 2026-06-21 05:21：Added project execution ledger and execution trace read model for task attempts, Provider events, evidence refs, and Web project detail review.
- 2026-06-21 04:58: Control Plane project coordinator Phase 1 planning profile now injects digital-employee capability, runtime, permission, and selection evidence into planner snapshots and task metadata.
- 2026-06-21 01:23：新增 OpenFGA shadow 试点基础：Control Plane 配置支持 `AUTHZ_ENGINE=db|openfga_shadow|openfga` 与 `OPENFGA_*`，默认仍使用 DB 授权；新增 OpenFGA HTTP client、tenant/team/project 映射模型、shadow authorizer、direct OpenFGA fail-closed authorizer、tuple sync/backfill 层和 `cmd/openfga-backfill`；项目 team scope 写路径在 DB 成功后最佳努力同步 OpenFGA，shadow 模式记录 DB/OpenFGA diff 与 OpenFGA 错误审计 snapshot；权限中心 overview 展示当前引擎、OpenFGA store/model 与近 24h diff 计数。开发环境新增 OpenFGA + SQLite volume compose 服务、`scripts/dev-services.sh start openfga`、模型 bootstrap 脚本和配置示例。
- 2026-06-20 23:18：团队借调接入项目协调线程（闭环借调最后一块）：协调线程在构建可执行数字员工池（`LoadProjectCoordinationSnapshot`，挑人前）新增借调闸门——归属团队不等于项目自有团队（`Project.TeamID`）的数字员工，只有当项目持有 (project, team) 维度的有效借调授权（status ∈ approved/auto_approved）时才进入可执行池；无授权的外团队员工被静默剔除并写 `project.lending.employee_skipped` 协调事件（最佳努力、不阻断规划）。无自有团队/无归属团队的员工不受闸门约束；借调查找出错时与就绪检查一致 fail-open（权威授权仍由审批流把守）。新增 sqlc 查询 `ListEffectiveLendingTeamsForProject` + `teamlending` repo `ListEffectiveLendingTeams` + service `EffectiveLendingTeams`；协调侧新增 nil-safe `LendingGatekeeper` 接口与 `ProjectStore.WithLendingGatekeeper`，app.go 以 `lendingGatekeeperAdapter`（employee 仓储解析员工归属团队 + teamlending 仓储解析项目有效授权）装配。验证：`go test ./internal/teamlending ./internal/workflow/projectcoordination` 通过（含闸门三态 + fail-open + 跳过事件断言、`EffectiveLendingTeams` 单测）；新 SQL 在真实开发库验证只返回 approved/auto_approved 授权团队、排除 rejected/revoked；`go build ./...` 通过、Control Plane(:8081) 重启加载新装配并健康、协调 worker 正常注册。
- [2026-06-20 18:29] ProjectTask durable closure now exposes execution packets, liveness projection, and real-chain smoke coverage.
- 2026-06-20 20:43：团队借调（lending）后端落地（D1 团队级 (project,team) 粒度 / D2 超纲强制转人工 / D3 请求自带 status + inbox/audit / D4 创建后配置）：新增 `internal/teamlending` 模块（types/repository/pg_repository/service[含 auto/manual + 超纲例外]/handler）+ sqlc 查询 `internal/storage/queries/team_lending.sql`（policy upsert/get、request create/list-by-team/list-by-project/approve/reject/revoke）；路由 `/api/v1/teams/{teamId}/lending-policy`(GET/PUT)、`/lending-requests`(GET)、`/lending-requests/{requestId}/approve|reject|revoke`(POST)、`/api/v1/projects/{projectId}/lending-requests`(POST/GET)；authz 新增 `team.lending.policy.read/edit`、`team.lending.request.read/decide`；`audit.Service.RecordEvent` 记录 `team.lending.*` 审计，`inbox` 新增 `team_lending` 事项类型投影到团队负责人（pending 创建 / 裁决时 resolve）；OpenAPI 补 lending paths+schemas 并 `pnpm generate:control-plane` + `pnpm verify:contracts` 通过；前端团队详情新增「借调」tab（编辑策略 + 待审批/历史，复用 LiquidTabsList/Badge）。真实链路验证：对运行中 Control Plane(:8081) 用 admin 会话 curl 走 PUT/GET policy、POST request(auto/超纲例外/manual)、approve/reject/revoke、状态机二次裁决 400、项目侧 list 全路径，并查库确认 team_lending_request 状态、audit_events(team.lending.*)、inbox_items(open→resolved) 一致；go test `internal/teamlending` 通过；headless chromium 真实浏览器点穿「借调」tab（登录→团队详情→借调 tab 渲染策略表单+待审批+历史，lending API 实返 200，5/5 断言通过）。
- 2026-06-20 17:47：技能包存储重构为 zip 整包对象存储模式：删除逐文件 `skill_files` 表与在线编辑链路（`UpdateSkillFile` handler/service/repo/web/contract），删除 `skills.status` 列（移除 installed/available 概念），改为上传 zip 整包存 S3（`storage.S3ObjectStore.PutObject`）、DB 仅存 archive 元数据（`archive_object_ref/archive_filename/archive_size_bytes/archive_checksum_sha256/archive_file_count`）；Web 技能页删除 Monaco 编辑器/文件树/保存按钮/已安装-市场双 tab，改为统一技能列表 + archive 详情 + 团队/Agent 绑定面板；Runtime Agent 新增 `skills.rs` 模块实现 `materialize_skills`（从 S3 拉 zip → 校验 SHA256 → 解压到 `agent_home/skills/{skill_key}/`，支持幂等跳过、路径穿越防护、200MB/10000 文件上限），`RuntimeSkillPayload` 改为 archive 字段格式，`RuntimeCommandExecutor` 在 `ProvisionInstance` 时自动 materialize skills；Control Plane `buildProvisionInstancePayload` 改用 `ListSkillsForRuntime` 查询员工生效技能并填入 archive 元数据。migration 025 清理 diagnose/tdd 种子数据。真实链路验证：migration 025 已 apply 到开发库、上传真实 zip 到运行中 Control Plane（:8081）验证 S3 落盘 + DB archive 元数据填充 + 列表/详情 API + 团队绑定 + 旧编辑路由 404。
- [2026-06-20 17:43] ProjectTask recovery now supports retry scheduling, typed waiting-human pauses, and acceptance-gated completion.
- 2026-06-20 17:15：新建团队页重构为方案 B「全页生命周期创建台」：抽屉式两步表单 `CreateTeamDrawer` 替换为独立路由 `/teams/new`（`CreateTeamView` + `CreateTeamPage`），左栏团队身份/团队负责人/初始成员三卡、右栏实时预览卡 + 创建后生命周期清单（治理、对外借调、数字员工、外部能力、审计）+ 提交区；团队负责人文案修正为团队管理者语义（成员增删与团队配置，审批/驳回/补证/验收归项目负责人）。仅复用既有 teams 接口，借调等生命周期项为信息占位。新增 `create-team-draft.ts`（草稿类型与 `CreateTeamInput` 映射）、`create-team-page.test.tsx`（5 项浏览器测试），删除 `create-team-drawer.tsx`/`create-team-basic-step.tsx` 并将创建用例迁出 `index.test.tsx`。新增原型 `docs/prototypes/create-team-concept-a-focused-sheet.html`、`create-team-concept-b-lifecycle-console.html`(+png) 与设计文档 `docs/design/team_lending/team-lending-and-create-team-design.md`（团队借调模型设计，后端待落地）。真实链路验证：web typecheck 通过、teams 浏览器测试 17 项通过、对运行中 Control Plane（:8081）以真实会话 GET/POST `/api/v1/teams` 验证页面负载契约（HTTP 201 持久化、metadata.display 正确）并清理测试数据。
- 2026-06-20 16:18：运行时 ProjectTask 回写切换到 attempt-scoped contract：Control Plane 新增 `/api/v1/runtime/project-task-attempts/{attemptId}` started/lease/complete/fail 路由与持久化写回，终态写回同步记录 `project_tasks.terminal_event_id` 与 attempt terminal facts，Runtime Agent 改用 attempt_id、lease_token、runtime_node_id 和 idempotency_key 回写完成/失败；项目协调分派预生成 deterministic attempt id/lease 并在 Runtime command metadata 中补齐 attempt 与 runtime node facts，旧 `/api/v1/runtime/project-tasks/{id}` 回写路由不再暴露。
- 2026-06-20 04:13：ProjectTask durable closure Phase 1 完成控制平面基础：新增 `project_task_attempts`、queued attempt 排队写入、dispatch 创建 attempt 的 durable 状态桥接，并以 accepted plan revision exact-once decomposition 防止规划重试重复生成项目任务。
- 2026-06-20 03:34：ProjectTask durable closure Phase 1 Task 5 将项目协调分派改为 queue-attempt 写入：`DispatchProjectTask` 保留真实 Runtime run 创建桥接，但以 `QueueProjectTaskWithAttempt` 绑定 `digital_employee_run_id`/`runtime_task_id`/`runtime_node_id` 并保持任务 durable 状态为 `queued`；`project_task.dispatched` 事件携带 attempt/run/runtime facts，queued attempt 的 execution context packet 补齐项目、需求、任务、员工、run、runtime 与 handoff contract 信息，兼容旧 Runtime project-task writeback 依赖的 run/task 绑定。
- 2026-06-20 03:06：ProjectTask durable closure Phase 1 补齐任务排队写入入口：新增 ProjectTask/Attempt 状态常量、attempt domain/repository contract、`QueueProjectTask` service 方法、`project_task_attempts` sqlc 查询与 PgRepository 事务实现；排队时写入 `project_task.dispatched` 事件、创建 queued attempt、更新任务为 queued 并绑定 current_attempt/attempt_count，补充 service memory repo 与 PgRepository 回归测试。
- 2026-06-19 02:44：新增数字员工 operational status 读模型，Control Plane 基于 Runtime、Provider、run、project task 与员工级人工决策事实解析 `working`/`idle`/`queued`/`waiting_human`/`error`/`unavailable`/`needs_configuration`，并保持 `project_acceptance` 为项目级状态、不投影成每个员工的 `waiting_human`；overview API/OpenAPI/TypeScript 类型新增 `operational_status_counts` 与 `operational_state`，Web 数字员工工作台改以 operational status 作为主徽标并保留 `workbench_status` 兼容信息。新增 employee operational resolver、SQL facts、sqlc 映射、索引迁移、API route 测试、Web 类型与浏览器测试，并以当前 worktree 的独立 Control Plane/Web 真实链路验证 overview 和员工页渲染。
- 2026-06-18 23:04：项目验收闭合(Gap 3):项目全部 demand 到达终态后,协调器自动把项目转入 `acceptance` 并向负责人发起一条 `project_acceptance` 决策(复用既有 approval + decision_request + inbox 投影,点亮「待确认」待办);人工 `accepted` → 项目归档(archived)并写验收结论,`rejected`/`needs_more_evidence` → 项目回 `running` 返工,均经 `SignalHumanDecisionSubmitted` 回流。新增 sqlc `CountProjectDemandsByTerminality`/`TransitionProjectStatus`、repo `AreAllProjectDemandsTerminal`/`TransitionProjectStatus`,协调器 `IsProjectAcceptanceReady`/`RequestProjectAcceptanceReview`/`ApplyProjectAcceptanceDecision` 活动与 workflow 触发+回流分支;状态迁移用前向守卫保证幂等(重复完成/重试不重复建验收请求)。无 schema 变更(`decision_type` 为 VARCHAR、`projects.status` 复用既有列)。补充 store 单测(幂等、accept→archived、reject→running)与 Temporal workflow 触发+决策回流测试。
- 2026-06-18 15:41：任务完成时自动把数字员工回写的结构化 `evidence_refs`/`artifact_refs` 物化进 `/projects/{id}/evidence`、`/artifacts` 读模型，复用既有 `CreateEvidenceRefWithEvent`/`CreateArtifactRef` 写入路径并产出 `project.evidence.linked`/`project.artifact.linked` 审计事件,使运行总览的「证据/工件」卡片有真实数据;解析器容忍字符串或 `{ref/id/title/type}` 结构、缺引用自动跳过,物化为完成主链的最佳努力步骤(失败仅审计、不回退已完成任务),完成状态守卫保证一次任务只物化一次。补充解析器纯函数单测与完成路径物化事件断言。

### Changed

- 2026-06-23 10:02：成本中心 `/costs` 继续迁移到 v3 Soft-Flat：保留 `project_id` search 参数、`CostsProjectView` 与项目预算摘要/流水接口调用，路由标题和无项目上下文状态统一改用 `V3PageHeader`、`IconTile`、`WorkSurface` 与 `V3EmptyState`，移除该路由旧 glass 卡片与旧语义图标引用。验证：新增 `/costs` 路由浏览器测试覆盖无项目态、项目预算接口透传、v3 surface 和旧 DOM 归零。
- 2026-06-23 02:26：审计中心 `/audit` 继续迁移到 v3 Soft-Flat：保留 `project_id` search 参数与 `/api/v1/audit/events` 真实接口查询，页面标题、无项目上下文、加载、错误、空状态和项目审计表格统一改用 `IconTile`、`WorkSurface`、`V3Table`、`StatusPill` 与 v3 状态组件，移除该路由旧 glass 卡片与旧状态徽标引用。验证：新增 `/audit` 路由浏览器测试覆盖 v3 surface 和旧 DOM 归零；`corepack pnpm --filter ./apps/web run typecheck`、`run build`、`run test` 通过；重启真实 Web 后用 admin 会话打开 `/audit?project_id=55da605e-8083-42a0-978c-e4d031c33451`，`/api/v1/audit/events` 返回 200，桌面与 390px 移动端旧 DOM 为 0 且无横向溢出。
- 2026-06-23 01:53：Runtime 节点、权限中心与共享占位菜单继续迁移到 v3 Soft-Flat：`/runtime` 改用 v3 指标卡、`WorkSurface` 节点/事件表格、`StatusPill`、软卡片和 v3 筛选区域，保留 Runtime 总览、接入审批、Provider 能力、事件筛选与批准/拒绝交互；`/permissions` 入口、授权概览、审计表、Runtime 范围、成员角色和权限诊断统一改用 v3 图标、Tab 容器、软卡片、`WorkSurface` 表格、`StatusPill` 与 v3 表单按钮，保留五个授权子面板原 API、表单校验和确认弹窗行为；`UnimplementedPage` 改用 v3 软卡片与图标，保持占位路由“不使用 mock 数据”语义。验证：本批目标测试 19 个通过，迁移范围旧 glass 符号 grep 为 0。
- 2026-06-23 01:36：项目管理、流程编排入口与收件箱继续迁移到 v3 Soft-Flat：`/projects` 列表与运行详情改用 v3 指标、`WorkSurface` 表格、`StatusPill`、治理 Tabs 和软卡片，删除已无调用点的旧项目列表/Workflow 实例卡片；`/workflows` 入口改为 v3 signature + 指标 + 流程实例表格；`/inbox` 改为 v3 指标、筛选面板、待办表格和动作 Dialog。保留原接口、筛选、分页、项目详情回调、Inbox 操作和 TanStack Router 跳转。验证：本批目标测试 55 个通过，`corepack pnpm --filter ./apps/web run typecheck`、`run build`、`run test` 通过；重启真实 `control-plane`/`web` 后用 admin 会话打开 `/projects`、`/workflows`、`/inbox`，真实数据加载成功，无页面崩溃、无横向溢出，本批实际渲染范围旧 glass slot/class 计数为 0。
- 2026-06-23 00:51：登录后首页与 Shell 迁移到 v3 Soft-Flat：`/` 工作台改用 `V3PageHeader`、`SignatureCard`、`V3MetricCard` 与 `WorkSurface` 登录日志表格，保留原 `getHealth`、`/api/auth/me`、`/api/auth/login-logs` 数据路径；现有左侧栏/Header/Auth 布局只重新着色为中性 v3 背景、白色侧栏、蓝色激活态和弥散阴影，不改成顶栏。验证：Dashboard/Shell 定向测试通过，`corepack pnpm --filter ./apps/web run typecheck`、`run build`、`run test` 通过；重启真实 `control-plane`/`web` 后用 admin 会话打开 `/`，`/health`、`/api/auth/me`、`/api/auth/login-logs` 均返回 200，桌面与 390px 移动端无页面横向溢出，Dashboard/Shell 旧 glass class 计数为 0。
- 2026-06-23 00:30：登录页 `/login` 迁移到 v3 Soft-Flat：认证壳层改用中性 v3 背景与软卡片，登录表单输入、错误态和提交按钮改用 v3 token 与 `V3Button`，保留原 redirect、登录接口调用、表单校验和 session 跳转行为。验证：登录页/表单定向测试通过，`corepack pnpm --filter ./apps/web run typecheck`、`run build`、`run test` 通过；重启真实 `control-plane`/`web` 后打开 `/login?redirect=/skills`，admin 登录返回 200 并跳转 `/skills`，移动端 390px 无横向溢出且页面无旧 auth/liquid class。
- 2026-06-23 00:17：技能管理首页迁移到 v3 Soft-Flat 样板页：新增复用型 `v3-components`（SoftCard、V3MetricCard、StatusPill、IconTile、WorkSurface、V3Table、V3Button、V3Segmented、状态覆盖组件等），技能市场首页改用 v3 token 和软壳/脆数据面容器，保留原搜索、筛选、分页、上传入口与列表/网格切换行为。验证：`corepack pnpm --filter ./apps/web run typecheck`、`run build`、`run test` 通过；重启 `control-plane`/`web` 后用真实 admin 会话打开 `/skills`，`/api/v1/skills` 返回 200。
- 2026-06-22 19:15：重构技能管理首页为技能市场视图：保留技能上传入口，新增市场指标、搜索筛选、列表/网格切换、风险/状态/绑定目标展示，以及“查看详情”“安装”占位按钮；详情和安装流程不在本次实现范围内。验证：`corepack pnpm --filter ./apps/web run typecheck`、`corepack pnpm --filter ./apps/web run test`、`git diff --check` 通过；真实链路将在合并到 `main` 后用当前 `main` 页面连接真实 Control Plane 复验。
- 2026-06-21 02:15：修复真实 OpenFGA 1.18 链路验证暴露的 tuple 写入兼容问题：`OpenFGAHTTPClient.WriteTuples` 现在接受 Write API 成功返回的 `200` 或 `204`，并在 writes/deletes payload 中分别带 `on_duplicate: "ignore"`、`on_missing: "ignore"`，确保 backfill、单记录 upsert/revoke 和重试同步具备幂等性。
- 2026-06-21 01:44：`scripts/dev-services.sh start openfga` 改为默认优先使用本机已安装的 `openfga` CLI 启动，自动执行 `openfga migrate` 后以 SQLite 文件 `.scratch/openfga/openfga.db` 运行服务，并继续支持 `SUPERTEAM_DEV_OPENFGA_MODE=compose` 走 Docker Compose；新增本机 CLI 启动、状态与停止的脚本回归测试。
- 2026-06-21 00:26：需求路由 planner HTTP 调用去 DeepSeek 专名化，重命名为 OpenAI-compatible planner/client/request/config 边界；生产装配继续从 `planner.baseURL`/`planner.model`（或对应环境变量）取模型与 endpoint，不再在代码默认值里绑定具体 DeepSeek provider/model，`config.example.yaml` 改为通用占位并标注 DeepSeek 与千问兼容端点示例。补充 qwen 模型请求构造与 provider-neutral 错误文案测试。真实链路验证：重启运行中 Control Plane 后，通过真实 admin 会话向现有项目提交需求，协调线程执行 `PlanDemandRoute` 并生成 route decision、task graph 节点与待审核决策，无 chat/model/provider 错误。
- 2026-06-20 23:49：修复项目需求编排到人工验收的真实闭环断点：Inbox API 对空 actions 返回空数组并在 Web 列表兼容旧数据；项目决策待办允许无 approval_request_id，使 `project_task_acceptance` 可投影到 Inbox；高风险/需验收任务完成后会生成验收待办，人工批准后将 `waiting_human` 项目任务推进到 completed；必需人审的新需求在 planner 输出不合规时按策略合成“执行任务 + route review”计划；项目无 team_id 时团队借调闸门 fail-open，避免把执行员工池过滤为空。真实链路验证：Web 提交需求 → route review 人工批准 → Runtime Agent 执行 → task acceptance 人工批准 → demand/task 均 completed。
- 2026-06-19 11:12：收窄数字员工 operational status 的 `queued` 口径：项目任务只有 `planned`/`assigned` 会计入员工级“排队”，`pending` 仍视为 intake/pre-dispatch 事实，`blocked` 交由项目/流程阻塞、员工级 `waiting_human` 或 `error` 等更高优先级状态表达；同步更新 sqlc 查询、状态文案、后端断言测试和状态口径 spec。
- 2026-06-18 21:43：修复项目任务派发后永不回写导致的闭环断裂:Runtime Agent 仅在 `handoff_contract.completion_path == "project_task_writeback"` 时才回写项目任务完成,而 control-plane 此前原样透传 planner 的 handoff_contract(该字段时有时无),导致任务跑完却静默跳过回写、项目任务恒停 `assigned`、需求不收敛、证据不上浮。现 control-plane 在 `DispatchProjectTask` 派发 metadata 里强制 `completion_path = "project_task_writeback"`(新增 `projectTaskDispatchHandoffContract`);并加 Runtime Agent 兜底——当 `source == "project_task_dispatch"` 且有 `project_task_id` 时,缺省 `completion_path` 也按回写处理(显式非匹配值仍尊重),使运行链路对 CP 漏设该字段更健壮。补充 Go 派发断言与 Rust 单测。
- 2026-06-18 20:37：协调器执行人选择改为只考虑 runtime-ready 数字员工:新增 `digital_employee_runtime_readiness` 视图(migration 022),把 overview 的 runnable 判定(employee/execution status、effective_config、runtime_node 在线、provider 健康、agent_home_dir、governance approved 等)提升为单一权威读视图,杜绝重复判定漂移;配套 `AreEmployeesRuntimeReady` sqlc 查询与 `employee.Repository.AreRuntimeReady`。协调器 `LoadProjectCoordinationSnapshot` 经注入的 `DigitalEmployeeReadinessChecker` 过滤掉未就绪成员,使推理 planner 只把任务派给真正可执行的员工,修复之前选中未绑定 runtime 的员工导致任务静默 stranded(`assigned` 无 run、`/claim` 恒 204)的断点;checker 缺省/报错时 fail-open 不阻断规划。
- 2026-06-18 16:10：需求路由规划改为「仅推理模型」:删除生产路径里的非推理 heuristic 兜底(`HeuristicRoutePlanner` 仅保留为测试桩),`routePlannerFromConfig` 一律构造 DeepSeek 推理规划器,`PlanDemandRoute` 活动在规划器报错或缺省时直接抛错而不再静默降级为全量派发。为让推理模型规划真实跑通,将 planner HTTP 请求超时从 20s 提到 120s、协调器活动 `StartToCloseTimeout` 从 30s 提到 180s;并放宽任务字段解析,推理模型把 `input_requirements`/`handoff_contract` 输出为数组或标量时归一化为对象(数组→`{items:[]}`、标量→`{value:...}`)而非判失败,修复 `deepseek-v4-pro` 规划真实返回被 schema 形状误拒导致需求恒停 planning_pending 的链路阻塞。
- 2026-06-17 03:15：重构流程编排为“入口实例卡片 + 任务图工作台”两层体验，入口页只展示工作流全局状态、进度、阻塞和最新事件，详情页接入真实 task graph 读模型并以 React Flow 展示任务节点、阶段摘要、Runtime run、人工决策和右侧节点检查器；Workflow Instances API 同步补齐进度、优先级、风险、SLA、阶段统计和节点阻塞/状态原因字段，并支持通过环境变量追加本地调试 Console CORS origin 以完成多端口真实浏览器验证。
- 2026-06-16 21:06：补齐项目需求生命周期收口，`project_demands.status` 新增 planned/executing/completed/failed 终态，并在任务图规划（planned）、任务分派（executing）、全部项目任务完成或失败（completed/failed）的写回点按前向守卫推进，与项目事件序列共用 per-project 咨询锁避免并发回退；新增 `UpdateProjectDemandStatus`/`CountProjectTaskStatusesByDemand` sqlc 查询、OpenAPI 枚举与迁移注释，并以真实 Postgres 集成测试验证需求从 planning_pending 走到 executing 再到 completed，修复需求恒显示“规划中”的状态失真。
- 2026-06-16 03:43：Web 流程编排详情页新增只读 `@xyflow/react` ProjectTask DAG，可从真实 task graph read model 渲染任务节点、依赖、人工决策附件、Runtime run 和执行结果，并提供节点详情检查器。
- 2026-06-16 00:09：Control Plane 新增 Workflow Instances read model API，基于项目需求事实返回当前 Console 用户可见的工作流实例摘要、进度、状态优先级和协调作业引用，并补齐 sqlc、HTTP route 与 OpenAPI 契约。
- 2026-06-14 22:08：项目协调底座新增 DAG 规划与任务图执行能力，包含 DeepSeek/OpenAI-compatible planner 配置、ProjectTask 图与依赖持久化、ready 节点分派、完成契约校验、失败恢复决策、任务图 read model，以及 Runtime Agent 对 ProjectTask completion writeback 的真实执行桥接。
- 2026-06-14 01:59：Runtime Agent 新增 Codex Provider 接入，包含 provider catalog 配置、Codex CLI JSONL adapter、`.codex` 工作区隔离、Runtime command/legacy task provider selection、capability 上报，以及 HTTP/CLI smoke 路径和回归测试覆盖。
- 2026-06-13 22:24：Control Plane 消费 Runtime `sync_workspace_files` 终态回写，按 receipt 目标更新数字员工 workspace file sync 投影，成功记录 synced hash，失败记录错误信息，并避免误触发 provisioning 生命周期状态或清理逻辑。
- 2026-06-13 03:01：Control Plane: 打通 ProjectTask 协调分派到 DigitalEmployeeRun 的执行桥接，成功分派先创建真实 run 再原子绑定 digital_employee_run_id/runtime_task_id（绑定先于事件，避免孤儿 run），重复分派与 Temporal 重试幂等；失败时按可重试性记录分派失败事件，终态失败标记为不可重试，单个任务分派失败不再中断同批其余任务。
- 2026-06-12 21:20：新增“任务发起”一级入口，替换原主导航“任务中心”，支持按项目提交需求、自动或显式选择人类审核人，并提供任务发起详情页追踪协调 Job、路由决策、项目任务和人类决策请求等首版协调事实。
- 2026-06-12 23:21 新增收件箱可操作工作队列：增加 `inbox_items` read model、Inbox API、审批/项目决策投影、左侧菜单入口和 `/inbox` 页面，支持个人待办、团队只读视图和轻量处理动作。
- 2026-06-12 16:25：新增当前登录用户账户自服务页 `/settings/account`，支持查看个人资料、DiceBear 头像、自己的最近登录记录，并通过自服务 API 修改资料和密码。
- 2026-06-12 11:45：补齐项目管理 V2 Web 治理写入入口，项目详情支持跳转审计/成本视图，并可在证据链、验收结论、归档预览中新增证据、验证证据、提交验收和生成归档快照。
- 2026-06-11 23:52：完成项目管理 V2 治理证据归档闭环，新增项目证据链、工件与报告引用、预算流水、验收结论、归档快照、归档工件保留锁、配置修订历史，以及审计中心和成本中心 project_id 联动。
- 2026-06-11 15:45：项目管理 V1 接入 Temporal 虚拟协调线程，新增 RouteDecision、CoordinationJob、ExecutionSummary、TransferRequest、人类决策投影和 Workflow signal 可观测能力。
- 2026-06-11 04:01：项目管理 V0 新增真实项目管理入口、项目事实模型、项目事件流、配置治理页与提交需求到当前项目能力。
- 2026-06-10 13:44：完成双层技能管理主链路收口，贯通团队公共技能/MCP 与数字员工个人技能/MCP 的 Control Plane 契约、服务接口、Web 管理入口和回归测试，形成团队共享能力与员工个性化配置分层治理能力。
- 2026-06-10 13:28：数字员工配置页升级为三页签工作台，新增宪法/人格工作目录文件编辑、个人技能与 MCP 配置，并保留高级 JSON 配置修订表单。
- 2026-06-10 13:11：团队详情“能力与知识”页签新增公共技能与公共 MCP 管理界面，支持查看已安装团队技能、从技能市场安装、移除技能，以及创建和移除团队 MCP 服务并绑定可选用户凭据。
- 2026-06-09 01:03：新增数字员工详情页单次调用测试 Dashboard 静态原型，参考 paperclip Agent 页的信息架构，突出调用记录、端到端健康、单次测试入口以及团队共享能力与员工覆盖关系。
- 2026-06-07 19:50：新增数字员工内置头像库，提供 20 个工程师头像资产、Control Plane 头像资产列表 API、创建时 `avatar_asset_id` 校验与 metadata 快照，并在 Web 创建、列表和详情页展示员工头像。
- 2026-06-07 15:35：数字员工页面新增"配置"功能，支持创建配置修订版本（Config Revision），包括 Role Profile、Constitution Addendum、Capability Selection、Context Policy Override、Approval Policy Override 和 Output Contract Addendum 的 JSON 配置编辑。
- 2026-06-07 02:28：Runtime Agent 完成执行能力底座核心模块开发，新增按执行实例隔离的工作目录管理、S3 工件上传基础设施和 Provider 会话状态扩展支持。
- 2026-06-07 02:52：Runtime Agent 执行器适配新工作目录结构，execute_task 使用 create_run_workspace 创建 workspace/logs/artifacts 隔离目录，完成端到端执行闭环基础改造。

### Changed

- 2026-06-22 18:25：重构上传技能页 `/skills/upload` 的单页工作台视觉布局：参考新原型改为顶部 zip 包状态带、左侧技能信息与运行依赖声明、右侧发布摘要卡片；保留技能包描述名称由 zip 文件名生成、技能中文名称单独填写、CLI/环境变量仅声明不检测的业务语义，并修复 CLI/环境变量逐字输入时 token 被截断为单字符的问题。验证：`corepack pnpm --filter ./apps/web run test`、`corepack pnpm --filter ./apps/web typecheck`、`git diff --check` 通过；本地 Web/Control Plane/Runtime 服务运行中，真实 Chromium 登录 `admin/admin` 后打开 `/skills/upload`，选择示例 zip 并填写依赖声明，桌面与 390px 移动端无横向溢出，逐字输入 `gh,node` 与 `GH_TOKEN,OPENAI_API_KEY` 可正确保留完整 token。
- 2026-06-18 15:45：对齐流程编排工作台与真实期望的偏移，移除流程详情页左侧「流程实例」侧栏（其操作顺序信息已在右侧流程图中渲染，属冗余），详情页改为全宽流程图；流程图节点详情由原右侧固定卡片改为点击「数字人执行任务」节点才弹出的居中弹窗 Dialog，进入详情页不再预选节点、弹窗不自动弹出，点击画布空白或关闭按钮收起弹窗；同步删除已无消费者的 `workflow-instance-list.tsx` 与节点检查器的固定卡片分支，并更新相关组件测试。
- 2026-06-17 04:02：项目创建抽屉改为基于当前登录人授权范围选择团队，加载 `/api/auth/me` 与用户可选团队范围，只允许提交 active 授权团队，并默认使用当前用户作为项目负责人。
- 2026-06-17 02:41：重做用户管理中新建人类平台用户流程，改为抽屉式表单，直接设置用户名、名称、密码、内置头像资产和多选可选团队，并在用户详情中展示可选团队范围。
- 2026-06-16 00:55：调整登录页品牌布局，移除右上角品牌横幅，裁剪主展示图透明留白并放大主视觉，同时压紧品牌图与账号登录卡之间的垂直间距。
- 2026-06-16 00:35：重做“炬枢平台 / 新炬网络”品牌资源，使用 gpt-image2 生成更高对比的控制平面 mark，并由本地精确排版合成透明登录展示图、右上角横幅和侧栏 mark，避免图片与浅色登录背景混色后不可读。
- 2026-06-16 00:11：调整“炬枢平台 / 新炬网络”品牌展示，放大认证后 Shell 左上角品牌 mark 与文字，并取消登录页品牌图的背景混合模式，改用原色透明 PNG、轻量光晕和对比度增强提升可读性。
- 2026-06-15 23:32：登录页与认证后 Shell 品牌替换为“炬枢平台 / 新炬网络”，新增 gpt-image2 生成后重排并透明化处理的登录横幅和主展示图，登录页使用品牌图融入背景，侧栏左上角使用图片 mark 搭配可读品牌文字。
- 2026-06-14 10:28：收紧 `AGENTS.md` 与 `$superteam-completion-check`，将前后端、跨层、Runtime/Provider、数据库/迁移和合并后的真实端到端验证提升为默认完成硬门禁；缺少服务、认证、Provider 或安全环境时必须标记阻塞，不能以“未做真实链路验证”作为完成说明。
- 2026-06-13 11:48：优化任务发起页项目、审核人、优先级和风险级别字段展示，改为响应式参数条布局，减少宽屏两列布局造成的大空档并保持移动端自动换行。
- 2026-06-13 11:45：优化任务发起页低高度与移动端自适应布局，将提交需求和保存草稿提升到首屏标题区可见位置，并固定侧边栏激活项在不同主题测试顺序下保持白色文字。
- 2026-06-13 11:10：重构 Web 任务发起页为提交前需求工作台，收敛为需求、项目、审核人、优先级、风险级别和补充资料，右侧仅用编号说明提交后的动态编排顺序，避免提前展示编排状态或上下文边界配置。
- 2026-06-13 10:58：进一步瘦身 `AGENTS.md`，移除阶段性参考、完整技术清单、常用命令和重复收尾检查，将宪法收敛为项目定位、架构职责边界、主栈边界、数据库入口、目录边界和核心协作不变量。
- 2026-06-13 10:53：精简 `AGENTS.md` 目录边界，删除完整目录树，保留 Web、Control Plane、Runtime、contracts、connectors 和 HTML 原型的关键放置规则，降低宪法上下文噪音。
- 2026-06-13 10:47：将 `$superteam-completion-check` 从全局 Codex skills 迁移为项目内 `.codex/skills/superteam-completion-check`，并调整 `.gitignore` 仅允许项目 skills 进入版本控制，避免 SuperTeam 专用规则污染其他仓库。
- 2026-06-13 10:45：扩展 `$superteam-completion-check`，从 `AGENTS.md` 提炼通用收尾检查项，覆盖变更日志、生成代码、数据库迁移、前端状态保持、真实浏览器验证、分层边界和项目/审批/人类决策不变量。
- 2026-06-13 10:43：新增本地 Codex skill `$superteam-completion-check`，将 SuperTeam 任务完成前的真实链路、运行中服务、数据库迁移和交付声明检查从 `AGENTS.md` 长规则中提炼为可复用收尾流程；`AGENTS.md` 改为短触发规则。
- 2026-06-13 10:36：收紧 `AGENTS.md` 开发宪法，要求跨层功能完成前区分 mock/局部验证与真实链路验证，确认运行中服务已加载当前代码，并验证数据库迁移实际落库后才能声明功能可用。
- 2026-06-13 10:29：修正收件箱 `016_inbox_items.sql` 对应的 Atlas checksum，并完成真实 Control Plane 链路验证，避免迁移未落库时 `/inbox` 页面长期停留在加载态。
- 2026-06-12 19:04：Web Browser-mode 测试脚本改为按最低支持 revision 选择本机已安装的最新 `chromium_headless_shell-*` 可执行文件，避免测试环境被锁死到单个 Playwright 缓存 revision。
- 2026-06-12 18:38：优化数字员工创建页专业模板卡片的宽屏自适应布局，模板区在大屏下切换为三列展示，并移除与模板选择无关的头像堆叠，改为展示默认角色等后端模板字段。
- 2026-06-12 18:13：数字员工创建页改为“选择路径/专业模板/预检摘要”与“员工画像蓝图配置”两段式工作台，并让创建候选接口返回 `creation_checks` 服务端预检摘要，保证前端预检展示与 Control Plane 创建校验口径一致。
- 2026-06-13 00:10：修复收件箱项目 ID 和目标用户 ID 筛选，非法或 nil UUID 不再进入查询参数，并为无效输入保留草稿值和中文校验提示。
- 2026-06-12 23:51：收件箱页面新增可操作筛选栏，支持按状态、事项类型、风险等级、项目 ID 和目标用户 ID 查询，并在筛选变更时重置分页偏移。
- 2026-06-12 23:36：收件箱团队只读视图接入现有授权器，具备租户级 `team.read` 权限时开放团队列表和 badge 团队数量，未授权仍按个人范围返回，并补充团队待办查询索引。
- 2026-06-12 16:17：收紧创建数字员工身份步骤的头像选择区，头像项改为固定小尺寸并自动换行，避免宽屏下圆形头像被网格撑得过大。
- 2026-06-12 16:08：数字员工工作台员工卡片列表改为前端分页浏览，默认每页 12 个，翻页和每页数量会下传 overview `limit/offset`，避免一次性拉取和渲染全部员工。
- 2026-06-11 17:19：补充项目管理 V1 后端端到端仿真测试，覆盖需求 Workflow signal 失败重试、Runtime 任务写回身份绑定、终态 signal 重试和读模型无重复事实。
- 2026-06-11 17:01：收紧项目管理 V1 协调运行态一致性，Runtime 写回需通过任务状态和数字员工运行记录绑定校验，高风险路由需等待人类审批后再分派，并为 Workflow signal 失败审计事件提供可重试入口。
- 2026-06-11 10:04：修复项目配置子路由被项目详情父路由吞掉的问题，确保 `/projects/{projectId}/config` 正确渲染配置治理页。
- 2026-06-10 13:59：修复双层技能管理后端终审问题，允许数字员工归属人管理个人配置/能力与用户自管理凭据，并对有效技能/MCP 结果去重且保留配置修订遗漏字段。
- 2026-06-10 13:54：修复数字员工高级配置表单的修订提交行为，改为只提交用户实际编辑过的 JSON 配置或预算策略，并在脏 JSON 字段无效时阻止保存。
- 2026-06-09 09:37：恢复 Runtime WebSocket 默认 Origin 校验，移除对所有跨源握手的全局放行，并补齐跨源连接拒绝的回归测试。
- 2026-06-08 22:37：调整数字员工卡片选中视觉，左侧选中标记仅在选中卡片显示且不承载团队或运行状态颜色，未选中卡片保持干净边界。
- 2026-06-08 22:31：优化数字员工卡片选中态，移除“选中/已选中”文字按钮，改为单层选中边框与轻量高亮，避免边框叠加。
- 2026-06-08 21:28：修复数字员工工作台筛选时清空旧数据造成的整页加载闪烁，并支持点击员工卡片本体切换选中状态。
- 2026-06-08 20:51：优化数字员工工作台布局，将筛选区收敛到员工卡片列表同宽左列，右侧待处理队列和选中员工面板提升到首屏顶部，并把员工卡片团队名合并到身份元信息行以减少空白。
- 2026-06-08 03:05：数字员工首页改为执行工作台卡片视图，新增每日 Token 预算治理配置、overview 预算摘要和运行前预算拦截。
- 2026-06-07 23:18：深度统一二级页面与表单页（如团队详情页、数字员工创建与配置页）的头部图标样式，将其从黑白描边容器全面替换为彩色的 `SemanticIconTile` 语义容器，实现全站内外视觉高度一致。
- 2026-06-07 23:05：统一全站所有业务模块与建设中占位页（任务中心、项目管理等）的页头（Page Header）图标视觉规范，全面替换为 `size="lg"` 的彩色语义容器（`SemanticIconTile`），消除跨模块切换时的图标尺寸不一致与单调感。
- 2026-06-07 22:45：团队列表卡片设计引入高级玻璃拟物风格（Premium Glassmorphism），增加底层光晕微渐变和悬浮交互呼吸光效。同时完成数据库、OpenAPI及Go服务层的联席负责人（多负责人）底层改造，并在前端支持头像堆叠组件（AvatarStack），全面打通真实数字员工头像资产数据渲染链路。
- 2026-06-07 18:07：优化团队管理列表界面视觉：卡片在大屏设备上由 3 列变更为 4 列布局，提升宽屏空间利用率；顶部搜索及筛选工具栏重构，采用 Flex 悬浮条样式和项目的原生 `Select` 下拉组件替代原生 `<select>` 标签，使整体界面风格更现代、统一。
- 2026-06-07 16:15：重构团队管理列表页面为响应式卡片网格布局，替代原有的表格视图。新版卡片顶部高亮人类负责人，底部引入数字员工头像堆叠展示设计，并提供全局团队/Agent 规模统计。
- 2026-06-07 14:44：修复团队列表、团队详情数字员工入口和用户管理页内部跳转使用原生 `<a href>` 导致 Web Shell 与左侧菜单整页刷新的问题，统一改用 TanStack Router `Link`，并让侧栏菜单在详情/创建子路由保持父级选中态。
- 2026-06-07 02:28：Runtime Agent 工作目录从按 task_id 隔离重构为按 execution_instance_id + run_id 隔离，支持 logs/workspace/artifacts 子目录结构；旧版 create_task_workspace 标记为 deprecated。
- 2026-06-07 02:28：Runtime Agent 添加 aws-sdk-s3 依赖和 S3Section 配置支持，为工件上传到 S3 兼容存储做准备。
- 2026-06-07 02:28：ProviderEvent::SessionStarted 扩展支持可选的 session_state 字段，为 Provider 会话状态回写 Control Plane 奠定基础。
- 2026-06-07 02:28：重新设计数字员工列表工作台，新增 Control Plane overview read model，支持指标、筛选、执行端、最近运行、治理摘要和预算摘要展示。
- 2026-06-06 20:24：修复创建数字员工时专业类型默认能力和上下文未按团队治理策略收敛导致 effective config blocking validation 的问题，并避免创建页默认选择 disabled 团队。
- 2026-06-06 14:44：精简 Web 技能市场操作区，移除顶部“同步市场”和市场右侧“技能详情”上传卡片，技能市场卡片在桌面宽度改为三列展示。
- 2026-06-06 01:02：修复 Web 全局搜索按钮在移动端 Header 中按长占位文案撑破视口的问题，保证数字员工创建页移动端视觉验证不出现业务 Header 溢出。
- 2026-06-06 00:38：完成数字员工创建闭环，实现 Owner 归属、专业类型注册表、创建候选接口、ready 创建编排、前端四步创建向导、OpenAPI 与数据库迁移。
- 2026-06-05 18:05：优化 `AGENTS.md` 协作宪法，将项目、项目协调员、数字员工和人类归属人的规则收敛为宪法级边界；明确 Project 是场景中立的业务闭环容器，不定义封闭项目类型枚举，详细对象和流程规则保留在项目协调员控制平面设计文档中。
- 2026-06-05 11:12：补齐 Runtime command writeback 与数字员工 run API 的 OpenAPI/Go route/Rust client/TypeScript client 契约守卫，合并后新增接口会纳入 `verify:contracts` 门禁。
- 2026-06-05 10:52：合并后真实验证数字员工执行闭环，补齐 Runtime `provision_instance`、Provider 事件与终态 command writeback、`start_session` 输入 payload、Provider 能力真实探测和 `provider-run/v1` 任务隔离，避免 legacy polling 抢占数字员工 run。
- 2026-06-05 02:15：数字员工详情页接入执行闭环，支持开始任务、活跃运行轮询、事件流、结果、失败原因、历史运行切换和停止操作；Control Plane run events 改为读取真实 `task_events` 时间线，并补齐 run OpenAPI 契约。
- 2026-06-05 01:44：数字员工 Runtime provisioning preflight 改为严格执行团队 Provider 与 Runtime allowlist，空或缺失 allowlist 不再默认放行，并补齐真实 SQL 查询测试覆盖策略拒绝与 enrollment 撤销。
- 2026-06-05 01:37：数字员工 Runtime provisioning 创建链路补齐独立清理上下文、团队 Provider/Runtime 策略准入、Runtime session/enrollment 一致校验和 provisioning command payload 脱敏。
- 2026-06-05 01:18：数字员工创建改为 Runtime provisioning 主链路，创建时要求 Runtime 节点和 Provider 类型，完成 DB preflight、WebSocket 在线校验、`provision_instance` command 下发、receipt 等待与失败清理，避免 Web 看到半成品执行实例。
- 2026-06-05 01:04：Runtime command terminal 重放修复 task event 的 `raw_event_ref` 与 `log_ref` 投影，确保最小重放请求也使用已持久化 run 的结果与日志引用。
- 2026-06-05 01:00：Runtime command writeback 继续收紧终态重放：Runtime 事件 metadata 写入前脱敏，同状态 terminal 重放改用已持久化 run 事实修补 receipt、task event 和 Provider projection，并拒绝冲突的结果投影。
- 2026-06-05 00:52：Runtime command writeback 改为校验已认证 Runtime 节点身份，终态与 provisioning 回写在事务内锁定 command receipt 并修补重放投影，同时合并 Provider session patch 并脱敏 Runtime 来源的结构化敏感字段。
- 2026-06-05 00:30：Runtime command writeback 补齐数字员工执行实例 provisioning 成功/失败的 ready 与清理状态回写，并允许终态 run 重放已持久化事件序号保持幂等。
- 2026-06-05 00:08：数字员工 run Console API 分页参数改为按 int32 范围解析，避免超大 `offset` 在下传查询前发生整数截断。
- 2026-06-05 00:03：数字员工 run Console API 移除响应中的内部 `idempotency_fingerprint`，补齐列表与事件分页参数校验和默认值，并将 Runtime dispatch 失败统一映射为运行时不可用。
- 2026-06-04 14:49：Runtime Agent 删除旧版 `auth_token` 配置兼容入口，YAML、`RUNTIME_AGENT_AUTH_TOKEN` 环境变量和 CLI `--auth-token` 不再作为 `bootstrap_key` 兜底。
- 2026-06-04 14:33：数字员工 API 授权从复用 `runtime_scope.manage` 改为独立 `employee.*` 业务 action，并按 tenant 集合资源和单个 employee 资源记录授权决策，为后续 OpenFGA 渐进接入保留稳定边界。
- 2026-06-04 11:00：Web `Main` 布局组件改为默认铺满内容区，并新增 `contained` 窄版选项，后续控制台页面无需逐页传 `fluid` 即可复用权限中心式全宽布局。
- 2026-06-03 23:15：`AGENTS.md` 明确要求每条新增 `CHANGELOG.md` 变更记录带具体时间，默认使用本地 `Asia/Shanghai` 时间。
- Web 设计系统收敛为浅色液态玻璃控制台方向，`DESIGN.md` 明确蓝绿色主强调、液态玻璃卡片、胶囊按钮、语义图标和“只迁移 UI/UX 样式、不带入示例业务内容”的规则。
- Web 登录页、认证后 Shell 和工作台首页按浅色液态玻璃设计语言更新，统一按钮、卡片、背景、导航激活态、搜索框和输入控件质感。
- `DESIGN.md` 补充页面级和模块级 Tab 设计规则，明确玻璃胶囊激活态、底边流光锚点、中文长标签适配和 SuperTeam 组合组件复用边界。
- Web 主按钮和侧栏激活菜单降低左侧白色反光强度，避免高光压住图标和中文文字。
- Web Shell 背景色彩调整为参考图方向的淡青绿、暖米色和冷绿低饱和 wash，并将认证后控制台背景收敛为单一 `sidebar-wrapper` 背景源；顶部栏和内容容器保持透明，不再在刷新完成后覆盖外层背景，侧栏面板补充柔和 tint 并弱化与主页面之间的深色分割线。
- Web 侧栏 icon 折叠态收紧内容区内边距并居中导航按钮，修复图标贴近右侧边界的问题。
- Web 侧栏菜单默认字号从 14px 提升到 16px，并将菜单行高调高到 44px，提升导航可读性。
- Web 侧栏选中菜单改为清晰白色文字和图标，移除文字阴影并避免默认 active utility 覆盖导致文字发暗发糊。
- Web 暗色主题补充独立玻璃覆盖，修复左侧菜单、卡片和图标容器复用浅色白色高光导致的刺眼与可读性问题。
- Web 登录页暗色主题补充独立 Auth Shell、品牌标识和登录卡片覆盖，避免浅色背景与暗色文字 token 混用。
- Control Plane 初始数据库 schema 重写为 UUID-first 形态，合并早期 auth session、Web 登录日志、操作日志和中文注释迁移，并新增默认租户/团队骨架以支撑后续分布式与多团队数字员工管理。
- 任务、执行、审计、工件、Web 登录日志和操作日志改为应用层校验的 UUID 引用，避免跨模块重 FK 和级联删除；任务、工件、用户、Runtime 节点等核心实体补充软删除、禁用、归档、取消或终止时间戳。
- Runtime 任务状态机允许已领取任务直接进入 completed/failed，修复当前 claim -> events -> complete/fail HTTP 合约没有单独 running 接口时的完成链路阻断。
- 将数据库表设计规则从 `AGENTS.md` 沉淀到根目录 `DATABASE_DESIGN.md`，统一后续 UUID-first、租户/团队、索引、迁移、sqlc 与 OpenAPI 设计规范。
- 数据库迁移规范明确普通功能开发必须新增 forward migration，禁止回写已存在于 `atlas.sum` 的迁移文件，并将 rebuild-only 限定为需当次确认和写明备份、重建、验证命令的例外流程。
- Control Plane 请求日志新增 `remote`、`ua` 和 `referer` 字段，便于定位未知请求来源。
- Control Plane 与 Runtime Agent 本地开发配置统一收敛为 YAML 文件：Control Plane 使用被 Git 忽略的 `apps/control-plane/config/config.yaml`，Runtime Agent 使用被 Git 忽略的 `apps/runtime-agent/config.yaml`；Runtime Agent 示例配置从 TOML 切换为 `config.example.yaml`，并移除 Control Plane / Runtime Agent 的 `.env.example` 示例入口。
- Web 与 Control Plane 示例配置同步本地默认端口到 `8081`，避免前端示例 API 地址和后端示例监听端口不一致导致开发登录误报为用户名或密码错误。
- Control Plane 本地开发脚本默认加载 `apps/control-plane/config/config.yaml`，并兼容 `pnpm dev:control-plane -- --config ...` 的参数传递形式；配置入口统一以 YAML 文件为准。
- Web 控制台从旧 Next.js + 前端 workspace packages 结构激进重铺为 Vite + TanStack Router + shadcn-admin 单应用结构；前端 API client、认证状态、页面和 UI 组件集中到 `apps/web/src`，后端 Control Plane API 契约保持不变。
- Web 控制台移除 shadcn-admin demo 路由和 mock 数据页面，改为 SuperTeam 工作台、用户管理和任务/审批/审计等领域入口。
- Web shadcn-admin 路由接入 `AuthProvider`，认证守卫统一未登录跳转 `/login`，并将登出流程切换为 Control Plane session logout。
- Web shadcn-admin 登录表单从 mock cookie/token store 切换为 Control Plane cookie session auth，新增 `AuthProvider` / `useAuth` 负责加载当前用户、登录、登出和窗口聚焦后的会话刷新。
- 将一键验收脚本扩展为开发门禁入口：`pnpm verify:foundation` 现在聚合契约、TypeScript、Go 和 Rust 基础验证，并新增 `verify:web`、`verify:control-plane`、`verify:runtime-agent`、`verify:db` 领域门禁。
- 在 `docs/development.md` 中新增“开发验证门禁”，定义基础门禁、领域门禁、场景 smoke 和后续功能开发时的动态更新规则。
- Web 控制台在 Vite 环境下使用 `VITE_CONTROL_PLANE_URL` 配置 Control Plane 地址；未显式配置时继续跟随当前浏览器 host 推导，避免本地开发时 `127.0.0.1` 与 `localhost` 混用导致登录 Cookie 不被后续请求携带。
- 调整 Control Plane storage sqlc 查询集成测试：
  - 移除 testcontainers 本机容器 fallback，避免完整 Go 测试依赖 Docker/Podman。
  - 测试仅在显式配置 `TEST_DATABASE_URL` 和 `TEST_REDIS_URL` 时连接远端或专用测试环境运行；也支持通过 `ALLOW_DATABASE_URL_FOR_QUERY_TESTS=1` 复用 `DATABASE_URL` 和 `REDIS_URL`。
  - 未配置测试环境时跳过 `apps/control-plane/internal/storage/queries` 集成测试。
- Provider 会话事件新增关联 ID 约束，要求 `request_id` 或 `command_id` 至少填写一个，避免无法回溯到平台请求或命令的事件进入审计链路。
- 数字员工执行链路补强 Runtime 与 Provider 可用性校验：执行实例绑定前要求 Runtime 已批准且 Provider 能力可用，Provider 会话和事件写入会拒绝禁用、错误或失效的执行上下文。
- Provider 会话事件追加时重新校验会话、数字员工与执行实例仍处于可接收事件状态，防止关闭、禁用或错误的执行上下文继续写入事件流。
- Provider 会话创建与事件追加只允许绑定 `ready` / `active` 执行实例，防止 `provisioning` 等未就绪状态进入 Provider 执行链路。
- Runtime Enrollment 和 Runtime Session 查询补强租户、Bootstrap Key、批准状态与撤销边界校验，移除可绕过执行前置条件的数字员工执行实例直接插入查询。
- Runtime hello 接入写入固定为 pending 状态，并要求有效 Bootstrap Key 与 Runtime 节点外部 ID 匹配，防止 hello 绕过人工审批或错绑节点。
- Runtime Agent 启动链路从旧版长生命周期 runtime token 注册切换为 bootstrap key hello 接入，批准后使用短期 runtime session token 访问 heartbeat、任务领取和事件回传接口，并按真实 Control Plane contract 上报扁平 capabilities。
- Runtime Agent 建立短期 session 后会主动连接 Control Plane Runtime WebSocket 命令通道，并支持接收 `ensure_instance` 命令创建数字员工执行实例目录。

### Fixed

- 2026-06-15 22:47：修复项目需求到多数字员工执行闭环：heuristic planner 可按策略 fan-out 到所有 active executor 且不触发人工审核，ProjectTask 提示不再暴露内部写回机制，Runtime Agent 支持 Control Plane 轻量 `stop_session` payload 取消活跃 run 并回写 cancelled 终态，Claude Code Provider 默认使用 Runtime 治理的非交互权限模式，避免真实执行链路卡在人类审批或 Provider 权限提示。
- 2026-06-15 18:24：修复项目任务发起到 task-graph 的真实链路：提交需求和 workflow signal 重试前会幂等确保 Temporal 项目协调 workflow 存在，DeepSeek planner 请求增加短超时并在外部 planner 超时后回退到 heuristic，避免 `PlanDemandRoute` activity 被 30 秒 StartToClose timeout 打断而无法持久化任务图。
- 2026-06-14 10:32：修复 Runtime command terminal event 与 active-run registry 清理之间的竞态，Provider turn 已完成后立即移除活跃 run，避免 `stop_session` 在短窗口内误取消已完成执行。
- 2026-06-13 23:28：修复 Runtime Agent 数字员工工作目录初始化时 `CLAUDE.md` 被写成普通副本的问题，改为在 Unix 环境下生成指向 `AGENTS.md` 的兼容软链，并覆盖已有旧副本，保持 Claude Code 与 OpenCode 的工作目录只创建对应 Provider 配置目录。
- 2026-06-13 13:21：修复数字员工详情页发起测试任务时被 stale 活跃运行卡住的问题；Control Plane 会根据终态 Runtime command receipt 回收仍显示为 queued/dispatching/running/cancelling 的旧 run，列表返回终态状态，新建 run 也可在同次请求内继续分派。
- 2026-06-13 13:38：修复数字员工显示 Ready 但 Runtime 命令通道未连接时仍可发起测试任务的问题；Runtime overview 现在返回实时 command channel 连接状态，数字员工详情页会在通道断开时禁用开始任务并给出原因。

### Added

- 2026-06-06 09:44：完成技能管理主链路，新增 Control Plane 技能包、文件、团队归属和 Agent 安装绑定 API，Web 技能页支持已安装技能树、Monaco 文件编辑、技能市场上传标签展示和彩色图标，以及 zip 弹窗上传并绑定多个团队。
- 2026-06-05 21:55：新增数字员工创建闭环设计规格，明确专业执行型数字员工创建为 `ready` 准备态，引入 `owner_user_id`、服务端专业类型注册表、创建候选接口和分步创建向导；项目协调员不进入数字员工创建模型。
- 2026-06-05 18:35：Web 控制台在“平台管理”下新增“成本管理”入口和 `/costs` 占位页，预留按数字员工查看 token 消耗成本以及每日、每月 token 成本统计视图，后续实现参考 `paperclip` 的 costs 模块。
- 2026-06-05 16:22：为项目协调员控制平面设计补充 gpt-image2 生成的架构流程图和项目内数据流转图，便于对齐 Project、Coordinator、执行员工、Runtime、Provider、证据和人类决策关系。
- 2026-06-05 16:06：新增项目协调员控制平面全局设计总结，明确一个项目一个协调员数字员工、人类归属人、RouteDecision/RouteOutcome、预算、置信度扩散、证据链和人类驳回机制。
- 2026-06-05 16:12：Runtime 节点页升级为总览工作台，新增 Runtime overview/events Console API、`runtime_events` 统一事件流、Provider 能力聚合、接入批准/拒绝操作和事件审计 Tab；本期不包含 Runtime 详情页、接入密钥和诊断包功能。
- 2026-06-05 01:00：Web 控制台新增“技能管理”“项目管理”和“协作集成”一级菜单及占位页，其中“协作集成”用于后续接入钉钉、飞书等企业通讯软件的消息交互、审批触达和结果通知。
- 2026-06-05 00:24：Control Plane 新增 Runtime command HTTP writeback API，Runtime session auth 可回写 provider 事件、完成、失败、取消和超时终态，并将事实持久化到 command receipt、task run/event 与 Provider session/event 双投影。
- 2026-06-04 23:48：Control Plane 暴露数字员工 run Console API 路由，支持创建、列表、详情、事件查询和停止，并接入独立 `employee.run.*` 授权 action 与真实 app wiring。
- 2026-06-04 14:28：新增 `scripts/dev-services.sh` 本地开发服务管理脚本，支持 Control Plane、Web 和 Runtime Agent 的状态检查、启动、停止和重启，并以 `.scratch/dev-services` 记录 PID 与日志；新增脚本级测试覆盖启停与重启流程。
- 2026-06-04 10:38：用户管理页按“用户 360 详情台”方向升级为主从详情工作台，使用权限中心一致的铺满页面布局，并调整为更宽的用户列表和三等宽概览卡片；页面接入用户列表、权限中心成员角色、登录日志和授权拒绝记录，并补齐新建用户、启用/禁用账号和重置密码入口。
- 2026-06-04 05:20：团队列表搜索补齐负责人用户名、显示名和邮箱匹配，并修复新建团队失败后关闭重开抽屉仍显示旧错误的问题。
- 2026-06-04 05:02：团队管理体验补齐团队图标、用户头像身份展示、弱分页、目标化两步创建抽屉和详情成员页用户搜索，继续沿用浅色液态玻璃企业控制台设计风格。
- 2026-06-04 04:02：Runtime Agent 新增 runtime command execution layer，支持 `start_session`、`resume_session`、`send_input`、`stop_session` 在本地解析 payload、创建执行实例目录、驱动 Provider run、维护 command/session/run 映射并取消 active run；Runtime 仅执行本地命令，不判断租户、团队或审批。
- 2026-06-04 02:29：团队接口头像回显兼容历史用户空头像种子，按用户名生成稳定 fallback，并修复团队 metadata display 规范化时修改调用方输入的问题。
- 2026-06-04 01:49：用户管理补齐 DiceBear adventurer 头像配置字段、OpenAPI 响应和 Web 列表展示；Control Plane 存储头像来源、样式、种子和扩展选项，前端使用本地 DiceBear JS 包生成稳定 SVG 头像。

#### 团队管理权限底座 (2026-06-03)

- Control Plane `authz` 新增 OpenFGA-ready 团队管理 action 语义，覆盖团队 CRUD、禁用/归档/恢复、成员增删改、特权角色申请/批准、治理配置读写/批准、能力绑定和团队审计读取。
- `DBAuthorizer` 新增团队管理授权矩阵：租户 owner/admin 可创建并管理所有团队；团队 owner/admin 可维护本团队基础信息和普通成员；团队 owner 或 approver 可批准治理配置；直接添加/提升 owner/admin/approver 会要求走特权角色申请/批准语义，并保留最后团队负责人保护。
- 团队 API 路由停止复用 `runtime_scope.manage`，改为按路由发送 `team.create`、`team.read`、`team.governance.edit`、`team.governance.approve` 和 `team.governance.read` 等业务 action。
- 权限中心诊断契约和 Web 表单补齐团队管理 action 枚举，能按 action 自动派生 tenant/team resource，为后续 OpenFGA Authorizer 或 tuple 同步保留稳定 actor/action/resource 边界。

#### Web 数字员工创建流程 (2026-06-03)

- 数字员工页面新增创建草稿员工并预览生效配置流程，可基于团队当前治理配置生成个人配置修订，成功预览后提示可提交负责人确认，阻断错误或预览失败时给出页面反馈。

#### Web 团队管理页面 (2026-06-03)

- 新增团队管理控制台基础计划的列表摘要、详情概览、生命周期操作和前端详情框架。
- 新增团队成员管理、普通角色直接变更和高权限角色申请审批流程。
- Web 控制台新增“团队管理”侧栏入口和 `/teams` 页面，可查看团队负责人、当前治理配置修订号、宪法硬性规则、能力边界和内部协作自动轮次；当前治理配置缺失或加载失败时按单行降级提示，不阻断团队列表展示。
- 新增团队治理草稿、能力与知识绑定、治理策略编辑和批准生效流程。
- 新增团队详情中的数字员工入口和团队管理审计记录。
- 2026-06-03 23:29：团队列表 API 新增 `governance_status` 筛选和负责人摘要响应，前端后续可按设计稿展示负责人姓名/邮箱并区分草案待批准、已生效、未配置等治理状态。
- 2026-06-04 00:52：团队管理页补齐真实筛选工具条和右侧两步新建团队抽屉，创建团队时通过 Control Plane 事务一次性写入团队、负责人和初始成员，并以真实接口刷新列表。
- 2026-06-04 01:08：新建团队抽屉将负责人选择改为独立搜索，避免团队名称和负责人候选查询耦合，补齐加载、空态和选中态反馈。

#### 团队治理后端 (2026-06-03)

- Control Plane 新增租户团队领域服务、PostgreSQL repository 和 API 路由，支持团队负责人、共享治理配置版本创建与当前版本查询；相关路由由 Web 控制台会话认证保护，并统一经过 `team.*` 业务授权校验。
- 数字员工服务新增个人配置版本、生效配置预览与校验、审批落库能力；预览会阻断团队能力白名单外的个人能力、团队上下文范围外的上下文覆盖以及降低团队审批要求的个人审批覆盖。
- 数字员工创建新增同租户团队存在性校验；生效配置预览与审批只接受 active 团队治理配置版本，避免 draft 配置绕过团队负责人确认。
- 团队治理配置版本创建不再接受客户端指定 `approved_by`，审批归属由当前 Web 控制台登录用户在服务端注入。
- 新增 `003_add_team_governance_config.sql` forward migration，补齐已执行旧版 001/002 的远端库中的团队治理配置、数字员工个人配置和生效配置快照表。
- 数字员工生效配置审批要求已有 ready 或 active 的唯一执行实例，并新增 `/api/v1/digital-employees/{employeeId}/config-revisions`、`/effective-configs/preview`、`/effective-configs/approve` API 路由。
- OpenAPI 与基础契约守卫补齐团队治理、数字员工配置版本和生效配置预览/审批 API，确保新增后端路由纳入契约核验。
- Web API client 新增团队列表、团队创建、团队治理配置版本创建与当前版本查询能力，并补齐数字员工个人配置版本、生效配置预览和审批调用入口。

#### Web 液态玻璃组件化 (2026-06-03)

- 新增 `apps/web/src/components/superteam` 项目级设计组件层，沉淀 `LiquidCard`、`LiquidPill`、`PrimaryLiquidButton`、`SemanticIconTile`、`StatusBadge` 和 `MetricCard`。
- 新增 `LiquidTabsList` 和 `LiquidTabsTrigger` 复用组件，将页面 Tab 统一为玻璃胶囊选中态与底边流光指示，并迁移权限中心和团队详情页。
- 工作台首页指标卡改为复用 `MetricCard`，减少页面内手写玻璃卡片、语义图标和状态胶囊样式。
- 为液态玻璃设计组件补充 Vitest 浏览器组件测试，锁定组件 slot、核心 class 和基础渲染行为。

#### 数字员工后端与执行实例服务 (2026-06-02)

- 新增 Control Plane 数字员工领域服务、PostgreSQL repository 和 HTTP handler，支持创建草稿、列表、详情、状态更新以及唯一执行实例查询和绑定。
- 数字员工执行实例绑定复用现有 sqlc upsert 查询，默认绑定为 ready 状态，并通过 Web 控制台 cookie session 注入租户上下文保护 `/api/v1/digital-employees` 路由。
- Web 控制台新增数字员工页面和 API client，支持查看数字员工业务身份、状态、风险等级和唯一执行实例绑定信息。

#### Runtime 接入与短期会话服务 (2026-06-02)

- 新增 Control Plane Runtime Enrollment / Runtime Session 领域服务，支持 Runtime hello 接入、人工批准/拒绝/撤销、短期 session token 签发、校验与续期。
- Runtime session token 采用确定性 lookup hash 加 bcrypt secret hash 的双哈希模型，避免原始 token 或可直接校验的单一 hash 明文落库。
- Runtime hello 会扫描有效 Bootstrap Key 并用 bcrypt 校验原始 secret；pending 接入不会返回 session，approved 且已挂接 Runtime 节点的接入才签发短期 session。
- Runtime enrollment 撤销会使关联 active session 失效，session 校验与续期会重新检查接入仍处于 approved 且未撤销状态。
- 接通 Runtime Enrollment、短期 Session 续期与 Capability 上报 HTTP 路由；`/api/v1/runtime/enrollments/hello` 支持公开 bootstrap hello，`/api/v1/runtime/session/renew` 和能力上报要求短期 session token。
- 新增 Runtime session middleware，并让 heartbeat、claim、task event、complete、fail 和 lease 路由在迁移期同时接受短期 session token 或旧版 `Authorization + X-Node-ID` runtime token。
- Runtime enrollment 管理路由改为 Web 用户 cookie session 保护，并将 canonical session renew 与 capability upsert 成功响应对齐 OpenAPI 契约。
- Runtime enrollment 管理路由补充 `runtime_scope.manage` 授权校验，并将当前 Web 用户与租户上下文传入批准、拒绝和撤销操作。
- 修正 Runtime 接入服务的多租户路径：hello 阶段不再创建默认租户 Runtime 节点，改为仅写入 pending enrollment；批准阶段按租户创建或复用 Runtime 节点并 attach，session 校验改为按全局 lookup hash 查找，支持非默认租户续期。
- 将 Runtime enrollment 批准改为单条 SQL 原子完成 pending 校验、tenant-safe Runtime 节点 upsert 和 enrollment attach，避免并发拒绝/撤销后留下未挂接节点，并修复 tenant-aware node upsert 的全局 `node_id` 冲突竞态。
- 将 Runtime session token 默认有效期修正为 12 小时，并补充签发和续期过期时间断言。
- 新增 Runtime WebSocket Command Channel：Control Plane 维护 Runtime 连接注册表，短期 session 认证的 Runtime 可通过 `/api/v1/runtime/ws` 建立命令通道并接收 JSON command；旧版 Runtime token 不能访问该通道。
- 加固 Runtime WebSocket Command Channel 连接注册表：Dispatch 不再持有全局锁等待满载 channel，Runtime 重连和断开清理不会被阻塞，并补充 registry 未配置与 client close 后注销测试。
- Web 控制台新增 Runtime 节点页面，支持查看待接入 Runtime enrollment、批准接入请求以及查看已接入 Runtime 节点状态和 Provider 能力。

#### Control Plane 渐进式授权边界 (2026-06-01)

- 新增 Control Plane 渐进式授权边界：`internal/authz` 统一 `Authorizer` 接口，第一版使用 PostgreSQL 权限事实判断 Web 控制台访问和 Runtime claim 范围。
- `/api/auth/me` 登录后增加 `console.access` 授权检查，认证和授权保持分层。
- Runtime claim 任务前增加 `task.claim` 范围检查，Runtime 节点不能领取超出 `runtime_node_scopes` 的任务。
- 授权决策接入 `web_operation_logs`，记录允许/拒绝结果、授权引擎、命中规则、Actor、资源和租户/团队上下文，为后续权限审计视图和 OpenFGA backend 留出稳定审计底座。

#### 权限中心 MVP (2026-06-01)

- 新增 Control Plane 权限中心 API 契约和 `internal/authzcenter` 应用服务层，提供授权概览、授权审计、Runtime 范围、成员角色只读视图和权限诊断接口。
- 新增 Web 一级菜单“权限中心”和 `/permissions` 页面，包含“授权概览”“授权审计”“Runtime 范围”“成员角色”“权限诊断”五个 Tab。
- Runtime 范围管理支持新增租户/团队 scope 以及启用、禁用已有 scope；Web 表单按租户或团队自动派生只读范围值，避免提交不符合后端约束的 payload。
- Runtime scope 写操作统一经过 `runtime_scope.manage` 授权检查并写入 `web_operation_logs`，权限中心读接口通过 `authz_center.read` 做租户边界控制。
- 权限诊断通过统一 `Authorizer.Check` dry-run 返回授权结果，并按 action 自动匹配资源类型与必要字段校验。
- Web 权限中心 API client 与页面补充 Vitest 覆盖，锁定请求方法、请求体、Runtime scope 写入确认和诊断行为。

#### Web Vite 控制台重铺 (2026-05-31)

- 新 Web 壳接入 `shadcn-admin` 的侧边栏、顶部栏、主题、命令面板和响应式布局。
- 保留真实 Control Plane 登录、当前用户、退出登录和路由保护主链路，继续使用 cookie session 与 `credentials: "include"`。
- 新增 Vite 环境变量 `VITE_CONTROL_PLANE_URL`，保留本地 `localhost` / `127.0.0.1` host 对齐策略。
- 删除不再服务 Web 的旧 UI、views、core 和 api-client 前端 workspace 拆分。

#### Web 外部能力占位入口 (2026-05-31)

- 新增 Web 控制台一级菜单“外部能力”及 `/capabilities` 占位页。
- 页面先说明后续外部能力扩展范围，包括 Dify Workflow、Deephub Agent、企业内部 HTTP 接口、数据分析服务、ITSM、CMDB/监控/日志平台、自研脚本服务和 MCP Server/Connector。
- `/capabilities` 当前作为公开占位说明页，不要求登录态，避免点击一级菜单后被全局登录守卫重定向到登录页。
- 为工作台、任务中心、数字员工、流程编排、审批中心和审计日志补齐公开占位页；除用户管理这类真实管理功能外，未开发一级菜单不再跳转登录页。

#### Web 平台 Shell 完善 (2026-05-31)

- 新增路由驱动的 `ConsoleAppShell`，统一 Web 控制台菜单 active、面包屑、登录用户展示和登出入口。
- `ConsoleShell` 支持面包屑渲染，并保留业务页面通过插槽注入页面操作。
- Web 控制台新增平台通用 empty、loading、error、forbidden 状态组件，用户管理页接入列表加载、空数据和加载失败状态。
- 新增 Web 控制台 `not-found`、`error`、`loading` 和 `/forbidden` 页面，先形成公共异常与权限不足入口。

#### Web 用户管理 MVP (2026-05-31)

- 新增 Web 控制台用户管理页 `/users`，支持用户列表、创建用户、启用/禁用用户和重置密码。
- Control Plane Auth API 新增用户管理接口：`GET/POST /api/auth/users`、`PATCH /api/auth/users/{id}/status`、`POST /api/auth/users/{id}/reset-password`。
- 用户管理写操作接入 `web_operation_logs`，记录创建用户、启用/禁用用户和重置密码操作。
- `apps/web/src/lib/api` 新增用户管理 client 方法，并将认证用户 ID 对齐为后端用户主键 `int64`。
- 新增迁移为 `auth_users` 与 `web_operation_logs` 补充中文表注释和字段注释。

#### Web 会话闭环体验 (2026-05-31)

- Web 控制台 Shell 新增右上角用户菜单插槽，支持展示当前登录用户和账户操作。
- 首页接入 `useAuth()` 当前用户状态，移除顶部账户区域的硬编码用户展示，并提供退出登录操作。
- Web 认证状态在窗口重新聚焦时复查 `/api/auth/me`，遇到 401 会清空当前用户并交由认证守卫回到登录页。
- 前端 API client 增加带 HTTP status 的 `ApiRequestError`，供前端认证层稳定识别会话失效。

#### Web 登录主链路 (2026-05-31)

- 接入 Web 控制台登录、当前用户和登出 API，使用 `auth_sessions` 持久化浏览器会话。
- 在 `apps/web/src` 内新增 auth client、AuthProvider/useAuth 和登录页。
- Web 根布局接入认证守卫，未登录用户进入 `/login`，登录成功后返回控制台首页。
- 将浏览器 session token 以 SHA-256 hash 写入 `auth_sessions.token_hash`，避免原始 cookie token 明文入库。
- 收紧 Control Plane CORS，使携带 cookie 的本地 Web 调用只允许 `localhost:3000` 和 `127.0.0.1:3000`。
- 新增幂等开发账号迁移，方便本地使用 `admin / admin` 完成 Web 登录烟测。

#### Web 登录日志与操作日志底座 (2026-05-31)

- 新增 `web_login_logs` 表，独立记录 Web 控制台登录成功、登录失败和登出事件，不复用人工审核相关的 `audit_events`。
- 新增 `web_operation_logs` 表，预留 Web 控制台后续功能操作日志、资源操作结果和请求上下文记录。
- 登录、登录失败和登出链路接入 `web_login_logs` 写入；日志写入异常不阻断主登录链路。
- 新增 `GET /api/auth/login-logs` 查询接口，要求有效登录 Cookie，并支持 `limit` / `offset` 分页参数。
- 前端 API client 新增 `listLoginLogs`，供 Web 端调用登录日志接口。

#### Core Summary 状态映射 (2026-05-30)

- 为任务和 Runtime 节点 summary helper 增加稳定状态 tone，供后续 Web 页面复用。
- 为 Runtime 节点 summary 增加负载百分比，避免每个页面重复计算槽位占用。

#### API Client 最小任务与 Runtime 覆盖 (2026-05-30)

- 为前端 API client 补齐任务详情、任务状态更新、任务取消和 Runtime 节点详情的最小 client 方法。
- 通过 Vitest 锁定这些方法使用的 Control Plane canonical path。

#### Foundation 契约漂移检查 (2026-05-30)

- 新增 `pnpm verify:contracts`，检查 Control Plane OpenAPI、Go route、Rust Control Plane client 和 TypeScript api-client 的关键路径一致性。
- 新增 `pnpm verify:foundation`，聚合契约检查、TypeScript 测试、TypeScript 类型检查和 Runtime Agent Rust 测试。

#### Foundation Readiness 底座收口设计 (2026-05-30)

- 新增 Foundation Readiness 设计文档，明确在进入具体功能开发前采用 Web、Control Plane、Runtime Agent、contracts 与共享 packages 的均衡收口方案。
- 定义本阶段的维护性、可扩展性、可复用性标准，并明确不提前实现登录认证、Temporal、OpenFGA、完整业务页面和生产级 Provider 治理。

#### Web 真实数据接入底座 (2026-05-29)

- 为任务和 Runtime 节点补充最小 API client 与 core summary helper，后续页面可从 mock 数据平滑切换到真实接口。

#### Foundation fake provider 端到端验收 (2026-05-29)

- 新增 fake provider 风格的最小端到端验收，覆盖任务创建、Runtime 注册、claim、事件回传和完成状态。
- 对齐 Runtime Agent 客户端的节点注册/心跳响应模型，移除未在 Control Plane contract 中承诺的内部数据库 `id` 依赖。
- 将 Control Plane Runtime 写入端点接入 Runtime token + `X-Node-ID` 认证，并让 Runtime Agent 对心跳、claim、事件、完成、失败和 lease 请求携带节点身份。
- 修正 Runtime token 生成脚本，使其写入当前 `auth_runtime_tokens(node_id, token_hash, expires_at)` schema。

#### Foundation Hardening 设计 Spec (2026-05-29)

- 新增 Foundation Hardening 设计文档，明确 Control Plane 启动边界、sqlc 生成闭环、契约事实源、Runtime Agent daemon 边界、执行事件流和 Web 真实数据接入底座。

#### Web 根布局 hydration 兼容 (2026-05-29)

- 在 Web 根布局 `<html>` 上启用 `suppressHydrationWarning`，降低浏览器扩展向根节点注入属性时触发的 hydration mismatch 噪音，并补充对应布局测试。

#### Web 控制台通用骨架 (2026-05-29)

- 沉淀 Web 控制台外部系统骨架复用组件：新增 `ConsoleShell`、状态胶囊、图标徽章、指标块、分区面板和时间线项，并将首页改为基于共享组件挂载。

#### Control Plane S3 对象存储接入 (2026-05-29)

- 使用 AWS SDK for Go v2 的 `config`、`credentials`、`service/s3` 初始化控制平面 S3 客户端。
- 新增 `S3ObjectStore` 边界，封装对象上传、下载、存在性检查和删除，并返回稳定的 `s3://bucket/key` 工件引用。
- 补齐 S3 配置校验，启动前检查 endpoint、region、bucket、access key 和 secret key。
- 更新配置模板和配置指南，保留 MinIO/localstack path-style 默认值，并补充 Volcengine TOS virtual-hosted 配置示例。

#### Runtime 任务执行结果 API (2026-05-29)

- 补齐 Runtime task events、complete、fail 和 lease endpoint 的基础处理，支持 Runtime Agent 回传结构化执行事件和终态。

#### Phase 4 - Runtime Agent Control Plane 集成 (2026-05-29)

- 添加 Runtime Agent Control Plane 客户端 (`apps/runtime-agent/src/controlplane/`)
  - client.rs: HTTP 客户端实现
    - ControlPlaneClient 结构：封装 reqwest HTTP 客户端
    - register(): 注册节点到 Control Plane
    - heartbeat(): 发送心跳更新节点状态和负载
    - claim_task(): 长轮询获取任务（支持超时）
    - 完整的错误处理和上下文信息
  - models.rs: API 模型定义
    - TaskStatus 枚举 (pending/claimed/running/completed/failed/cancelled)
    - NodeStatus 枚举 (online/offline)
    - RegisterNodeRequest/Response
    - HeartbeatRequest/Response
    - Task 模型（包含完整任务信息）
    - 所有模型支持 serde 序列化/反序列化
  - mod.rs: 模块导出
- 更新 Cargo.toml
  - 将 reqwest 从 dev-dependencies 移至 dependencies
  - 启用 json 和 rustls-tls 特性
- 添加集成测试 (`apps/runtime-agent/tests/controlplane_client_test.rs`)
  - 客户端创建测试
  - 请求序列化测试
  - 集成测试（需要运行的 Control Plane，默认 ignored）
    - 节点注册测试
    - 心跳更新测试
    - 任务 claim 超时测试
  - 所有单元测试通过

#### Phase 2.3 - 任务调度器 (2026-05-29)

- 添加任务调度器 (`apps/control-plane/internal/runtime/scheduler.go`)
  - Scheduler 结构：负责任务到节点的调度
  - SelectNode 方法：智能节点选择
    - 查询支持指定 Provider 且在线的节点
    - 过滤负载已满的节点 (current_load >= max_slots)
    - 选择负载最低的节点实现负载均衡
    - 自动更新节点 current_load
  - 错误处理：ErrNoAvailableNode
- 添加调度器测试 (`apps/control-plane/internal/runtime/scheduler_test.go`)
  - 单节点调度测试
  - 负载均衡测试（多节点选择最低负载）
  - Provider 过滤测试
  - 容量过滤测试（排除满载节点）
  - 无可用节点错误测试
  - 复杂场景测试（混合 Provider、负载、容量）
  - 11 个测试用例全部通过

#### Phase 2.2 - Runtime 服务 (2026-05-29)

- 添加 Runtime 节点管理服务 (`apps/control-plane/internal/runtime/`)
  - models.go: 领域模型定义
    - NodeStatus 枚举 (online/offline)
    - Node 模型及辅助方法 (IsOnline, HasCapacity, SupportsProvider)
    - RegisterNodeRequest, UpdateHeartbeatRequest 请求模型
    - ListNodesFilter 过滤器模型
    - pgtype 类型转换辅助函数
  - repository.go: 数据访问接口
    - CRUD 操作 (CreateNode, GetNode, ListNodes, UpdateHeartbeat, UpdateLoad, UpdateStatus, DeleteNode)
    - ListOnlineNodes: 查询心跳在阈值内的在线节点
  - service.go: 业务逻辑实现
    - RegisterNode: 注册新节点或更新已存在节点
    - UpdateHeartbeat: 更新心跳和负载，自动检测节点状态
    - GetNode: 根据 ID 查询节点
    - ListNodes: 列出节点，支持状态过滤和分页
    - ListOnlineNodes: 列出在线节点（60秒心跳阈值）
    - JSON 序列化支持 (providers, metadata)
  - service_test.go: 完整的单元测试
    - 使用 testify/mock 实现 MockRepository
    - 覆盖所有服务方法的正向和负向测试用例
    - 输入验证测试
    - 分页和限制测试
    - 15 个测试用例全部通过

#### Phase 2.1 - 任务服务 (2026-05-29)

- 添加任务管理服务 (`apps/control-plane/internal/task/`)
  - models.go: 任务领域模型
  - repository.go: 任务数据访问接口
  - state_machine.go: 任务状态机
  - service.go: 任务服务实现
  - service_test.go: 单元测试

#### Phase 1.3 - 数据层测试 (2026-05-29)

- 添加完整的数据层测试套件 (`apps/control-plane/internal/storage/queries/queries_test.go`)
  - Runtime 节点测试：创建、查询、心跳更新、在线节点列表
  - 任务测试：创建、查询、列表过滤、状态更新、状态转换、事件流
  - 认证测试：用户创建、查询、Runtime token 创建和验证
  - 审计测试：事件创建、列表查询、统计、时间过滤
- 添加 Runtime 节点查询 (`apps/control-plane/internal/storage/queries/runtime.sql`)
  - CreateRuntimeNode, GetRuntimeNode, UpdateRuntimeNodeHeartbeat
  - UpdateRuntimeNodeLoad, UpdateRuntimeNodeStatus
  - ListOnlineNodes, ListRuntimeNodes, DeleteRuntimeNode
- 添加认证查询 (`apps/control-plane/internal/storage/queries/auth.sql`)
  - CreateUser, GetUser, GetUserByUsername, GetUserByEmail
  - UpdateUser, ListUsers, DeleteUser
  - CreateRuntimeToken, GetRuntimeToken, ValidateRuntimeToken, DeleteRuntimeToken
- 添加测试辅助脚本 (`apps/control-plane/test.sh`)
  - 基于显式远端或专用测试环境变量运行 storage 查询集成测试
- 添加测试文档 (`apps/control-plane/internal/storage/queries/README.md`)
  - 测试覆盖说明
  - 运行指南
  - 故障排查
- 添加测试依赖
  - stretchr/testify (latest)

#### Phase 1.2 - 配置 sqlc (2026-05-29)

- 配置 sqlc 代码生成 (`apps/control-plane/sqlc.yaml`)
- 生成任务查询代码 (`apps/control-plane/internal/storage/queries/tasks.sql.go`)
- 生成审计查询代码 (`apps/control-plane/internal/storage/queries/audit.sql.go`)

#### Phase 1.1 - 数据库迁移 (2026-05-29)

- 初始数据库 schema (`apps/control-plane/internal/storage/migrations/001_initial.sql`)
  - Runtime 节点表 (runtime_nodes)
  - 认证表 (auth_users, auth_runtime_tokens)
  - 任务表 (tasks, task_executions, task_state_history, task_events, task_artifacts)
  - 审计表 (audit_events)
  - 索引和触发器

### Changed

#### Foundation Readiness 文档收口 (2026-05-30)

- 同步 README、开发指南、API 文档和下一步指引，明确底座阶段的启动、验证和契约守护边界。

#### Foundation 文档同步 (2026-05-29)

- 同步 README、开发指南、API 文档和下一步指引，使文档状态与已验证的 Foundation baseline 保持一致。

#### Runtime Agent daemon 默认语义 (2026-05-29)

- 将 Runtime Agent 正式运行边界收敛为受 Control Plane 管理的 daemon，并补充认证 token 配置、环境变量和 CLI 覆盖。

#### Runtime API 契约路径收敛 (2026-05-29)

- 将 Runtime 任务 claim、事件、完成、失败和 lease 续约路径统一收敛到 Control Plane 的 `/api/v1/runtime/tasks/...` canonical contract，并将 Runtime Agent 本地契约保留为诊断和本地 run API。

#### Control Plane 启动边界收敛 (2026-05-29)

- 收敛 Control Plane 主启动入口，明确 health-only router 与产品 API server 的边界，并通过统一装配路径连接存储、服务和 handlers。

- 将 Control Plane PostgreSQL 和 Redis 配置示例切换到 `doc/database/conn_info.md` 记录的远端地址，并修正连接验证命令。
- 在远端 PostgreSQL 创建 `superteam` 应用用户、数据库和 schema，并从本地 `127.0.0.1` 的 `superteam` 数据库迁移当前 schema 与迁移记录。

### Deprecated

### Removed

### Fixed

#### 项目需求启动详情分页前过滤 (2026-06-12 17:48)

- 修复需求启动详情读模型先按项目分页再内存过滤导致相关事实被较新无关记录截断的问题，改为在仓储查询层按需求、任务、决策和事件关联条件过滤后再限制返回数量。

#### Control Plane 迁移命令目录对齐 (2026-05-30)

- 修正 `apps/control-plane/Makefile` 的 Atlas 迁移目录，统一指向实际 schema 源 `internal/storage/migrations`。

#### Control Plane API 响应契约对齐 (2026-05-30)

- 为任务与 Runtime 节点 API 响应补充显式 DTO，统一输出 snake_case 字段，避免直接编码领域模型时泄漏 Go 字段名。
- 将任务响应中的 `params` 规范化为 JSON object；空值、无效 JSON 或非对象输入统一回退为 `{}`，避免返回 base64 字符串。
- 更新 API/e2e 测试，锁定 `create/get/list/update/cancel/claim/complete/fail` 等任务路径及 Runtime 节点路径的真实 JSON shape。
- 收敛 Runtime claim 到 canonical `/api/v1/runtime/tasks/claim`，移除旧别名路由，并同步 API/OpenAPI 文档对 complete 与 lease 当前能力边界的描述。

#### Runtime Agent 配置入口统一 (2026-05-29)

- 统一 Runtime Agent 配置模型，支持 `--config` 加载 TOML 配置文件。
- 将配置优先级收敛为：CLI 参数 > `RUNTIME_AGENT_*` 环境变量 > `config.toml` > 默认值。
- 同步 `.env.example`、`config.example.toml`、README、配置指南和 `dev:runtime-agent` 脚本，避免 `RUNTIME_NODE_ID` / `RUNTIME_AGENT_NODE_ID` 等命名漂移。
- 忽略本地真实配置 `apps/runtime-agent/config.toml` 和 `.superteam/` 运行状态目录，保留可提交示例配置。

### Security

#### 配置文件忽略规则收敛 (2026-05-29)

- 扩展 `.gitignore` 环境配置规则，忽略项目内真实 `.env*` 和 `config.yaml` 文件，仅保留 `.env.example`、`config.example.yaml` 等示例配置可同步。

## [0.1.0] - 2026-05-29

### Added
- 项目初始化
- 基础目录结构
- CLAUDE.md 项目文档
